// Example script — transforms page content to uppercase
// This is a basic example showing how scripts work.
// The `page` object provides access to the current page content.

const text: string = page.content;
return text.toUpperCase();
