# Completed Tasks

All completed tasks, newest first.

| ID | Title | Epic | Notes |
|----|-------|------|-------|
| US-140 | Log View editor — dialog entries (input.text, confirm, buttons) | EPIC-004 | Three interactive dialog views: `ConfirmDialogView`, `TextInputDialogView`, `ButtonsDialogView` in `items/` folder. Shared building blocks: `DialogContainer` (active/resolved border), `DialogHeader` (optional title), `ButtonsPanel` (`!` prefix required-button support, check icon on result). Refactored rendering to direct state subscription: `LogEntryWrapper` receives `vm + index`, subscribes via `vm.state.use(s => s.entries[index])`. `LogViewContext` provides ViewModel to dialog components. `LogViewModel`: removed model cache/eviction, added `updateEntryAt()` + `dirtyIndices` with 300ms debounced JSONL serialization, refactored `resolveDialog()` to use `state.update()`. |
| US-139 | Log View editor — basic rendering of log entries | EPIC-004 | RenderFlexGrid virtualized rendering with variable row heights. `LogEntryWrapper` (cell root + container with timestamp, accent border), `LogEntryContent` (type router), `LogMessageView` (5 log levels with colored text), `StyledTextView` (plain/styled segment renderer). Auto-scroll to bottom on new entries. Toolbar toggle for timestamps (off by default). Dialog/output stubs. Height cache on LogViewModel. |
| US-136 | Define LogEntry types, models, and LogViewModel | EPIC-004 | Foundation for Log View editor: `logTypes.ts` (LogEntry, StyledText, dialog/output types, type guards), `LogEntryModel` (reactive wrapper with lazy instantiation/eviction), `LogViewModel` (ContentViewModel with JSONL parsing, incremental parse, dialog promises, model cache), placeholder `LogViewEditor` component. Registered `log-view` content-view editor for `.log.jsonl` files. |
| US-138 | Grid editor support for JSONL/NDJSON files | EPIC-004 | Registered `grid-jsonl` content-view editor. Added JSONL parsing (line-by-line `JSON.parse`) and serialization (compact, one object per line). Updated `asGrid()` facade, `PageEditor` type. |
| US-137 | JSONL language support for Monaco | EPIC-004 | Registered `jsonl` language with Monarch tokenizer for `.jsonl`/`.ndjson`/`.log.jsonl` files. Reuses JSON token names for automatic theme colors. Added green `{ }` file icon. Prerequisite for Log View editor. |
| US-135 | Script Library enhancements & example script fixes | EPIC-003 | Fixed example scripts: replaced `Buffer.from()` with browser APIs (`atob`/`btoa`) since Buffer fails in script sandbox. JWT parser strips "Bearer " prefix. format-json sets `page.grouped.language`. Script Library sidebar supports double-click/icon-click to open in tab with NavigationPanel. ScriptPanel "Open in New Tab" button opens selected script (or empty page) in tab with NavPanel rooted at script-panel/. |
| US-134 | Library setup wizard (link/create folder dialog) | EPIC-003 | Setup dialog (`LibrarySetupDialog.tsx`) replaces raw folder pickers in sidebar, settings, and script panel save. Folder input + Browse + "Copy example scripts" checkbox. Bundled example scripts in `assets/script-library/` (base64 encode/decode, sort lines, parse JWT, format JSON, shared helpers module). `copyExampleScripts()` in library-service.ts copies files recursively, skipping existing. All integration points use dynamic `import()`. |
| US-133 | Script panel dropdown & save to library | EPIC-003 | ComboSelect dropdown in script panel toolbar lists saved scripts from `script-panel/{language}/` and `script-panel/all/`. Save button writes modified content back to library (direct overwrite for selected scripts, InputDialog with folder choice for ad-hoc). Ctrl+S shortcut. InputDialog extended with optional radio buttons. State: `selectedScript` + `dirty` flag, persisted to cache. |
| US-132 | Path completion for require("library/...") | EPIC-003 | Monaco `CompletionItemProvider` auto-suggests library folders/files inside `require("library/...")` strings. Folders re-trigger suggestions; files shown without extension. Registered once per language (JS/TS), reads `allFiles` dynamically. Also fixed: `.js` library files with ES module syntax (`export`/`import`) now transpiled via sucrase `imports` transform (scoped to library folder only). Renamed `registerTsExtension()` → `registerLibraryExtensions()`. |
| US-131 | IntelliSense for library modules (addExtraLib + path mapping) | EPIC-003 | New `library-intellisense.ts` in `api/setup/` reads all library `.ts`/`.js` files and registers with Monaco via `addExtraLib()`. Virtual paths `file:///library/{path}`. `paths` compiler option maps `library/*` for module resolution. Subscribes to `LibraryService` state for live updates on file changes. Added `require()` and `preventOutput()` global declarations to `index.d.ts`. |
| US-130 | LibraryService — folder scanning, caching, file watching | EPIC-003 | Lazy-initialized `LibraryService` in `/api/library-service.ts`. Scans `script-panel/` subfolders for file index, recursive `walkDir` for all `.ts`/`.js` files. `fs.watch({ recursive: true })` with 300ms debounce. Calls `scriptRunner.invalidateLibraryCache()` on changes. Subscribes to `script-library.path` setting for re-init. `TModel<T>` + `TGlobalState` for reactive state. |
| US-129 | `require("library/...")` resolution in ScriptContext | EPIC-003 | Patched `require()` in script context to resolve `library/` paths to script library folder. New `library-require.ts` with `.ts` extension handler (sucrase), extension auto-resolution, cache invalidation via dirty flag. Clear error when library not linked. |
| US-128 | Script Library folder setting & sidebar integration | EPIC-003 | New `script-library.path` setting, "Script Library" static entry in sidebar with custom icon, ScriptLibraryPanel component (placeholder when unlinked, FileExplorer when linked), context menu (Change/Open in Explorer/Unlink), Settings page section with Browse/Unlink. |
| US-117 | Fix editor switch buttons for structured JSON editors | — | Structured JSON editors (notebook, todo, link) now embed a `"type"` property in serialized JSON. New `isEditorContent()` hook on `EditorDefinition` uses fast regex to detect content type. `TextFileModel` runs detection on restore, editor change (immediate), and content change (debounced 2.5s). `TextToolbar` merges detected editor into switch options. ViewModels serialize `type` field (`note-editor`, `todo-editor`, `link-editor`). Backward compatible — file extension matching still works. |
| US-126 | Script result handling improvement | — | Phase 1: TextDialog component (Monaco-based multi-purpose dialog) with `app.ui.textDialog()` API. Phase 2: Output suppression — `preventOutput()` global function and automatic `page.grouped.content` write detection via `GroupedPageWrapper`. `ScriptOutputFlags` tracks suppression state. Errors shown in TextDialog when output is suppressed. Added `page.runScript()` for programmatic script execution. |
| US-125 | TypeScript Script Execution via Sucrase | — | Added TypeScript support to script execution using `sucrase` (~275 KB, lazy-loaded). New `transpile.ts` with `transpileIfNeeded()` and `isScriptLanguage()`. `ScriptRunner` methods accept optional `language` parameter. F5 runs `.ts` files, Script Panel uses TypeScript language (accepts both JS/TS transparently), Notebook TS notes executable. MCP `execute_script` tool has new `language` parameter. Quick-add menu has "Script (TS)" option. |
| US-124 | UI Polish & NavPanel Bug Fix | — | External link routing (prefer active browser, reuse empty tab), Image Viewer save button (URL→file), EditLinkDialog image URL clear button, minimap click-to-scroll, auto-hide scrollbars, sidebar folder double-click opens NavPanel tab. **Bug fix:** NavPanel expanded folder state lost on first navigation — race condition where async `restore()` in `addEmptyPage()` overwrote the NavPanel set by `addEmptyPageWithNavPanel()`. Fix: skip `restore()` when creating page with pre-attached NavPanel. |
| US-123 | Lazy-load MCP SDK for faster startup | — | Replaced 4 static `@modelcontextprotocol/sdk` + `zod` imports with dynamic `import()` in `loadSdk()`, called on first `startMcpHttpServer()`. Deferred MCP auto-start by 1.5s after window init. SDK only loads when MCP is actually enabled. |
| US-122 | Phase 6: Test Preview & Browser Facades | EPIC-002 | Tested all preview facades via MCP: `asMarkdown()` (viewMounted, html), `asSvg()` (svg), `asHtml()` (html), `asMermaid()` (svgUrl, loading, error), `asBrowser()` (url, title, navigate, back, forward, reload). Browser facade has async state — url/title getters may return stale values when checked immediately after navigation in same script. All passed. No code changes needed. |
| US-121 | Phase 5: Test Content Editor Facades | EPIC-002 | Tested Notebook, Todo, Link facades via MCP. Notebook: all 11 methods passed. Todo: `addItem()` silently failed without selected list — added 4 new methods (`selectList`, `selectTag`, `setSearch`, `clearSearch`) to TodoEditorFacade and ITodoEditor. Link: all methods passed. Also added editor validation to MCP `create_page` handler (returns error with valid editor list). Created US-117 for switch button fix. |
| US-120 | Phase 4: Test Text & Grid Facades | EPIC-002 | Tested `asText()` and `asGrid()` via MCP. Text: `editorMounted`, `getSelectedText()`, `insertText()`, `replaceSelection()`, `revealLine()`, `setHighlightText()`, `getCursorPosition()` all passed. Grid: `rows`, `columns`, `rowCount`, `editCell()`, `addRows()`, `deleteRows()`, `addColumns()`, `deleteColumns()`, `setSearch()`, `clearSearch()` all passed. No gaps. No code changes needed. |
| US-119 | Phase 3: Test Page Object API | EPIC-002 | Tested `page` properties via MCP: read-only (`id`, `type`, `title`, `modified`, `pinned`, `filePath`), read-write (`content`, `language`, `editor`), `data` storage, `grouped` auto-creation. All passed. No gaps. No code changes needed. |
| US-118 | Phase 2: Test Pages Collection API | EPIC-002 | Tested `app.pages` via MCP: queries (`activePage`, `findPage`, `getGroupedPage`, `isLastPage`, `isGrouped`), lifecycle (`openFile`, `addEmptyPage`, `addEditorPage`, `showAboutPage`, `showSettingsPage`, `showBrowserPage`), navigation (`showPage`, `showNext`, `showPrevious`), layout (`moveTab`, `pinTab`, `unpinTab`, `group`, `ungroup`). All passed. No gaps. No code changes needed. |
| US-116 | Test & Improve app.menuFolders API | EPIC-002 | Tested all `app.menuFolders` methods via MCP: `folders` (returns IMenuFolder[] with id/name/path), `add()` (path-based + virtual with files[], returns string UUID), `find()` (found returns object, not-found returns undefined), `move()` (reorders correctly), `remove()` (removes folder, non-existent is no-op). All passed. No gaps. No code changes needed. |
| US-115 | Test & Improve app.settings API | EPIC-002 | Tested all `app.settings` methods via MCP: `theme` (returns "default-dark"), `get()` (known keys return values, unknown returns undefined), `set()` (round-trip works, value persists), `onChanged` (subscription fires with {key, value}, dispose stops events). All passed. No gaps. No code changes needed. |
| US-114 | Test & Improve app.downloads API | EPIC-002 | Tested all `app.downloads` methods via MCP: `downloads` (returns DownloadEntry[] with id/filename/url/savePath/totalBytes/receivedBytes/status/startTime), `hasActiveDownloads` (false when all completed), `aggregateProgress` (0 when no active), `clearCompleted()` (no throw), `cancelDownload`/`openDownload`/`showInFolder` with invalid ID (all no-ops, no throw). All passed. No gaps. No code changes needed. |
| US-113 | Test & Improve app.recent API | EPIC-002 | Tested all `app.recent` methods via MCP: `load()` (reloads from disk), `files` (returns string[], 100 items capped), `add()` (adds to top, deduplicates, respects cap), `remove()` (removes from list). `clear()` skipped to preserve user data. All passed. No gaps. No code changes needed. |
| US-112 | Test & Improve app.editors API | EPIC-002 | Tested all `app.editors` methods via MCP: `getAll()` (15 editors, correct id/name/category), `getById()` (found + not-found), `resolve()` (JSON/MD/CSV/TXT/unknown all resolve to monaco), `resolveId()`, `getSwitchOptions()` (json→[monaco, grid-json], markdown→[monaco, md-view], plaintext→empty). All passed. No gaps. No code changes needed. |
| US-111 | Test & Improve app.ui API | EPIC-002 | Tested all `app.ui` methods via MCP with user interaction: `confirm()` (default buttons, custom 3-button, dismiss→null), `input()` (default, with prefilled value/selectAll/custom buttons, cancel→null), `password()` (encrypt mode with confirm field, decrypt mode, cancel→null), `notify()` (all 4 types fire-and-forget, click interaction returns "clicked"). All passed. No gaps identified — API is complete. No code changes needed. |
| US-110 | Test & Improve app.window API | EPIC-002 | Tested all `app.window` methods via MCP: `windowIndex` (returns 0), `isMaximized`/`menuBarOpen`/`zoomLevel` (reactive state reads), `maximize()`/`restore()`/`toggleWindow()`/`minimize()` (visual window actions), `toggleMenuBar()` (sidebar toggle), `zoom(delta)`/`resetZoom()` (zoom in/out/reset), `openNew()` (opens new window, returns index). All passed. `close()` skipped (would kill MCP session). No gaps identified — API is complete. No code changes needed. |
| US-109 | Test & Improve app.shell API | EPIC-002 | Tested all `app.shell` methods via MCP: `openExternal()` (opened URL in browser), `encryption.encrypt()`/`decrypt()`/`isEncrypted()` (round-trip, wrong password throws, invalid text throws, detection works), `version.runtimeVersions()` (returns electron/node/chrome), `version.checkForUpdates()` (returns update info). All passed. No gaps identified — API is complete for its scope. No code changes needed. |
| US-108 | Test & Improve app.fs API | EPIC-002 | Tested all `app.fs` methods via MCP `execute_script`: file I/O (read/readFile/readBinary/write/writeBinary), exists/delete, encoding variants (utf-8, utf-8-bom, utf-16le), dialogs (showOpenDialog/showSaveDialog/showFolderDialog with select and cancel), path resolution (resolveDataPath/resolveCachePath/commonFolder), OS integration (showInExplorer/showFolder). All passed. Added two new methods: `listDir(dirPath, pattern?)` and `mkdir(dirPath)` — exposed existing private helpers `_listDirFiles` and `_ensureDir`. Updated `IFileSystem` interface, `assets/editor-types/fs.d.ts`, `assets/mcp-api-guide.md`, and `docs/api/fs.md`. |
| US-107 | MCP Multi-Window Support | EPIC-001 | Added multi-window targeting to all MCP tools via optional `windowIndex` parameter (defaults to first open window for backward compatibility). New `list_windows` tool runs in main process (no IPC) — returns all windows with `status: "open"\|"closed"`, page count, and page metadata. New `open_window` tool reopens closed windows with persisted pages. Closed windows return actionable error suggesting `open_window`. Shared `windowIndexParam` Zod schema across 7 existing tools. Updated `assets/mcp-api-guide.md` with multi-window documentation. |
| US-106 | MCP Title Bar Indicator | EPIC-001 | Push-based MCP status indicator in title bar. Added `eMcpStatusChanged` IPC event broadcast from main process on server start/stop and session connect/disconnect. `Window` class holds reactive `mcpRunning`/`mcpClientCount` state. Green dot + "MCP" label + connection count in `MainPage.tsx`, absolutely positioned below system buttons. Replaced 5s polling in SettingsPage with event subscription. |
| US-105 | MCP API Docs Resource | EPIC-001 | Added `assets/mcp-api-guide.md` — condensed API documentation (~344 lines) exposed as MCP resource `notepad://docs/api-guide`. Registered via `server.registerResource()` in `mcp-http-server.ts`, loaded dynamically from assets using `getAssetPath()`. Gives standalone AI clients (Claude Desktop, ChatGPT, Gemini) scripting API context without project-level docs. |
| US-104 | MCP Settings UI & Polish | EPIC-001 | Added MCP Server section to Settings page (`SettingsPage.tsx`). `McpSection` component with: enable/disable checkbox toggle, port input (disabled when enabled, validated 1024-65535), live status indicator (green dot + client count via `getMcpStatus` polling), URL display with Copy URL button, AI client config JSON snippet with Copy button. Updated `docs/mcp-setup.md` Quick Start for Settings UI. Updated `docs/whats-new.md`. |
| US-103 | MCP Tools & Configuration | EPIC-001 | Added 3 new MCP tools: `create_page` (with content/language/editor/title), `set_page_content` (text-based pages), `get_app_info` (version/pageCount/activePageId). Refined descriptions for all 7 tools. Added `pageId` optional param to `execute_script`. Created `.mcp.json` for Claude Code. Created `docs/mcp-setup.md` user guide with AI client configs (Claude Code, Claude Desktop, ChatGPT, Gemini CLI). |
| US-102 | HTTP MCP Transport | EPIC-001 | Replaced Named Pipe MCP server with Streamable HTTP server (`mcp-http-server.ts`) using `@modelcontextprotocol/sdk`. Per-session McpServer + StreamableHTTPServerTransport on `http://localhost:7865/mcp`. 4 MCP tools: `execute_script`, `list_pages`, `get_page_content`, `get_active_page`. Reuses existing IPC bridge (MCP_EXECUTE/MCP_RESULT). `whenReady` guard for renderer initialization. Bound to 127.0.0.1 for security. `mcp.port` setting added (default 7865). Universal compatibility: Claude Desktop, Claude Code, ChatGPT, Gemini CLI. |
| US-101 | MCP Bidirectional Pipe Protocol | EPIC-001 | Named Pipe server (`mcp-pipe-server.ts`) with JSON-RPC 2.0 protocol for AI agent integration. Renderer-side command handler (`mcp-handler.ts`) dispatches `execute_script`, `get_pages`, `get_page_content`, `get_active_page`. Console capture in ScriptContext (`ConsoleLogEntry`). `ScriptRunner.runWithCapture()` for headless execution. Settings-driven lifecycle via `mcp.enabled`. IPC endpoints `setMcpEnabled`/`getMcpStatus`. Settings `wait()` method for async readiness. |
| US-100 | Mermaid Diagram "Open in Editor" Button | | Added "Open in Editor" button to mermaid diagram hover toolbar in Markdown Preview. Clicking opens the diagram source in a new Mermaid editor tab via `pagesModel.addEditorPage()`. Toolbar replaces single copy button with two-button layout (open + copy). |
| US-072 | Phase 7e: Future Architecture Cleanup | | Deleted `doc/future-architecture/` folder (30+ design/migration planning docs — all content superseded by rewritten architecture docs, diagrams, and user API reference). Deleted 17 completed task folders (US-052 through US-068). Removed migration notice from `active.md` — App Object Model migration is complete. |
| US-071 | Phase 7d: Documentation Automation Skills | | Created `/project:review` command (architecture validation against `doc/`), `/project:document` command (developer docs update in `doc/`), and `/project:userdoc` agent (Sonnet, isolated context, user docs update in `docs/`). Created Epic system: `doc/epics/` with template and active tracker, added Epic column to `completed.md`, updated task template with Epic field. Overhauled CLAUDE.md workflow: auto-task creation for ad-hoc work, mandatory `/project:review` + `/project:document` + `/project:userdoc` on task completion, work discovery flow (active task → active epic → ask user), epic-task linking. |
| US-070 | Phase 7c: API Reference → User Scripting Guide | | Created `docs/api/` with 12 user-facing API reference pages: `index.md` (overview + navigation), `page.md` (IPage + all 10 editor facades), `app.md` (IApp overview + menuFolders), `pages.md` (IPageCollection — queries, lifecycle, navigation, layout), `fs.md` (IFileSystem — file I/O, path resolution, dialogs, OS integration), `settings.md` (ISettings — get/set, change events), `ui.md` (IUserInterface — confirm, input, password, notify), `shell.md` (IShell — openExternal, version, encryption), `window.md` (IWindow — actions, zoom, multi-window), `editors.md` (IEditorRegistry — resolve, switch options), `recent.md` (IRecentFiles), `downloads.md` (IDownloads). All verified against `.d.ts` type definitions. Updated `docs/scripting.md` (added links to API pages, added pages + downloads to services table). Updated `docs/index.md` (added API Reference link). |
| US-069 | Phase 7b: Mermaid Architecture Diagrams | | 5 standalone diagrams in `doc/architecture/diagrams/`: (1) Application Layers — process boundaries + renderer layer dependencies, (2) Bootstrap Sequence — full init timeline with guard flags, (3) Editor System — resolution flow + rendering architecture + content-view switching, (4) ContentViewModel Lifecycle — class hierarchy + acquire/release sequence + multi-consumer ref-counting, (5) Script Execution Flow — F5 → wrappers → facades → auto-release cleanup. |
| US-068 | Phase 7a: Architecture Docs Rewrite | | Full rewrite of 4 docs (`overview.md`, `folder-structure.md`, `state-management.md`, `scripting.md`), significant update of 2 docs (`editors.md`, `pages-architecture.md`), minor update of 1 doc (`browser-editor.md`). All verified against actual source code. Added: Object Model concept, bootstrap sequence, ContentViewModel pattern, 10 scripting facades with auto-release lifecycle, wrapper architecture, type definitions. Fixed: all stale `/store/`/`/features/`/`/core/services/` references. Updated CLAUDE.md (folder structure, key files, doc map). |
| US-067 | Phase 6d: Browser + Read-Only Facades | | `BrowserEditorFacade` (direct model wrap, no ViewModel/ref-counting; url, title, navigate, back, forward, reload). `MarkdownEditorFacade` (viewMounted + html from DOM container). `SvgEditorFacade` (svg source). `HtmlEditorFacade` (html source). `MermaidEditorFacade` (svgUrl, loading, error). 5 new `.d.ts` interfaces, 5 new facade classes. `PageWrapper` updated with `asMarkdown()`, `asSvg()`, `asHtml()`, `asMermaid()`, `asBrowser()`. Browser uses `state.get().type === "browserPage"` check (no instanceof, preserves code splitting). |
| US-066 | Phase 6c: Content Editor Facades (Notebook, Todo, Link) | | `NotebookEditorFacade` (notes CRUD, categories, tags; maps `NoteItem.content.content` → flat `INote.content`). `TodoEditorFacade` (items CRUD, lists, tags; maps `done` → `completed`, `tag: null` → `""`). `LinkEditorFacade` (links CRUD; maps `href` → `url`, computes `pinned`). Added `skipConfirm` parameter to delete methods in `NotebookViewModel` (`deleteNote`) and `TodoViewModel` (`deleteItem`, `deleteList`, `deleteTag`). `NotebookViewModel.addNote()` now returns `NoteItem`. 3 new `.d.ts` interfaces, 3 new facade classes. `PageWrapper` updated with `asNotebook()`, `asTodo()`, `asLink()`. |
| US-065 | Phase 6b: Text + Grid Editor Facades | | `ITextEditor` interface with `editorMounted` property + 6 methods (`getSelectedText`, `revealLine`, `setHighlightText`, `getCursorPosition`, `insertText`, `replaceSelection`). `IGridEditor` + `IColumnInfo` interfaces (rows, columns, editCell, addRows/deleteRows, addColumns/deleteColumns, setSearch/clearSearch). `TextEditorFacade` wraps `TextViewModel`, `GridEditorFacade` wraps `GridViewModel` (maps Column→IColumnInfo). `PageWrapper.asText()` and `asGrid()` acquire ViewModels via `ContentViewModelHost`, return facades, track releases in `releaseList`. Grid editor ID auto-detected from current editor or language. 3 new methods added to `TextViewModel`. |
| US-064 | Phase 6a: Scripting Infrastructure | | `IPage` in `shared/types.ts` renamed to `IPageState` (16 files). `page.data` promoted from `ScriptPanelModel` to `PageModel` base class (all page types). Created `IPage` interface in `api/types/page.d.ts` with content/language/editor/grouped/data + stub `asX()` methods. `IPageInfo` deleted — all returns use `IPage`. `PageEditor`/`Language` types added to `common.d.ts`. Created `PageWrapper`, `PageCollectionWrapper`, `AppWrapper` in `/scripting/api-wrapper/`. Shared `releaseList` flows through all wrappers. `ScriptContext` refactored to use wrappers, returns `{ context, cleanup }`. `ScriptRunner` calls `cleanup()` in finally block. |
| US-063 | Phase 5 — Migration Review | | Audited all remaining old folders. `/store/` deleted (menu-folders→api, link-open-menu→editors/shared, language-mapping→core/utils). `/features/` deleted (tabs/sidebar/navigation→ui). `/setup/` moved to `/api/setup/` with `app.initSetup()`. `/core/services/` deleted (scripting→`/scripting/`, file-watcher→core/utils). Icons extracted to `/components/icons/`. `app.menuFolders` (IMenuFolders) wired. All decisions documented in [10.migration-review.md](../future-architecture/migration/10.migration-review.md). |
| US-062 | NoteItemEditModel — Formal IContentHost | `NoteItemEditModel` now formally `implements IContentHost`. Removed `as unknown as TextFileModel` cast in `NoteItemActiveEditor` and `this as any` in `acquireViewModel`. Fixed `changeLanguage` signature (`string` → `string \| undefined`). `AsyncEditor` prop type widened to `PageModel \| IContentHost`. `FileEditorPage` generic constraint widened to match. Individual content-view editors keep `TextFileModel` prop type unchanged. |
| US-061 | Pure Renderer ViewModels (SVG, HTML, Mermaid) | | `SvgViewModel`, `HtmlViewModel`, `MermaidViewModel` created as `ContentViewModel` wrappers. Factories registered for `"svg-view"`, `"html-view"`, `"mermaid-view"`. SVG/HTML are near-empty ViewModels (pattern consistency + `pageModel` getter for Task 10). MermaidViewModel has real state (`svgUrl`/`error`/`loading`/`lightMode`) — 400ms debounced render moved from `useEffect` to `onContentChanged()` + `lightMode` subscription. All three components use `useContentViewModel` + `useSyncExternalStore`. Parallel imports in `loadModule`. |
| US-060 | LinkViewModel (Link ContentViewModel) | | `LinkEditorModel` refactored to `LinkViewModel extends ContentViewModel`. Factory registered for `"link-view"`. `LinkEditor` uses `useContentViewModel` hook + `useSyncExternalStore`. `swapLayout` prop eliminated from model — component calls `vm.initBrowserSelection()` explicitly. `BrowserBookmarks` switched from direct construction to `acquireViewModel("link-view")` for shared ref-counted instance with BookmarksDrawer; required `language: "json"` in state for registry validation. Selection state caching switched to `host.stateStorage`. Child components updated (`LinkItemList`, `LinkItemTiles`, `PinnedLinksPanel`). `LinkEditorModel.ts` deleted. |
| US-059 | MarkdownViewModel (Markdown ContentViewModel) | | Inline `MarkdownViewModel` extracted from `MarkdownView.tsx` to standalone `MarkdownViewModel extends ContentViewModel`. Factory registered for `"md-view"`. `MarkdownView` uses `useContentViewModel` hook + `useSyncExternalStore`. Read-only preview — no content parsing, `onContentChanged()` only triggers search re-evaluation. `effect()` with deps replaced by `state.subscribe()` + `onContentChanged()`. Container DOM ref kept in reactive state (needed by Minimap). Fixed `containerSrollTop` typo. No child component changes needed (`MarkdownSearchBar`, `CodeBlock` are stateless). |
| US-058 | TodoViewModel (Todo ContentViewModel) | | `TodoEditorModel` refactored to `TodoViewModel extends ContentViewModel`. Factory registered for `"todo-view"`. `TodoEditor` uses `useContentViewModel` hook + `useSyncExternalStore`. `gridModel?.update()` moved to component `useEffect`. Selection state caching switched from direct `fs` to `host.stateStorage` (consistency with GridViewModel). Child components updated (`TodoListPanel`, `TodoItemView`). `TodoEditorModel.ts` deleted. |
| US-057 | NotebookViewModel (Notebook ContentViewModel) | | `NotebookEditorModel` refactored to `NotebookViewModel extends ContentViewModel`. Factory registered for `"notebook-view"`. `NotebookEditor` uses `useContentViewModel` hook + `useSyncExternalStore`. `gridModel?.update()` moved from model to component `useEffect` (React rendering concern). `pageModel` getter for script context access. Debounced save flushed in `onDispose()`. Child components updated (`NoteItemViewModel`, `ExpandedNoteView`, `NoteItemEditModel`). Partial Task 9: `NoteItemEditModel` got `acquireViewModel`/`releaseViewModel`/`stateStorage` via `ContentViewModelHost` — required for embedded grid/markdown editors. `NotebookEditorModel.ts` deleted. |
| US-056 | GridViewModel (Grid ContentViewModel) | | `GridPageModel` refactored to `GridViewModel extends ContentViewModel`. Factory registered for both `"grid-json"` and `"grid-csv"` (same factory, reads editor type from host state). `GridEditor` uses `useContentViewModel` hook + `useSyncExternalStore` (Rules of Hooks fix — cannot call `vm.state.use()` after early return). Auto-focus moved from model to component `useEffect`. `CsvOptions` updated. `GridPageModel.ts` deleted. |
| US-055 | TextViewModel (Monaco ContentViewModel) | | `TextEditorModel` refactored to `TextViewModel extends ContentViewModel`. Factory registered for `"monaco"` via `loadModule` + `await import()`. `TextEditor` uses `useContentViewModel` hook. `TextToolbar` uses `tryGet` + `useOptionalModelState` (no extra ref). External consumers migrated to TextFileModel delegate methods. `ContentViewModelHost.tryGet()` added. Pending ops (revealLine, setHighlightText) work before Monaco mounts. |
| US-053 | TextFileModel implements IContentHost | | `TextFileModel` now implements `IContentHost`. Added `ContentViewModelHost` composition, `acquireViewModel`/`releaseViewModel` delegates, file-based `stateStorage`, and `_vmHost.disposeAll()` in dispose. Purely additive — no consumer changes. |
| US-054 | TextFileModel Decomposition | | Decomposed TextFileModel (~567 lines) into 3 focused submodels: `TextFileIOModel` (file I/O, watchers, cache), `TextFileEncryptionModel` (encrypt/decrypt, password, content mapping), `TextFileActionsModel` (keyboard, scripts, nav panel, compare). Core retains state, lifecycle, and flat API delegates for backward compatibility. Zero consumer changes. |
| US-052 | Content View Models Foundation | | `IContentHost` interface, `ContentViewModel<TState>` abstract base, `ContentViewModelHost` ref-counting helper, `useContentViewModel` hook. `EditorRegistry` extended with module cache + view model factory methods. `EditorModule.createViewModel` optional field. Purely additive — no existing editors modified. |
| US-051 | Window API Consolidation | | Merged window commands, zoom, and dev tools into `app.window` (IWindow). Removed `window-commands.ts`, absorbed into `api/window.ts`. 8 consumers migrated. Created Phase 5 architecture doc `9.content-view-models.md`. |
| US-050 | Phase 4b — Pages API (`app.pages`) | | All page logic moved from `store/pages-store.ts` into 5 submodels under `api/pages/` (Lifecycle, Navigation, Layout, Persistence, Query). `page-actions.ts` absorbed. 29 consumers migrated. Bridge files deleted. `IPageCollection` added to `IApp`. Removed fs.ts 100ms hack. See [migration/8.app-pages.md](../future-architecture/migration/8.app-pages.md). |
| US-049 | Phase 4a — Event Services + Downloads API | | 3-layer bootstrap (`initServices` → `initPages` → `initEvents` → `windowReady`). EventHandler deleted, MainPage simplified. 4 internal services (GlobalEvent, Keyboard, WindowState, RendererEvents). Downloads API (`IDownloads`) absorbed from store. See [migration/8.app-pages.md](../future-architecture/migration/8.app-pages.md). |
| US-048 | Phase 3a — `app.ui` (IUserInterface) | | IUserInterface: wrap dialog functions (confirm, input, password) + absorb alert notification API (notify). `features/dialogs/` moved to `ui/dialogs/`. 11 consumer files (~38 call sites) migrated, old exports removed. |
| US-045 | Phase 3b — `app.shell` (IShell) | | IShell: absorb encryption logic into `api/shell/`, wrap version service IPC, add `openExternal()`. 6 consumer files migrated, `encryption.ts` deleted. |
| US-047 | Phase 2 Revision — Full Absorption into API | | Full absorption: `nodeUtils` file I/O + `filesModel` data/cache/path ops merged into `api/fs.ts`. `windowIndex` added to `IWindow`. `uuid()` replaced with `crypto.randomUUID()`. `files-store.ts` and `node-utils.ts` deleted. `watchFile`/`getFileStats` inlined into `file-watcher.ts`. 18+ consumers updated. |
| US-046 | Phase 1 Revision — Move Logic into API | | `app.settings`: moved logic from `app-settings.ts` to `api/settings.ts` (14 consumers). `app.editors`: confirmed facade pattern correct (no code change). `app.recent`: moved logic from `recent-files.ts` to `api/recent.ts` (4 consumers). Old stores deleted. |
| US-044 | Phase 2 — File System & Window | | `app.fs` (IFileSystem: read/readFile/write/readBinary/writeBinary/exists/delete, resolveDataPath/resolveCachePath, dialogs, showInExplorer/showFolder), `app.window` (IWindow: minimize/maximize/restore/close, isMaximized, zoom/resetZoom/zoomLevel, openNew). Dynamic `import()` in `initServices()`. API reference docs created. |
| US-043 | Phase 1 — Independent Interfaces | | `app.settings` (ISettings wrapping appSettings with onChanged event), `app.editors` (IEditorRegistry — read-only subset of editorRegistry), `app.recent` (IRecentFiles with lazy load()). Dynamic `import()` via `initServices()` in app.ts. API reference docs created. |
| US-042 | Phase 0 — App Object Model Infrastructure | | `/api/` folder with IDisposable, IEvent, IApp types. `app` singleton with async init (version via IPC). Script context wiring. Vite plugin auto-copies .d.ts to assets for Monaco IntelliSense. |
| — | Phase -1: Pre-Migration Complete | Enhanced TComponentModel with effect()/memo()/auto init-dispose. Migrated all page editors (-1.3 through -1.8) and app shell (-1.1, -1.2). Views are now pure render functions. |
| US-041 | Tabs & Navigation Migration to Enhanced Model-View | | Pre-migration -1.2: TabsModel + MenuBarModel → effect()/memo(), refs and state moved to model. PageTabModel skipped (no lifecycle) |
| US-040 | App Shell Migration to Enhanced Model-View | | Pre-migration -1.1: MainPageModel + EventHandlerModel → effect() pattern, auto init/dispose, removed useEffect boilerplate from Views |
| US-039 | Browser Find in Page | | Inline search bar with match counter, Ctrl+F interception via main process + preload script + renderer keydown, webview.findInPage() API |
| US-030 | Browser Download Progress & History | | Toolbar download button with SVG progress ring, Popper popup with download list, main-process DownloadService with IPC broadcasting, native save dialog for reliable path capture, global state shared across windows, last 5 completed persisted. Also fixed external URL routing (left-to-right search, default profile only). |
| US-036 | Register as Default Browser | | HKCU registry management (no admin), Settings page UI, deep-link to Windows Default Apps, separate eOpenExternalUrl event for OS URLs, launcher version info for proper app name/icon |
| US-038 | Switch to electron-builder + NSIS | | electron-builder replaces Forge makers, NSIS installer with custom options page (shortcuts, context menu, file associations, browser registration), AllowSetForegroundWindow for bring-to-front, GitHub Actions with Rust toolchain |
| US-037 | Lightweight Rust Launcher | | 308KB Rust launcher with Named Pipe IPC for instant file/URL/diff opening, pipe server in main process, concurrent connection support |
| US-035 | Miscellaneous Enhancements | | Link Editor hostnames panel, Link/ToDo Editor session state persistence, pinned tab file path tooltip, browser popup/tab spam blocking |
| US-034 | Link Editor Enhancement | | Favicon caching & display, drag-and-drop (links→categories, categories→categories with reparenting), pinned links panel with drag-to-reorder |
| US-028 | Browser Bookmarks (Links Editor Integration) | | Per-profile bookmarks with star button, sliding drawer with full Link Editor, image discovery (meta tags + click tracking + context menu), encrypted bookmarks support via async password dialog, auto-save |
| US-033 | Link Editor | | Structured link manager for `.link.json` files with categories, tags, 5 view modes (list + 4 tile variants), edit dialog, image context menu, per-category and per-tag view mode, quick-add menu |
| US-032 | Browser Integration — Link Opening & Context Menu | | Link open behavior setting (default browser vs internal), smart browser tab routing (right→left→create new), Markdown link context menu (Open in Default/Internal Browser, Incognito), Settings page UI |
| US-031 | Browser Enhancement #1 | | OAuth popup support, toolbar changes, active tab styling, transparent splitter, compact tab extension popup, tab audio mute controls, home button, tabs panel initially collapsed, search engine selector, URL bar suggestions dropdown with search/navigation history |
| US-027 | Browser Profiles & Incognito Mode | | Named browser profiles with isolated sessions (cookies/storage/cache per profile), incognito mode (ephemeral partition), profile management in Settings (add/remove/color/default/clear data), incognito icon in URL bar, profile-colored globe icons on page tabs, quick-add submenu for profiles |
| US-029 | Browser Web Page Context Menu | | Contextual right-click menu for webview content (links, images, text, SVG), View Source/Actual DOM, Open Image/SVG in editor, URL bar with navigate button and Paste and Go, dynamic webview background |
| US-026 | Browser Internal Tabs | | Multi-tab browsing within a browser page, left-side tabs panel, target="_blank"/window.open() open new internal tabs, tab context menu (close/close others/close below), resizable panel, session restore for all tabs |
| US-025 | Basic Browser Editor | | Built-in web browser as page-editor using webview, multi-process architecture (renderer + main + guest preload), favicon/title detection, session restore, protocol blocking |
| US-023 | NavigationPanel File Content Search | | VS Code-style file content search with streaming results, include/exclude patterns, FileExplorer filtering, Monaco highlighting, configurable extensions in Settings |
| US-024 | ToDo List Enhancement — Tags | | Colored tags with filtering, tag assignment on items, predefined color palette, drag-drop warnings |
| US-022 | ToDo Editor | | Structured task list editor for `.todo.json` files with multiple lists, drag-to-reorder, search, comments, virtualized grid |
| US-020 | Markdown View Search | | Ctrl+F search in Markdown Preview with match highlighting, F3/Shift+F3 navigation, match counter, active match background |
| US-019 | Open User Folder in Tab | | Chevron on sidebar folders opens new tab with NavPanel, hover+tooltip for discoverability, built-in items unchanged |
| US-018 | Implement Grouping for Pinned Tabs | | Non-adjacent grouping for pinned+unpinned tabs, script execution works in pinned tabs, grouping preserved through pin/unpin |
| US-017 | Pinning Page Tabs | | Compact icon-only pinned tabs with sticky positioning, drag constraints, window persistence, navigation preservation |
| US-016 | Reusable File Explorer Component | | Tree-based FileExplorer replacing flat lists in sidebar and NavigationPanel, lazy loading, search, file operations, state persistence, navigate up/make root, collapse all |
| US-015 | Document Navigation Panel | | Folder-based nav tree from markdown links, in-tab navigation with auto-preview, pages-store lifecycle refactoring, global keyboard shortcuts fix |
| US-014 | Application Theming | | CSS Custom Properties theming with 6 dark themes, Settings page with theme selector, Monaco integration |
| US-013 | Create HTML Viewer | | Sandboxed iframe preview for HTML files with script execution support |
| US-012 | Markdown View Enhancements | | Code block syntax highlighting, copy-to-clipboard button, inline Mermaid diagrams, shared render-mermaid module |
| US-011 | Mermaid Diagram Viewer | | .mmd/.mermaid preview with light/dark toggle, copy to clipboard, syntax highlighting |
| US-010 | Copy Image to Clipboard | | Copy button in Image Viewer header and SVG View toolbar, Ctrl+C shortcut |
| US-009 | Notebook Editor | | Structured notes editor with categories, tags, search, drag-and-drop, expand-to-overlay |
| US-008 | TypeScript and Dependencies Upgrade | | TypeScript 4.5→5.4, @types/node 16→20, fixed vite compatibility |
| US-007 | About Page and Version Check | | About page with version info + auto update check via GitHub API |
| US-006 | Create Image View Editor | | Image viewer for binary images + SVG preview as content-view |
| US-003 | ContentPageModel Extraction | | Closed - architecture review concluded existing design is correct |
| US-002 | Editor Registry Pattern | | Declarative editor registration in `register-editors.ts` |
| US-001 | Fix Circular Dependencies | | Direct imports instead of barrel exports |
| US-005 | Create User Documentation | | Completed - see `/docs/` folder |
