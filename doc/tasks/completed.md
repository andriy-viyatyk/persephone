# Completed Tasks

Completed tasks, newest first.

| ID | Title | Epic | Notes |
|----|-------|------|-------|
| US-1360 | Malformed-input acceptance run and regression suite | EPIC-091 | Replayed the evaluation report's own appendix coverage list against the fixed build with a fresh Haiku agent through `mcp-test-agent-call`, and left `qa/surfaces/malformed-input.md` behind - the one file in that suite whose surface is not a screen. PASS: the agent completed all four requests unaided. The measurable result is the hint ratio - a first unknown member of a kind still returns the full member list (~2,500 bytes), a second returns the kind summary plus a `$help` pointer (~200 bytes). **The agent normalized two malformed probes into valid calls** (a string `"123"` for "the number 123"; wrapping a single line in an array), so those rows were replayed directly and marked as such rather than blurred. That is a finding, not a gap: a competent agent rarely sends a number where a string belongs, so this defect class is rare per session - but the report's evaluator hit it repeatedly while probing deliberately, and a failure mode that is rare is one an agent will *trust*, which is exactly why it must not lie. |
| US-1359 | Surface documentation and consistency | EPIC-091 | Six report items about what a caller is told; two were behaviour bugs. **3.1 was a silent no-op, not a signature disagreement**: `addEditorPage`'s fourth `content` argument is implemented in the lifecycle model and declared in `pages.d.ts`, but the scripting wrapper forwarded three of four - so following the arity error's own copy-paste example discarded the content and reported success. **3.7** trims Persephone's own frames from script stack traces in every build, not only packaged ones, since the main-process twin already returned no stack at all; live verification caught the first cut dropping the *caller's* frames too, because a script frame names an internal file in its eval origin while carrying the real position in `<anonymous>`. Also the four unescaped `output.grid` examples (which exist to be copy-pasted), the read-only `settings.theme` used as the writable example, and `script.execute`'s two failure channels now stated in the contract. **3.5 changed no return values** - epic decision 5 declines the report's uniform `{ ok: true }`. |
| US-1358 | Language validation, returned-object identity, and grid row keys | EPIC-091 | Three items, grouped because two are one-line fixes with tree-wide effect. **1.4's silent editor downgrade was not in `addEditorPage`** but in `editorRegistry.validateForLanguage`, which returns monaco whenever a matcher rejects the language - so the fix had to separate a real language the editor cannot handle (a legitimate fallback, kept) from a language that does not exist (an argument error). Plan review moved the check to the argument boundary: inside `validateForLanguage` it would have thrown during tab restore at startup. **1.5's dead hint path** was the resolver building hint paths from `walked`, which includes the call segment - every method returning a node was affected; descriptors now supply a live identity callback and a returned page addresses itself by its real id. A node with no identity keeps its children listed by bare segment name rather than losing the block. **3.2's row keys** live in an off-object WeakMap, so reading `rows` could never have revealed them; `rows` keeps its shape and `editor.rowKeys` is the parallel read path. `addRows` still returns sparse `{}` rows - verified intended, not a defect. |
| US-1357 | `call` parameter bounds - `limit`, `maxLength`, `windowIndex` | EPIC-091 | Three symptoms of one thing: no lower bounds anywhere. A negative `helpSearch` limit reached `slice(0, limit)` and dropped entries off the *end* (12 became 7); clamped in the shared core rather than the wrapper, because `helpSearch` is reachable from scripts that never pass through US-1356's check, and a non-finite limit falls back to the default rather than to zero - `Math.max(1, NaN)` is `NaN`, which would have kept the bug in a new disguise. `maxLength` and `windowIndex` were unbounded integers, so the published schema advertised zod's range; they now carry minimums and are rejected before the handler. And `maxLength` now bounds structured results, where the blowup risk actually lives - whole elements dropped, `shown`/`total` reported, element strings measured once and the prefix serialized once for real, so the output is always parseable. Depth cap unchanged: `call`'s own input schemas must not be truncated. |
| US-1356 | Extract the argument validator and end the silent no-op class | EPIC-091 | The epic's central task, and the one the report got wrong. It recommended applying an existing validation helper and named four sites; **no such helper exists** - the four do not share code, the pattern is re-implemented ad hoc in at least eleven places, and `deleteRows`, quoted as the gold standard, has no validation at all (the message attributed to it is nowhere in the codebase, and it was a thirteenth silent no-op). So `src/shared/ai-vision/argument-validation.ts` was extracted first, generalising `normalizeUiPushEntry`'s shape, then applied to thirteen sites. Two decisions: a **method** with wrong arity throws while a **property** given arguments returns its value with a warning - a property's value is unambiguous, whereas computing a method result from a signature the caller does not believe in is how a wrong answer gets reported as a right one; and `closePage` now throws with the open page ids instead of returning `false`, which meant three different things while its sibling `showPage` already threw for identical input. The eight sites that already validate well were left alone (decision 2). **`/review` at epic close caught a real defect the per-task smoke test missed**: `arrayOfChoicesRule` compared an array against the choices as a whole, so *every* `deleteRows` with valid keys threw - the smoke test had only exercised the error path. |
| US-1355 | Hint economics - honour `hints` and dedupe on the error path | EPIC-091 | The report's largest *cost* finding: six typos in one session re-emitted the full root member list six times at ~2.5k tokens each, more than every successful call in that session combined. The leak was narrower than it looked - `errorAt` already routed through the correct gate, but three branches passed `forceMembers`, which built its hint inline and bypassed both the dedupe and the mode check. Plan review found a fourth leak the report had counted but not isolated: the malformed-path branch hard-coded `hints: "always"`. Near-match suggestions (report 3.8) are what made the dedupe safe rather than a trade, since the epic keeps the member list as the surface's self-teaching mechanism - so a suggestion had to be *better* than the dump, not merely cheaper. **Live verification changed the shipped design**: the reviewed plan suppressed a suggestion on any tie, and `pagez` ties between `pages` and `page`, so the flagship example got the full dump; the matcher now names every equally-good candidate, ranked by distance then by length difference. Distance 2 only for names of five characters or more, since on short names almost everything is within two edits of everything else. |
| US-1354 | Guard `logView.push` against a non-array argument | EPIC-091 | The epic's only correctness bug and the report's HIGH item: a bare string passed to `push` was iterated and wrote one `log.info` entry per character - twelve characters, twelve log lines, reported as success - in the primary documented output channel, where a plain string is documented shorthand. Plan review added two things the report had not probed: a non-entry value such as a number returned `{ entryIds: [], dialogIds: [] }`, a success-shaped result for input that did nothing and therefore the very class the epic exists to remove, reached by its own first task; and entries were written as the loop iterated, so a bad element at index 3 left 0-2 applied and then threw - a half-applied mutation reported as a failure. Normalization is now a full first pass and nothing is written unless every entry is valid. |
| US-1353 | Retire `execute_tool` — the manifest is `call` alone | — | The last withheld tool of the [agent transparency roadmap](../agent-transparency-roadmap.md), and the row EPIC-090 refused to buy with an agent-answered trust dialog. **The user ran the check on 2026-09-07:** `tools.execute` against a registered toolset in a user project, with human authorization, exercising all three capability rows — run by id returned `ok: true` with real result rows, parameter overrides were honored, and a failure came back cleanly shaped when the tool script's own read-only guard refused a statement (a false positive *inside the tool script*, not a Persephone defect). `tools.search` and the `tools` overview were exercised in the same session. So the row was marked retirable and the tool deleted, following US-1349's pattern: `agent-tools.ts`, the `execute_tool` registry entry, and the dead `handleExecuteTool` adapter — dead because `tools.execute` calls `executeToolById` directly and never went through it. **`PERSEPHONE_MCP_CALL_ONLY` went with it**, as EPIC-090 decision 2 had pre-authorized: its only effect was hiding `execute_tool`, so keeping it would have left a documented switch that provably does nothing. The manifest is **34 → 1**, with no flag needed to reach the end state the roadmap set out for. Also swept `assets/tool-template/`, which US-1349 missed — the toolset authoring guide an agent reads was still instructing `search_tools` and `refresh_toolset`, both deleted a day earlier. |
| US-1349 | Delete the thirty-two retired MCP tools and rewrite the manifest instructions | EPIC-090 | The deletion the gate authorised. 34 tools to 2: `call` and `execute_tool`. Five tool-definition files and three renderer command adapters removed, each proven dead by grep first; every MCP resource kept, including `readGuideFile` and its mtime cache, which are shared with the resource callbacks and would have served stale guides if deleted as tool-only code. `SERVER_INSTRUCTIONS` went from 54 lines to 12, call-first - the old text was not merely stale but actively wrong, and the gate caught Codex trying `read_guide` as a *path* because of it. The standalone highlight recipe and the six per-tool QA files went too. Claude's review caught a double-encoded guide file, a dangling "the raw API below" pointing at a deleted section, two four-column rows in a three-column roadmap table, and five agent-visible strings still naming deleted tools. |
| US-1348 | The two-model deletion gate | EPIC-090 | Ten scenarios covering all 32 deleted-tool capabilities, each starting from `call` with no path, run on Haiku via `mcp-test-agent-call` and on Codex against the genuinely reduced manifest. No failures; two PARTIALs, both fixed. The reduced manifest was verified by raw JSON-RPC in both directions (1 tool with the flag on, 34 off, 13 resources either way). Produced three fixes and surfaced US-1352. |
| US-1347 | Rewrite the QA suite for `call`, and add the deletion gate | EPIC-090 | Every scenario in fifteen surface files now starts from a bare `call` and records its wrong turns. `qa/surfaces/gate.md` is new: coverage per *tool* rather than per scenario, which is what the abort criteria actually require. Its preamble carries the dialog policy (a Log View question may be answered by the runner; a trust dialog never), a cleanup field on every stateful scenario - added because EPIC-088's scratch boards were still in the user's board list two epics later - and the live-target rule. |
| US-1346 | The `PERSEPHONE_MCP_CALL_ONLY` flag | EPIC-090 | An environment variable rather than a setting, because EPIC-089 had just deleted a settings-shaped manifest flag across eight files for reading as a privacy control it was not. Ten lines in `server-factory.ts`, off by construction; resources unaffected in both modes. Also settled EPIC-089's open `waitForNavigation` question: no runtime change, documentation only. |
| US-1345 | Retire `read_guide`'s prose: resources stay, operations move into `$help` | EPIC-090 | An audit of twelve guides, sentence-family by sentence-family. Eight `$help` owners gained the operational prose that had lived only in a guide, and each moved sentence was deleted from its guide rather than duplicated. Verified from source that the resources, the guide-name enum and `readGuideFile`'s cache survive the tool's deletion independently. |
| US-1344 | `script.execute(code)` - the renderer half of gated scripting | EPIC-090 | Claims the `script` root name reserved since EPIC-083. Both it and the old handler call one extracted helper, so the reuse is structural rather than promised. No gate, because `execute_script` never had one. The plan review stopped a change that would have made every non-`Error` throw report `isError: false` - a silent success in the one task whose job was to prove parity. |
| US-1343 | The `call("")` overview - optional `path` and a high-level area map | EPIC-090 | `path` is optional and the overview is a 20-line area map emitted ahead of the member list, and in `$help` too. Three plan-review corrections shipped with it: the map was to sit under the per-session dedupe, which would have shown it only to the first agent of a session and made the gate's central measurement meaningless; `$help` would have silently lost the sentences moved out of `ROOT_HELP`; and four of the examples were caution-carrying actions, which a scanned map invites an agent to simply try. |
| US-1352 | Every `process.env` read in the main process compiled to `{}` | — | **Found by EPIC-090's gate**, when `PERSEPHONE_MCP_CALL_ONLY=1` reached the app (verified in the renderer) yet the MCP manifest still served all 34 tools. The built bundle showed why: `isMcpCallOnlyEnabled({}.PERSEPHONE_MCP_CALL_ONLY)`. `scripts/dev.mjs` and `scripts/build-prod.mjs` hand-roll the main-process Vite config and never marked it as a Node build, so Vite treated the Electron main process as a browser bundle and statically replaced every `process.env` with `{}` — 19 such accesses in the shipped bundle. The blast radius was wider than the flag: `PERSEPHONE_BOARDS_BRANCH` was inert; all of `windows-env.ts` was inert (it repairs missing Windows environment variables by writing `process.env[key] = value`, which wrote into a throwaway object); the proxy variables were unreadable; and worst, `command-runner.ts`'s `env: { ...process.env, ...opts.env }` compiled to `env: { ...opts?.env ?? {} }`, so every child process spawned through the command runner — Agent Tools, board backend scripts — was started with the parent environment stripped, including PATH. Fixed by adding `ssr: { target: "node" }` and `build.ssr: true` to the main config in both scripts; the dev log now reports "building ssr environment" and the bundle has zero `{}.X` accesses with `...process.env` restored. Verified end to end: with the flag set, the MCP endpoint now serves exactly one tool (`call`) while all 13 guide resources stay listed. |
| US-1351 | `call helpSearch(...)` opened the MCP Log page as a side effect | — | **Reported by the user**: running the `call` tool with path `helpSearch("add rows")` returned the right answer and also opened and focused the "MCP Log.log.jsonl" page, every time. The cause was not in `helpSearch` and not in MCP request logging. `helpSearch` walks the descriptor graph through every `node: true` property and every declared child, and its own contract says the walk is "side-effect free by construction: it visits only what a node declared safe" (`shared/ai-vision/help-search.ts:9-11`). `pages.logView` is declared **both** ways (`PageCollectionWrapper.ts:36` and `:121`), and its getter called `getOrCreateMcpLogViewEditor()` → `requireWellKnownPage("mcp-ui-log")`, which get-or-creates **and focuses**. So every search opened the page — and so did any plain read of `pages.logView`. Fixed by making the read/write split explicit: `getMcpLogViewEditor()` resolves the page without creating it, `LogViewEditorFacade` takes a resolver plus an optional `ensure`, and only `push`/`clear`/`toggleTimestamps` create. Host-backed state reads as `undefined` (omitted from the call answer, never `null`) until the page exists — the facade already had exactly that attached/detached concept, so the fix slots into it rather than adding one. `pages[i].editor` on a real log-view page is unaffected: it still receives a concrete host and has no `ensure`. Introduced by US-1322 (EPIC-087) at 5.0.0 and never released, so no What's New entry. Verified live: `helpSearch` in both the inline-literal and `args` forms left the page count and active page unchanged, a bare read of `pages.logView` created nothing, and `push` still created the page and reported real state. |
| US-1350 | MCP Inspector result editor did not fill the RESULT panel | — | **Reported by the user against the `call` tool's result.** The RESULT panel had 583px free while each result item sat at 40px — measured live before the fix. The break was one missing link in the flex chain: `ToolResultView`'s root grows and `TextResultView` inside it declares `flex: true`, but the `ResultItemView` between them was created with no flex at all, so it resolved to `flex: 0 1 auto` and took its content height. `MonacoEditorHostView.css` sets `height: 100%`, which against an auto-height parent is nothing, so the editor collapsed onto `TextResultView`'s 40px floor. Fixed by marking a result item that hosts a Monaco editor with `data-grow` and giving it `flex: 1 1 0; min-height: 0` in the editor's co-located CSS — image and resource-link items keep their natural size, so a one-line link never claims half the panel. The `resource`-with-text branch had the same collapsed inner panel and was fixed with it. Present since the vanilla conversion in EPIC-071 and shipped in 4.0.23, so it carries a What's New entry. Verified live: two result editors filled a 222px panel exactly, followed a resize to 430px (108→212px each) and returned. |
| US-1342 | pages.showPage silently accepted an unknown page id | — | **Found while verifying EPIC-089's `window.screen` privacy gate**, whose refusal message tells the agent to "activate a non-private page with `pages.showPage(pageId)`, then retry". Testing that advice showed the call was a no-op for an id it did not recognise: `PageCollectionWrapper.showPage` forwarded straight to `pagesModel.showPage`, which ignores an unknown id, so a mistyped or stale page id returned quietly, left a different page active, and the caller went on to act on the wrong page believing it had switched — and an agent recovering from that refusal would have looped forever. It now throws, naming the open page ids, per the standing rule that a node never silently accepts guessed input (the same failure class EPIC-087's acceptance run found in `pages.logView.push`). |
| US-1341 | The Tor info button showed on every browser page | — | **Found by EPIC-089's browser `elements` list on its first live run**, which reported `toolbar-tor-info` as visible on a non-Tor page. The element list was right: `IconButtonView.applyProps` destructured its known props and discarded the rest, so `hidden` — declared on `IconButtonProps` and passed by `BrowserView.ts:329` as `hidden: !state.isTor` — was never applied on `update()`; only `applyConstructionRestProps` applied rest props, and that runs once at construction. A "Tor connection info" control therefore rendered in the toolbar of every browser page. The matching CSS rule `[data-type="icon-button"][hidden] { display: none; }` already existed with a comment explaining why the author display rule beats the UA one — only the attribute was missing. One caller in the repo passes `hidden`, so the blast radius was exactly this button. Verified live before and after. This is the UI-regression property the transparency roadmap predicted the per-surface QA files would have, arriving on the first run. |
| US-1332 | Boards and tools acceptance run, QA files, and six retirable tools | EPIC-088 | The epic's gate: Haiku, `call` only, no guides - **passed with no wrong turns**, the first surface epic to do so. It found `boards` and `tools` from the root hint and reached `page.editor.reload()` and a tool's full argument list unaided. Two call-wide defects fixed first, neither catchable by a build: **`MAX_DEPTH` was 4** in the shared result shaper, truncating every tool's `inputSchema` to `{ note: "depth limit" }` - an agent could read a tool's description but not learn how to call it, making the replacement strictly worse than `search_tools`; and **a key set to `undefined` reaches an agent as `null`**, so absent optionals must be omitted. `execute_tool` **withheld**: its rows need a tool that actually runs, and every registered toolset here calls a live company service with the user's credentials while a scratch one needs a trust-dialog click that an agent must not take on its own behalf. Log: `qa/runs/2026-09-06-epic-088-boards-and-tools.md`. |
| US-1331 | Mneme config and Mneme root | EPIC-088 | Two facades and the second missing page opener - `showMnemeConfigPage` existed on the model but was never declared, so an agent could not open the screen. The boundary is stated rather than implied: these describe the configuration and browsing screens, and the Mneme MCP server remains the document API. Everything destructive or expensive carries a caution (removeRoot, reindex, setRootConfig, updateModel, restart). `addRoot` is deliberately not a method - it needs a native folder picker and an input dialog, so the facade points at the control instead of offering a call that cannot honestly complete. No credential returned; the transport token is omitted. |
| US-1330 | The MCP Inspector surface | EPIC-088 | The one surface that already had a facade, describing the connection and saying nothing about the three panels the user reads. Added Tools/Resources/Prompts state and curated elements - and closed a pre-existing privilege hole: `command` and `args` were **writable**, so an agent could set a command line of its own choosing and call `connect()`, spawning a process with the user's privileges and no dialog. Both setters removed. The plan had also proposed removing `url` and `showMcpInspectorPage({ url })`; that was **rejected in review** as overreach - an address is not a credential value, the same reasoning would condemn `openFile(path)`, and pointing the Inspector at a server is the whole purpose of the surface. The url setter now enforces the credential-free claim instead. The existing `""`-when-disconnected members were checked against the model, found correct, and left alone. |
| US-1329 | The toolset editor and the Tools hub | EPIC-088 | Two facades plus `pages.showToolsHubPage({ tab })`, which PagesModel had all along but had never declared on the wrapper. Neither facade re-projects the registry: US-1328's `tools.toolsets[...]` is the canonical manifest and tool-list node, and these surfaces describe the screen and point at it. Two projections of one registry would have drifted the first time a manifest field changed. |
| US-1328 | The `tools` root node | EPIC-088 | The Agent Tools registry was invisible to `call`; four MCP tools were its only access path. `tools` claims the root name reserved for it since EPIC-083 (not the roadmap's `toolsets`). Two contracts carried across rather than reinvented: the **self-repair** failure shape, so a broken tool can be fixed at its `toolsetRoot` instead of routed around, and **trust** - `createToolset` offers registration through the user's dialog and never grants it. Refresh is whole-registry only because the registry ignores the tool's `path` argument, so a per-toolset `refresh()` would silently refresh everything. Two review corrections mattered: the unknown-id throw went in the **wrapper**, not `executeToolById`, which would have changed the legacy `execute_tool` contract just before the run that compares them; and `validateArgs` stays **non-blocking** as its own contract and the guide both promise, surfacing advisory `argWarnings` instead, so a tool that runs through the tool still runs through the path. |
| US-1327 | The Board Info surface | EPIC-088 | A facade for the install and properties modes. The install lifecycle is deliberately not duplicated - `boards.*` owns download/install/uninstall and this reports what the screen shows. The secret audit was run **separately** for the two questions the screen raises, as EPIC-087 decision 7 requires: the editor is a content host so `pages[i].content` already returns the adopted file's text and the facade claims no protection it cannot enforce; board environment variables are a different matter and became the epic's first Needs-user-check, since `app.boardVars.get()` already returns stored credential values to an agent unrestricted - pre-existing, and a user-owned decision. Conditional controls verified live: on a locally trusted board only Open and Unregister resolve visible. |
| US-1326 | The `boards` node completes - local enumeration | EPIC-088 | `boards` had fourteen members that all take a root path and **no way to discover one**. `boards.list()` returns the merged local inventory - trusted roots, installed roots with version, and the page ids of open boards - keyed by the value `openBoard()` takes. Deliberately local; `searchPublished()` stays the remote catalog and every summary now says which is which. **No network call**: `getBoardUpdate()` is synchronous over the in-memory catalog, and when that catalog has not loaded update availability is reported as *unknown* rather than a fabricated `false`. Reading never provisions - the registries' own `load()` can create a trust file and persist a pruned list, so the read path uses parsers **extracted** from them rather than copies. Live verification caught absent keys arriving as `null` across the MCP boundary; absent optionals are now omitted, a rule the rest of the epic follows. |
| US-1325 | The board page surface | EPIC-088 | Board pages answered `pages[i].editor` with `GenericEditorFacade`. Now a real facade for both `board-view` and the dynamic `board-editor:<root>` ids. `renderState` (trusted / untrusted / not-found) comes from model state and the trust registry, never from the rendered branch, with the content-host error reported separately so a failed board is not mislabelled untrusted; `restricted()` applies to untrusted content alone, since a not-found board is empty rather than private. `reload()` is the `board_refresh` path and reuses the existing frame waiter through one shared model helper the legacy handler now also calls - one ordering, not two copies. Where a board can never mount a frame it skips the five-second wait and returns the `renderState` that explains why, instead of blocking and reporting an unqualified success. |
| US-1333 | Fix: an Explorer file click did not open the file | — | **Regression from US-1323** (EPIC-087), which moved the Explorer tree's navigation out of `ExplorerSecondaryView` into `ExplorerEditor.openItem()`. The already-selected guard and the selection write moved with it, but were also left behind in the view's `handleItemClick`, so the handler wrote `selectedHref = item.href` and then called `openItem`, whose own guard (`current === item.href`) saw the value it had just written and returned before sending `openRawLink`. Every click on a file in the Explorer panel was swallowed — no navigation, no way to open or see the file. The duplicated guard and write were removed from the view; the model owns both. **Verification:** typecheck, lint and `build-prod` pass, and a real click on a tree item in the live app window navigated the page to the clicked file. The sibling views moved in the same commit (Search, Boards, Git Tree, Folder View) were checked for the same duplication and have none. Not in `whats-new.md`: the regression never shipped — it was introduced by unreleased 5.0.0 work. |
| US-1324 | Data-surface acceptance run, QA files, and the retirable `ui_push` | EPIC-087 | Ran the epic's gate - Haiku via `mcp-test-agent-call`, `call` only. **Three runs, three fixes, then a pass.** Run 1 answered the REST and notebook questions from the descriptors alone but *failed* the one that mattered: asked to show the user a table and ask a question, it never found `pages.logView`, tried to build a grid page by hand, and reported the task impossible. Two defects behind that. **(a) `call` could not assign JSON text at all** - MCP clients parse `value` as JSON, so the error's advice to "stringify first" was impossible to follow and any agent filling a JSON page was in a dead end; the resolver now serializes an object or array when the target property holds a string. **(b) the output channel described itself in implementation terms** ("the get-or-created MCP Log View writer and dialog read-back surface") - reworded to lead with purpose and, decisively, pointed at from the **root** node, since an agent asked to show the user something reads the root member list before it reads `pages`. Run 3's agent named that hint as the reason it succeeded. **(c) Run 3 then exposed a silent success:** it guessed the entry types `"markdown"` and `"dialog"`, `push` accepted them, rendered three *blank* entries and returned ids - validation only ever ran for `input.*`. `pages.logView.push` now rejects an unknown type and names the sixteen valid ones; `ui_push` keeps its lenient behaviour unchanged. `ui_push` marked **retirable**, every capability exercised live including `windows[1].pages.logView.push(...)` against a real second window. Log: `qa/runs/2026-09-06-epic-087-data-surfaces.md`. |
| US-1323 | Folder View, Git Tree, and the sidebar panel nodes | EPIC-087 | `page.panels` previously answered "the Explorer is open" and nothing about its contents. Panels are now live children resolved through the established `children()`/`index()` pattern, so `page.panels.explorer` works as a bare member, and each node's elements are double-scoped by page id *and* the panel's own collapsible-panel root - two panels on one page cannot answer for each other. Nodes exist for every panel earlier tasks deferred here (notebook categories/tags, REST, archive, file history), so no cross-reference this epic wrote is left dangling. **Reading never provisions:** `children()` is contractually cheap and side-effect free and the `page-explorer.ts` helpers are off the read path, so looking at a page cannot grow it a sidebar - the trap `page.grouped` fell into. Git is read-mostly per decision 9: the commit/stage/push/pull controls are declared as elements so an agent can point at them, but no facade action invokes them. Verification found the git panel dumping ~170 branches and tags into `summarize()` on every read; capped to counts plus a 15-item sample. `/review` separately caught an element declared with a literal template placeholder in its name, which could never have resolved. |
| US-1322 | Log View, `pages.logView`, and the `ui_push` replacement path | EPIC-087 | The epic's headline. `ui_push` now has a complete path - get-or-create of the `mcp-ui-log` page, `push(entries)` covering every entry type, `dialogResult(id)`, and a facade with twelve curated elements answering on any log-view page. **`push` does not block, and the investigation established why it must not:** Log View dialogs are inline page entries, not modals, so the `dialogs` node - which builds from `dialogsState` - cannot reach them, and the `pending` + `dialogs[0]` mechanism that rescues every other blocking call does not apply. Unresolved is distinguished from resolved-but-falsy by the model's existing `isDialogResolved()` test (`button !== undefined`), not truthiness; `ui_push` maps an unanswered button to `null` and the new path deliberately does not inherit that. **Two errors in the epic's own decision 5 were caught here:** `UiFacade` is the script global `ui`, not `app.ui.log`, and the proposed `app.ui.log` was then rejected on merit - three names for one surface, naming nothing the user sees. It also fixed a real split-brain bug: in MCP mode `initializeUiFacade` could create a random-id log page, so a script's `ui.log` and an agent's `ui_push` could write to two different Log Views. |
| US-1321 | Env vars and archive facades | EPIC-087 | Two surfaces with no facade at all. Decision 7 was applied *separately* to each, as the rewritten decision requires, and the answers differ. Env vars is a content-host editor, so an unlocked file's values are already in `page.content` and the facade claims no redaction - but while `status` is `"locked"` it returns no variables rather than the stale decrypted data it still holds, and "locked" is reported as distinct from "empty". The archive has no content host and no password anywhere in its model, so *not* exposing entry bytes is a scope decision and the document says so rather than calling it a boundary. Unlocking reuses EPIC-086's `showEncryptionDialog` name; a `call` to it returns `pending` with the dialog in `attention`. The archive's tree-click handler moved out of both views into one model path - and that move had to be corrected by hand: it had been *reimplemented* rather than moved, rebuilding the inner path itself and so always taking the directory branch, dropping the sidebar's selection update, and throwing where the views returned. |
| US-1320 | The REST client surface | EPIC-087 | 64 named controls curated to 21, plus the request collection, selected request, response state and model-backed actions including an async `send()`. **The plan's redaction design was killed by live verification:** rest-client is a content-host editor, so `pages[i].content` already returns the whole `.rest.json` - URL tokens, `Authorization` values and body included - one path segment away on the same node. A facade redacting those fields would protect nothing while telling the agent a protection exists, which is worse than exposing them. The other half of the rule stands on independent reasoning and is unchanged: no member *accepts* a secret, because an argument is written into the call transcript, which is a place the secret was not before. EPIC-087 decision 7 was rewritten around that asymmetry, and the page-level boundary it raises is recorded as Needs user check 1. |
| US-1319 | The notebook surface | EPIC-087 | Eleven curated page-scoped controls - five singleton, six rendered once per note - plus filtered notes, search, selected category and tag, expanded panel and note, and the parse error. `INote` gained language, editor, comment and both dates, read from `note.content` where they actually live. **The repeated controls are declared honestly rather than omitted:** their purpose text says they occur once per note, and the facade passes `highlightOptions: { all: true }` so highlighting rings every instance and reports `count` and `highlighted` - verified as 2 and 2 on a two-note notebook. Without that flag the overlay rings only the first match, which would have pointed at one arbitrary note while the text promised every note. Note execution is deliberately unexposed: its models are created and disposed by virtualized rows, so a facade action would reach into view logic or run the wrong note. |
| US-1318 | The grid surface | EPIC-087 | Nine curated page-scoped elements across the toolbar and both option popups, plus the state the toolbar shows but the facade could not report - search text, sort, filters, cell-range selection, hidden columns, displayed row count and the CSV options. Both popups are portaled to the overlay layer, so each popover root now carries the owning page's `data-page-id`. **Absence is attached-host-only:** the plan had keyed it on row count, which would have made a legitimately empty grid answer `undefined` to every question - dishonest in the opposite direction. Live verification caught a defect a green build could not: UIKit views *delete* `data-name` when a later `update()` omits the `name` prop, so the three new CSV control names were set on mount and stripped on the first re-render, leaving three declared elements that could never resolve. Plan review also removed an `updateColumn` that reused a view component's validation path, the clipboard members, and the focus/selection actions that would have queued silently against an unmounted body. |
| US-1317 | Editor-surface acceptance run, QA files, and the retirable tools | EPIC-086 | Ran the epic's acceptance gate — Haiku via `mcp-test-agent-call`, `call` only, two markdown pages open so an unscoped selector would answer for the wrong one. **Passed both questions.** One finding acted on: the markdown `$help` never said the preview re-renders automatically on content change, so the agent burned two `helpSearch` calls ("refresh", then "render") hunting a control that does not exist and had to infer the answer from its absence — true, discoverable, and nowhere written down. `create_page`, `get_page_content` and `set_page_content` marked **retirable**, each path exercised live first. **`open_url` deliberately not marked, and the roadmap's premise corrected instead:** it claimed this epic retires the tool "for non-browser targets", but the handler calls `openUrlInBrowserTab` unconditionally with no test of URL kind, so a URL pointing at an image lands in a browser tab and the content pipeline is never consulted. There is no half for the editor epic to replace; it belongs wholly to EPIC-089, which now carries the two facts it needs (the planned `pages.openUrl` does not exist; the capability lives on the wrong node as `app.openRawLink`). Log: `qa/runs/2026-09-06-epic-086-editor-surfaces.md`. |
| US-1316 | The graph surface | EPIC-086 | 33 page-scoped curated controls across the main toolbar, force-tuning and expansion panels, detail overlay and legend; seven names added. Plan review resolved three open either/or questions the document had left (detail edits stay UI-only via `page.content`; image actions **are** added, routed through the mounted view; no `detail`/`legend` summary members) and required the seven new names to come from explicit key-to-name maps rather than tab indices, since the search-results tab is conditional and would shift them. **Stops at the canvas:** the graph body is one `HTMLCanvasElement`, so no per-node selector is declared — declaring an element that can never be found is worse than declaring nothing. Detail and legend are editor-internal overlays, not `page.panels` registrations, on three independent lines of evidence. Verified live: `graph-detail-panel` correctly reports `visible: true` while dimmed and empty, which `offsetParent` makes true and which the QA file now asserts deliberately so it is not filed as a bug. |
| US-1315 | Diff — the file-diff facade, and compare mode on `pages` | EPIC-086 | File diff had no facade and fell through to `GenericEditorFacade`; it now answers `from`, `to`, `hasStaged`, `readOnly` and its four controls, cross-referencing the git-revisions panel on `page.panels` rather than re-listing it. Compare went to `pages.compare` because it is a property of a *pair*. The investigation caught that the epic's cited path had moved and established with evidence that `CompareEditor` mounts only when `compareInfo.leftId === page.id`, so its DOM lives in the **left** slot alone — elements scope there and `highlight` activates the left page, never the right. Plan review found a fabricated "Before" snippet quoting a `FACADE_FOR_EDITOR` key `"text-editor"` that does not exist (the real key is `"monaco"`), and that `enter()` would have returned `PagesLayoutModel`'s bare `false`, which covers two unrelated causes; it now preflights both and throws a diagnostic naming which failed. Verified live: an ungrouped page gives "no grouped pair exists", and a real git file answered a commit hash and `unstaged`. |
| US-1314 | Media — the image surface, and a new video facade | EPIC-086 | Image gained its three curated controls and the save/drawing/clipboard actions its toolbar already had, sharing one helper with the viewport. Video had **no facade at all**; it now has a registered read-mostly one with ten controls and live playback state. **The plan review's main correction was the live-media bridge:** the plan read playback state with a `data-part` DOM query branching on `format`, which couples a scripting facade to a view-internal attribute nothing declares — and whose failure to match is indistinguishable from nothing playing. Replaced with `VideoEditor.setMediaElement`, following `MarkdownEditor.setContainer` and `TextFileModel.setEditorOverlayRef`; the format branch and the read-scoping question both disappeared. Counts corrected against source (image 9 to 3, video 14 to 10) by excluding structural roots, status labels, gestures and generated native media controls. Reads never activate a page, so playback members carry a caution that audio may start from a page not on screen; `shuffle` and `visualizerEffect` are documented as global settings, not per-page state. Verified live on a pinned audio page. |
| US-1313 | The preview family — markdown, HTML, SVG and mermaid | EPIC-086 | 7/4/4/6 curated controls plus the facade members each view already implemented but never exposed. **HTML declares host chrome only:** its body is a sandboxed `srcdoc` iframe with an opaque origin, so `querySelectorAll` cannot cross it, and `$help` points at the automation surface EPIC-089 will hang on the same node. The SVG and mermaid clipboard/drawing actions moved out of toolbar view callbacks into the models so the toolbar and facade call one implementation; members needing the mounted body view throw a diagnostic rather than resolving silently. `/review` later caught that `html`/`svg` still returned an empty string when detached — indistinguishable from a genuinely empty document — and they now return `undefined`, with `viewMounted` added to HTML and SVG; mermaid's `svgUrl` keeps the empty string deliberately for "not yet rendered" and says so. Verified live: `openSearch()` flipped the four `find-*` entries from invisible to visible in the same `elements` read, so the surface works as a UI regression test as well as an agent surface. |
| US-1312 | The Monaco/text surface | EPIC-086 | `TEXT_ELEMENTS` grew from 4 to 12 (footer script toggle plus the seven script-panel controls), and the facade gained encryption state, file save/rename, script execution, script-panel state and a native Monaco find/replace bridge added as two new `MonacoQueueEvent` members. **Two plan-review corrections shaped the result.** Every getter reading through `MonacoEditor.host` (`TextFileModel | null`) returns `undefined` rather than `false`, which would assert "not encrypted"; and **no member accepts a raw password** — the plan had proposed `encrypt(password)`/`decrypt(password)` while simultaneously saying to keep passwords private, and a member that *accepts* one writes a secret into agent call arguments and MCP transcripts. Only `showEncryptionDialog`, `encryptWithCurrentPassword` and `makeUnencrypted` are public. Verified live on a JavaScript page: 12 scoped declarations, two visible, ten correctly false. |
| US-1310 | The page node redesign — `page.editor` as the current facade | EPIC-086 | Removed thirteen `as*()` methods from `PageWrapper`, the typings, the guides and the docs, replacing them with `page.editor` as the **current** editor's facade — a discriminated union on `id` — plus `editorSwitches`, and keeping `panels`. The old shape was the fossil of a single-text-host architecture Persephone no longer has: the page's main model *is* the editor model, so `asGrid()` was a redundant segment in front of the thing the caller wanted. No compatibility was kept (there are no scripts to protect), which made the release **5.0.0**. Plan review produced three amendments: the switch widget does not use `editorRegistry.getSwitchOptions()` but merges `findCompatibleEditors()` with board editors and the board-info entry, so that merge was extracted into a shared helper and `options` is exactly what the user sees; `switchMainEditor` returns normally *without switching* when the user declines the release prompt, so `switchTo` verifies the editor id after the await and throws; and `page.editor` is never `undefined` — an editor with no operation facade returns `GenericEditorFacade` carrying `id` and `name`. Every operation facade's `summarize()` gained `id` and `name` afterwards, since twelve of them answered without saying which editor they were. **Reviewed mid-epic**, deliberately, so the node was settled before seven tasks built on it. Two incidents recorded: the MCP call to Codex timed out after 30 idle minutes while Codex kept working (watch the session log's **size**, not its mtime — Windows does not update mtime while the writer holds the file open), and Codex's bulk PowerShell edits double-encoded every file they touched, churning 2,803 + 446 lines that were repaired mechanically. Every later thread in the epic was instructed to use `apply_patch` only, and none recurred. |
| US-1311 | Page-scoped elements, activation, and `page.tab` | EPIC-086 | Added `data-page-id` to the page slot and tab as an identity attribute, so every page-owned selector addresses its own page rather than the first matching `data-name` in the document — the silent-wrong-target defect EPIC-085 found, in a new place. `highlight` on a slot-hosted surface activates the page and waits for its retained slot's layout box before drawing, while `elements.visible` keeps its literal meaning. Added `page.tab` as the page's own tab-strip entry. The plan review's one finding was real: the overlay scrolls its target with `inline: "nearest"` unconditionally, which would have parked a centred tab behind the sticky pinned tabs, so the tab node passes `{ scroll: false }`. Reviewed at epic close. |
| US-1309 | Explorer file icons vanished as rows scrolled in and out | — | Pooled `TreeItemView` rows now refill cached icon and trailing nodes unless the node is still parented by the current host; identity alone was not sufficient when pooling moved a node to another cell. Verified live through eight scroll-out/scroll-in cycles with zero empty `.tree-icon` hosts. `/review` found no concerns; `/document` updated the component guide; `/userdoc` added the bug fix to `docs/whats-new.md`. No tests or commit performed. |
| US-1308 | Sidebar opened on a page that started without one rendered on the right | — | `PageContentView` now inserts a late-created sidebar before `contentRoot`, preserving append behavior when no content root exists. Verified live with the sidebar at x=0 and the editor following it. `/review` found no concerns; `/document` updated the pages architecture guide; `/userdoc` added the bug fix to `docs/whats-new.md`. No tests or commit performed. |
| US-1288 | Link panel highlight stopped following the auto-advanced track | — | User-reported, with the mechanism correctly guessed ("LinkEditor used to detect the page navigation and select the new link"). A **real De-React regression.** Auto-advancing a track navigates the page, which *replaces* the VideoEditor: the page adopts a fresh instance and the old one is detached (`this.page` → null) and then disposed, all before its own post-navigation callback would run. EPIC-077 (`abf988fa`) had moved that callback from a bare `requestAnimationFrame` guarded only by `if (!page) return;` onto `this.schedule.raf` behind `this.navigationGeneration !== navigationGeneration || this.page !== page`, both of which are tied to the dying editor — so it either never fired (highlight stuck on the first clicked track) or, when disposal won the race, threw `Cannot register a cleanup on a disposed DisposableStore` as an unhandled rejection with an alert popup. Restored the unowned `afterPaint`; deleted the now-dead `navigationFrame`/`navigationGeneration` fields and their `dispose()` cleanup. The comment records **why** it must stay unowned — the callback closes over `page`, `itemHref` and `sourceId`, touches no editor state, and re-reads `page.panelEditors` (empty once the page is gone) — so a later lifecycle-hygiene pass does not re-adopt it. Diagnosed and verified live over MCP on the user's own music page: before the fix, `playNext()` gave `mainIsSameObject: false`, `videoPageStillSet: false`, zero `selectByHref` calls and an unchanged `selectedLinkId`; after, `selectedHref === url` with no rejections. The downstream chain was proved intact separately by calling `selectByHref` directly (selection moved, one highlighted node). Tags rides the identical `sourceId === "link-tag"` branch. `/review` passed with no findings; `/document` needed no developer-doc change; `/userdoc` added the 4.0.24 release note. **User confirmed the fix.** |
| US-1287 | HTML preview blanked in a packaged build when the page rewrote its history URL | — | User-reported as a dev-vs-release difference: an HTML mockup rendered in `npm start` and in Persephone's browser, but showed an empty grey screen in the HTML viewer of the installed build at the same commit. The preview iframe is sandboxed without `allow-same-origin`, so the previewed document has an opaque origin and the URL `about:srcdoc`, and rewriting the history URL from there is refused — but **how** Chromium refuses depends on the host document's scheme: over http (the Vite dev server) it throws a catchable `SecurityError` and the page carries on, while under `file://` (a packaged build) it destroys the frame — no `load` event, no `error` event, a blank grey rectangle. The mockup's init ends in `history.replaceState(null, "", "#" + name)`, so it rendered in dev only. Fixed with a new injected prologue that no-ops `replaceState`/`pushState` when `location.origin === "null"`, inserted after the doctype by a new `withInjectedScripts` helper so the preview stays in standards mode. **Not a De-React regression** — `sandbox="allow-scripts"` + `srcDoc` is unchanged since v4.0.22 (`HtmlBody.tsx:66`), so every packaged build has had this. Diagnosed live in the installed 4.0.23 renderer: bisecting the document isolated the final init script rather than its size (a 458KB padded document loads fine), and hosting the identical guest from a throwaway `http://127.0.0.1` page inside Persephone's own browser reproduced the dev half — `Uncaught SecurityError … origin 'null'` followed by a normal `load` — which is how the scheme was confirmed without starting the dev server. Fix verified the same way (`load`, 19 body children, `compatMode=CSS1Compat`); end-to-end confirmation still needs a packaged build. `/review` passed with no findings; `/document` needed no developer-doc change; `/userdoc` added the 4.0.24 release note. **Known limitation, unchanged by the fix:** a hash-routed page always opens on its default tab in the viewer, because `location.hash` is empty in an `about:srcdoc` document; the durable fix is serving previews from a real origin through a custom protocol as boards do, offered to the user and not taken up. |
| US-1286 | Host menus did not close when clicking inside a browser page | — | User-reported: with a browser page open, the page-tab context menu, downloads menu and the toolbar's three-dot menu stayed open when clicking in the page, and the guest covers nearly the whole tab so there was barely anywhere left to click to dismiss. `PopoverView`'s outside-click listener is on the host `document`, which a press inside the `<webview>` guest never reaches. The guest preload now reports presses on `guest-pointerdown` (capture phase, no `defaultPrevented` deferral — a guest press is outside every host overlay regardless of whether the page also handles it) and `BrowserView` replays it through a new shared `dismissOverlays()` in `uikit/shared/overlayLayer.ts`. **Not the De-React regression it looked like:** v4.0.22 listened on `document` `mousedown`, which a guest press does not produce either, so the browser case was equally broken in the last release — it earned a release note. The adjacent regression is real and shares the fix: the HTML viewer's iframe has pinged the host since US-729 and its handler dispatched `mousedown`, inert since `PopoverView` moved to `pointerdown`. Verified live (menu open → guest pointerdown → overlay layer 0); the remaining link, that a real mouse press fires `pointerdown`, is a platform guarantee and no ordering subtlety is involved. **`/review`, `/document` and `/userdoc` skipped by user decision** — a self-contained bug fix. **Known gap:** the preload is main-frame only, so a press inside a cross-origin iframe in the page still does not dismiss (same boundary as US-1284). Two instrument traps recorded: `browser_click` calls `element.click()` and produces no pointer events at all (measured pointerdown=0 click=1), and the running app paired a dev-watched preload with a prod-built renderer, so the preload change applied live while the renderer change did not. |
| US-1285 | The sidebar show-in-main-view chevron is hidden while a panel is collapsed | — | Removed the unintended `expanded` gate from the show-main zone callback in Git, Collections, and Wiki secondary views, restoring the always-available chevron while panels are collapsed. Verified live by the agent over MCP — the collapsed Git and Collections panel headers both carry the button (31x31, in-DOM) — not yet confirmed by the user. `/review` found no code concerns, confirmed the `expanded` registry comment remains accurate, and identified the stale gated example in `secondary-views.md`; `/document` corrected that example and clarified that `expanded` applies to body-only actions; `/userdoc` updated `docs/editors.md` and added the Version 4.0.23 release note. Deliberately not changed: the separate `expanded` gates on Git refresh/close, Collections save, Wiki close, Archive close, and File History refresh, pending a user decision; only Boards' switch-bar gate matches the known intent. No tests or commit performed. |
| US-1281 | Paste any rich HTML into the HTML viewer, not only image-bearing HTML | — | Broadened the rich-HTML clipboard fallback to any markup-bearing HTML and moved it to the bubble phase so paste-owning components win first; bitmap capture remains the capture-phase path, with handled, editable, and grid stand-downs preserved. User verified the shipped behavior. `/review` found no implementation concerns and identified stale “image-bearing HTML” wording; `/document` corrected `doc/architecture/folder-structure.md`; `/userdoc` clarified `docs/editors.md` and `docs/shortcuts.md` and added the Version 4.0.23 release note. No tests or commit performed. |
| US-1284 | Let the page claim Ctrl+F and Escape in the browser editor | — | Removed the main-process `before-input-event` branches that preempted page `Ctrl+F` and `Escape`; the preload now makes a per-keystroke decision from `defaultPrevented` in a deferred `setTimeout(0)` task, preserving page-first handling for real browser-dispatched events while retaining cinema-mode Escape behavior. Fixed `e.key.toLowerCase()` handling so `Ctrl+Shift+F` and Caps Lock reach the browser shortcut. `/review` found no implementation concerns and removed two stale Layer 1 statements from `browser-editor.md`; `/document` normalized the architecture heading and added browser-service, preload, and webview-model ownership to `key-files.md`; `/userdoc` documented page-first `Ctrl+F`/`Escape` handling in `docs/browser.md`, `docs/shortcuts.md`, `docs/editors.md`, and `docs/whats-new.md`. Known gap left documented: focus inside a cross-origin iframe, because the preload is main-frame only. No tests or commit performed. |
| US-1283 | Replace `SecondaryViewsView` state props with the model and named commands | — | Replaced the state snapshot props with `SecondaryViewsModel` and stable `onActivatePanel` / `onResizeWidth` commands; both feeding bridges now use element-wise `views` and navigation identity gates, and the browser nav-to-`version` mirror is gone. `/review` found no architecture concerns; `/document` updated the task record; `/userdoc` found no stale user-facing docs. A shared `sameItems<T>` in `core/utils/utils.ts` replaced three verbatim copies of the gate helper, and `doc/standards/model-view-pattern.md` was corrected — it had called secondary-view headers lifecycle notifications rather than ordinary child props, which the deletion of the callback protocol in US-1282 made false. Runtime-verified only that the rewired stack mounts and renders (three panels via the browser bridge, splitter present). **Interactive verification never performed, by agent or user:** splitter drag smoothness, width persistence across restart, last-panel detach closing the sidebar, `panelExpanded` driving LinkBody breadcrumb synchronization, and the performance claim itself (zero `onUpdate` calls during a drag, which needs instrumentation). Closed on review, not on test. |
| US-1282 | Retire the `headerRef` callback protocol and rename the element prop to `headerHost` | — | Deleted the unreachable callback-ref protocol and renamed the surviving element prop across secondary views and editor secondary views. `/review` found no architecture concerns; `/document` updated the task record; `/userdoc` found no stale user-facing docs. Runtime-verified live in the browser bookmarks drawer: the panel stack mounted at 240x955 with three panels whose headers were populated through the renamed `headerHost` path (Collections with one action button, Tags, Hostnames). Outstanding: collapse/expand reparenting of header actions, which needs an interactive pass. |
| US-1257 | Two browser internal-tabs panel defects | — | **Origin:** both user-reported, with a correct diagnosis attached to the first ("looks like tab content has flex-direction: column instead of row"). **(1) The collapsed-panel hover strip laid out vertically.** `TabExtensionView` adopts the `PopoverView` root as its own host, so the host already carries `.popover-shell`, which declares `flex-direction: column` and — because `scroll` defaults to true — `overflow: auto`. `[data-tab-extension]` declared `display: flex` but no direction, so the shell's `column` won: the title stacked above the close button inside a 28px-tall box and the spill became a scrollbar instead of being clipped, which is why the title read as `..`. Fixed by declaring `flex-direction: row` explicitly and passing `scroll: false` — the strip is a fixed 140×28 box that clips its own title and must never be a scroll container. **(2) The “+” add-tab row neither looked nor behaved like a tab.** It was an `IconButtonView` inside a left-justified `createPanelElement` row, so the row had no tab styling and hovering highlighted only the 24px glyph. **The row is the button now** — a `<button>` carrying `data-tab-item` (inheriting the tab geometry, radius and hover background rather than restating them) plus `data-add-tab` (centres the glyph at every width, `::before { content: none }` to drop the group bar, a `:focus-visible` background, glyph sizing). The nested icon button is gone, the click is bound on the row, and `sync` toggles `data-compact` on it alongside the tabs so its margins track theirs. `background-color: transparent` went on the shared `[data-tab-item]` base rule, not the add-tab rule, so it neutralises the UA button background **without** outranking the `:hover` highlight — the two selectors have equal specificity, so a later declaration would have silently killed the hover. `data-name="add-tab-button"` is preserved and now sits on the real click target. Verified live at three panel widths (200/120/40px): geometry identical to a real tab with the glyph centred in each; the row matches `[data-tab-item]:hover` but not `[data-tab-item][data-active]`; one `row.click()` creates exactly one tab (no double-fire from a nested handler) and leaves `document.activeElement` unchanged. **Completion:** Codex's `/review`, `/document` and `/userdoc` all reported no changes needed; two of those held up on check, one did not. `uikit/CLAUDE.md` enumerates “the price of adoption” for a popover `contentView` that adopts the host — three writes the shell reasserts — but omitted that adoption also inherits the shell's **layout**, which is the whole of defect 1; added as a fourth item. And `docs/whats-new.md` → Bug Fixes carries fixes of exactly this granularity, so both defects got entries there. |
| US-1256 | Adopt av-grid 2.6.1 | — | Upgraded the pinned av-grid dependency and lockfile, exposed the updated DataGrid callback/type surface, adapted Grid Editor's single-sort declaration with the readonly-aware `isSortList` predicate, and took the retained-cell pool fix from av-grid 2.6.1's `RenderGrid`. The intentional `CALLBACK_OPTION_KEYS` derivation, untouched `keepCellsAttached` call sites, and existing verification were reviewed clean. `/document` made no developer-doc changes; `/userdoc` added the rapid-scrolling stale-row fix to `docs/whats-new.md`. |
| US-1254 | Split agent instructions | — | Moved shared agent context to `doc/agents-common.md` and canonical review/document/userdoc skills to `.agents/skills/`, with thin Claude wrappers. Documentation/agent-instruction-only, with no runtime changes; `/review`, `/document`, and `/userdoc` made no additional changes. |
| US-1253 | The tag suggestion popover would not reopen without clicking away first | — | **Origin:** user-reported, adding several tags in a row in the Link editor. **Cause:** `PathInputModel` opened the popover from `onInputFocus` and `onInputChange` only, and `selectSuggestion` closes it on a leaf pick **while deliberately leaving the input focused** — so the next click produced no `focus` event and nothing else reopened it. Fixed with an `onInputClick` handler wired through `PathInputView`'s `inputProps`. Placed in `PathInput`, not `TagsInput`, because `PathInput` owns the popover and the notebook's category/tag inputs — the other consumers — want the same gesture. Checked against the popover's own dismissal logic first: `outsideClickIgnoreSelector: '[data-type="path-input"]'` already excludes the field from outside-click detection, so open-on-click cannot race a close. **A latent defect was fixed alongside, because the multi-add flow makes it reachable:** `selectionMade` was set by a leaf selection and cleared only by the blur timer, and `onInputBlur` bails out while it is set — so a value *typed after* a selection was silently dropped on blur. A keystroke now clears it, since it begins a fresh edit session. Also added `onClick` to `InputView.updateTargetedRestProps`; it was applied at construction only, which is harmless for a stable model handler but a trap for the next caller. Verified live on a mounted `TagsInputView`: focus → open, pick `beta` → closed with the tag added, click the field again → **open**; then typing after that selection and blurring committed the value rather than dropping it. |
| US-1252 | A light focus ring appeared around the main editor area | — | **Origin:** user-reported, with a correct diagnosis attached — "not editor but some main editor container". **Cause:** Chromium's UA `:focus-visible` rule (`outline: auto 1px`, painted in the platform accent colour, which reads white-ish on the dark theme). The app has **no global focus-ring reset** — the only `outline: none` in `global-styles.ts` is scoped to `.monaco-editor` — so any focusable container without its own rule shows it. `TextChromeView` sets `tabIndex = 0` on its root panel, the chrome shared by the text-based editors, which is why it appeared across different editors; `MarkdownBodyView` sets `-1`. Both are `.panel-root`. It looked intermittent because `:focus-visible` does not match a plain mouse click. Fixed on the layout primitive: `.panel-root:focus, .panel-root:focus-visible { outline: none }` in `uikit/Panel/Panel.css`. A Panel is a layout box and never a control, so the rule cannot reach a real one — Button/Input/Checkbox keep their own affordances and the app's `[data-focus-selection]:focus-within` / `selectionStyle: "focus"` styling is untouched. Deliberately **not** a global `*:focus-visible` reset, which would strip affordances keyboard users depend on. `.browser-root` needed its own copy: it is a plain div rather than a Panel, and the browser is also a main editor area. **Measurement note:** a blurred window paints no focus ring at all, which silently invalidated the first probe and produced a false negative until the user pointed out Persephone was inactive. **Left ringing, deliberately:** the `.tree-chevron` buttons (real `<button>`s, where a ring is arguably an affordance), `[data-type="note-item"]`, and the MCP inspector's resource list host — all inner widgets rather than the editor container this task is about. |
| US-1251 | Refuse app-window automation while a private browser page is active | — | *(enhancement, privacy — user-requested.)* The incognito/Tor block in `getTarget` guarded the *browser* target, but `pageId: "app"` returned `appTarget` before ever reaching it — so an agent barred from driving a private session could aim at Persephone's own window and read that session off the rendered UI. **The guard covers the whole app target rather than just the two capturing commands**, and the reason is the deciding design point: `browser_take_screenshot` captures the private page's pixels, and almost every app-window command — `browser_click`, `browser_hover`, `browser_type`, `browser_press_key`, `browser_wait_for` — returns a full-window `snapshot()` as its *result*, so gating only `browser_snapshot` and `browser_take_screenshot` would have been bypassed by a single click. Only the **active** page is checked: an inactive private page is `display: none`, so it is in neither the snapshot nor the screenshot — which is also what makes the suggested remedy honest rather than cosmetic. The error names the mode and points at `list_pages` + `app.pages.showPage(pageId)`. Verified live with an incognito page active: `browser_snapshot`, `browser_take_screenshot` and `browser_click` on `pageId: "app"` all refused; after activating a markdown page the snapshot succeeded; with a *normal* browser page active `browser_click` reached the DOM ("Element not found"), so normal-mode automation is untouched. **Not exercised live:** Tor mode (needs `tor.exe` and a live circuit) — it shares the identical predicate. **Unaddressed by design:** `execute_script` can still read private-session state. |
| US-1250 | Incognito and Tor browser pages leaked the active tab's title | — | *(enhancement, privacy — user-reported.)* The agent was already barred from driving these sessions and `list_pages` already withheld their `url` (`toPageSummary`), but the Persephone **page title** still tracked the active tab, so `list_pages` named the site being viewed. Added `browserPageTitle(flags, pageTitle)` beside `getPartitionString` and routed all four assignment sites through it: `BrowserEditor` (post-restore, `applyRestoreData`, live tab updates) and `BrowserTabsModel.syncTopLevelFromTab`. Suppressed at the point of assignment rather than masked at the MCP boundary, so one rule covers the page title, the tab strip and the tool output, and no new caller can bypass it. The browser's *own* tab labels are untouched — they read `pageTitle`, which still tracks the page, so the user can still tell which tab is which inside the session. **Ordering trap:** in `applyRestoreData` the privacy flags are copied from `data` *after* the tab restore (the Tor branch replaces the tabs, so it cannot move earlier), so that one site reads the flags from `data`, not from the draft state, which would still hold the pre-restore defaults. Verified via `list_pages` with two pages on the same URL: normal reported `Example Domain`, incognito reported `Browser` with no `url`. Tor shares the identical predicate but was not exercised live. |
| US-1249 | Double-click a folder in the menu's right-hand file tree opens it in an Explorer page | — | *(enhancement, user-requested alongside US-1248.)* Pure wiring: `TreeProviderViewModel` already declared **and dispatched** `onFolderDoubleClick` for directory rows, but nothing consumed it. `MenuBarView` now passes it at both prop sites, sharing a new `openFolderPathInTab` helper with the saved-folder path so both gestures land on one behaviour. Verified live: double-clicking `_temp` under `projects` opened an Explorer page rooted at that folder. |
| US-1248 | Double-click on a saved folder in the App side menu did nothing | — | **Origin:** user-reported. **Cause was one layer deeper than the symptom.** `MenuBarView.folderRecord` passed `onDoubleClick` into `createFolderItemRecord`, which builds a plain `ListBoxView` record and **does not copy the callback**; the only `dblclick` listener for it lived in `FolderItemView`, a class nothing instantiates. So the prop was dead on arrival while looking correctly wired at the call site. The root gap: `ListBox` had **no** double-click support at all, unlike its sibling `Tree`. Added `onItemDoubleClick?: (item, index)` to `ListBoxProps`, a matching dispatch in `ListBoxModel` (skipping section and disabled rows), and a `dblclick` listener alongside the other cell listeners in `ListBoxView`; `MenuBarView` now consumes it, with the `canOpenInTab` gate moved to where the gesture actually arrives, and the dead `onDoubleClick` pass-through removed. **Two verification traps hit on the way:** `installCellListeners` is installed once per wrapper and never per render, so HMR cannot attach a newly added cell listener — the fix looked dead until a `location.reload()`. And `app.pages.all` excludes Empty pages, so the obvious page-count assertion read a flat 6 while the tab strip genuinely grew; `[data-type="page-tab"]` counts (11→12→13) were the honest measure. **Completion:** `/review` caught a defect in this change — `onItemDoubleClick` was added to `ListBoxProps` but not destructured out of the rest bag in `ListBoxView`, so it would have been forwarded onto the row element as a stray DOM attribute. Excluded there now. That is the characteristic cost of adding a prop to a primitive that forwards residual props to the DOM: the prop works, and the leak is silent. |
| US-1247 | Tree selection highlight went stale — the clicked row did not paint and the previous one did not clear | — | **Origin:** user-reported immediately after US-1246 shipped, and **caused by it.** `TreeModel.repaintSignature()` compares `isSelected` **by identity**, and the tree provider expresses selection solely through that predicate (it passes no `value` prop, so `selectedKey` stays null). Rebuilding `selectedSet` in place changed what the predicate *answers* while leaving every signature entry untouched, so the repaint gate saw nothing and `deriveRepaintChange` returned `undefined`. **US-1246 exposed it rather than introducing it:** `getTooltip` is in the signature *and* in `globalChanged`, so while it was a fresh arrow on every `applyState` it forced a full-viewport repaint that had been masking this gap all along; stabilising it removed the accidental cover. Fixed at the root rather than by reverting — reverting would have restored correct-looking behaviour by repainting all ~42 visible rows on every state change while leaving selection invisible to the gate. The predicate is now swapped for a new closure whenever the selection set actually changes, which is the channel `TreeView` documents ("selection predicate" is one of its listed global-repaint inputs), guarded by a real set comparison so unrelated state changes do not repaint everything. Verified: four consecutive clicks each leave exactly one selected row; ctrl-click multi-select and hover both unaffected. **Completion:** `/review` was pointed at `FileListView` and `OpenTabsListView`, which share this shape, and found `FileListView` genuinely affected — its `isSelected` was a stable bound field reading `this.props.selectedPath`, with no `selectionSignal`, so `ListBoxModel.repaintSignature()` (which carries `liveIsSelected` and is documented with this exact failure mode) could not see a selection move. Only the *first* selection repainted, because `selectionStyle` flips `undefined` → `"accent"` and is also in the signature; moving between files after that was invisible. Fixed the same way, by swapping the closure when `selectedPath` changes. `OpenTabsListView` was confirmed unaffected. Verified live on a mounted `FileListView`: three consecutive `selectedPath` changes each leave exactly one marked row, where the old code would have stuck on the first. |
| US-1246 | The Explorer file tree stopped showing the hover highlight | — | **Origin:** user-reported. The Tree paints the hovered row via `[data-active]` and reports the row under the pointer through `onActiveChange`, but `TreeProviderViewImpl.onActiveChange` only assigned `this.activeIndex` and stopped. `treeProps()` reads that field, yet it runs only from `applyState` — i.e. on a *model state* change — so hovering updated a field nothing read. Selection still worked because it routes through `model.setSelection`, which does change model state. **Introduced by the US-1037 De-React conversion:** the React original was `useState` + `onActiveChange={setActiveIndex}`, where the re-render re-passed the prop for free. Fixed by pushing the props update, and by making `getTooltip` a bound field — it is one of the identities `TreeModel` compares to decide on a *global* repaint, so a fresh arrow would have forced a full-viewport repaint on every `mouseenter` instead of a two-row one. All four other `onActiveChange` consumers already pushed the update, so this was the lone outlier. Also fixes the Archive tree and the link panels, which share the component. **Verification trap:** `mouseenter` does not bubble and the listener sits on the grid **cell wrapper**, not the row — dispatching on `[data-type="tree-item"]` shows nothing and looks like a failed fix. Stabilising `getTooltip` here is what exposed US-1247. |
| US-1245 | Enter in the password dialog reopened the dialog it had just closed | — | **Origin:** user-reported, via the browser's Open Bookmarks button on an encrypted bookmarks file. Typing the password and pressing Enter closed the dialog and reopened it immediately, every time; clicking **Decrypt** worked. **Cause:** `handleInputKeyDown` called `submit()` without `event.preventDefault()`. `submit()` closes the dialog synchronously inside the keydown listener, `DialogView` then restores focus to the control that opened it, and Enter's *default action* — activate the focused element — runs **after** all listeners, so it clicked the restored bookmarks button and re-ran `handleOpenBookmarks`. A mouse click has no keydown and so no default action, which is why the button worked. The text editor's decrypt icon had the identical bug but hid it: re-activation on an already-decrypted file is a no-op. The stale-password theory was checked and disproved — the dialog resolves with the full `"123"`. Corroborated by convention: all seven other Enter-to-submit dialogs already called `preventDefault()`. Verified live (`defaultPrevented: true`, one close, correct value); the browser-level double-activation itself is not reproducible from a script, since synthetic key events carry no default action. |
| US-1244 | The password dialog's Cancel button was blank and inert | — | **Origin:** user-reported — a button-shaped element beside **Decrypt** with no label whose clicks did nothing. **Cause:** `PasswordDialogView` registered the button with `this.child(cancelButton)` but never called `cancelButton.mount()`. The two are separate operations, as `component-guide.md`, `model-view-pattern.md` and `uikit/CLAUDE.md` all already state: `child()` only takes ownership for disposal, while `ButtonView.onMount()` is what applies the `data-type`/`data-variant` hooks the CSS selects on, renders `children`, and binds `onClick` via `applyConstructionRestProps`. The unmounted view therefore stayed a bare `<button>` — shaped by the panel's flex row, but unlabelled and unclickable. **Invisible to the toolchain:** typecheck and lint both pass. Escape and the title-bar close were unaffected; they reach `model.close` by other paths. A sweep of the six other bare `this.child(x);` call sites found all six mounted via a helper or loop, so this was a single omission, not a pattern. |
| US-1243 | Log View and Notebook rendered every row at the top under a 2-3x oversized scroll area | — | EPIC-079 fallout, user-reported after the epic closed. **Cause: a responsibility shift the migration missed.** The retired fork's `VirtualFlexGridView` applied cell geometry itself, so `LogBodyView` and `NotebookBodyView` never had to; av-grid's `MeasuredRowGrid` owns observation and row-height updates but delegates cell positioning to the renderer, as every `RenderGrid` renderer does. Without `applyCellStyle` the cells carried no inline `top`/`height`, fell into normal flow and stacked at the top, while the scroll area kept the model's computed extent. It slipped through because all five *fixed*-height consumers already applied it in their own renderers — a grep of the seven migrated renderers returned 2, 2, **0**, **0**, 2, 2, 2, and the two zeros are exactly the two the fork had been covering for. **Invisible to the toolchain:** the broken state typechecks, lints and builds clean; the defect is runtime geometry only. Traced live — extent growing 4700→7360 on scroll, 80 hidden/3 visible cells, cells sitting 6,485px above an empty viewport at the bottom. Fixed in both renderers; verified 0px uncovered at both ends with the extent settling at 6158px. `doc/standards/model-view-pattern.md` said the engine applied the geometry — true of the fork, false of av-grid — and was corrected at completion. |
| US-1242 | The custom hover-reveal scrollbar stopped applying after the av-grid migration | — | EPIC-079 fallout, user-reported after the epic closed. The retired fork added the `scroll-container` class to its own container inside `VirtualGridView`, in one place, so every grid inherited the hidden-until-hover treatment automatically. `RenderGrid` builds its own container, leaving the class nowhere to land. Fixed by extending the existing global rule in `theme/global-styles.ts` with a `[data-type="render-grid-scroll"]` selector, rather than adding the class at seven construction sites — which would have reintroduced the can-be-forgotten failure mode the fork did not have. Extending the selector rather than restating the declarations keeps a single definition of the scrollbar look. Applies to the DataGrid too, at the user's request. **Note:** the fix is CSS inside a TypeScript template literal — a backtick in a comment there terminates the literal and breaks the renderer. |
| US-1232 | Explorer tree jumped to the top with its first screenful unrendered | — | **Origin:** user-reported, two repro paths (activate-then-close a tab; and close an inactive tab while the tree's page stays active). **The cause was `fillSlot`, not the grid.** `fillSlot` called `host.replaceChildren()` and re-appended unconditionally, even when the host already held exactly those nodes. Re-appending detaches the subtree, and Chromium resets the scroll position of every scroller inside it **without firing a scroll event** — so `VirtualGridModel` never learned, kept `offset.y` at the old value, and went on painting rows there: scrollbar at the top, first screenful blank until a real scroll re-synced the two. `CollapsiblePanelStackView.updateContent` refills on *every* update and `SecondaryViewsView.updateRecordContent` returns a retained `lazyView.root`, so any unrelated page change re-appended the whole sidebar subtree. Fixed with `slotMatchesHost()`: the DOM work is skipped when the requested nodes are already in place, while the fill generation still advances so cleanup/`activeSlots` bookkeeping is unchanged. `DocumentFragment` deliberately never matches (appending one consumes its children); text is deliberately not compared. **A first fix was written, verified, and then reverted.** It restored the scroll offset on a hidden -> shown edge in `VirtualGridModel`, on the premise — taken from that file's own `onScroll` comment — that `display: none` resets `scrollTop`. **That premise is false, measured both ways:** a hidden element merely *reports* `scrollTop` as 0 because it has no scroll box, and Chromium hands the real value back on show (verified across a synchronous hide and a 900ms hide). It passed its test only by compensating for the real bug, and would never fire once `fillSlot` was fixed, so it was reverted; the misleading comment was corrected in place so the next reader is not sent the same way. **Verification:** live, on a 19,556px Explorer tree scrolled to 8,000px — both repro paths hold `scrollTop: 8000` with the viewport fully covered (`uncoveredPx: 0`) and **zero** childList mutations on the panel host; re-verified after cycling three pages. The mechanism itself was proven directly first: re-appending the same node took `scrollTop` 700 -> 0 instantly with `scrollFired: false`. **Follow-on:** av-grid was then tested for the same defect and **has it** — a host re-append blanked its entire 882px viewport with no scroll event, and its `scrollLost` flag cannot detect it because the grid's size never changes. Tracked as US-1236 under EPIC-079. **Completion:** `/review` found no concerns and confirmed no caller relied on the unconditional re-append; `/document` recorded the idempotence guarantee in `uikit/CLAUDE.md`, `component-guide.md` and `model-view-pattern.md`, and corrected four now-stale comments (`InputView`, `Toolbar.story`, `TreeItem`, `TreeItemView`) that asserted the old behaviour; `/userdoc` added a what's-new entry. |
| US-1229 | The "Unsaved Changes" dialog rendered below the page instead of centered | — | **Origin:** user-reported. **The cause was a wiped CSS class, not layout.** `.dialog-shell` is what gives the shell `position: absolute; inset: 0; z-index: 100` — the overlay that `[data-position="center"]`'s flex centering then works inside. Measured live, the shell was `class="dialog"`, `position: static`, `inset: auto`, sitting at y=749 in normal flow. `DialogView` adds `dialog-shell` in its constructor, then `onMount` calls `applyRestProps`, which writes attributes wholesale via `setAttribute("class", …)`; `DialogsView` and `PoppersView` both passed `className: "dialog"`, which landed in the rest bag and replaced the structural class. **Two details explain why it survived review.** `DialogProps` *omits* `className`, so all 14 consumers cast it back in with `as DialogProps & { className?: string }` — and an `as` cast on an object literal disables checking for the whole literal, which is exactly how the prop reached the rest bag unnoticed. And `applyProps` re-adds the class but only re-runs from `onUpdate`, so any dialog that ever updated healed itself; `ui.confirm` never updates. **No `.dialog` rule exists anywhere in the codebase** — the prop has been dead since the React era — so it was removed end to end (`ViewProps`, both hosts, all 14 consumers, the casts, and the then-unused `DialogProps` imports) rather than merged with the structural class. That is what UIKit Rule 7 asks for and it means the clobber cannot recur under a different prop name. `DialogView` also re-asserts `dialog-shell` *after* the rest pass, mirroring what the file already did for `data-position`. **Verification:** live — content box at exactly 0px offset from the viewport centre on both axes, `class="dialog-shell"`, `position: absolute`, `z-index: 100`; title, message and all three buttons render. |
| US-1230 | Tooltips rendered as bare text — no background, border or shadow | — | **Origin:** user-reported, with the correct instinct that it predated nothing in the CSS: `Tooltip.css` was never edited and has always carried background, `1px solid` border, radius, shadow, padding and `max-width`. **The stylesheet was simply never loaded.** Its only importer was the React wrapper `Tooltip.tsx` — confirmed at `2b53d75a^`, line 5 — which EPIC-071 deleted without moving the import to `attach-tooltip.ts`, the surviving module that creates the element and sets `data-type="tooltip"`. One import line fixes it. **A first attempt to verify by walking `document.styleSheets` returned zero rules for every sheet and was discarded as a broken instrument, not as evidence**; the real check is the computed style of a live tooltip. **Class audit:** every `.css` in the renderer was checked for the same shape — a stylesheet no `.ts` imports — since one deletion sweep can orphan more than one. Exactly one other was found, `TruncatedText.css`, orphaned identically; lower impact because `TruncatedTextView` has no consumers outside its own story, but it would have lost its ellipsis truncation the moment anyone used it. Both now import from their owning module, and nothing else in the tree is orphaned. Note the first audit pass produced a **false negative** for `Tooltip.css` because a substring match let `import "./GraphTooltip.css"` satisfy it — the filename match has to be anchored. **Verification:** live — hovering the tab close button gives a "Close Page" tooltip measuring `background: rgb(31,31,31)`, `border: 1px solid rgb(60,60,60)`, and its shadow. |
| US-1231 | A "Close Page" tooltip appeared with no pointer near it, around the close dialog | — | **Origin:** user-reported — hover close, click close, dismiss the unsaved-changes dialog with Cancel, and the tooltip reappears under a button the pointer left seconds ago. **Two independent defects.** (1) **No dialog ever suppressed tooltips.** `overlayRegistry`'s own header names dialogs as one of the surfaces that should, but only `showPopupMenu` and the DataGrid cell-tooltip ever registered — in any version, React included. Caught directly: an accessibility snapshot with the dialog open showed `tooltip "Close Page"` beside it. `DialogView` now registers its shell on mount and unregisters on dispose; registering the *shell* rather than setting a global flag also keeps tooltips inside the dialog working, which is how the registry is designed to be opted into. (2) **`attach-tooltip`'s `focusin` channel could not tell a user from a restore.** `DialogView` records the active element on open and re-focuses it on close — correct for keyboard users, but not a user action, and `focusin` is identical either way. **A `:focus-visible` gate was written first and then abandoned:** measured live it reported `false` in one probe and `true` in another for what should have been the same situation, and CDP-synthesised clicks do not set pointer modality the way a real mouse does, so there was no way to verify it under the input that matters. Shipping an unverifiable heuristic is worse than shipping none. Instead `uikit/shared/focus-restore.ts` exports `restoreFocus()`, which increments a counter around `element.focus()`; because `focus()` dispatches `focusin` synchronously, `isRestoringFocus()` is exact for that event. Deterministic, no inference. **Six restore sites adopted it** — `DialogView`, `showPopupMenu`, the draw and link editors, and (found by `/review`, which correctly separated them from `ToolbarView`'s two roving-tabindex `focus()` calls, where real keyboard focus must still show a tooltip) `ImageToolbarView` and the HTML editor's menu-close handler. **Verification:** the reported sequence with real clicks produces no tooltip while the dialog is open — it did before — and none after Cancel, while a plain programmatic `focus()` still shows one, so the keyboard channel is intact rather than switched off. **Not verified:** in every run focus was never actually restored to the close button after Cancel (something on the cancel path, likely `focusPage`, takes it first), so the restore-suppression half was never exercised end to end. The dialog-suppression fix alone accounts for the measured improvement; the restore fix closes a real hole but rests on code reading. |
| US-1039 | Clearing a tree search collapsed the tree to its root | — | **Origin:** user-reported in the MenuBar folder tree. **The task document's hypothesis and its title were both wrong, and testing them was the work.** **Hypothesis refuted:** the document blamed remount timing — that a zero-match display tree leaves nothing to apply the expansion map to, `searchKey` and `displayTree` landing in one state update. Tested directly with a **single** input event jumping from empty straight to a deep zero-match term, skipping the shallow phase entirely: still failed. So `savedExpandMap` was captured while the tree was fully expanded and the restore *still* collapsed everything. The capture was never the problem. **Root cause:** `TreeModel` resolves expansion in three tiers — explicit toggle, then the `defaultExpandedValues` hint, then `defaultExpandAll` (`TreeModel.ts:196-204`) — but `getExpandedMap()` returns only `state.expanded` (`:623`). **A tree expanded via the hint has an empty `state.expanded`**, which is every restored or freshly-built tree the user has not clicked in. `setSearchText` saved that raw map, so `savedExpandMap` was empty and clearing restored nothing. **The codebase had already met this and fixed it in exactly one place:** `buildTree` carries a comment reading *“getExpandedMap returns only state.expanded, which is empty for hint-only expansions”* and merges hint + state for the refresh path (`:310-327`), with `expandedResolver` (`:543-548`) as the reusable rule. The search path never adopted it; the fix reuses that resolver rather than restating the merge. **The title's premise is wrong — this is not about zero matches.** The capture happens on the crossing *into* deep search, before that search's filtering reaches the model, so the map is identical whether the term later matches everything or nothing. What differs is **how the tree got expanded**: clicking creates explicit toggles (captured fine); a restored expansion is hint-only (captured as empty). The user's working “partial” case was almost certainly a tree they had clicked in. **Regression from US-1037? No — pre-existing,** answered by archaeology rather than by building the old binary the plan asked for (stated plainly, since that step was skipped). The defective line and `getExpandedMap`'s semantics both predate US-1037, which converted the *view*; nothing in the fix touches remounting. **Verification:** measured the scroll container's `scrollHeight`, not rendered rows — clear-from-deep, `hideSearch`, and the shallow path all restore 944px to 944px exactly. **Two instrument corrections, each of which produced a false result first:** counting `[role=“treeitem”]` counts *rendered* rows in a virtualised list and reported 41 vs 42 for identical states, making the fix look one row short; and a missed selector left both readings null, so an equality assertion passed on null === null. Every probe now carries a guard proving the filter actually ran. **Not verified:** the second consumer (MenuBar folders). The fix is in the shared model every consumer routes through, but only Explorer was exercised. **Carried forward:** two more `getExpandedMap()` callers have the same blind spot (`:514` persistence, `:685` shallow search) — filed in `backlog.md`, deliberately not fixed inside a batch under review. |
| US-1041 | A disposed file-search view could not cancel its own worker | — | **Scope correction at plan review:** the document's second acceptance criterion — *“a second concurrent search in the same window is unaffected”* — **was unachievable by this change**. Concurrency is prevented at the `start` path, not the cancel path: `activeSearches` is keyed per *window* and `handleStart` calls `terminateSearch(senderId)` before every search, under the comment *“Only one search per window — replace whatever was running.”* Criterion rewritten to the guarantee that is deliverable; supporting concurrent searches is a separate, larger decision with worker-count implications. **What shipped:** the cancel message carries a `searchId`; the main process terminates only on a match; a stale or unknown id is a silent no-op. `dispose()` now cancels its own search, where it previously **deliberately did not** — a documented leak that let an abandoned worker walk the tree to completion because an un-scoped cancel would have killed whatever else that window was running. **The contract was already half-built:** `SearchRequest` carried `searchId`, every streamed message carried it, and `activeSearches` **already stored it** on its entry (`search-service.ts:29`) — the main process needed no new state, only a comparison. **Added at review:** the handler takes `SearchCancel | undefined` and optional-chains the id, because a malformed message must not throw in the **main** process. **Verification, and it took four attempts to make the test valid.** The first three runs reported success while proving nothing — the search completed *before* the cancel was sent, so every assertion was vacuous, and a warming file cache made it worse each retry (2.5s to under 400ms). Only once the probe asserted **“was the search still live at the moment of cancel?”** did the numbers mean anything. Final run, both cases live at cancel: a stale id kept streaming and ran to completion; its own id produced **zero** further messages and no `complete`. **Generalisation worth keeping:** *a timing-dependent probe must assert that the condition under test was still true when the stimulus was applied.* Without that guard, “nothing bad happened” and “nothing happened at all” are indistinguishable, and both read as a pass. **Not fixed, pre-existing:** when one view's search replaces another's, the losing view never receives `complete` and its spinner stays on. |
| US-1191 | The Edit Columns popover rendered its grid as a 51px sliver | — | **Origin:** user-reported. The popover sized correctly while the column list inside it stayed ~51px tall. **The first question answered was whether US-1109 caused it** — it did not: `ColumnsOptions` never calls `invalidatePushed()`, so that change cannot reach it. Pre-existing. **The diagnosis was not what the symptom suggested.** The `DataGridView` root *did* stretch correctly to 281px inside the 300px panel; the collapse was one level deeper, in av-grid's own root (`render-grid`, inline `height: 100%`, computing to 51px). **Cause:** `[data-type=“data-grid”]` is `flex: 1 1 auto`, so the host's basis is its **content**, making its height *indefinite*. A percentage height cannot resolve against an indefinite parent, so av-grid fell back to the scroll area's initial ~51px; the host then grew to fill the popover around it and av-grid never re-measured. `height: 0` gives the host a definite base while the inherited `flex-grow: 1` still fills the panel. **Two measurements did the work, and one nearly misled.** Perturbing any style corrected the grid to 281 — and **reverting the perturbation left it at 281**, which proved the height chain was sound and the fault was a *stale first measurement* rather than CSS. A clean close-and-reopen then reproduced it (panel 160, host 141, grid 51); after the fix a fresh open gives panel 300, host 281, grid **281**. 0 React roots. **Deliberately deferred:** the other seven `DataGrid` consumers work only because their containers happen to supply definite heights. `flex: 1 1 auto` on a grid host is arguably wrong everywhere — the shim's own CSS comment says the height *“has to be definite”*, and `flex-basis: auto` is exactly what makes it not. Changing the shared rule under all eight consumers sight-unseen was not worth it inside a bug fix; it is a decision to take on its own. |
| US-1109 | A cleared DataGrid option could not be represented after invalidation | — | **The task document's own recommendation would have produced a fix that does not work.** It proposed option 2 — stop dropping `undefined` in `collectValues` — as *“closer to the shim's stated exclusion-not-allow-list design”*. True as a design argument, irrelevant as a fix: after `invalidatePushed()` the baseline is empty, so the diff becomes `undefined !== undefined`, which is **false**. No delta forms, `setOptions` never sees the key. Option 2 changes nothing except letting `undefined` reach `create()` at mount — pure risk. Reversed at plan review and verified against source. **What shipped (option 1):** `invalidatePushed()` retains its key set with a module-private `Symbol` sentinel. Since the sentinel equals nothing, every key the new owner supplies is re-pushed (preserving the method's documented contract) **and** a key the previous occupant set but the new owner omits now diffs to `undefined` and is cleared — **a second latent defect the ticket never named**, where a recycled host silently inherited stale options. **A gap the corrected document still had, found by review:** it analysed `highlightString` as the one conditional prop that would now be actively cleared and **missed `growToHeight`** (`GridBodyView.ts:100-106`), which flips on exactly the embedded-to-non-embedded repoint this feature exists for — while the document had already named `growToHeight` as construction-time-applied and then excluded it with an argument about `rows`/`columns`. Verified inert independently: av-grid's `setOptions` forwards only className/rowHeight/fitToWidth/overscan*/whiteSpaceY to its render layer (`av-grid.js:4584`), and the render layer's own `setOptions` (`:2736`) is the sole writer of the options object that sizes the grid. Had it gone the other way, this fix would have *introduced* a resize regression. **Consumer inventory corrected:** 8 shim instances across 7 files, not the “five” the ticket asserted; only `GridBodyView` and `GridOutputView` call `invalidatePushed()`, and only `GridBodyView` has conditional props — so the behavioural blast radius is **one consumer**. **Verification:** live on a scratch grid page (created and closed for the purpose) — 21 cells, search `beta` gives 9, clear returns 21, with every keystroke running `invalidatePushed()`. **Honest limit:** the newly-fixed case is latent by definition — no current consumer maps a cleared value to `undefined` — so what is demonstrated is that existing behaviour is intact and the mechanism now works. |
| US-1111 | ScriptPanel bypassed app.fs with require(“fs”) | — | **The document framed this as a genuine toss-up** between porting and documenting an exception, on the grounds that the calls are *“synchronous inside user actions, so the port is a behaviour change, not a rename.”* **That premise is wrong for six of the seven call sites:** `saveToLibrary` (5 calls) and `openInTab` (1) are **already `async` and already awaiting** a dialog, a dynamic import, or `pagesModel.openFile` — so porting them is a rename with an `await` in front. The seventh, `selectScript`, is a dropdown change handler returning `void` that nothing awaits, so async costs one unobservable tick. **And the file was already half-ported** — it *already imported `fs` from `../../api/fs`* and used it elsewhere, carrying both APIs at once. `ScriptPanel` also fits none of the exception-list categories (circular dependency, pre-init startup, low-level provider, `fs.watch`, transpiler), and the list's own closing rule applies directly: *“if `app.fs` or `file-path` can do the job, use them.”* Ported. **Both behavioural risks verified rather than assumed:** `fs.mkdir` is recursive (it delegates to `_ensureDir`, which passes `recursive: true`) — confirmed at runtime by creating a two-level path with both intermediates absent; and `selectScript`'s single caller (`ScriptPanelView.ts:311`) discards the returned promise, so nothing awaits it and the existing try/catch prevents an unhandled rejection. **Scope added at review, flagged rather than buried:** two `err.message` interpolations on `catch` bindings were replaced with `errMessage(err)` per `CLAUDE.md`. Not in the original ticket, but on lines this task edits anyway — leaving them meant a later standards sweep reopening the same file. **Verification:** the ported paths were unreachable with no script library configured, so `script-library.path` was pointed at a scratch directory for the real sequence (nested `mkdir` with missing intermediates, `exists`, `write`, `exists`, `read`), the probe file deleted, and the setting restored to **genuinely absent** rather than empty string. |
| US-1190 | Audio player stopped advancing to the next track | — | **Origin:** user-reported, and correctly diagnosed by the user as a De-React regression — *"before De-React work it worked"*. Opening an audio file from Explorer or a link-editor Collection played it, but the **Next** button did nothing and a finished track did not auto-advance. **Cause — a lost receiver, not lost logic.** `VideoView.playerProps()` passes `this.model.playNext` **unbound** as both `onEnded` and `onNext`. `playNext` was a prototype method (`async playNext(): Promise<void>`), so it arrived at the view with no receiver and its first statement, `await this.getSiblingTracks()`, threw a swallowed `TypeError`. One line accounts for **both** symptoms. **Why it looked fine:** `hasNext` is a plain boolean read from the `canPlayNext` getter, so the button rendered and enabled correctly — only the click was dead. Nothing logged, and all three gates stayed green. **Confirmed against the React original** (`c137b47b`), which wired both call sites as inline arrows — `onEnded={() => model.playNext()}` and `onNext={() => model.playNext()}`. Those preserve the receiver; the conversion rewrote them as bare method references, which do not. **What shipped:** `playNext` becomes an arrow property. Chosen over wrapping the two call sites because it removes the trap rather than patching its two current exits — and because it restores the file's **own** convention: all six other handlers `VideoEditor` hands to its view (`submitUrl`, `setInputText`, `onPlayerStateChange`, `onMutedChange`, `toggleShuffle`, `openInVlc`) are already arrow properties. `playNext` was the sole exception, which is exactly why a uniform "pass the method reference" conversion transform was right five times and silently wrong once. `canPlayNext` is a getter read as a value and is unaffected; nothing outside `editors/video/` references `playNext`. **Verification:** `typecheck`/`lint`/`build-prod` green, and **the user confirmed the fix in the running app** — which is the only honest test here, since the alternative was clicking Next on whatever they had playing. An attempt to prove the receiver via `execute_script` failed: the scripting facade's page objects do not expose the editor instance. **Class sweep, with the numbers that make it actionable:** the narrow shape — a prop named `on[A-Z]*` assigned a bare `this.model.<prototype method>` reference — finds **0** elsewhere, and would have found **exactly** this defect before the fix. Widening it to any prop name yields **95** candidates that are essentially all noise (`error`, `items`, `refs`, `status`). **So the detector must key on the prop being callback-typed, not on the referenced method's name.** Recorded as **US-1131 clause (5)**, and it is the cheapest of that task's five clauses to land: zero existing violations, so no remediation backlog. **Generalisation:** *a class method handed across a model/view boundary as a bare reference must be an arrow property.* React's inline callback hides the distinction; a native callback prop does not. Now in `model-view-pattern.md`. |
| US-1189 | The Mneme configuration view rendered no content | — | **Origin:** user-reported while checking US-1153's `mneme-root` half. A **different editor** — `editors/mneme-config/`, which shows configuration only and is therefore outside the customer-data constraint that governs `mneme-root`; it was probed with geometry- and count-only queries and no text content was read. **Measured before the fix:** `mneme-config-root` was correctly 1569x1015, but its only child — the wrapper `MnemeConfigEditorView.onMount` created — measured **1569x37**, and `RunningConfigView` inside it measured 37 as well. `mneme-body` resolved to **0**. The status bar rendered and the Model and Roots panels were laid out at zero height. **Cause:** `this.page = createPanelElement({})` — a *bare* panel, so `display: flex` with `flex: 0 1 auto`. It does not grow, so in the column root it collapsed to its tallest content, the 37px toolbar, and `mneme-body`'s `height: 0` + flex then resolved against nothing. **This is EPIC-073's Excalidraw defect a second time** — that commit records *"the island host div was created bare, so `display:block;height:0` made Excalidraw's own `height:100%` resolve against zero"*. Same class: **a bare wrapper between a sized root and a `flex`/`height:100%` child.** It is also why nothing caught it — the elements exist, are correctly built, measure correct *widths*, report 0 React roots, and all three gates were green while it was broken. Only height was wrong, and no structural check reads height. **What shipped:** the wrapper is **deleted** rather than sized, and the page view is appended to `this.root` directly. `sync()` already styles the root for both modes (column + `overflow: hidden` when running, centred when stopped) — the wrapper was defeating exactly that styling — and `releaseChild()` detaches a child's root itself, so the mode swap never needed a container. **Verification:** live — `RunningConfigView` 1569x1015, `mneme-body` **1569x978** with both panels and **73** visible descendants, 0 React roots. `typecheck`/`lint`/`build-prod` green. **The stopped-mode branch is reasoned, not measured:** exercising it means stopping the user's live Mneme service, which was not worth it; `StoppedConfigView` is content-sized and the root centres it, so removing the wrapper can only improve that path. **Generalisation, now twice-observed:** *a wrapper between a sized root and a flex-filling child must itself be told to fill.* A bare `createPanelElement({})` is never a safe pass-through — and **height is the dimension every structural check misses.** |
| US-1153 | Mneme tree and link-editor tiles were never rendered | — | **Closed 2026-08-29 as not reproducible.** A verification task, not a fix: it existed because EPIC-071 closed two surfaces with green gates and **no runtime evidence at all**. Both are now user-confirmed working. **(a) `mneme-root`** — the user opened it and reports the tree renders correctly. No instrument was pointed at it and no content was read or recorded, per the standing constraint that rendering it displays live customer notes. Its verification was **permanently user-only** and this is the only form of evidence it can ever have. **(b) link-editor tiles** — the user switched list <-> tiles manually and reports no issues. The list->tiles **teardown** therefore has *user* evidence rather than the measured absence-of-the-previous-branch the plan asked for. That gap is accepted knowingly: the view-mode switch never responded to synthetic input, and this is the strongest evidence obtainable without it. **The pass was not wasted, though its own two items were clean.** Opening the Mneme *configuration* view — a neighbouring editor, not in scope — found it rendering a 37px status bar and nothing else (**US-1189**). **Worth keeping:** two surfaces conceded by an epic as unverifiable were, on inspection, simply *not looked at* — one needed nothing more than the user opening it. Asking the user was cheaper than every instrument attempt that preceded it, and it is the only route for anything behind a data boundary. |
| US-1152 | Five secondary views stopped tracking when reused for another editor | — | **Origin:** surfaced by EPIC-071's close review but **not caused by it** — all six files were untouched by that epic. The shape is *a view that accepts a replaceable model but binds as if the model were fixed*: it shows a correct one-time snapshot and then stops tracking, while the **old** editor's sources keep invoking callbacks against the reused view. **Root cause was an asymmetry in the base class, not five separate mistakes.** `VanillaView.bind()` routed its unsubscribe through `own()`, which only appends to the final `disposers` list — there was **no early-release path for an owned cleanup**, while children have had `releaseChild()` all along with a docstring describing exactly this hazard. So a view that re-bound on model change had no way to drop the previous subscription. **What shipped:** `bind()` now returns an idempotent release handle registered through a new private `ownReleasable()`, which also **removes its own entry from `disposers`** so a repeatedly-retargeted view accumulates nothing. Six views retain and release that handle: both link-category files, the Mneme tree secondary view, both tags files, and hostnames navigation. `LinkCategorySecondaryView` additionally tracks page/host **source** identity, so a source swap on the same editor rebinds too. Widening the return type from `void` was invisible to all **162** existing `bind()` callers. **Two corrections made at plan review, both the same class the project keeps meeting.** The first draft proposed recovering a late-arriving tree provider by putting `editor.treeProvider` in the state *selector* — but it is a lazy getter over the plain `_host` field, and a selector is only re-evaluated on state dispatch, so it would never be observed. **That is the EPIC-067 `pipe` regression exactly: a plain field read as if it were reactive.** The recovery does work, via `onHostAttached` → `loadData` → `state.update`, and the document now states that chain plus its implicit dependency. The second: released handles would have lingered in `disposers` forever, which is unbounded growth in precisely the views this task is about. **A documentation rule already existed and was violated five times.** `model-view-pattern.md` already said *"use `bind()` only when the observed state source outlives the view"*. Prose did not prevent five instances — which is the argument for making it **US-1131 clause (3)**, alongside the update-path asymmetry detector. `/document` has now added the release-handle contract and the selector rule to both `model-view-pattern.md` and `uikit/CLAUDE.md`. **Verification is partial, and deliberately recorded as such.** `typecheck`/`lint`/`build-prod` green; the app renders with **0** React roots and the category panel shows the correct per-editor tree (2 rows on editor A, 4 on B, 2 again on A). But **the rebinding behaviour itself has no runtime evidence.** Measured live: **switching tabs does not reuse these panels** — three open link editors produced three distinct panel elements, and a `data-probe` attribute placed on the visible panel never reappeared after a switch. So the task document's own verification step (*"open two link editors and switch tabs"*) would have passed while proving nothing. The real retarget path is **LK7 within-links navigation**, which needs a `.link.json` linking to another `.link.json` and a real click on that row; it was attempted and abandoned. `mneme-root` is **source-verified only** and permanently so — rendering it displays live customer notes. **One reviewer finding closed as correct-by-design:** two selectors read `editor.isMain` directly, which the new selector rule appears to forbid. `EditorModel.ts:210-214` documents it as the sanctioned pattern — *"NOT reactive on its own — a view that must re-render on promote/demote subscribes to `editor.page?.state`"* — so the subscription is to page state and `isMain` derives from it. Noted because a future lint rule for the selector clause **must not fire on this shape**. |
| US-1173 | Interactive verification pass for the converted UI | — | **Closed 2026-08-29 by user decision — a verification task, not a fix, and everything it ever listed is verified or recorded as unreachable with a stated reason.** **Why it existed:** the De-React programme closed with 24 epics' worth of surfaces conceded as *"could not reach with the available instrument"*, and that was false the whole time — the `browser_*` MCP tools have accepted `pageId: "app"` and driven Persephone's own window with real CDP input since **US-810, 2026-07-05**, six weeks before the roadmap was written. **What it produced:** four defects (US-1182 filed and withdrawn, plus **US-1186**, **US-1187**, **US-1188**), the closure of **US-1164**'s never-rendered Tor half, and clearance of dialog commit/focus, `event.key`, av-grid editing end-to-end into file content, all five programmatic graph items, rest-client's Monaco hosts, file-diff's revision pick, the four repointed stories, and eight of ten browser surfaces — **0 React roots everywhere**. **Its central finding:** all seven defects of this batch passed typecheck, ESLint, `build-prod` and the React-root count **while broken**, and the symptom in six of them was *absence*. Three shared one class — *the parent's idea of its children's props diverges from the model's actual state, and nothing type-checks the difference* — routed to **US-1131** clause (3) as a lint rule rather than a hand audit, because a 77-site sweep showed "not retained" is the wrong detector and **asymmetry in `sync()`** is the right one. **Instrument rules worth keeping, all learned by nearly producing a false result:** (1) **verify negative results on a freshly reloaded renderer** — one filed defect (US-1182) dissolved on a `location.reload()`, because after hours of HMR a stale module instance is indistinguishable from a broken listener; (2) **menus, popovers and av-grid cell editors open asynchronously** — a same-tick `querySelector` after dispatch returns null and looks exactly like a broken feature, so re-read in a *separate* `execute_script` call; (3) **`offsetParent` is `null` for `position: fixed`**, so the visibility flag under-reports overlays — use `getComputedStyle` + `getBoundingClientRect`; (4) there are **two** `[data-name="page-editor"]` hosts and the first is inactive — select by `offsetHeight > 0` or every geometry reading is 0×0; (5) **pointer capture:** `setPointerCapture(1)` succeeds but `hasPointerCapture(1)` returns false until a real pointer event — so *handlers that **call** capture are drivable; handlers that **check** it are not*, which is why `SplitterView` is immune and `PopoverView`'s resize handle was not. **Two surfaces recorded as unreachable with precise reasons:** Tor pages **deliberately refuse** browser automation (*"Browser automation is disabled for privacy protection"* — not allowed by design, not instrument-limited), and last-tab replacement needs a real splitter drag to reach the close button past `CLOSE_BUTTON_THRESHOLD`. A canvas-rendered graph tooltip was cleared by the user, since no instrument here can hit-test a canvas node. **A data boundary set mid-pass and now standing:** `browser_snapshot`/`browser_hover`/`browser_type` against `pageId: "app"` serialise the **entire application UI including sidebars**, and pulled the user's real bookmarks, browsing history and download filenames into the transcript. **Those tools must not be used while a browser, bookmarks or link-editor surface is open** — use `execute_script` with targeted, count-only queries. The `evergreen` profile was never touched. |
| US-1188 | The bookmarks drawer opened invisible the first time on any browser page | — | **Origin:** found by the US-1173 browser pass. On a **new** browser page the first click on the bookmarks button opened the drawer at **0px wide** — `bookmarks-panel` 1x984, `bookmarks-editor-host` 0x913, panel wrap inline `width: 0px` — with the splitter the only way back. Closing and reopening gave a correct 941x984, so the feature looked fine to anyone who tried twice and broken to anyone who tried once. **Cause — a state write during a child's `mount()`, undone by the line after it.** `BookmarksDrawer.sync()` recovers a zero width itself (`if (props.open && props.width === 0) props.onChangeWidth(root.offsetWidth * 0.6)`), and that runs inside `mount()`. But `BrowserView.syncDrawer` captured its `props` object *before* mounting and then called `this.drawerView?.update(props)` on the very next line, re-pushing `width: 0`. `bookmarksWidth` defaults to **0** (`BrowserEditorModel.ts:286`) and is per-page persisted state, so every new page hit it; the second open worked only because the recovered 941 had by then survived in state. **What shipped:** `syncDrawer` builds props through a `drawerProps()` factory that reads `this.model.state.get().bookmarksWidth` at call time, so the post-mount `update()` observes the recovery instead of clobbering it. **Verification:** live on a freshly reloaded renderer with a brand-new browser page — first open **941x984 with 48 rows**, 0 React roots; previously 0px. `typecheck`/`lint`/`build-prod` pass. **Generalisation:** *if a child can write parent state during `mount()`, the parent must not push a props object it captured before mounting.* |
| US-1187 | Any control change emptied the Toolbar story | — | **Origin:** found by finally *rendering* the four repointed stories, the last programmatic item in US-1173 — which is exactly what *"compiles but was never rendered"* was hiding. The Toolbar story mounted correctly and then **emptied itself** on the first control change: switching orientation took it from 298x30 with all four child views to **4x8 with zero children** — no button, no icon button, no spacer, no segmented control, no svg. **Cause:** `ToolbarView.onUpdate` calls `fillSlot(this.root, props.children)`, and `fillSlot` opens with an unconditional `host.replaceChildren()` (`uikit/shared/fill-slot.ts:69`). The story passed `children: null` and appended its five nodes *directly into `toolbarView.root`* instead. That looks correct at mount only because `onMount`'s own `fillSlot` runs **before** the append and nothing has updated yet. **What shipped:** the story stores its nodes in a `contents` field and hands them over as `children` (`children: this.contents ?? null`), calling `toolbarView.update(...)` once they exist. Re-passing the same stable nodes each update is what `fillSlot` expects — it re-appends them in order. **Verification:** live on a freshly reloaded renderer — horizontal mount 298x30 with all four parts, `-> vertical` now **keeps** all four (155x114, `flex-direction: column`, svg intact) where it previously collapsed to 4x8, and the *nested* segmented control still switches (Default -> Light) after the re-append, proving the re-appended views keep their listeners. 0 React roots. **Recorded, not fixed:** `StorybookEditorView.ts:81` appends into `this.toolbar.root` the same way. It gets away with it only because it never calls `this.toolbar.update(...)` — latent, not live, and one future `update()` from an empty application toolbar. Both of `ToolbarView`'s two callers made the same mistake, which reads as under-documented rather than carelessly hit. **A finding withdrawn on reading the contract:** `onUpdate` reassigning `childrenCleanup` without invoking the previous one is *not* a leak — `fillSlot`'s docstring states callers must not run a previous cleanup, because superseded cleanups no-op themselves (`fill-slot.ts:62-67`). **Generalisation:** *appending into another view's root is not a supported way to give it children* — the owning view reserves the right to refill it. |
| US-1186 | Switching a rest-client request's BODY type did nothing | — | **Origin:** found by the US-1173 interactive pass, and it is the defect class that pass exists to catch — **a correctly-built view whose update wiring is missing.** Clicking `raw`, `form-data`, `binary` or `x-www-form-urlencoded` under BODY left the panel showing whatever type the file had when opened; for a fresh request, the permanent message *"This request has no body."* No error, no console warning, and `tsc`/ESLint/`build-prod` all green. **What made it look like it worked:** the click *did* reach the model — the segmented control's `aria-checked` moved and the body-language button (`plaintext`) appeared. That button is created by `RequestBuilderView.syncLanguage()` from `this.props.request.bodyType === "raw"`, **the same prop the body panel reads**, so the parent provably held `raw` while the panel below showed the `none` branch. *Two children reading one prop and disagreeing is the tell.* **Cause:** `sync()` is the view's only update path and it explicitly updates `methodButton`, `urlInput`, `sendButton`, `headersView`, `bodyTypeView`, then `syncHeaders()` and `syncLanguage()` — **but never `bodyContent`**, which was not even retained: `onMount` assigned it to a local `const` (`RequestBuilderView.ts:135`). `this.child()` registers a child for **disposal ownership only — it does not propagate updates** (`uikit/shared/vanilla-view.ts:174`), which is why nothing failed loudly. `BodyContentView.onUpdate` — and its `SubtreeSwap` — never ran after mount, which also stranded the `valueGate` path at `:279`, so external changes to `request.body` never reached the raw Monaco. **What shipped:** retain the child in a field, add a `bodyContentProps()` factory beside the other prop factories so the shape is stated once, update it in `sync()` alongside its siblings, and clear the field in `onDispose()` as its siblings do. **Verification:** live on a freshly reloaded renderer — `none -> raw` swaps to a live Monaco inside `body-content`, `raw -> form-data` tears that Monaco down (0 left) and renders `form-data-editor` with real rows; 0 React roots throughout. Before the fix the identical click left `NoneBodyView` in place. **Class sweep, with its limits recorded:** 77 sites match the raw shape (`const x = this.child(new X(...))` never retained, in a class with an update path), but almost all are benign — dividers, spacers, per-row views held in collections, or static-literal props. Two high-risk singletons were checked and cleared (`ToolsHubView.ts:47`'s `PinnedRailView` takes a static literal; `ThemeSection.ts:108`'s `ThemePreviewView` is fed from immutable theme definitions). **So "not retained" is the wrong detector; the useful signal is asymmetry — a `sync()` that updates every sibling and skips one.** That belongs with **US-1131** as a lint rule, not a hand audit of 77 sites. |
| US-1185 | Opening a *file* whose editor module fails to load was also silent | — | **Origin:** the follow-up US-1163 deliberately left open — same shape, the `openFile` side. A failed editor-module load rejected out of `createEditorFromFile` into callers that never caught it, so clicking a file did nothing at all. **Three of six call sites were silent; a fourth was already correct.** `PageNavigator.ts:149` already wrapped the call in try/catch, toasted `Failed to open <basename>: <message>` and fell back to an empty text model — so it set both the precedent and the message format, and was left alone. It also ruled out the tidier-looking fix: guarding *inside* `createEditorFromFile` would have stopped it throwing and silently broken PageNavigator's fallback. **What shipped:** `guard()` per `CLAUDE.md` at the three silent sites — **`openFile`** (the dominant path: explorer click, link, drop), **`openDiff`** (both sides), and **`editor-switch.rebuildEditorOverFile`**. `openFile` already returned `PageModel | undefined`, so its contract is unchanged and no caller needed updating. Each failure path disposes the pipe, which `createEditorFromFile` would otherwise have handed to the editor being discarded. `confirmRelease` was checked before aborting the editor switch: it is a predicate and disposes nothing, so the old editor stays installed and the toast explains why the switch did not happen. **Verification cost two wrong probes, both instructive.** Breaking `editors/env-vars/index.ts` produced no toast and no page, which looked like the fix failing — wrong editor: `env-vars-view` is `hasContentHost: true`, and `buildEditorById` returns a plain `TextFileModel` host early for **any** content-host editor *without loading the editor module at all*. So **only *no-host* file editors** (those declaring `newEditorModel`: image, archive, video, category, git-tree, mneme-root, board, toolset) load their module inside the guarded call. Re-probed with `image-view`: toast `Failed to open icon.png: ...`, `openFile` returned `undefined`, **no page created, nothing escaped**; probe removed, `assets/icon.png` opens normally with zero error toasts. Second wrong turn: the scripting facade's `app.pages.openFile(path)` takes *only* a path and silently drops `pipe`/`options`, so an early probe never delivered the `target` it appeared to. **Carried forward:** a content-host editor's module failure surfaces *after* `createEditorFromFile` — in `attachEditorToPage` or the view layer — a **third** silent path this fix does not cover, and the one governing env-vars, file-diff and the text editors. Also noted: `app.ui.notify` resolves on **dismissal**, not display, so awaiting it in a probe hangs. |
| US-1164 | Browser toolbar button order — fix now fully verified, including the Tor-only half | — | **Origin:** user-reported (2026-08-27) — the browser toolbar's **download** and **Tor info** buttons rendered after **Close Tab** instead of before **Page Menu**. An EPIC-072 conversion defect: `BrowserToolbarView` appended `this.controls` in *construction* order and then patched the DOM with two `insertBefore` calls, the second targeting `downloads.root` — already stranded past `close` — dragging `torInfo` down with it. **Two** buttons were misplaced, not one; only `downloads` was visible because `torInfo` renders in Tor mode only. Fixed in `BrowserView.ts:292` by spelling the left-to-right DOM order out once (`this.controls` keeps construction order because `sync()` indexes into it positionally). **Why it stayed open:** it was held deliberately for the interactive browser pass rather than closed on its own, because the `torInfo` half had never been rendered by anyone. **Closed 2026-08-28 by that pass.** In a real Tor page `toolbar-tor-info` is visible at 24x24 and sits exactly where the fix put it — after `toolbar-bookmarks`, before `downloads-button`, and well before `toolbar-more` (Page Menu), `toolbar-devtools` and `toolbar-close`. In a non-Tor page the same element is present but 0x0 and hidden, which is correct. The toolbar measures **11** children in the intended order in both modes, `close` last, 0 React roots. **Its argument held up:** this was the second EPIC-072 defect found by a human opening the UI rather than by any instrument, and both were *relationships between* correctly-created elements, which no marker or root count can see. The pass it was waiting for then found **two more** defects of exactly that shape (US-1186, US-1188). |
| US-1184 | Markdown checklists rendered as vertical columns spilling off the page | — | **Origin:** reported by the user from a snip of the dashboard — every `- [ ]` entry rendered as a stack of narrow one-word columns running off the right edge. The user asked whether the dashboard text or the markdown editor needed fixing. **It was the editor.** **The bug:** `MarkdownBlock.css` styled `.task-list-item { display: flex; align-items: center; }` to line the checkbox up with its text. Making the `<li>` a flex container promotes **every inline child to a flex item**, so each `<strong>`, `<em>`, `<code>` and bare text run became its own shrink-to-fit column. Any checklist item containing bold or code exploded; plain ones looked fine, which is why it survived. This affected **every markdown file with a checklist**, not just the dashboard. `align-items: center` was independently wrong for a multi-line item, centring the checkbox against the whole block rather than the first line. **What shipped:** GitHub's approach — no flex. `list-style-type: none` on the item, and the inline SVG checkbox pulled into the list gutter with `margin: 0 .3em .2em -1.5em; vertical-align: middle`, so normal inline flow returns and wrapped lines align under the text at the `ul`'s existing 2em indent. **Verification:** live — every task item at full content width (857px) with **zero** horizontal overflow, 28 items flowing normally, confirmed by screenshot. **Generalisation:** *`display: flex` on a text-bearing element silently reinterprets its inline markup as a row of columns* — the damage scales with how much inline formatting the text has, so the richest content looks the most broken. **Follow-on (docs, not code):** the dashboard's entries were *also* unreadable for an unrelated reason — several had no title at all, being single unwrapped lines of 600–2,200 characters. 13 entries now lead with a short title and an indented detail paragraph. No content removed. **Review:** no concerns. |
| US-1183 | Env Vars: the locked and invalid-file states collapsed to the top of the window | — | **Origin:** reported by the user during the interactive verification pass, from a pasted screenshot — the page rendered only in the top strip of the window and the footer sat directly under the message instead of at the bottom. **The bug:** `EnvVarsBodyView`'s root is a deliberate `display: contents` span (`createContentsRoot`), so each branch view's own root becomes a direct flex child of `text-chrome-root`. Two of the three branches never asked to grow — `LockedStateView` and `ErrorStateView` built centred `createPanelElement` roots with **no `flex`**, while `NormalStateView` had `flex: 1`. A `display: contents` element has no box, so it cannot itself carry the flex; the growth has to live on each branch. **Measured before:** toolbar `top=45 h=29`, `env-vars-body h=0` (no box), footer stranded at **`top=179`** in a 1011px window with ~800px of empty page below. **After:** footer `top=980 bottom=1009`, body filling 74 → 980, message centred. The valid-file state was correct before and after — **which is exactly why this survived: the branch nobody looks at was the broken one.** **What shipped:** `flex: 1, minHeight: 0` on both branch roots, with a comment recording that it is load-bearing because the parent is `display: contents`. **Swept for the class, not the instance:** every comparable chrome-body branch already had it — `BoardEditorView` `ContentErrorView`, `DrawBodyView` `DrawErrorView`, `FileDiffEmptyView`, and both `RestClientErrorView` / `RestEmptyView`. The only other centred panel without `flex` is `MermaidOutputView`, a log-list content block where growing would be wrong. So env-vars held the only two instances. *Not* checked: `GraphBodyView`'s `SearchEmptyView` and `McpInspectorView`'s `EmptyErrorView`, which use plain divs in different editor shells. **Generalisation:** *a `display: contents` wrapper cannot carry layout — every child that must fill has to say so itself, so a multi-branch body repeats the same flex declaration per branch and one missing copy is invisible until that branch renders.* **Verification:** geometry and screenshot on a freshly reloaded renderer; typecheck, lint, build-prod pass. **Review:** no concerns. |
| US-1163 | Opening a standalone editor whose module fails to load was a silent no-op | — | **Origin:** found by a deliberate-throw probe in EPIC-072/US-1160 and carried on the dashboard since. **Re-confirmed as reproducible** before being fixed — a throw in `editors/about/index.ts`, *then* a renderer reload so the fresh module graph carried it: the rejection escaped to the caller, pages stayed 7 → 7, and **zero** notifications appeared. That ordering mattered: the same session had already produced one false defect report (US-1182) that a reload dissolved. **The original diagnosis named the wrong cause as dominant.** It blamed `editorRegistry.createEditor` → `loadModule`. That path is real, but every `showXxxPage` wrapper *also* did its own `await import("../../editors/<x>")` purely to read the page-id constant — and that import runs **first**, so it throws before `showEditorPage` is reached. A first fix guarding only `createEditor` would have left the dominant path silent while claiming the task closed; the probe caught it, because breaking the module made the wrapper fail before the newly guarded code. `video-view` is the only wrapper without such an import, and its module is already cached whenever a video tab is open — so `createEditor` is the *rarely* reached half. **What shipped:** `showEditorPage` now takes the page id as `string | (() => Promise<string>)` and resolves it **inside** one `guard(...)` labelled `Failed to open "<editorId>"`, so the module import and `createEditor` are both covered. All five wrappers (`about`, `settings`, `mneme-config`, `storybook`, `tools-hub`) pass the loader form; the return type became `PageModel | undefined` and `showToolsHubPage`, the only caller that dereferences it, now checks. **Verification:** with the probe in place the exception no longer escapes and one toast reads `Failed to open "about-view": …`; with it removed, About and Settings open normally (7 → 8 → 9) with zero error toasts. typecheck, lint, build-prod pass. **Generalisation:** *when a guard is added to the second step of a two-step path, verify by breaking the thing the first step needs* — otherwise the probe never reaches the code under test. **Left open deliberately:** `createEditorFromFile` (the `openFile` path) has the same shape and is untouched, so a *file* whose editor module fails to load is still silent. **Review:** no concerns. |
| US-1182 | Settings "Open Environment Variables" did nothing — **not a defect** | — | **Filed and closed the same day (2026-08-28) during the first interactive pass that could drive the app UI.** Clicking the button produced no page, no exception, no console output. Every ordinary explanation was eliminated first: the page was active with its root visible, the button connected/enabled/unique in the document, a listener I attached to the same element fired on the same dispatch, the handler body worked when called directly with the same configured path, and instrumenting `EventTarget.prototype.addEventListener` across a forced re-sync proved `applyRestProps` **did** install a click listener on that exact connected `BUTTON`. Handler installed, event delivered, effect absent — indistinguishable from a real defect in EPIC-074's rest-prop code. **Cause: a stale HMR module instance.** One `location.reload()` (confirmed by `performance.now()` ≈ 9.6s) fixed it — the same click then took pages 8 → 9 and opened `board-vars.env.json`. "View Settings File" was silent for the same reason and afterwards opened `appSettings.json`, so `settingsFilePath` was never empty either. The app had hot-reloaded for ~2.5 hours across several source edits and the settings section held a module instance whose `app.openRawLink` was wired to a dead event channel — the "second, empty-state instance" trap that scripts hit importing app modules by path, arriving via HMR instead. **Lesson, now recorded in the dashboard and in memory:** the existing rule was *don't report a wedged renderer as a defect until a cold start reproduces it* — crashes only. Widened to silent no-ops: **no negative UI result on a hot-reloaded renderer is trustworthy; reload before filing.** Useful asymmetry that keeps this cheap — *positive* results survive staleness because the mechanism demonstrably ran, only *negative* ones need the reload. The theme-switch verification was re-run post-reload and returned byte-identical hashes, so it stood. |
| US-1132 | Three `uikit/` lifecycle findings from EPIC-069's close review — **obsolete** | — | Re-measured 2026-08-28 after the De-React programme closed; two of the three no longer exist and the third no longer matches its own description. (a) `ListBoxView` "retains obsolete entries in `rowViews`, added at `:329`/`:335`, removed only at `:424`" — the file was restructured around a wrapper pool: `releaseCell` (`:405-414`) now deletes from `rowViews`, and its comment states the retention is deliberate, *"for a kind change only — never on eviction"*, because a recycled wrapper keeps its view (`:383`). Cited lines no longer correspond; the residual question (does a wrapper leaving the pool take its view with it, or does the set grow with scroll distance?) went to the backlog as a question rather than a defect. (b) `DataGridView` "omits `releaseChild()` on replaced branches" — that view was rewritten around av-grid `setOptions` deltas and now contains no `this.child()`, no `releaseChild` and no child views at all (250 lines). There are no branches to release. (c) `ToolbarView` "reuses a single-use DOM `IconRef`", to be investigated alongside a nested React root EPIC-069 measured in it — neither exists: `ToolbarView.ts` (180 lines) has no `IconRef` and no icon handling, forwarding children through `fillSlot`, and a nested React root is impossible now React is confined to `editors/draw/`. |
| US-1091 | `data-part="react-slot"` stamped unconditionally before the native/React arm was chosen — **obsolete** | — | Closed 2026-08-28 by the De-React programme's completion. The defect was arm-conditional: the marker was stamped before a view chose its native or React arm, so a host holding plain DOM carried the React marker and Rule 4's root-counting instrument counted roots that did not exist. **There are no arms left.** `DialogView.ts:73` and `TagView.ts:124` still stamp it, but every path through them is native, `fillSlot` stamps nothing, and `data-react-root` is now written in exactly one place — `editors/draw/react-island.ts:22` — so no instrument can be misled. The measurement it corrupted retired with the programme. **Residual moved to the backlog:** the attribute is now a misnomer on a permanently native host and three stylesheets select on it (`ListItem.css`, `Panel.css`, `TreeItem.css`), making it a coordinated rename rather than a correctness fix. |
| US-1055 | `MermaidBodyView` builds child DOM in its constructor — **not a defect** | EPIC-071 | Closed by EPIC-071, which measured it and found the premise did not hold. `MermaidBodyView` and its `MermaidLoadingView` create → claim via `this.child(...)` → hand the child root to `createPanelElement` → `mount()` in `onMount()`, exactly like ~75 other classes; nothing reads an uncreated field and nothing is claimed twice. The rule *sentence* in `uikit/CLAUDE.md` was stricter than the codebase's own documented create → claim → mount pattern, which 157 constructor `this.child(...)` calls follow, and was narrowed to match. The programme had carried this as a "known live violation" since EPIC-060. |
| US-1110 | Script panel unusable over a Grid editor — no splitter, then one that buried itself | — | **Origin:** reported by the user while walking EPIC-067's deferred visual verification, as three symptoms: no splitter when the script panel was opened over a Grid editor; a splitter that appeared only after switching editor away and back; and dragging it upward growing the script panel while the grid refused to shrink, hiding the splitter and the toolbar's dark background under it. **First question answered: was it mine?** No — `GridBodyView.ts` and `DataGridView.ts` were both untouched by EPIC-067, so this is pre-existing. The epic *exposed* it rather than causing it. **The bug:** all three symptoms are one cause. The grid's content panel was created with `createPanelElement({ direction: "column", flex: true })` and no `minHeight: 0`, so as a flex item it defaulted to `min-height: auto` and could not shrink below its content — and av-grid writes its own measured height into that subtree, which makes the grid's *previous* size a hard floor. **Measured:** the panel stayed 925px inside a 763px container, overflowing 162px downward and covering the whole 160px script panel; `document.elementFromPoint` over the splitter returned `render-grid-scroll`, proving the splitter was unreachable rather than absent. "Switch editor away and back" worked because the remount sized the grid with the script panel already present. Monaco and Markdown were unaffected because neither body writes an internal height. **What shipped:** `minHeight: 0` on that panel, with a comment recording why it is load-bearing so it is not "cleaned up" later. **Verification:** live across script-panel heights 160/400/700/850 — the grid tracks its container exactly (763 → 523 → 223 → 200, flooring at its own intended 200px minimum), overlap stays negative, and `elementFromPoint` returns the splitter at every step. First open from closed now shows a grabbable splitter with no editor switch. **Not verified:** the pointer drag itself — `SplitterView` uses pointer capture and synthetic events do not drive it, so the layout the drag produces was tested rather than the drag; the user confirmed the fix by hand. **Generalisation:** a component that writes its own measured height into its subtree turns that height into its ancestor's content floor. **Review:** no concerns. **Docs:** reusable flex-sizing guidance added to `editor-guide.md`, generalised past av-grid to any imperative or virtualized component that stores measured height in the DOM; a `whats-new.md` bug-fix entry. |
| US-1108 | Grid toolbar search did not clear | — | **Origin:** reported by the user while walking EPIC-067's deferred visual verification — typing filtered, deleting characters kept re-filtering, but emptying the box left the grid filtered on the last non-empty term. **Two wrong diagnoses were published before the right one, both corrected in the same session.** First, av-grid's minified `setOptions` was read as merging options without re-filtering, and the library was blamed for violating its own docstring; that was the *render shell's* `setOptions`, not the public one, and av-grid 2.2.4 — the version installed — correctly routes `searchString`, `filters` and `sort` through their dedicated setters. Second, it was framed as possibly an EPIC-067 regression; `GridBodyView.ts` and `DataGridView.ts` are both untouched by that epic. **The bug:** `searchString: state.search || undefined` coerced an empty search to `undefined`, and three layers then conspired. `updateDataGrid` calls `DataGridView.invalidatePushed()` before every update, so there is no previous baseline; `collectValues` drops `undefined` entries; and `AVGrid.setOptions` acts on `searchString` only when the key is **present**. A cleared box was therefore in neither the old baseline nor the new values, the delta omitted the key entirely, and av-grid was never told. Typing hid it because a non-empty value is always present. **What shipped:** pass `state.search` verbatim — an empty box is the value "no search", not an absent option — with the three-layer interaction recorded in a comment. **Verification:** live on a scratch CSV — `paris` → 2 rows, `p` → 2, empty → **4**, `london` → 1, empty → 4. **Generalisation:** coercing a cleared value to `undefined` destroys the difference between "no value" and "option not supplied", which a diffing layer needs. **Follow-up:** US-1109 — the same interaction makes the clear unrepresentable for *any* `DataGrid` consumer that maps a cleared value to `undefined`; no other consumer does today, so it is latent. **Review:** no concerns. **Docs:** a `whats-new.md` bug-fix entry; the option-propagation detail was judged not worth a developer doc. |
| US-1092 | CSV grid errored after toggling "First row is header" or changing the delimiter | — | **Origin:** reported by the user on a real CSV while testing EPIC-065. Toggling the header showed `Unknown column "ID". Available columns: 0, 1, 2, 3, 4.` where the table should be; changing the delimiter to `;` and back gave the mirror, `Unknown column "1". Available columns: 0.` **First question answered: was it mine?** No — reproduced at `HEAD` with the entire epic stashed and the original React popover running, so it is pre-existing and predates the epic. Establishing that first was worth the restart it cost. **The bug:** in `GridEditor.setRows`, **rows and columns reach av-grid on two different channels** — rows directly via `grid.setRows()`, columns through state, and `TOneState.update` dispatches *synchronously* into `GridBodyView.applyProjection` -> `DataGridView.setOptions`. Columns were published first, so av-grid validated the new columns against the rows it still held and rejected a column no row has. Both reported symptoms are that one cause: the header toggle renames every column, the delimiter changes the column count. **What shipped:** hand the rows over before publishing the columns. Checked av-grid's own `setRows`, which validates only that it received an array, so it is safe that early rather than merely appearing to be. **Verification:** on the reporter's own file, both scenarios error-free with columns and rows in step at every step — 1 column under `;`, 5 under `,`, 14 rows header-off vs 13 header-on — and the file left unmodified. Counts were used rather than joined column names, because a single column keyed `ID,Name,Age,...` renders identically to five joined by commas and nearly produced a false pass. **Generalisation:** when one value travels on two channels, the push order is load-bearing. **Review:** no concerns. **Docs:** a `whats-new.md` bug-fix entry; no developer doc covered this seam. |
| US-1085 | `createPanelElement` dropped the `overflow` shorthand and the shrink component of `flex` | — | **Origin:** reported by the user as three separate Git-panel symptoms — the **Unstaged** list not filling its pane (~40px of dead space), the **Staged** splitter draggable down but not back up, and the list not growing into space freed by dragging. **The bug:** one cause behind all three. `applyPanelAttributes` cleared each property *inside* the setting loop, and `STYLE_PROPERTIES` deliberately mixes shorthands with their own longhands — so the `overflow-x`/`overflow-y` removals erased the `overflow` written two iterations earlier, and `flex-shrink`'s removal stripped it from `flex`. Effect: **all 84 `overflow:` panel call sites had no overflow at all** (80 `hidden`, 4 `auto`; `.scroll-container` supplies none in CSS either), and `flex: "0 0 auto"` behaved as shrink 1. Without overflow containment the whole flex chain sized from content upward instead of from the pane down. **What shipped:** clear in a separate pass before setting anything, which keeps both the reset and the shorthand-then-longhand override order the list encodes. **Blast radius, stated rather than discovered later:** `overflow: hidden` is now real at 80 sites that never had it, so content that previously escaped a panel now clips and scrolls. Only 11 panels were visible during the automated sweep — a thin sample, not a clean bill of health. **Verification:** the user confirmed the fix for the Unstaged panel, then with both lists populated, then with only the Staged list populated. **Docs:** its `whats-new.md` entry was **missed** when it shipped and was backfilled later, together with the clipping caveat — the fix rode in on its own commit and the docs pass that followed did not cover it. |
| US-1049 | Closing a grouped page threw an immer `MapSet` error and orphaned the page | EPIC-059 | **Origin:** found live over MCP while verifying US-1043 — closing one half of a side-by-side pair failed and left that tab orphaned and unclosable. **The bug:** `PagesModel.removePage` read the `compareGroups` Set off the immer *draft*, which this repo cannot do because it deliberately never calls `enableMapSet()` (US-970 recorded that decision). **What shipped:** the read moved to the pre-update snapshot (`state.compareGroups` rather than `s.compareGroups`). Auditing the class showed line 130 was the only draft read of a Map/Set in the codebase, so the fix is local and the class is closed. **Review/docs:** rode along with EPIC-059's close rather than running the standalone flow for a one-line fix in the same code area; `/userdoc` gave it a `whats-new.md` entry, since an unclosable tab is user-visible. |
| US-1040 | Cap menu popover height — `Popover`'s `maxHeight` was inert | — | **Origin:** the user reported the tab language menu opening at the full height of the window — ugly, and pre-existing rather than an EPIC-058 regression. **The bug:** not a missing cap. `MenuView` already passed `maxHeight: MAX_HEIGHT` (500), but floating-ui's `size` middleware in `PopoverModel` unconditionally overwrote it with the available space; its own comment said so and deferred the alternative — *"This intentionally overwrites a manual maxHeight prop… Taking the minimum is a separate behavior decision."* So `maxHeight` was **dead for every `Popover` caller** and `MAX_HEIGHT` had never applied to anything. **What shipped:** the middleware now takes the minimum of the available space and a caller-supplied `maxHeight`. A non-pixel string (`50vh`, `calc()`) cannot be compared numerically and yields to the available space rather than being guessed at. `MAX_HEIGHT` stays 500 — the value was always right, only the mechanism was broken; an intermediate commit changed it to 420 and the user correctly asked for it back. **Blast radius:** two production call sites change behaviour, both now honouring an author's stated intent. `MenuView` (500) is the reported fix. `PathInputView.tsx:209` passes `maxHeight: 240` for its suggestion dropdown, previously ignored — reachable through `EditLinkDialog` and the notebook note views, and worth one visual look. `Popover.story.tsx` is a harness, not production. Everything else matching `maxHeight` is a `Panel` style prop or an inline style and does not reach the middleware. **Verification:** typecheck, lint and build-prod pass. Measured live — the 96-item language menu went 1008px (in a 1071px window) to 502px with its list scrolling at `scrollHeight` 2500, and a four-item tab context menu is unchanged at 125px and does not scroll. **Review:** Codex found no concerns; the `PathInputView` behaviour change was found by my own per-call-site audit and is recorded above rather than treated as a defect. **Docs:** `doc/architecture/context-menu.md` now states that menu sizing is bounded by the smaller of available space and the caller's `maxHeight`, and that only numeric/`px` values participate. A `whats-new.md` bug-fix entry was added over Codex's recommendation — a menu filling the window is a glitch the user noticed, so it is user-visible. |
| US-1004 | Skip page-state saves while a restore is in progress | — | **Origin:** a startup during EPIC-054 came back with two pages where several — including pinned ones — had been open, and `openFiles0.json` had already been rewritten with the short list. **The bug:** `PagesPersistenceModel.applyState` attaches restored pages one at a time, and `PagesModel.attachPage` subscribes each page and its editors to `saveStateDebounced`, so restore generates save triggers while `s.pages` is still `[]`. A save firing in that window serialises an empty or partial list over the real session. Usually a later save wins and nobody notices; if the window closes or the renderer reloads first — routine under `npm start` with HMR — the short list is what persists, and there is no second copy. **What shipped:** a `restored` flag on `PagesPersistenceModel`; `saveState()` returns early while it is false, and `saveStateDebounced` passes it as `debounce`'s existing `canRun` gate, which re-schedules rather than drops so a suppressed save still runs once restore completes. The guard sits on `saveState` itself because `PageNavigator.ts` and `PagesLifecycleModel.ts` save without the debounce. `init()` sets the flag in a `finally`. **Invariant:** the gate tracks restore-in-progress only. A rejected descriptor is expected — restore drops it and carries on, and a `schemaVersion` bump discards every page by design — so persistence must keep working afterwards; disabling saves on a rejected descriptor would turn a one-time loss of open pages into a session that never persists again. **Explicitly not shipped:** no backup file and no recovery affordance, per user decision — the user reopens what is missing. **Verification:** `tsc --noEmit` and `eslint` clean; `/review` found no concerns. |
| US-993 | Fix `AsyncRef` never resolving its first promise | — | **Origin:** found while reading av-grid during EPIC-053 planning (Concern 8). **The bug:** `uikit/RenderGrid/AsyncRef.ts` declared `async = new Promise(...)` as a class field whose initializer installed `resolveAsync`; the constructor body then ran `this.resolveAsync = undefined` and wiped it, because field initializers run first. The first `ref()` therefore took the `else` branch and *replaced* `async` instead of resolving it, so any caller already awaiting the original promise waited forever. Reachable when a `scrollTo` / `scrollToRow` / `scrollToCol` / `scrollBy` / `onRenderInfoChanged` runs before the grid container mounts (`RenderGridModel.ts:326`, `:495-547`): the continuation that sets `scrollLeft`/`scrollTop` never runs and the promise stays pending for the life of the model. It presents as "the grid ignored my scroll-to-row", never as an error, which is why it survived. **What shipped:** the implementation was taken from av-grid's `core/AsyncRef.ts`, which fixed the same defect during its port — the promise is now built in the constructor body after the reset. The default export is preserved so `RenderGridModel.ts:2` is unchanged, `resolveAsync` is now private, and a comment records why `async` must not move back to a field initializer. **Verification:** `npm run typecheck` and `eslint` clean; both variants were reproduced standalone and the pre-mount await confirmed as `HUNG` before / `resolved` after. **Review/docs:** skipped at the user's request — a one-file behavioural fix with no architecture or user-facing surface. |
| US-964 | Align content-pipeline routing, link persistence, and pipe ownership | — | **Origin:** refactoring survey §3.6. **What shipped:** HTTP content classification is now a focused extension set with registry-based built-in editor resolution and PDF browser fallback; link data has composable pipeline/navigation/core types plus a `StoredLinkData` persistence boundary; `PipePair` owns TextFile source/cache replacement and disposal; transformer writes receive lazy, memoized original reads so ordinary writes do not reread archives. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed. **Review:** no architecture/standards concerns found. **Docs:** content-pipeline, overview, state-management, folder-structure, and key-files docs now describe the ownership and lazy-write boundaries; `/userdoc` found no user-facing change warranted. **Live testing:** the local Persephone MCP endpoint did not respond during completion, so the URL/save smoke matrix remains unverified. No commit was created. |
| US-963 | Remove Emotion from editors through focused UIKit composition | — | **Origin:** refactoring survey §3.3. **What shipped:** the reusable image viewport moved into `uikit/ImageViewport/`; Monaco and Boards use existing UIKit primitives; Browser, Graph, and Markdown styles are editor-local CSS; and Bookmarks, Video, webview, and floating-ui adapters retain only scoped native DOM integration. Browser webview background uses a semantic theme token. Mermaid SVG contrast and CircularEffect canvas colours remain documented output-generation exceptions. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed. **Review/docs:** completion workflows were run; no user-facing documentation change was warranted for this internal styling refactor. No commit was created. |
| US-962 | Break the tree-provider ↔ link-editor dependency cycle | — | **Origin:** refactoring survey §3.2. **What shipped:** shared favicon caching and `TreeProviderItemIcon` moved to `components/icons`; `LINK` / `FILE_LINK` trait ownership and registration moved to `core/traits`; `CategoryView` now receives a typed item renderer from `CategoryEditor`, removing its direct Link-editor presentation dependency while preserving list/tile, selection, search, actions, and drag/drop behavior. Tree-provider consumers use the intended public barrel. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed; Link Editor and Category flows were manually accepted for completion. **Review:** clean; no architecture concerns. **Docs:** architecture folder/key-file, trait-system, and secondary-view docs were updated; `/userdoc` found no user-facing or What's New change warranted for this internal refactor. No commit was created. |
| US-961 | Renderer MCP command-handler decomposition | — | **Origin:** refactoring survey §2.3. **What shipped:** `mcp-handler.ts` is now a 30-line IPC/distribution shell; focused `api/mcp/` modules own page, Board, tool, UI-push, request-log, and registry behavior. The 16 non-browser methods use an exhaustive typed registry, while `browser_*` retains its lazy automation import and unknown methods retain the `-32601` response. Dialog specifications are module-scoped, page summaries/browser privacy extraction are shared, and standalone-editor MCP hints come from editor metadata. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed; page operations and `ui_push` were smoke-tested against the running local MCP HTTP endpoint. **Review:** clean after correcting three mojibake user-facing strings. **Docs:** architecture ownership/maps were updated; `/userdoc` confirmed no user-facing or What's New change was warranted. No commit was created. |
| US-960 | Board shim and main IPC controller decomposition | — | **Origin:** refactoring survey §2.10. **What shipped:** the board browser IIFE now keeps boot, port plumbing, external-link routing, and the public `window.persephone` facade while focused browser-safe modules own the existing context-menu and diagnostics concerns. All host-frame messages cross the shared source/origin trust gate. Board RPC and outbound runner traffic use exhaustive protocol-keyed handler maps, preserving malformed-input behavior. The former 615-line main IPC controller is a four-call composition root; core, Git, and Board registrars use a typed request/reply binder derived from `Api`, retaining lazy imports, channels, argument order, and error semantics. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed; Boards were manually smoke-tested in the running development build. **Review:** clean after standardizing shared caught-error conversion on `errMessage`. **Docs:** architecture folder/key-file maps describe the new Board and IPC boundaries; `/userdoc` confirmed no user-facing or What's New update was warranted. No commit was created. |
| US-959 | UIKit Tree model decomposition | — | **What shipped:** `TreeModel` now coordinates focused keyboard and drag/drop submodels while preserving its public handler surface, lazy loading, expansion, and reveal APIs. A shared UIKit drag-enter counter removes duplicate nested-event bookkeeping from Tree and Category; `MultiListBoxModel` owns controlled filtering, active-row, selection, and select-all state; AVGrid focus navigation now dispatches through typed key handlers. **Regression fixed during smoke testing:** Grid editor host-state mirroring now uses the correct unfiltered subscription overload, restoring `.grid.json` opening; Tree-only drag props are no longer forwarded to the DOM. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed; Tree, Explorer, and Grid were smoke-tested in the running development build. **Review:** clean. **Docs:** key-files records the new UIKit ownership points; `/userdoc` found no user-facing documentation or What's New update warranted. No commit was created. |
| US-958 | Tree-provider shared actions | — | **Origin:** the thirteenth item actioned from the refactoring survey (§2.8), following US-957. **What shipped:** shared `item-crud-actions.ts`, `item-menus.tsx`, `drop-dispatch.ts`, and `href-utils.ts` now own duplicated CRUD/dialog operations, context-menu construction, trait-drop routing, and href comparison. `TreeProviderViewModel` and `CategoryViewModel` compose those modules and are JSX-free `.ts` implementations; tiny `.tsx` re-export shims preserve compatibility with active Vite/HMR sessions. Explorer root restrictions, Category’s `Open` action, menu ordering, provider contracts, move guards, and cross-provider file/link routing remain unchanged. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed; after a fresh dev build the Explorer tree rendered correctly. **Review/documentation/user-doc workflows:** completed; no user-facing docs or What's New entry was warranted for this internal behavior-preserving refactor. No commit was created. |
| US-957 | Browser editor model leaks | — | **Origin:** the twelfth item actioned from the refactoring survey (§2.7), following US-956. **What shipped:** `BrowserTabsModel` now owns internal-tab lifecycle, URL/favicon caches, and bookmark resources; `BrowserTorModel` owns Tor partition/proxy/listener lifecycle; `webview-context-menu.ts` owns context-menu construction and bounded DOM probes; navigation handling shares one helper while preserving full-navigation-only behavior. `BrowserEditor` remains the coordinator for restore/persistence, profile/navigation policy, keyboard shortcuts, popup handling, and composed-model lifecycle. Browser views and bookmark UI now read the focused models directly. **Verification:** `npm run typecheck`, `npm run lint`, and `git diff --check` passed; the production build passed and browser code remained in lazy renderer chunks. **Review:** clean; no architecture or standards concerns. **Docs:** `browser-editor.md`, `folder-structure.md`, and `key-files.md` now describe ownership and bookmark initialization; `/userdoc` found no user-facing change or What's New entry warranted. No commit was created. |
| US-956 | SettingsView model-view decomposition | — | **Origin:** the eleventh item actioned from the refactoring survey (§2.6), picked after US-955. `SettingsView.tsx` combined the page shell with fourteen settings sections and their stateful behavior. **What shipped:** the page is now a 98-line composer; focused views and models live under `src/renderer/editors/settings/sections/`. `BrowserProfilesSectionModel` owns profile CRUD, bookmark/native-dialog operations, Tor configuration and partition-data clearing; `McpSectionModel` owns MCP/Mneme status, port validation, toggles, config generation and clipboard feedback; `DefaultBrowserSectionModel` owns registration status and actions; theme, file search and the remaining settings sections are isolated views. Existing section order, setting keys, persistence and behavior were preserved. **Verification:** `npm run typecheck`, `npm run lint` and `git diff --check` passed. The live smoke test was unavailable in this session because no dev listener was visible and the Persephone MCP request timed out. **Review:** clean, including reactive Tor port binding and direct UIKit imports. **Docs:** developer architecture/key-file/model-view docs now describe the settings sections and model ownership; `/userdoc` found no user-facing documentation or What's New change appropriate for an internal behavior-preserving refactor. No commit was created. |
| US-955 | GraphEditor decomposition along existing sub-model seams | — | **Origin:** the tenth item actioned from the refactoring survey (§2.5), following US-954. `GraphEditor.ts` still combined host lifecycle and serialization with three cohesive interactive concerns. **What shipped:** `GraphTooltipModel` now owns delayed show/hide timers, tooltip hover state and status hints; `GraphGroupActionsModel` owns dialog-driven group creation, membership, reparenting, ungroup/delete flows, recursive selection and empty-group cleanup; `GraphMutationModel` owns node/link CRUD, batch property updates, selected-node deletion, Markdown/grid export and subgraph extraction. `GraphEditor` remains the coordinator for host lifecycle, parsing and unknown-field-preserving serialization, rebuild orchestration, visibility/search, context-menu composition and selection snapshots. `GraphBody` calls the composed models directly for the extracted view actions. The editor shrank from 1,667 to 814 lines with behavior and serialized graph format preserved. **Verification:** `npm run typecheck`, `npm run lint` and `git diff --check` passed; a live dev/HMR smoke test created and rendered a temporary grouped four-node graph through Persephone MCP without an error boundary, then closed it. **Review:** clean, with no architecture concerns or stale callers. **Docs:** the architecture folder tree and key-files ownership map now describe the three models; `/userdoc` found no user-facing documentation or What's New change appropriate for a behavior-preserving internal decomposition. No commit was created. |
| US-954 | MCP server split — tools as data under `main/mcp/` | — | **Origin:** the ninth item actioned from the refactoring survey (§2.4), picked by the user after US-953. `src/main/mcp-http-server.ts` was 1,119 lines, 714 of them one `createMcpServer()` function in which ~30 of 33 tools repeated the identical `server.tool(name, desc, schema, args => toToolResult(sendToRenderer(method, args)))` boilerplate. **What shipped:** the transport file is 279 lines (sessions, HTTP, start/stop) and contains no tool definitions; everything the server *offers* moved to `src/main/mcp/`. Tools are now `IMcpToolDef` data — name, description, zod schema, optional `method` / `toResult` / `timeoutMs` — and `register-tools.ts` supplies the one pass-through that implements all 30: strip `windowIndex` (a target selector, not a parameter), forward the rest under `method ?? name`, map with `toResult ?? toToolResult`. Only `list_windows`, `open_window` (answered in main from `windowStates`) and `read_guide` keep handlers. Supporting modules: `sdk.ts` (the lazy MCP-SDK/zod loader, now `loadSdk()`/`requireSdk()` instead of four module-level `let`s), `renderer-bridge.ts` (`initMcpIpc`, `sendToRenderer`, `cancelPendingRequests`), `tool-results.ts`, `manifest.ts` (identity, the instructions block, the 12 guides), `server-factory.ts`, and `tools/{params,window,page,board,agent,browser,guide}-tools.ts`. **Defects fixed on the way:** the `if (browserToolsEnabled) { … } // end browserToolsEnabled` sandwich with its deliberately un-indented body is now a list filter over tool groups; the byte-identical GET and DELETE branches of `handleHttpRequest` are one branch; guides were `readFileSync`-ed on every `read_guide` call and every resource read (`guides/full` read all 12 at once) and are now cached — keyed on `mtimeMs`, not blindly, so an app left running still picks up an edited `assets/mcp-res-*.md`, which this repo's own `/document` workflow produces. **Why zod is passed around:** the SDK bundle is lazy-loaded, so `z` does not exist at module-eval time — each tool group is a factory taking a `IToolContext` (`z` plus the shared `windowIndex` / browser-targeting param schemas) rather than a module-level constant array. **Trade-off accepted:** the registrar cannot infer callback arg types from the schema the way the SDK's own overloads do, so `ToolArgs` is `Record<string, unknown>` and the three custom handlers cast; that is precisely what lets the 30 pass-throughs collapse. **Verification:** tsc + eslint clean, production build OK, and the registered tool-name set diffs identical to the pre-change file (33 tools); descriptions, schemas, renderer methods and the three special timeouts (`execute_tool` 0, `create_toolset` 0, `ui_push` 0-when-dialogs) were moved verbatim. Review: no concerns, no suggestions. `/userdoc`: nothing — `/docs/` documents tool names and setup, none of which changed. **Untested at close:** nothing was exercised against a running app — the change needs a rebuilt binary. Worth checking after restart: the `initialize` handshake (instructions arrive), `read_guide`, one pass-through tool, `browser_*` with the setting on and off, and a screenshot (the one mapper whose `mimeType` now falls back to `image/png`). |
| US-953 | PageModel decomposition — editor-switch, page-explorer, NavBackStack extracted | — | **Origin:** the eighth item actioned from the refactoring survey (§2.2), picked by the user after US-952. `PageModel.ts` (915 lines) held three concerns that aren't page state, plus the survey's 4-copy "explicit content-host target wins" predicate. **What shipped:** `PageModel.ts` 915 → 734 lines across three new modules. (1) `editors/base/editor-switch.ts` — `switchMainEditor` moved verbatim (board host-transfer vs dispose-and-rebuild branches, the Board Info exemption, the host-less Archive source path — all comments intact); its two literally-duplicated 18-line dispose-and-rebuild blocks are now one `rebuildEditorOverFile()` helper. The page keeps a dynamic-import delegate, dropping `parseBoardEditorId`/`BOARD_INFO_EDITOR_ID`/`TComponentState` from its static imports; the redundant nested dynamic import of `customEditorRegistry` (module already statically imported for `parseBoardEditorId`) was collapsed to static, with a comment so nobody "fixes" it back. (2) `editors/explorer/page-explorer.ts` — `toggleNavigator`, `autoInitExplorer`, `explorerRootForPanels` as functions taking `page`; constructing `ExplorerEditor` is an editor concern. The page keeps the `IPageHost` delegates plus the sync guards (`findExplorer`, `canOpenNavigator`, `_maybeAutoInitExplorer`'s root check + microtask queueing) — solved with a static import (no cycle: page-explorer only type-imports PageModel; chunking unchanged since ExplorerEditor was already a static import). (3) `api/pages/NavBackStack.ts` — the Markdown back-nav stack as a class with an `onCountChanged` callback; page keeps `pushNavBack`/`popNavBack`/`seedNavBack` delegates and serializes via `snapshot()`. **Predicate dedup:** `isExplicitHostTarget(target, filePath)` exported from `editorRegistry.ts`, replacing the verbatim copies in `PageNavigator.applyEditorSelection` and lifecycle `openFile`; the two switch copies collapsed into the shared rebuild helper. **Review:** no concerns; its one optional suggestion (comment the intentional static `customEditorRegistry` import) was taken. **Verified:** `tsc --noEmit` + `eslint` clean; production build confirms the browser chunks stayed lazy (a mid-task false alarm came from grepping a stale `dist/` — the real renderer output is `.vite/renderer/main_window/`, where the entry chunk has zero browser/Tor strings). **Untested at close:** no app run. Worth exercising: editor switching across all three shapes (Monaco↔Grid host transfer; board↔built-in both kinds; Archive/zip-based file → text rebuild with a confirmRelease prompt), the navigator toggle button + auto-Explorer beside a Link collection, and the Markdown Back button incl. persistence across restart. |
| US-952 | PagesLifecycleModel decomposition — browser pages, PageNavigator, link/draw builders relocated | — | **Origin:** the seventh item actioned from the refactoring survey (§2.1), picked by the user after US-951. The file had shrunk 1,422 → 1,233 lines since the survey (earlier items), but all four misplaced concerns remained — and its static `import { BrowserEditor }` was the ONLY value import of `editors/browser` in startup code, silently defeating code splitting for the whole browser chunk. **What shipped:** `PagesLifecycleModel.ts` 1,233 → 845 lines across three new modules. (1) `editors/browser/browser-pages.ts` — `showBrowserPage` (Tor gate + arm-before-mount fail-closed sequence, comments intact) and `openUrlInBrowserTab` (profile matching + nearest-tab reuse) moved verbatim as functions taking `model: PagesModel`; being inside the browser folder they use typed `BrowserEditor` access instead of the old `as unknown as` casts (one narrowed `const openUrl` added — closure narrowing doesn't survive into `state.update` callbacks against the now-typed state). The lifecycle keeps thin `await import(...)` delegates, so no caller changes. (2) `api/pages/PageNavigator.ts` — `navigatePageTo` (191 lines, the US-617/637/808/901 edge cases) moved byte-identical but decomposed into named steps: `confirmLeaveCurrentEditor`, `reuseNavigationTarget`, `reuseEditorForFile`, `buildEditor` (missing-file/error fallbacks), `applyEditorSelection` (explicit-host-target vs preview), shared `finishNavigation` (show/focus-unless-sidebar/save). It reaches `attachEditorToPage` via dynamic `import("./PagesLifecycleModel")` — the same pattern PageModel already uses — because the lifecycle statically imports the navigator. (3) `editors/link-editor/link-open.ts` — `buildLinkEditorContent`; deliberately NOT in `linkTypes.ts`, whose side-effect `linkTraits` import would be statically pulled into startup by the sync `openLinks` API. **Small items:** `drawExport.buildExcalidrawJsonFromDataUrl` (MIME-sniff knowledge now lives in `editors/draw/`); six `show*Page` singletons collapsed onto one `showEditorPage(editorId, pageId?)` helper (the `wrap()` calls on mneme-config/tools-hub were identity no-ops and were dropped; McpInspector stays custom for state seeding + autoConnect); private `sendOpenRawLink` replaces four copies of the openRawLink dispatch (`handleOpenUrl`/`handleExternalUrl` stay as distinct subscription points); `PagesQueryModel.findPageByFilePath` replaces the five inline "is this file already open" scans (PagesModel.openFile, lifecycle openFile/openDiff×2, `open-env-vars.ts`); dead `PagesPersistenceModel.withFreshEditorId` and the zero-importer `isTextFileModel` re-export deleted. **Review:** no concerns; two optional notes (the deliberate dynamic-import cycle navigator↔lifecycle; a possible future doc note if a third "dependency-light for startup" module appears) — no action taken. **Verified:** `tsc --noEmit` + `eslint` clean; full production build confirms the split — browser code now in lazy `BrowserEditor-*.js`/`browser-pages-*.js` chunks not referenced by `index.html`, and the entry chunk contains no Tor/browser strings. **Untested at close:** no app run. Worth exercising (navigatePageTo edge cases): Explorer tree clicks (focus stays in sidebar), re-selecting an already-open file, Git-Tree singleton promotion, "Open in File Diff" with preselected revisions, markdown anchor links, a missing file; plus Tor browser open and `openUrlInBrowserTab` (nearest-tab + about:blank reuse). |
| US-951 | Shared `ExecuteHandle` — one state machine for `app.proc.execute` and `persephone.execute` | — | **Origin:** the sixth item actioned from the refactoring survey (§1.6), picked by the user after US-950. The report called it "three copies"; it is two full ones — `proc.ts` (class `ExecuteHandle`) and `board-shim.ts` (`createHandle`) were identical down to the `idle/buffered/streaming` mode machine, the two chunk buffers, the four callback arrays, `waitDone`/`ensureBufferable`, the chunk replay on late listener attach, `getBytes`/`getText`/`getJson` with all four error strings, and the `concat`/`lastMatch` helpers. `tool-executor.ts` is not a copy of the state machine at all — it consumes a handle in streaming mode and duplicated only `concat`. **What shipped:** `src/shared/execute-handle.ts` (263 lines) owns `RunnerError`, private `lastMatch`, the `ExecuteTransport` interface and `createExecuteHandle(transport, start)`; the two clients keep only their transports — `proc.ts` 353 → 86 lines (four `ipcRenderer.on` subscriptions filtered by `jobId`, plus the compile-time drift guard against `types/proc.d.ts`, which stays), `board-shim.ts` −240 lines (`createHandle` is now 24 lines over the `runnerHandlers` map + `post`); `concat` became `concatChunks` in `shared/utils.ts` and `tool-executor.ts` consumes it. Net −190 lines. **Beyond the plan:** `RunnerOutbound*`/`RunnerInbound*` unions added to `runner-channels.ts` so `ExecuteTransport` is typed by protocol direction — without them the shim needed an `as BoardToMain` cast on its `post` envelope, and that cast would have hidden exactly the mismatch the contract exists to catch; `runnerHandlers`' map value type narrowed to match. **Placement:** `shared/` (not `ipc/`) because the board shim bundles it into a standalone browser IIFE, so it must stay dependency-free — it imports only `ipc/runner-channels` and `shared/utils`, both already dependency-free and already bundled into the shim. **Deliberate behavior change:** a board's `getJson()` rejection is now a `RunnerError` (`err.name === "RunnerError"`) instead of a plain `Error` with `exitCode`/`stderr` folded on; message and both properties are unchanged, and `instanceof` never worked across the iframe realm anyway. **Review:** no concerns. **Verified:** `tsc --noEmit` clean, `npm run lint` clean, and the shim rebuilt through `build-prod.mjs`'s exact rollup config — 5 modules, 44.42 kB, no surviving `require`/`module.exports`, `createExecuteHandle` inlined. **Untested at close:** no app run. Worth exercising: a board `persephone.execute()` (buffered `getText`, then a streaming `on("stdout")`), `executeNode` (the only caller of the `{ node, args }` start-message extra), a script `app.proc.execute(...).getJson(/…/)`, and one agent-tool run for the streaming + timeout-`kill` path. |
| US-950 | Error-handling helpers — `errMessage` / `guard` / git `mutation` | — | **Origin:** the fifth item actioned from the refactoring survey (§1.3, "24+ hand-rolled copies"), picked by the user after US-949. The report undercounted: the sweep found **114 occurrences across 60 files** in six dialects, ~35 of them the unsafe `(e as Error).message` cast that renders `undefined` for a thrown string. **What shipped: 58 files.** (1) `errMessage(e, fallback?)` in `src/shared/utils.ts` — `shared/` because main, renderer and the board shim all need it, and dependency-free because `board-shim.ts` bundles it into a standalone browser IIFE. It deliberately checks for a string `.message` **before** falling back to `String(e)` rather than testing `instanceof Error` first: errors crossing the main↔renderer IPC boundary and MCP JSON-RPC replies arrive as plain objects that fail the prototype check but still carry a real message — which is exactly why ~15 sites were already written defensively as `err?.message \|\| String(err)`, and why a prototype-only helper would have regressed them to `"[object Object]"`. (2) `guard(label, fn, level?)` in `renderer/core/utils/guard.ts` (that layer already reaches into `api/` — cf. `copy-files.ts`, `file-watcher.ts`); the label is the full prefix, so toast text is byte-identical. Adopted at 13 sites only — `RendererEventsService` ×8, `GlobalEventService.openDroppedPaths`, `draw/index.tsx` ×4 — strictly where the catch body was *exactly* a notify; catches that also set state or re-throw kept their `try`/`catch`, because the wrapper would hide that logic. (3) private `mutation()` in `main/git-service.ts` over stage/unstage/discard/commit/switchTo/createBranch/fetch; push/pull kept their richer result shapes. **Bug caught by lint:** the scripted sweep rewrote `worker-host.ts:119`, which sits inside the `WORKER_CODE` template literal — the worker thread's own source, with no access to host imports — a `ReferenceError` on the worker's error path. Reverted with a comment saying why; a follow-up scan confirmed it was the only in-template hit in `src/`. **Deliberate behavior changes:** git result strings lose the `"Error: "` prefix (`String(e)` on a simple-git `GitError` produced it; `errMessage` prefers `.message`), and the unsafe-cast sites now render a fallback instead of `undefined` — both recorded in `docs/whats-new.md` under Bug Fixes. **Docs:** `coding-style.md` gained "Turning a Caught Value Into a Message" and "Reporting a Failure as a Toast", and its own try/catch example — which taught the retired pattern — was fixed; `CLAUDE.md` quick-reference bullet; `key-files.md` rows for `shared/utils.ts` and `guard.ts`; `folder-structure.md`. **Review:** no concerns; the one cosmetic suggestion (import placed above a file's leading JSDoc in `open-with-default-app.ts`) was taken. **Untested at close:** typecheck, lint and grep only — no app run. Worth exercising: a failing git mutation (commit with no identity, non-fast-forward push) to confirm the text still reads well without the prefix, a drop of an unreadable file, and one draw-editor export failure for the `guard` toasts. |
| US-949 | Registry-driven editor construction — legacy module system removed | — | **Origin:** the fourth item actioned from the refactoring survey (§1.2, "kills two parallel registries"), picked by the user after US-948 — which set up the win: every text-host editor's `onHostAttached` already held the bootstrap that `attachEditorToPage`'s 15 inline blocks duplicated. **What shipped (commits f88590ff + completion): 31 files, +322/−1,303.** (1) `attachEditorToPage` is ~40 lines: it builds the editor named by `host.state.editor` via a new `editorRegistry.createEditorSync(id, instanceId)`, asserts `instanceof TextHostEditorModel`, `adoptHost(host)`, then the new public `bootstrapFromHost()` bridge → `onHostAttached` (no-op for editors that parse inside adoption). It MUST stay synchronous — it sits under the sync scripting APIs `addEditorPage`/`openLinks`/`addEmptyPage`/`page.grouped` (`PageWrapper` returns `page.grouped` as a sync property) — so `createEditorSync` reads the module cache warmed by `preloadContentHostModules()` at the end of registration (load failures `console.warn` and surface on first use). **Monaco stays a static import** — the guaranteed-sync floor: the startup empty page (`checkEmptyPage` → `addEmptyPage`, sync, after async restore) and `requireGroupedText` construct it before preload can be assumed complete. Modules construct with `id: ""` (no module generates ids — the registry's old "module-generated id" comment was stale and is fixed); the host's id rides in as `instanceId`, preserving the old `legacy.id \|\| randomUUID()` seeding. `createEditor` now delegates to `createEditorSync` after `loadModule`, so the id-stamp rule lives once. (2) `buildEditorById`'s 8-case dynamic-import switch became optional `newEditorModel?(filePath)` on the registry `EditorModule`, implemented by image/archive/video/category/git-tree/mneme-root/board/toolset (link-decode/path-seeding bodies moved verbatim into the barrels; category keeps its inner dynamic imports). `board-info` deliberately got none — never a file-open target, must not become one. (3) **The legacy `editors/types.ts` module system is gone.** All 14 `newEditorModelFromState` implementations were provably unreachable (restore has been registry-driven since EPIC-031); the six `showX` pages use plain `editorRegistry.createEditor(id)` — the legacy `EditorType` self-guard could never fire since the caller always picked the owning module, so the `if (model)` null-checks went too; the browser legacy module had zero importers. `editors/types.ts` keeps only `FileEditorComponent` + `EditorViewModule` (AsyncEditor/RenderEditor). (4) `register-editors.ts` 507 → ~230 lines: an `EDITORS` table + loop; `match` derives from `EDITOR_MATCHERS[id]`, `accepts` defaults to `makeAccepts(match)` or `() => -1`; monaco + file-diff keep explicit `accepts`; each row's `load` is a literal `import()` (code splitting); **row order preserved — it breaks priority ties in `resolveForFile`** (verified against the original registration order). **Accepted edge:** between launch and preload completion a sync `addEditorPage("draw-view")` throws a descriptive "module not loaded yet" error — window is a few local chunk loads; the old static imports had no such window. **Deliberately unchanged:** `BrowserBookmarks` sync-constructs `LinkEditor` (documented exception); `PageWrapper`/`mcp-handler` keep static editor-class imports for `instanceof` narrowing (mutes part of the bundle-split win; orthogonal). **Process:** two Explore surveys (per-module `createEditor` shapes + legacy-system callers/importers) before any code; registry/base/lifecycle/table by hand, the 8 moves + 5 deletions by two parallel agents against verbatim specs; every agent hunk diff-reviewed. **Review:** no concerns; took the preload `console.warn` suggestion, declined extracting page-id constants. **Docs:** editors.md — "EditorModule Interface & Registration" rewritten (the old section described `create()`/`Editor` fields that never existed) + new "Editor Construction Paths" section + `onHostAttached` contract updated (now runs on switch/restore/open paths); editor-guide.md steps 4–5 + hook note + checklist rewritten around the table; key-files.md registry/registration/TextHostEditorModel rows; folder-structure.md; pages-architecture.md singleton note. `/userdoc`: pure internal refactor, no `/docs/` changes. `tsc --noEmit` + `eslint` clean. **Untested at close:** typecheck/lint/review only — the startup path changed, so one full app restart matters (first-launch empty page), plus file opens per editor, editor switching, session restore, new-page toolbar items, and the standalone editors (image/archive/video/git-tree/board/toolset/About/Settings/MCP Inspector/Storybook); renderer-only change, HMR applies except the restart-worthy startup check. |
| US-948 | `TextHostEditorModel` base class — one host-adoption lifecycle for 14 editors | — | **Origin:** the third item actioned from the refactoring survey (§1.1, "the single biggest win"), picked by the user after US-947. Sixteen editors had independently copy-pasted the same host plumbing: `CONTENT_HOST_TRAIT` registration, `switchFrom`/`restore`/`adoptHost`, identity-only persistence, `confirmRelease`/`saveState`/`setPage`/`dispose`, host accessors. **What shipped:** `src/renderer/editors/base/TextHostEditorModel.ts` (~330 lines) between `EditorModel` and every `TextFileModel`-wrapping editor; 14 editors migrated (monaco, grid, markdown, mermaid, svg, html, graph, link, notebook, rest-client, draw, env-vars, file-diff, log-view) — **−2,904/+395 lines** across them, net ≈ −2,180. Subclass surface: `displayName` (error/notify strings), `adoptHost` override (`super` first, then domain wiring), `onHostAttached` (initial load on switch/session-restore paths ONLY — `attachEditorToPage` open-file callers invoke that load themselves after a bare `adoptHost`, so the hook split preserves that contract exactly), `onHostExtracted` (switch-away cleanup), `untitledName` (e.g. "untitled.link.json"). **Three helpers replace the subtle copy-pastes:** a single host-subscription registry (`registerHostSubscription`) torn down as one set on re-adopt/extract/dispose; the `writeToHost`/`subscribeHostContent` content echo guard (the `skipNextContentUpdate` flag six editors hand-rolled); `mirrorHostSettings` (seed from `host.getEditorState(editorId)` + mirror back — the HS1 pattern). Grid deliberately keeps its own string-compare content guard (its skip semantics differ: content equal to the last self-write must not reparse). **Scope decision — 14, not 16:** `BoardContentEditorModel` extends `BoardEditorModel` (single inheritance) and `BoardInfoEditorModel` deliberately deviates (optional host, tolerant `switchFrom`, no host `editor` stamp, no fallback host on restore failure); both keep their own plumbing, recorded in editors.md. **Two latent leaks fixed:** FileDiff's trait extraction tore down only `_hostStateUnsub` and leaked its gitRepo-root subscription on switch-away; LogView's inline teardowns never nulled their fields. **Accepted micro-differences, all bounded:** error/notify strings unified and parameterized ("Host already extracted from Link editor" vs "…from LinkEditor"); `secondaryView`/domain subscriptions now run after the base title/id stamp instead of before (only effect: a few guarded no-op mirror/save fires during adoption are suppressed); FileDiff's explicit no-op `focus()` deleted (`EditorModel.focus` is already a no-op). **Process:** two parallel survey agents produced per-editor deviation reports (every member classified CANONICAL vs quoted deviation) before any code; base + 5 reference migrations by hand, 9 by three parallel agents against the surveys; every agent-written hunk then diff-reviewed against the survey. **Review:** no concerns — spot-checked Draw/Graph/Grid/Notebook/RestClient/FileDiff/LogView diffs line-by-line, confirmed both leak fixes real, board exclusion justified, no color/barrel/`!` violations. **Docs:** editors.md gained a TextHostEditorModel section and — fixing pre-existing drift — its `CONTENT_HOST_TRAIT`/switching sections now describe the real `switchFrom` flow (the documented `inheritContentHost`/`switchEditorViaContentHost` API never existed in code); editor-guide.md's text-bearing recipe rewritten around the base (decision tree, example, hook order, checklist); key-files.md + folder-structure.md rows. `/userdoc`: pure internal refactor, no `/docs/` changes. `tsc --noEmit` + `eslint` clean. **Untested at close:** typecheck/lint/review only — editor switching (Monaco↔Grid/Markdown/Mermaid), session restore, and data-editor save round-trips (Link/Notebook/Rest/EnvVars/Grid/Graph/Draw/LogView) were left to the user's manual pass; renderer-only change, HMR applies. |
| US-947 | Shared `SidecarProcess` — one lifecycle for tor-service and mneme-service | — | **Origin:** the second item actioned from the refactoring survey (§1.5), picked by the user after US-946. tor-service.ts and mneme-service.ts had independently grown the same child-process lifecycle: spawn with failure message, stdout readiness sentinel (`Bootstrapped 100%` vs `listening on`), readiness timeout, line logging, stale-child guard on `close`, unexpected-death broadcast, `stopAndWait` with a 5 s reap cap, in-flight start dedupe, restart. **What shipped:** `src/main/sidecar-process.ts` — a `SidecarProcess` class configured with `{ name, isReady, readinessTimeoutMs, timeoutMessage?, exitTimeoutMs?, log, onReady?, onUnexpectedExit? }`; services call `start`/`restart`/`stop`/`stopAndWait` with `(exe, args, spawnOptions?)` and keep every domain concern. tor-service went 583 → 405 lines (partitions, fail-closed proxying, torrc, checkIp/geo, broadcasts), mneme-service 212 → 134 (port/config/URL, `MnemeStatus` mapping via a small `toStatus`). Public surfaces unchanged — same exports and IPC handlers, so no other file moved. **The two subtle invariants now exist once, with their rationale:** the stale-child guard (a replaced process's late `close` must not null out its healthy successor — `stop`/`restart` null `proc` synchronously, which is also what makes a current, ready child's close an *unexpected* death), and restart's pending promise registered **synchronously** so two concurrent restarts join one attempt instead of racing two daemons onto Tor's locked DataDirectory. `onUnexpectedExit` fires only for post-readiness deaths of the current child (`wasCurrent && ready`) — pre-ready failures report through the start result. **Three accepted micro-differences, all bounded:** (1) a caller *joining* an in-flight Tor restart resolves when the daemon is ready, possibly before the initiator re-applies proxies — safe because the initiator still re-applies and broadcasts, and the result value is identical; commented at the join site. (2) Mneme's timeout log/error unified into one string; Tor keeps its exact "Tor bootstrap timed out (90 s)" via `timeoutMessage`, and the spawn-failure message now names the actual exe basename rather than hardcoded "tor.exe" — more accurate when a drop-in like tnet is configured. (3) Mneme gained robustness bits Tor had: the "did not exit in time; continuing anyway" log, `clearTimeout` in stop-and-wait, and the `exit`-event listener. Also fixed latently: Mneme's old close handler could let a stale child clobber module state when `!settled`; the unified guard is strictly tighter, and a joined `startMneme` no longer mutates `currentPort` under a pending start bound to the old port. **Review:** no concerns — verified line-by-line against the diff as a faithful extraction; confirmed main-process `fs`/`path` imports are convention there (rules 9/10 are renderer-scoped); its optional suggestion (mention `SidecarProcess` in overview.md) was taken via `/document`. **Docs:** `key-files.md` row for `sidecar-process.ts`; `folder-structure.md` rows for all three files reworded to "domain concerns on top of sidecar-process"; `overview.md` §8 sidecar sentence now names the shared class. `/userdoc`: checked all 12 `/docs/` files mentioning Tor/Mneme — pure internal refactor, nothing to update. `tsc --noEmit` + `eslint` clean. **Untested at close:** verified by typecheck/lint/review only — live Tor connect/reconnect and Mneme start/restart need a full app restart (main-process change; HMR doesn't reload main) and were left to the user's manual pass. |
| US-946 | Shared `tryParseJson` helper — retire hand-rolled JSON-parse-with-fallback blocks | — | **Origin:** the first item actioned from a codebase-wide refactoring survey, picked as the smallest safe win. **What shipped:** `tryParseJson<T>(text, fallback)` in `core/utils/parse-utils.ts`, alongside the existing `parseObject`/`parseJSON5`. It treats absent, blank and malformed input alike, so it also subsumes the `text.trim() ? JSON.parse(text) : {}` idiom, and it types the result at the call site instead of leaking `any`. Swept 9 sites across 8 files: `core/traits/dnd.ts`, `api/board-install-registry.ts`, `editors/mneme-config/mnemeTypes.ts`, `editors/notebook/NotebookEditor.ts`, `editors/notebook/note-editor/NoteItemEditModel.ts`, `editors/mcp-inspector/McpInspectorEditorModel.ts`, and three JSONL parsers in `editors/log-view/LogViewEditor.ts`. Every fallback matches the original `catch` branch exactly, so there is no behavior change. **The survey's "~25 sites" estimate was wrong, and finding out why was the substance of the task.** The rule that actually holds is *convert only where the `try`/`catch` exists solely for the parse*, which excludes three groups. Six sites wrap **file I/O** inside the same `try` (`board-manifest.ts`, `tools-manifest.ts`, `FolderViewModeService.ts`, `McpConnectionStore.ts`, `drawLibrary.ts`, `themes/index.ts`) — the helper would not remove the `try`, so it buys nothing; those six are really one *other* duplication, "read a data file → parse → fall back", and want a `readJsonDataFile` helper instead. Another group **needs the error, not a fallback**: `LogViewEditor`'s content loader and `GridEditor.parseJsonl` build user-facing `Line N: <message>` text, and `BoardEnvStore` returns the message as an error status — flattening those into a silent fallback would destroy the diagnostic. The four base64 link schemes were also left alone because their `try` covers **`atob` as well as `JSON.parse`**, and `atob` throws on malformed input, so a bare `tryParseJson` would let that escape; they belong to the separate `createLinkScheme` factory that wraps both. **Why a doc rule and not just a helper:** `parseObject` already existed and was essentially unused, which is *why* the hand-rolled copies accumulated — a helper nobody knows about does not stop duplication. So `coding-style.md` gained a **"Parsing JSON That May Be Malformed"** section under Error Handling stating the decision boundary in both directions, which is the part that makes this stick. **Two review suggestions declined, with reasons:** a unit test would mean introducing Vitest (`testing.md` is an explicit placeholder and the project has no test infrastructure at all), and dropping the explicit `<unknown>` generic would make `T` infer as `null` from the fallback and break the `Array.isArray` narrowing that follows it. **Docs:** `key-files.md` row for `parse-utils.ts`; `folder-structure.md` comment refreshed; `coding-style.md` as above — and while in that section, two examples calling a **non-existent `alertWarning`** were corrected to `ui.notify(…, "warning")` (zero occurrences in `src/`; the prose two paragraphs below already documented `ui.notify`, so the file contradicted itself), with the `error.message` sample narrowed since a catch binding is `unknown`. `/userdoc`: nothing user-visible, no `/docs/` changes. `tsc --noEmit` + `eslint` clean; `/review` no concerns. **Note for whoever picks up the next item:** the working tree carried 9 unrelated deletions (`doc/visualization/*.fg.json`, `mockups/mcp-inspector-*.html`) that arrived from outside this session and were deliberately left untouched. |
| US-936 | Board chrome — `.p-toolbar` / `.p-btn` / `.p-input` in `board-base.css` | — | **Ask:** agent-built boards keep coming out with an oversized, pale toolbar that doesn't match the app. **Diagnosed by measuring, not by looking at the screenshot** — `browser_evaluate` on the live Dev Dashboard board gave the real numbers, and they were narrower than the complaint: the *buttons* were already right (26px, `0 8px`); the toolbar was the defect — `padding: 8px 12px` producing **45px** against the app's 30, painted on `rgb(49,49,49)` = `--p-panel` instead of `--p-bg-dark` = `rgb(24,24,24)`, with two buttons in the same bar disagreeing on radius (0px vs 4px). **Why CSS and not just guidance:** prose alone loses, because an agent told to "make it compact" reaches for a comfortable-looking vertical padding again. `board-base.css` now ships the classes with the app's exact metrics ported from `uikit/Toolbar.tsx` / `Button.tsx` / `Input.tsx` / `tokens.ts`: `.p-toolbar`, `.p-btn` (+ `primary` / `ghost` / `danger` / `link` / `selected` / `icon` / `sm` / `on-dark`), `.p-input` / `.p-select`, `.p-sep`, `.p-spacer`, `.p-toolbar-title`. **Opt-in by class, not by element** (the user's decision over two alternatives): `board-base.css` is linked *before* a vendored library's stylesheet, so styling bare `<button>`/`<input>` would have reached into av-grid's icon buttons, flatpickr and tom-select, and every board author would then have to undo it. **Three details that are ports, not inventions:** the button's rest/hover/active fills ride three local CSS vars, the same mechanism the real `Button` uses to adapt to the surface under it — so `.p-btn` lightens toward `--p-panel` on the page but toward `--p-bg` inside a toolbar, which is already the darkest surface; `.p-toolbar .p-input` is lifted off `--p-bg-dark` because a field sharing its bar's fill disappears into it; and `.p-btn > svg` is pinned to 16px because an unconstrained inline SVG defaults to 300×150 and tears the button apart. **`.p-btn.danger` is a deliberate simplification** — the app tints its hover with an error *background*, which the `--p-*` contract does not expose (`--p-error` is a text color), so the fill stays generic and only the label carries the warning; noted in the file rather than left as a silent divergence. **Verified by measurement:** injected into a live board frame — toolbar `rgb(24,24,24)`, buttons 26/24px at radius 4 and 14/12px, accent `rgb(0,120,212)`, field lifted to `rgb(31,31,31)`. That run also corrected the docs: the bar is **31px**, not the 30 first written — the bottom rule adds a pixel, which is the same arithmetic the app's own `Toolbar` produces. **Existing boards deliberately untouched** (the user's call): `board-base.css` is *copied* at creation, not linked, so it is a snapshot — the change reaches new boards only, and no refresh mechanism was added. **Demo board converted** — its 18 action buttons (plus one built in `app.js`) now carry `class="p-btn primary"` and the local `button.action` rule is deleted, so the living example demonstrates the shared classes instead of a private copy; the Theming tab gained a working toolbar showing the whole set. **Review:** no concerns; took its suggestion to record the `danger` divergence. |
| US-935 | Markdown view — "Open in New Tab" on a link's context menu | — | **Ask:** the markdown view already navigated in place on a plain click and opened a new page on Ctrl+click; the user wanted the same new-tab result reachable from the context menu, for when the other hand isn't free. **Where it went:** `MarkdownBlock`'s `onContextMenu`, which builds its items locally rather than firing `linkContextMenu` — its target is an anchor in rendered HTML, not an `ILink` tree item. First in the menu, above "Copy Link". **Shown for every href except two:** `http(s)`, already served by the "Open in …Browser" items that the same handler appends, and `#` anchors, which scroll in place and have no document to open. So `file://` and `mneme://` links get it. **The interesting decision is what the item does NOT pass.** `MarkdownBody`'s in-place navigation must send `target: "md-view"` because an existing page keeps its current editor unless told otherwise; a *new* page has none to keep and picks from the file name via `EDITOR_MATCHERS` (`md-view` claims markdown at priority 10, monaco is the `() => 0` catch-all). Copying the `target` across would therefore have rendered a linked `.ts` file as markdown. Omitting `pageId` is the other half: `content/open-handler.ts` branches on it, `openFile` for a new tab versus `navigatePageTo` for in-place. **Review:** no concerns; took its suggestion to add `startGroup: true`, after confirming in `MenuModel.ts:115` that a leading separator is suppressed (`&& out.length > 0`), so it is inert when ours is the first item and correct when a code-block handler pushed items first. |
| US-934 | Explorer — "Open with Default App" and open-on-double-click | — | **Ask:** a `.vsdx` — or any format Persephone has no editor for — could not be opened from the Explorer panel at all. Monaco is the catch-all matcher (`acceptFile: () => 0`), so clicking one dumps binary into a text editor and there is no route to the OS application. **Most of it already existed:** `shell.openPath` was already in main as `Endpoint.showFolder` (exposed as `api.showFolder` / `app.fs.showFolder`), and the tree already had an `onItemDoubleClick` hook — bound to the *same* handler as single click, so double-click was a distinction without a difference and the slot was free. **Why a new `Endpoint.openPath` anyway:** `shell.openPath` does not throw on failure — it RESOLVES with an error string (typically "no application is registered for this extension"), and both existing call sites discard the return value. Reusing `showFolder` would have made an unopenable file a menu item that silently does nothing. The new endpoint returns the string; `content/open-with-default-app.ts` turns it into a toast and is shared by the menu item and the double-click. **Files only, deliberately:** a folder's existing "Show in File Explorer" *is* `shell.openPath` — on a directory that opens an Explorer window there — so a second entry would have duplicated it. On a file the same label is `shell.showItemInFolder`, which reveals rather than opens, which is why the file branch needed a separate item. **Double-click opens in both places** (a Persephone tab *and* the OS app) because the first of the two clicks has already navigated. Suppressing it would mean debouncing every single click by ~250 ms, and single-click navigation is the tree's hot path — the user accepted the double open instead. Directories route to the separate `onFolderDoubleClick` hook and keep expand/collapse, which is also what double-click does in Explorer's own tree pane. **Free reach:** the menu is built once in `tree-context-menus.tsx` and fired over the `linkContextMenu` channel, which the folder-contents (Category) view uses too, so the item appears in both without extra wiring. |
| US-933 | Board bridge — `readFile`/`writeFile` `encoding: "binary"` | — | **Ask:** came out of the Excel-viewer load investigation, where the base64 round-trip was measurable overhead on the way to a 20.5 MB workbook. **What shipped:** `BoardFileEncoding` gains `"binary"`; `readFile` returns a `Uint8Array` and `writeFile` accepts `Uint8Array` / `ArrayBuffer` / any typed-array view through a `toBytes()` normalizer; bridge API version 1.1.0, app 4.0.21. Only *ports* are transferable over `MessagePortMain`, so the payload is still a structured-clone copy — but of bytes, not of a base64 string. **The ceiling mattered more than the speed:** base64 of a file over ~400 MB exceeds V8's maximum string length, so such a file was unreadable by *any* board at any speed. The decode also cost real time — 1352 ms on the 20.5 MB workbook via `Uint8Array.from(…, charCodeAt)`, 26 ms with an indexed loop, none now. (`fetch("data:…")`, the usual fast decode, is blocked by the board CSP's `connect-src 'self'`.) **Unknown encodings throw** instead of falling back to `utf8`: the silent fallback would have handed a board running on an older app mangled bytes rather than an error. **Boards do not probe `persephone.version`** — the user's call; they declare `minAppVersion: 4.0.21` and let the existing compatibility gate do the work. **Migrated:** excel-viewer 1.1.1, pdf-viewer 1.0.2, pe-viewer 1.0.3. drawio / word / powerpoint viewers were deliberately left on base64 — the gain is ~10 ms and would cost each of them a `minAppVersion` bump. **Release coupling:** persephone-boards `develop` must not merge to `main` until 4.0.21 ships, or the catalog would carry boards no released app can run. |
| US-932 | Recommend av-grid as the default board data grid | — | **Ask:** av-grid should be the recommended grid for boards ahead of Tabulator — it is a port of Persephone's own internal VAGrid, so it is more native to the app, and it runs more smoothly even on small datasets. **What it is in the catalog:** the first entry in `boards-assets/manifest.json`, and the only one with `skin.type: "none"` — it reads the `--p-*` theme contract itself through `--avg-*` fallbacks, so there is no skin file to fetch and no zero-JS retint step. **Pinned to `latest`, not a version** (the user's decision), unlike every third-party entry; `versionNote` records the last verified version instead, so an agent knows what was actually tested without the catalog going stale. **Tabulator stays** as the documented fallback, with the gap list established by reading av-grid's `api.md` rather than guessed: variable row heights, row grouping, tree/nested rows, nested column headers, pagination, footer calculations, built-in export, remote-ajax and server-side operations, row drag-reorder, responsive collapse, undo/redo, freezing arbitrary data columns, and ready-made formatters. **Gotchas recorded for board authors** in the scaffold guide, because each one produces a silently broken grid: the UMD bundle ships as `.cjs` and must be renamed to `.js`, `injectStyles: false` must be passed when the vendored CSS is loaded separately, the host needs a definite height, and `filterBar` wraps the grid in `.avg-grid-wrap`, which then needs `height: 100%`. **No av-grid demo on the demo board** — the user did not want one; it appears only in the components card. |
| US-931 | Settings — make close-to-tray optional | — | **Ask:** Persephone hides into the tray instead of quitting when its last window closes. Fine for the user, but some users won't want it always running, so it should be a setting. **Setting:** `window.close-to-tray`, boolean, **default `true`** so existing behavior is unchanged; new **Window Behavior** section on the Settings page (placed right after Theme — it is app chrome, not an integration) with one checkbox whose help text swaps to state the actual consequence in each state. **The wiring needed no new IPC, which is the interesting part.** Main cannot read settings — it never loads `appSettings.json` — but `setCanQuit(canQuit: boolean)` already carried a boolean from renderer to `open-windows.ts`, where the param was named `_canQuit` and **ignored**. So the renderer now answers with the policy already applied, via a new `signalReadyToQuit()` in `api/window.ts` used by both callers (`RendererEventsService.handleBeforeQuit`, `PagesPersistenceModel.onAppQuit`). **Deliberately not the `mcp.enabled` pattern** (push on startup + subscribe to `onChanged`): that mirrors state into main, needs per-window sync, and has a real race — the actuation push is deferred 1500 ms after `settings.wait()`, so a window closed early would consult a stale default. Resolving at the decision point has no state to sync and no race, because whichever window is closing holds the current value by definition. **Mechanism it toggles:** a hidden `BrowserWindow` still counts as open, so `window-all-closed` never fires and the app stays resident; destroying the last window lets it fire → `app.quit()` → the `will-quit` teardown of Tor, child commands, board ports, the pipe server, MCP, the video stream server and Mneme. The setting is just a choice between those two paths. The two pre-existing guards are untouched: non-last windows always really close, and tray→Quit (`doQuit`) still overrides everything. **Two hazards checked rather than assumed:** dragging the last page out of a window also calls `closeWindow()` (`PagesLifecycleModel.ts:1035`) and looked like it might now quit the app — it cannot, because that flow is driven by `movePageToWindow()` in main and only runs when a target window already exists, so `isLastWindow` is false; and "Open with persephone" still works with the app fully quit, because the launcher falls back to spawning `persephone.exe` when the named pipe is absent (`launcher/src/main.rs:172-173`). **Review:** no concerns. Took both suggestions — one was the 2 s unresponsive-renderer watchdog (`open-window.ts`), which calls `close()` directly and so skips all three guards; **left as force-close by design** and documented: hiding a renderer that never answered would leave a frozen window behind the tray icon, looking like a working background app until clicked, and failing closed is recoverable where failing into a zombie is not. **Also verified live (unrelated to this change, asked as a follow-up):** the modified-**or-pinned** rule that decides whether a closed secondary window is remembered already covered pinned tabs — proven by pinning an unmodified page in window 1, closing it, and confirming `openFiles1.json` survived with `pinned: true`; the user's window was then restored to its original state. **Docs:** new `pages-architecture.md` **§11 Window Shutdown, Tray, and Quit** (there was no coverage of this lifecycle anywhere — appended as §11 rather than inserted near the bootstrap section because `scripting.md` links to `#8-well-known-pages`); a `state-management.md` paragraph recording resolve-at-the-decision-point as the alternative to `onChanged` actuation; `key-files.md` rows for `open-windows.ts` and `tray-setup.ts`; `window.close-to-tray` added to the agent guide's "Settings worth knowing about". Fixed two pre-existing standards violations in `pages-architecture.md` while there (a ticket id in a heading, and "restored in US-618"). |
| US-930 | Installer — folder context menu, retire text-file associations | — | **Ask, from a screenshot of the installer's Additional Options page:** keep "Open with persephone" for files, **remove** "Set as default app for text files", and **add** an equivalent option for folders — plus "check if Persephone can currently open folders". **Investigation result: it already could, and the chain broke in exactly one function.** The renderer's Layer-2 file resolver (`content/resolvers.ts:126-139`) stats a plain path and routes a directory to `pagesModel.addEmptyPageWithNavPanel()` — the same empty-page-plus-Explorer that the "Open Folder" tool produces, with five existing callers. But `isValidFilePath` (`main/utils.ts`) rejected anything that was not a regular *file*, and it gated all three "open this path" entry points: the launcher named pipe, Electron `second-instance`, and cold-start argv. So a folder argument did nothing — and via the pipe it did nothing *visibly worse* than nothing, because `bringToFront()` runs before the check, so the window popped up and then ignored you. **Fix:** extracted `pathExists()`, added `isValidOpenPath()` (file **or** directory) and switched the three OPEN sites to it; the two DIFF sites keep `isValidFilePath`, since comparing a directory is meaningless. Nothing downstream changed. The Rust launcher was already folder-safe (`resolve_path` returns absolute paths as-is; `clean_path` strips the `\\?\` prefix from canonicalized relative ones). **Installer:** folders needed **two** registry keys, not one — `*` matches files only, and Windows splits folder right-clicks into `Directory\shell` (on a folder, `%1`) and `Directory\Background\shell` (empty space inside an open folder, `%V` — `%1` is empty there); registering only the first makes the entry appear about half the time. Both are registered, persisted as `FolderMenu`, and cleaned up on uninstall. **Retiring the text-file option kept the code, per the user's explicit request to remove only the checkbox.** `$OptTextFiles` is now forced unchecked in `customInit` rather than read from the registry, which drives `customInstall` down the existing `${Else}` branch and calls `_UnRegisterFileAssoc` for all 15 extensions, restoring each to its `PrevAssoc` handler. Deleting the macros instead would have stranded anyone who ticked the old box — permanently associated, with no installer path back. The `Persephone.Document` ProgID is kept even though nothing points at it, so Windows' "Open with" list still shows a name and icon rather than a bare exe path. Rationale recorded in the file: claiming a fixed extension list was arbitrary once Persephone opens essentially anything and boards cover the rest. **Review:** no concerns. Confirmed the `$R5`/`_UnRegisterFileAssoc` register reuse in `customUnInstall` is safe by construction (all six values are read up front and `$R0`/`$R1` are consumed before the macro loop overwrites them) and took its suggestion to pin that ordering hazard in a comment, since a future reorder would break it silently. **Untested at close:** the installer half cannot be exercised without `npm run dist` — the launcher→pipe hop needs a packaged `persephone-launcher.exe` and the checkbox layout needs a real NSIS run. The two new labels are 4 and 6 characters wider than the one they replaced, and NSIS clips rather than wraps, so that is the thing to eyeball on the next draft release. |
| US-923 | MCP guides improvements — overview, targeting, dedup, errors | — | **Origin:** an independent Claude agent connected via Claude Desktop reviewed the eight `read_guide` guides and produced a prioritized report (verified claim-by-claim before planning; its one wrong claim — rest-client "duplication" — was dropped, and its "open_url doesn't focus" finding turned out to be a concurrent-agent collision, not a bug). **Docs:** two new guides — `overview` (start-here mental model + task→tool→guide routing, listed first in read_guide) and `browser` (exact 4-step page-targeting resolution from `getTarget()`, snapshot format, ref lifecycle — refs are CDP backendDOMNodeIds so main-frame refs survive re-snapshots until the element leaves the DOM, iframe refs only valid from the latest snapshot — waiting modes, errors table). Every guide gained an empirically-grounded "Errors & verification" section (probed live: broken JSON in `create_page` succeeds silently → parse error or `Editor crashed` boundary in the editor; `get_page_content` echoes broken content, so rendering is verified via `browser_snapshot({pageId:"app"})`; script exceptions return full stack; `page` is a reserved script global; `closePage` on a modified page blocks on the Unsaved Changes dialog). Graph format single-sourced in `graph.md` with defaults fixed against `graph/constants.ts` (published −40/30/0.5 were wrong; real −70/40/0.7). `scripting.md` gained the security model (no sandbox, user privileges, 30 s MCP timeout with the script continuing) and the missing `app.pages.all`/`closePage`. `pages.md` editor list split creatable (14, per registry `hasContentHost`) vs standalone (with the right opener each). `notepad://` scheme renamed `persephone://` (near-zero external consumers — cheapest moment); `resources/list` verified working over HTTP, so the reviewer's empty listing was client-side. **Code:** `open_url`/`open_board` return `{ opened, pageId, title }` (board page found by normalized `boardRoot` after the openRawLink pipeline); `openUrlInBrowserTab` returns the page id, `showBrowserPage` the `PageModel`; script facade keeps `showBrowserPage` void (no internal models to scripts). **QA:** mcp-test-agent skill hardened (persephone-tools-only after Haiku escaped to Write/Artifact mid-run; `read_guide`+`browser_*` allowed; `model: haiku` — weakest model = strongest docs test), stale `claude --agent` runner replaced with Skill invocation, new browser scenario file + rest-client/JSONL/standalone-refusal tests, and a live Haiku run: 9 tests, 7 PASS / 2 PARTIAL / 0 docs failures (`qa/runs/2026-08-09-haiku.md`) — including the exact collision scenario from the original review, now closed by the pageId return. `/review` clean (one optional note: thread the page id through `app.boards.openBoard` someday to drop the path-match heuristic). |
| US-922 | Board screenshots in the "Search boards" catalog | — | **Goal:** the Search boards tab listed every published board as a wall of text; each catalog entry can now declare a screenshot, shown on its card and on both Board Info modes (image left, text right). **Two repos.** *App:* `PublishedBoardInfo` gained `screenshot` (a bare file name from the board's manifest) and `screenshotUrl` (that name resolved against the catalog repo's raw base). `validateBoard` accepts `screenshot` only through a new `isSafeAssetName` — the same charset rule that guards board ids — because the value is interpolated into a raw URL, so a separator, `..`, a leading dot or a scheme must never survive; verified against `..`, `../../etc/passwd`, `/abs/x.png`, `x/y.png`, `http://evil.com/x.png`, `//evil.com/x.png`, `.hidden.png`, `screenshot.png?x=1` and `""`. `screenshotUrl` is derived by `withScreenshotUrls` as a catalog **leaves** the service (both `getPublishedBoards` returns + the `ePublishedBoardsUpdated` broadcast) and never stored, so the cache stays branch-agnostic, `PERSEPHONE_BOARDS_BRANCH` applies without a refetch, and a changed base URL isn't read as changed content by `catalogsEqual`. New `BoardScreenshot.tsx` renders a plain remote `<img>` at 200×125; no URL, a 404 and no network all collapse to a same-size placeholder so card heights never jump. Properties mode resolves the URL by catalog id, since the screenshot is excluded from the release ZIP and so is never on disk. *Boards repo (`fa6104f`):* all eight boards got a screenshot + `"screenshot": "screenshot.png"` + a patch bump + a `WHATS-NEW.md` line; `publish-board.mjs` carries the field into the catalog and adds `screenshot.png` to `EXCLUDE`. **Five design decisions the user approved up front** (raw branch URL over a versioned release asset; direct `<img>` over fetch-and-cache; excluded from the ZIP; no UIKit `Image` primitive; placeholder rather than an omitted column). **Screenshots were captured through Persephone's own MCP server**, not by hand: the existing `capturePageRegion` IPC grabs the app content area below the page-tab strip — which is what includes a board's secondary view (SQLite's Tables list, Todo's Lists & Tags) — then a canvas scales it to 1120×700. Sample files were synthetic on purpose, since the images are published publicly, and absolute-path labels are hidden for the duration of the capture so no user profile path is baked in. The whole procedure is now `.claude/skills/board-screenshot/SKILL.md` in the boards repo. **Two false starts worth knowing:** a maximized window produced 2556 px captures that were illegible at card size, and `app.pages.showPage()` does not actually switch the visible tab — the first batch silently produced eight byte-identical images before switching to clicking the tab element. **Verified end to end:** publish workflow succeeded, all eight catalog entries carry the field, all eight raw URLs return 200, and after a catalog refresh the running app loads every image at 1120×700. **Review:** no concerns; two optional suggestions left unactioned — the fixed-width screenshot has no wrap/shrink path in a row layout, so text can get cramped below ~250–300 px (Tools hub with the Pinned rail open, or a narrow sidebar host), and `withScreenshotUrls` copies the catalog on every call including the cache-hit path (irrelevant at a few dozen boards). **Untested at close:** the 404→placeholder fallback and the no-catalog-entry placeholder are both proven mechanically but not exercised in situ, since every catalog board now has a real screenshot. |
| US-921 | Rewrite README.md — reposition as universal viewer + AI-agent workspace | — | The README read as a feature catalog: ten headline sections listing every editor and subsystem, which is a lot to take in on what is effectively the project's welcome page. It also led with "developer notepad … scripting engine", framing that predates how the app is actually used. Reframed around three ideas — **one viewer for everything** (built-in editors, on-demand installs from the persephone-boards catalog, and viewers an AI agent builds to order in minutes), **a workspace shared with AI agents** (the MCP server makes the app transparent to an agent, which shows rendered content instead of chat walls, sees and drives the same UI, and edits documents alongside the user), and **boards as a platform for mini apps** (describe a tool to an agent and have it built and running within a coffee break). Still called a notepad, and still opens as one. Scripting Engine, Event System and Browser Automation lost their headline sections: scripting survives as a single bullet, and browser automation is now framed by what it does for the user (find something online, summarize the open page) rather than by its API surface. Kept the download badges, the built-in editors table and the contributing section; reordered the docs links so MCP and Boards precede Scripting. **Closed without `/review`, `/document` or `/userdoc`** at the user's explicit request — it is a documentation-only change with no code impact. |
