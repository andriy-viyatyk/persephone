# Completed Tasks

All completed tasks, newest first.

| ID | Title | Epic | Notes |
| US-320 | Rename remaining editor names for consistency | EPIC-017 | Phase 1.4 — `ImageViewerModel` → `ImageEditorModel`, `PdfViewerModel` → `PdfEditorModel`, `McpInspectorModel` → `McpInspectorEditorModel` (+ file rename). EditorCategory `"page-editor"` → `"standalone"`. Removed all backward compat aliases. ~38 files. |
| US-319 | Rename editor subclasses + EditorModule interface | EPIC-017 | Phase 1.3 — renamed 5 subclasses (Zip/Category/Browser/About/Settings), 7 file renames (git mv), EditorModule methods (newEditorModel, etc.), EditorDefinition.editorType, FileEditorComponent, EditorViewModule, state interfaces, helper functions. Removed compat aliases. ~72 files. |
| US-318 | Rename PageModel → EditorModel | EPIC-017 | Phase 1.2 — renamed base class, file (`PageModel.ts` → `EditorModel.ts`), `getDefaultPageModelState` → `getDefaultEditorModelState`, barrel export, ~46 files. Updated architecture docs, standards, diagrams. |
| US-317 | Rename core types (IPageState → IEditorState, PageType → EditorType, PageEditor → EditorView) | EPIC-017 | Phase 1.1 — pure mechanical rename across ~48 files. `IPageState` → `IEditorState`, `PageType` → `EditorType`, `PageEditor` → `EditorView` in `shared/types.ts` and all consumers. Updated architecture docs, standards docs, CLAUDE.md, and script API `.d.ts` files. |
| US-315 | ZipPageModel + ZipSecondaryEditor | EPIC-016 | Dedicated `zip-view` page-editor with ZipTreeProvider. `beforeNavigateAway` + `setOwnerPage` for navigation survival. `expandSecondaryPanel` event for auto-expand. `NavigationData.ownerModel` + `PageModel.ownerPage`. Explorer `sourceId` metadata. Removed old secondaryProvider system. Simplified CategoryEditor. |
| US-316 | Refactor PageNavigator for secondary editor models | EPIC-016 | PageNavigator renders secondary panels from `secondaryModels[]` via registry. `secondaryModelsVersion` reactive counter. `LazySecondaryEditor` async loader. Close button for survived models only. `_pendingActivePanel` defers restore. Old secondaryProvider system preserved. |
|----|-------|------|-------|
| US-314 | Secondary editor registry + PageModel integration | EPIC-016 | `secondaryEditor` getter/setter on PageModel (manages secondaryModels[] membership). `beforeNavigateAway(newModel)` lifecycle hook. SecondaryEditorRegistry singleton. `restoreSecondaryModels(ownerModel)` with deduplication. `navigatePageTo()` restructured for secondary survival. `newPageModelFromState` made public. |
| US-313 | Design secondary editor lifecycle | EPIC-016 | `secondaryModels[]` in NavigationData with add/remove/find/confirmRelease. Dispose integration. Tab close checks secondary models via `confirmRelease(closing: true)`. Persistence as descriptors in NavigationData cache. Updated pages-architecture.md with close flow, NavigationData lifecycle, multi-window transfer detail, and secondary models sections. |
| US-312 | Source link persistence in IPageState | EPIC-016 | Added `ISourceLink` interface + `sourceLink` field to `IPageState`. Open handler builds sourceLink from OpenContentEvent metadata (strips ephemeral fields). Stored via openFile/navigatePageTo, persisted across restarts. Script API: `page.sourceLink`. |
| US-311 | Explorer auto-refresh | EPIC-015 | FileTreeProvider.watch() with recursive fs.watch + 500ms debounce. TreeProviderViewModel subscribes on init, cleans up on dispose/provider change. Works in PageNavigator, Sidebar, Script Library. |
| US-310 | Remove old FileExplorer component | EPIC-015 | Deleted 4 files (FileExplorer.tsx, FileExplorerModel.tsx, file-tree-builder.ts, index.ts). Renamed fileExplorerRef → treeViewRef in MenuBar. Kept app.events.fileExplorer compat bridge and NavigationData migration code. |
| US-309 | Expose ZipTreeProvider in script `io` namespace | EPIC-015 | Added `io.ZipTreeProvider` to IoNamespace.ts + `IZipTreeProviderConstructor` type in `io.d.ts`. Scripts can enumerate ZIP contents, stat entries, and open via navigation URL. |
| US-308 | Decommission NavigationPanel search | EPIC-015 | Deleted 4 dead code files: NavigationPanel.tsx, nav-panel-store.ts, NavigationSearchModel.ts, SearchResultsPanel.tsx. Replaced by PageNavigator + FileSearch. |
| US-307 | Search panel in PageNavigator | EPIC-015 | FileSearch integrated as collapsible panel. Search icon + "Search in folder" context menu. activePanel local state for reactivity. onResultClickRef for stable RenderGrid callbacks. revealLine/highlightText on result click. Explorer reveal-only from Search. State persisted + restored. |
| US-306 | FileSearch component | EPIC-015 | Standalone search: FileSearchModel (IPC, search ID, debounce) + FileSearch (RenderGrid virtualized, file/line rows, match highlighting, expand/collapse). Reuses main-process search-service. |
| US-305 | CollapsiblePanelStack: expand history | EPIC-015 | Clicking expanded panel returns to previously expanded panel (history-based). useRef tracks previous panel + external activePanel changes. |
| US-302 | Secondary provider support in PageNavigator | EPIC-015 | NavigationData gets secondaryDescriptor + lazy async createSecondaryProvider(). PageNavigator shows Archive panel for zip files. Panel switch with getNavigationUrlByHref. CategoryEditor uses activeProvider/activeSelectionState. open-handler reconstructs archive paths from pipe transformers. |
| US-304 | Move persistence from NavPanelModel to NavigationData | EPIC-015 | NavigationData owns save/restore/cache. Switched from NavPanelModel to PageNavigatorModel (rootPath, treeState). NavPanelModel kept as reference. Backward-compatible cache format. |
| US-303 | Extract link parsing and pipe creation utilities | EPIC-015 | `resolveUrlToPipeDescriptor()` in `content/link-utils.ts`. Resolvers use descriptors + `createPipeFromDescriptor()` instead of direct provider instantiation. Removed "!" archive detection for HTTP URLs. `extractEffectivePath` uses `parseArchivePath`. |
| US-301 | Redesign PageNavigator with collapsible panel headers | EPIC-015 | Extended CollapsiblePanelStack with optional `icon` and `buttons` props. PageNavigator uses CollapsiblePanel with "Explorer" label + action buttons. No chevrons. Panel header styled to match PageToolbar. |
| US-300 | Replace FileExplorer with TreeProviderView in Sidebar | EPIC-015 | MenuBar + ScriptLibraryPanel now use TreeProviderView + FileTreeProvider. Provider caching in MenuBarModel. Selection styling improved. Right-click selects folder. Context menu label unified. |
| US-299 | Unify PageNavigator toggle button across editors | EPIC-015 | PageModel.ensureNavigationData() replaces duplicated NavigationData creation in TextToolbar, PdfViewer, ImageViewer, ScriptPanel, TextFileActionsModel. All editors now use toggleNavigator/canOpenNavigator. |
| US-297 | CategoryEditor + tree-category:// link resolution | EPIC-015 | CategoryEditor wrapping CategoryView, tree-category:// link format with encode/decode, Layer 1 parser, ITreeProvider.getNavigationUrl, shared selection via NavigationData.selectionState, revealItem in TreeProviderView, page restore support, navigator toggle button. |
| US-298 | Introduce NavigationData class | EPIC-015 | NavigationData wraps PageNavigatorModel + ITreeProvider + renderId. Survives page navigation. renderId as stable key in AppPageManager keeps PageNavigator mounted. Replaced navPanel→navigationData on PageModel. All creation sites updated. |
| US-296 | Create PageNavigator component | EPIC-015 | Replaces NavigationPanel — PageNavigator + TreeProviderView + FileTreeProvider. RawLinkEvent enhanced with target/metadata passthrough. 3-layer context menu (generic → event channel → parent). No search, no archive logic. |
| US-295 | Create CategoryView component | EPIC-015 | `components/tree-provider/CategoryView.tsx` + `CategoryViewModel.tsx` — list mode with virtualized RenderGrid, search, context menus, folder navigation. Tiles mode deferred (needs TreeProviderItemTile). |
| US-293 | Create TreeProviderView component | EPIC-015 | `components/tree-provider/` — TreeProviderView, TreeProviderViewModel, TreeProviderItemIcon. Generic tree viewer with lazy loading, search, context menus, drag-drop, show/hide links toggle. Added `rootPath` to ITreeProvider. |
| US-292 | Implement ZipTreeProvider | EPIC-015 | `content/tree-providers/ZipTreeProvider.ts` — read-only ZIP archive ITreeProvider. Delegates to archiveService. Sorts folder→extension→name. |
| US-291 | Implement FileTreeProvider | EPIC-015 | `content/tree-providers/FileTreeProvider.ts` — local filesystem ITreeProvider. Direct Node.js fs/path (bypasses archive transparency). list/stat/resolveLink/mkdir/rename/deleteItem. |
| US-290 | Define ITreeProvider & ITreeProviderItem types | EPIC-015 | `io.tree.d.ts` with 6 interfaces (ITreeProvider, ITreeProviderItem, ITreeStat, ITreeSearchOptions, ITreeSearchHandle, ITreeSearchResult). Added `isCategory` to LinkItem and ILink with facade mapping. |
| US-289 | Browser-webview images: persist across app restart | | HTTP images: HttpProvider pipe (re-fetches on restart) + .img cache fallback. Blob images: cached to disk immediately. applyRestoreData override for url field. |
| US-288 | Review EPIC-012 Changes and Architecture | EPIC-012 | 9-phase review. Tasks 1-4 implemented: doc updates, pipeline hardening (ITransformer clone, #password, async fs, ILinkMetadata, OpenContentEvent), text editor pipe bugs (HTTP restore, rename transformers, save errors, spelling fixes), reference editors (ensurePipe, archive guard removal, openDiff pipes). Task 5 → EPIC-015. |
| US-271 | Script API & docs | EPIC-012 | `io` global namespace: FileProvider, HttpProvider, ZipTransformer, DecryptTransformer, createPipe(), RawLinkEvent, OpenLinkEvent. Type definitions in `io.d.ts`. AppWrapper events proxy exposes send/sendAsync. |
| US-274 | Migrate reference editors | EPIC-012 | PDF Viewer and Image Viewer use content pipes. Non-local sources (HTTP, archives) cached as temp files via appFs. Cache cleaned on page dispose. Replaced direct `require("fs")` with `appFs`. |
| US-273 | cURL parser & Open URL dialog | EPIC-012 | Layer 1 parsers for HTTP URLs and cURL/fetch commands. Open URL dialog (Ctrl+O) with textarea input, "Open"/"Cancel"/"Open File" buttons. HTTP resolver with self-contained extension→editor mapping. TextAreaField paste fix for context menu. |
| US-270 | HttpProvider | EPIC-012 | HTTP/HTTPS provider (read-only), lazy node-fetch import. HTTP raw link parser, HTTP resolver with content-vs-browser routing. External URL migration (handleExternalUrl → openRawLink pipeline). |
| US-268 | Migrate TextFileIOModel | EPIC-012 | Full rewrite: dual pipe model (primary + cache), FileWatcher replaced by pipe.watch(), encoding detection in ContentPipe (BOM/jschardet), encryption via clone-and-try with DecryptTransformer, CacheFileProvider for encrypted cache, legacy state compatibility (auto-create pipe from filePath). |
| US-276 | Pipe serialization | EPIC-012 | `IPageState.pipe` field, `PageModel.pipe` property, `getRestoreData()`/`applyRestoreData()` save/restore pipe descriptors, `createPipeFromDescriptor()` reconstruction. |
| US-275 | DecryptTransformer | EPIC-012 | AES-GCM decrypt/encrypt transformer, `persistent: false`, reuses `encryption.ts` service, registered as `"decrypt"` type. |
| US-269 | ZipTransformer | EPIC-012 | ZIP entry extract/replace via jszip, `persistent: true`, registered as `"zip"` type. File resolver updated to detect `!` and build archive pipes. |
| US-267 | Migrate entry points | EPIC-012 | Replaced 9 `pagesModel.openFile()` call sites with `app.events.openRawLink.sendAsync()`. `PagesModel.openFile()` redirects through pipeline for backward compat. All file opening now flows through the three-layer link pipeline. |
| US-266 | Open handler | EPIC-012 | Layer 3 handler on `openContent` — bridge mode: extracts filePath from pipe, delegates to `pagesModel.lifecycle.openFile()`/`navigatePageTo()`, disposes pipe. |
| US-265 | Pipe resolvers | EPIC-012 | Layer 2 file resolver on `openLink` — builds `FileProvider` + `ContentPipe`, resolves target editor via `extractEffectivePath()` + `editorRegistry.resolve()`, fires `openContent`. |
| US-264 | Raw link parsers | EPIC-012 | Layer 1 parsers on `openRawLink`: archive parser (detects `!`), file parser (fallback, strips `file://` protocol, decodes URI). Registered during bootstrap in LIFO order. |
| US-263 | Link event channels | EPIC-012 | Added `openRawLink`, `openLink`, `openContent` channels to `AppEvents`. Event classes: `RawLinkEvent`, `OpenLinkEvent`, `OpenContentEvent`. Script-facing types in `io.events.d.ts`. |
| US-262 | FileProvider & ContentPipe | EPIC-012 | `FileProvider` (binary I/O, watch, stat), `ContentPipe` (chain provider + transformers, read/write/clone/serialize), provider/transformer registry with `createPipeFromDescriptor()`. New `src/renderer/content/` layer. |
| US-261 | Interfaces & types | EPIC-012 | `io.provider.d.ts` (IProvider, IProviderStat, IProviderDescriptor), `io.transformer.d.ts` (ITransformer, ITransformerDescriptor), `io.pipe.d.ts` (IContentPipe, IPipeDescriptor). Script-facing type definitions for content pipe architecture. |
| US-260 | EventChannel LIFO | EPIC-012 | `sendAsync()` now LIFO (newest subscriber first). Removed `subscribeDefault()` from EventChannel, IEventChannel type, and AppWrapper. `send()` remains FIFO. |
| US-241 | Tray Quit Timeout | | 2s timeout in window close handler — if renderer is crashed/unresponsive, force-closes window without waiting for renderer response |
| US-287 | Miscellaneous Adjustments & Bug Fixes | | Draw image offset, graph tooltip wrap/scroll, graph export buttons, Rest Client auto-expand, browser tab insert after active, window position fix, file explorer drag-drop move |
| US-286 | app.runAsync — Worker Thread Execution | | Run script functions in background worker threads via main-process IPC + worker_threads. Two-parameter design: data (cloned) + proxy (transparent RPC). Sucrase helpers in worker. Revocable proxy cleanup. |
| US-284 | Cleanup — Rebrand Remaining Files | EPIC-013 | Updated backlog.md, mockups (6 HTML files), visualization graphs (3 fg.json, 657 refs), renamed js-notepad-scripts → persephone-scripts. Full-color theme-aware PersephoneIcon (lily badge) for sidebar button and About page. Historical epics kept as-is. |
| US-282 | Repo Rename — js-notepad → persephone | EPIC-013 | Renamed GitHub repo, updated local git remote, fixed stale URLs in CONTRIBUTING.md and whats-new.md. |
| US-285 | Version Bump to 2.0.1 | EPIC-013 | Major version bump to signal breaking change (rebrand). |
| US-281 | Doc Rebrand — js-notepad → Persephone | EPIC-013 | Renamed all documentation references across 33+ files (~130 refs). Developer docs, user docs, README, MCP guides, QA docs, Claude agents, script library. Added v2.0.1 entry in whats-new.md. Historical changelog entries kept as-is. |
| US-280 | Core Rebrand — js-notepad → Persephone | EPIC-013 | Renamed all runtime identifiers across 30+ files (~260 references). Updated package.json, electron-builder, Rust launcher/snip binaries, NSIS installer, main process, renderer, IPC, registry handlers. New lily flower app icon (ICO/PNG/SVG). Renamed JsNotepadIcon → PersephoneIcon. |
| US-253 | Native Rust Screen Snip Tool | | Replaced Electron overlay windows (BrowserWindow + desktopCapturer) with native Rust exe (`js-notepad-snip.exe`). Fixes silent crash on mixed-DPI multi-monitor setups (Castlabs Electron 39). Uses Win32 GDI for screen capture, native overlay windows for selection UI, PNG via stdout. Per-Monitor DPI V2 aware. |
| US-252 | Rest Client — Header View Switch | EPIC-010 | Table/JSON toggle for request and response headers. Request JSON is editable Monaco with live sync — valid JSON updates headers immediately, invalid shows warning on switch/send. Response JSON is read-only Monaco. |
| US-251 | Rest Client — Binary Data Support | EPIC-010 | Two new body types: "binary" (file picker, fs.createReadStream streaming) and "form-data" (multipart with text/file toggle per row, streamed via multipartBuilder.ts). Binary response detection by Content-Type. Binary response panel with save-to-file. Image preview + "Open in Image Viewer". Base64 in-memory cache, skip stateStorage persistence for binary. |
| US-250 | Rest Client — MCP & API Integration | EPIC-010 | Updated `mcp-res-pages.md`: added `rest-client`, `log-view`, `grid-jsonl` to editor types list and language table. Added Rest Client data format section with full JSON schema, field descriptions, tips. Added empty content template. Updated `README.md` editors table. No editor facade — scripts use `app.fetch()` and JSON directly. |
| US-247 | Rest Client — Result Integration | EPIC-010 | "Open in new tab" button on response body (NewWindowIcon). "Copy as JSON" buttons on response/request headers. "Copy as..." popup menu in REQUEST header (cURL bash/cmd, fetch, fetch Node.js) via new `serializeRequest.ts`. "Open in New Editor" context menu on tree items. `pages.d.ts` updated with `content` param on `addEditorPage`. Copy buttons show 200ms progress animation. |
| US-249 | Rest Client — Requests Management | EPIC-010 | Collection grouping in TreeView (virtual, derived from `collection` field). Inline editable collection/name in REQUEST header. Delete button with `app.ui.confirm()`. Collection context menu (Add Request, Delete Collection). Drag-drop across collections. Empty names/collections show italic "(empty)". New requests inherit selected request's collection. |
| US-248 | Rest Client — Request Body Types & Enhancements | EPIC-010 | Body type selector (none / x-www-form-urlencoded / raw). Raw body uses Monaco Editor with language sub-selector (plaintext, json, javascript, html, xml). Form-urlencoded key-value editor. Content-Type auto-set on body type/language change. Method→bodyType sync (GET→none, POST→raw). Extracted `KeyValueEditor` component (shared by headers and form-data). Clipboard paste auto-detects body type. Replaced ComboSelect method selector and showAppPopupMenu language selectors with `WithPopupMenu`. Styled `Checkbox` component. URL input wraps with `TextAreaField`. |
| US-246 | Rest Client — Paste Request from Browser | EPIC-010 | `parseClipboardRequest()` auto-detects and parses fetch (browser + Node.js) and cURL (bash + cmd) from clipboard. Handles quoting, escaping, line continuations, `-b` cookies → Cookie header, `-d` body, `-X` method. Wired to URL input `onPaste` — intercepts if clipboard starts with `fetch(` or `curl`, otherwise normal paste. |
| US-245 | Rest Client — Response Viewer | EPIC-010 | Monaco Editor for response body (editable for Alt+Shift+F formatting). Tabs: Body / Headers. Language auto-detected from Content-Type, clickable language label with popup menu (json, html, xml, javascript, css, yaml, plaintext). Headers displayed as a two-column table. Response size in panel header. |
| US-244 | Rest Client — Request Builder | EPIC-010 | Method ComboSelect, URL input, Send button (Enter key), headers with auto-add empty row + ComboSelect for header names, body textarea (hidden for GET/HEAD). Split-panel layout (MCP Inspector pattern): request 60% / body 40% with draggable Splitter + double-click toggle. Response panel below with status/headers/body, cached via stateStorage. `nodeFetch` wired for execution. |
| US-243 | Rest Client editor — basic shell | EPIC-010 | Content-view editor for `.rest.json` files. `RestClientViewModel` extends `ContentViewModel` with parse/serialize lifecycle. Two-panel layout: TreeView collection (left) + request detail placeholder (right). Drag-drop reorder, context menu (rename/duplicate/delete). `RestClientIcon` (blue/green arrows). Registered in editor system + "+" create menu. |
| US-242 | nodeFetch — Node.js HTTP Client | EPIC-010 | `nodeFetch()` in `src/renderer/api/node-fetch.ts` — pure Node.js `http`/`https`, full header control, redirect handling, auto-decompression, streaming, timeout, `rejectUnauthorized` option. Exposed as `app.fetch()` via `App` class + `AppWrapper`. `IFetchOptions` types in `app.d.ts`. Runs in renderer (no IPC — `nodeIntegration: true`). |
| US-240 | Widevine CDM Support for Built-in Browser | | Switched from standard Electron to Castlabs ECS fork (`v39.8.0+wvcus`) for Widevine DRM support. `components.whenReady()` in `main-setup.ts` ensures CDM download. Production builds VMP-signed via Castlabs EVS (`scripts/vmp-sign.mjs`, `afterPack` hook in `electron-builder.yml`). GitHub Actions pipeline updated with Python + EVS setup. Netflix/Disney+ playback works in signed production builds. |
| US-239 | Browser Bookmark EventChannel | EPIC-009 | `app.events.browser.onBookmark` fires before Add/Edit Bookmark dialog. `BookmarkEvent` with mutable title, href, discoveredImages, imgSrc, category, tags + readonly isEdit. Refactored to single `showBookmarkDialog()` method — both star button and context menu paths route through it. `BrowserEvents` namespace on `AppEvents`. `IBrowserEvents`, `IBookmarkEvent` types for IntelliSense. |
| US-238 | LogView re-creation after page closed | | `ensureFacade()` checks if LogView page still exists via `pagesModel.findPage()`. If closed, clears cached facade and re-creates on next `ui.log()` access. `initializeUiFacade` returns `{ facade, pageId }`. Stale release functions are safe no-ops. Also fixed `await ui()` to not eagerly create LogView (yield-only via callable proxy). |
| US-237 | Progress Dialog Component | EPIC-009 | `Progress` component in `ui/dialogs/progress/`. Three modes: `showProgress(promise, label)` with 300ms debounce, `createProgress(label)` with updatable label + `.show(promise)`, `notifyProgress(label, timeout)` toast, `addScreenLock()`/`release()`. Two-zone overlay (header-block covers tabs with drag, content-block below). Exposed via `app.ui`. IntelliSense types with `IProgressHandle`. |
| US-236 | Script Context Coexistence | EPIC-009 | Per-instance `ScriptContext` with context-bound `customRequire`. `fn.call(context)` for scripts, `__activeScriptContext__` for library modules. Stack-based `ui` getter (save/restore). Always-fresh require cache per module. Removed `globalThis.__scriptContext__`, `CONTEXT_PREFIX`, `createLibraryRequire`. Multiple contexts coexist — autoload survives F5 runs. |
| US-233 | Script Autoloading from Script Library | EPIC-009 | `AutoloadRunner` in `scripting/` loads `.ts`/`.js` files from `library/autoload/` at window startup. Scripts with `export function register()` are called; files without are skipped. Shared `ScriptContext` with all-or-nothing error handling. `autoload-service.ts` thin wrapper in `api/`. Reactive `TOneState` drives yellow reload indicator in header (`MainPage.tsx`). `LibraryService` calls `markNeedsReload()` on file changes. |
| US-235 | Unified Script Context Injection | EPIC-009 | Removed `with(this)` proxy chain and `lexicalObjects`. Top-level scripts now use same `CONTEXT_PREFIX` as library modules — injects `app`, `page`, `React`, `styledText`, `preventOutput`, `require`, `console` as local vars from `globalThis.__scriptContext__`. `ui` is lazy getter on `globalThis`. `console` override scoped to prefix (no longer breaks React). |
| US-234 | Fix Script Context in Library Modules | EPIC-009 | Inject `app`, `page`, `React`, `styledText` as local variables in library modules via `CONTEXT_PREFIX` in extension handlers. `ScriptContext` sets/clears `globalThis.__scriptContext__`. Library require cache cleared on every execution for fresh context. |
| US-232 | Script Scope — Auto-Cleanup of Event Subscriptions | EPIC-009 | Refactored `ScriptContext` to class with `dispose()`. Split `ScriptRunner` into `ScriptRunnerBase` (core engine: transpile, execute, library) + `ScriptRunner` (orchestrator). Extracted `convertToText()` to `script-utils.ts`. `AppWrapper.events` recursive proxy auto-tracks `subscribe()`/`subscribeDefault()` in `releaseList` — scripts never need to manually unsubscribe. Fixed duplicate folder context menu items. |
| US-231 | File Explorer Item Context Menu EventChannel | EPIC-009 | `app.events.fileExplorer.itemContextMenu` EventChannel. Fires `sendAsync()` in background handler after all built-in items collected. Scripts can subscribe and modify menu items. `AppEvents` namespace on `app.events`. `IAppEvents`, `IFileExplorerEvents` types for IntelliSense. `contextMenuPromise` on native event for async awaiting. |
| US-230 | Context Menu ContextMenuEvent Refactor | EPIC-009 | Replaced `e.nativeEvent.menuItems` with `ContextMenuEvent` on native event across 12 handlers. `fromNativeEvent()` static helper. `ContextMenuTargetKind` union (15 kinds). `GlobalEventService` now async — awaits `contextMenuPromise`. Removed `addItem()`/`addGroupItem()` — `items` array is directly mutable. Removed duplicate `MenuItem.ts` — `api/types/events.d.ts` is single source of truth. New `doc/architecture/context-menu.md`. |
| US-229 | App Event Primitives | EPIC-009 | New `api/events/` module: `BaseEvent` class with `handled` flag, `EventChannel<T>` with `subscribe()`, `subscribeDefault()`, `send()` (sync+freeze), `sendAsync()` (async pipeline with `handled` short-circuit). `ContextMenuEvent<T>` generic subclass. `MenuItem` interface moved from PopupMenu to canonical `api/events/MenuItem.ts`. Script API types in `api/types/events.d.ts`: `IEventChannel`, `IBaseEvent`, `IContextMenuEvent`, `IFileTarget`, `MenuItem`. |
| US-228 | Browser (Tor) Mode | | Tor-routed ephemeral browser with lazy tor.exe lifecycle. TorService in main process manages tor.exe child process with per-partition SOCKS5 proxy. TorStatusOverlay with live log, spinner, reconnect. Tor indicator in URL bar with status dot. Settings: tor.exe path, SOCKS port, bookmarks file. Tor profile row in Settings page. windowClosing event for cleanup. Favicon fallback on load error. MaxListeners fix in browser-service. |
| US-227 | Small improvements and enhancements | | 8 fixes: Link list view cleanup (removed href, border, full-width title), rich LinkTooltip component, browser `onGetLinkMenuItems` callback ("Open in New Tab"), pinned links context menu, browser tab drag-to-reorder, URL bar blur selection fix, link category normalization (trailing slash), page tab sound indicator always visible on hover. |
| US-226 | Log language for Monaco with syntax highlighting | | Custom Monarch tokenizer for `.log` files — timestamps, log levels (error/warn/info/debug/trace with bracketed abbreviations), strings in gray/silver, numbers, GUIDs, URLs, hex literals, constants, exception types, stack traces. 12 token color rules. |
| US-225 | Add `read_guide` MCP tool | | New `read_guide` tool with `z.enum()` for all 7 guides. Rewrote MCP instructions block (shorter, scenario-focused, GitHub link). Updated all tool descriptions to reference `read_guide()` alongside `notepad://` URIs. Fixed `openUrlInBrowserTab` bug where active browser page was skipped, causing duplicate pages on consecutive `open_url` calls. |
| US-224 | AppPageManager — Portal-based DOM stability with grouping for app pages | | `AppPageManager` with `GroupContainer` and `ImperativeSplitter`. CSS-based positioning (no reparenting) preserves iframes/webviews during group/ungroup. Pages.tsx rewritten to use portals. Compare mode handled per-page via `PageContent` component. |
| US-223 | PageManager — Portal-based DOM stability for browser tabs | | New `PageManager` component using React portals with imperative DOM placeholders. Applied to browser editor internal tabs — closing/reordering tabs no longer reloads remaining webviews. BlankPageLinks moved inside portal per-tab, fixing scroll position preservation. |
| US-222 | Show Link Editor on browser empty pages | | Bookmarks displayed on blank tabs (about:blank). Eager preload with silent encryption mode. Portal refs via props pattern for multiple LinkEditor instances. `onInternalLinkOpen` callback routes links to correct browser page. Ctrl+Click opens in new tab. BlankPageLinks component with own toolbar (hides Add Link/browser selector). |
| US-221 | MCP documentation improvements | | Test-driven MCP doc improvement. New resources: notebook, todo, links guides. Improved tool descriptions with strict warnings to read resources. Dialog validation (known properties, required fields, usage examples). output.grid validation (string content, valid JSON). addEditorPage() argument validation. MCP Log page .log.jsonl suffix. QA test framework with test agent and 4 test files. |
| US-220 | Bug fixes and small enhancements | | 7 fixes: Graph switch button, legend always visible, toolbar overflow, browser sidebar icon color, pinned links favicons, URL bar about:blank, FileExplorer menu label rename. |
| US-218 | MCP Inspector — explore advanced capabilities | EPIC-008 | Investigation task: reviewed logging, completions, tasks, experimental capabilities. Decision: implement completions (US-220), defer logging/tasks/experimental. |
| US-219 | Tools & Editors — sidebar panel + configurable new-page menu | | New sidebar panel with all creatable editors/tools. Two sections: Pinned (drag reorder) and All (alphabetical). Pin/unpin buttons, `pinned-editors` setting. "+" menu shows only pinned items + "Show All...". Browser profiles as separate items. MCP Inspector accessible. `app.window.openMenuBar(panelId?)` API. |
| US-217 | MCP Inspector — Server Info tab with full server metadata | EPIC-008 | New "Info" panel tab (first, default) showing server name/title, version, description, websiteUrl (clickable, opens in browser), instructions (markdown via MarkdownBlock). McpServerInfo expanded with title/description/websiteUrl/instructions. js-notepad MCP server now provides all metadata. Scripting facade exposes serverTitle, serverDescription, serverWebsiteUrl, instructions. |
| US-214 | MCP Inspector — scripting API (connection & troubleshooting) | EPIC-008 | `page.asMcpInspector()` facade: connect/disconnect, read/write connection params (url, transportType, command, args, connectionName), read-only status (connectionStatus, serverName, serverVersion, errorMessage), history (historyCount, history, clearHistory, showHistory). Direct model wrap (no ViewModel). Also improved `create_page` MCP tool error messages with editor-specific hints for all page-editors. |
| US-216 | Rename mcp-browser → mcp-inspector across codebase | EPIC-008 | Renamed folder `mcp-browser/` → `mcp-inspector/`, files `McpBrowserModel` → `McpInspectorModel`, page type `mcpBrowserPage` → `mcpInspectorPage`, API `showMcpBrowserPage()` → `showMcpInspectorPage()`. Session restore backward compat via `PAGE_TYPE_MIGRATIONS` map in PagesLifecycleModel. Updated all docs, mockups. Call Tool button restyled with selection color tokens. |
| US-212 | MCP request log (new log-view entry type) | EPIC-008 | New `output.mcp-request` log entry type with `McpRequestView` renderer (direction arrow, method, detail, duration, error badge, collapsible colorized JSON). MCP Inspector "History" tab with "Open in Log View" / "Clear". MCP server handler logs all incoming requests (capped 200) to well-known `mcp-server-log` page. MCP indicator in title bar is clickable → opens request log. |
| US-215 | Well-known pages — predefined singleton page IDs | | `well-known-pages.ts` registry with `registerWellKnownPage()` / `requireWellKnownPage()`. MCP Log page uses fixed `mcp-ui-log` ID — shared between `mcp-handler` and `ScriptContext`. Deleted `mcp-log-state.ts`. Documented in pages-architecture.md § 8. |
| US-213 | MCP Browser — connections store & management UI | EPIC-008 | `McpConnectionStore` singleton — reads/writes `mcp-connections.json` in app data folder. Auto-save on successful connect with deduplication. Saved connections dropdown in connection bar. Connections list when disconnected (click to fill, delete). Fixed stdio transport by using `require()` to bypass Vite `node:process` externalization. `require()` pattern documented in coding-style.md. |
| US-211 | MCP Browser — resources & prompts panels | EPIC-008 | Resources: sidebar (260px, Splitter) with resources + templates sections, read resource with adaptive content rendering (MarkdownBlock for markdown, Monaco for JSON/text, inline img for images). Prompts: sidebar (220px), TextAreaField arg form, getPrompt with message display (role badges). `TOneState<McpResourcesPanelState>` and `TOneState<McpPromptsPanelState>`. Auto-load on connect. |
| US-210 | MCP Browser — tools panel (list, inspect, call) | EPIC-008 | Tools sidebar (resizable via Splitter, 200px initial). Tool detail: name, description, annotations. Dynamic arg form from JSON Schema (TextAreaField for strings, TextField for numbers, Monaco for objects/code, checkbox, select). `callTool()` with result in flex Monaco editor. Auto-loads tools on connect. `TOneState<McpToolsPanelState>` for tools state. |
| US-209 | MCP Browser — editor scaffold & connection manager | EPIC-008 | Page-editor `mcp-view` with `McpBrowserModel`, `McpConnectionManager` (HTTP + stdio transports via `@modelcontextprotocol/sdk`). Connection bar UI, server info display, capability badges, sidebar/panel layout skeleton. `showMcpBrowserPage()` on pages API. McpIcon in `icons.tsx`. No file association — standalone page. |
| US-206 | Drawing editor — library persistence & folder config | EPIC-007 | `useHandleLibrary` hook with `LibraryPersistenceAdapter` in `drawLibrary.ts`. Reads/writes `library.excalidrawlib` JSON via `app.fs`. Setting `drawing.library-path` auto-defaults to `<userData>/data/excalidraw-lib/`. Settings UI section with Browse/Reset. "Browse libraries" flow: intercept `<a>` click → open in internal browser; generic `browserUrlChanged` event in `events.ts` fired by browser on `did-navigate`/`did-navigate-in-page`/`new-window`; DrawView subscribes, parses `#addLibrary` URL, fetches `.excalidrawlib`, calls `updateLibrary()`. `event.handled` flag prevents duplicate handling across multiple draw pages. `libraryReturnUrl` prop set to custom URL for redirect detection. |
| US-204 | Drawing editor — MCP & scripting API | EPIC-007 | `page.asDraw()` facade: `addImage()` (live canvas via ExcalidrawImperativeAPI), `exportAsSvg()`, `exportAsPng()` (data URL), `elementCount`, `editorIsMounted`. `app.pages.addDrawPage(dataUrl, title?)` creates new draw page with embedded image. All heavy imports (Excalidraw, drawExport) are dynamic in facade to keep scripting bundle small. ExcalidrawImperativeAPI ref stored on DrawViewModel, set by DrawView on mount. Scene-data export overloads in drawExport.ts. |
| US-208 | Drawing editor — screen snip tool | EPIC-007 | "Screen Snip" toolbar button (scissors icon) in draw editor. Hides all windows, captures each monitor via `desktopCapturer`, shows fullscreen overlay per display with dimmed screenshot and drag-to-select. Inserts cropped PNG into Excalidraw canvas. Multi-monitor + mixed DPI support. Pre-created overlay windows for faster startup. Raw PNG buffer IPC (blob URL) instead of base64. Cancel via Escape or right-click. |
| US-207 | Open image/SVG in Excalidraw editor | EPIC-007 | "Open in Drawing Editor" button (DrawIcon) added to SVG editor, Image viewer, and Mermaid viewer toolbars. Shared `buildExcalidrawJsonWithImage()` in `drawExport.ts` builds Excalidraw JSON with embedded image element via `convertToExcalidrawElements`. Image dimensions capped to 1200px on longer side. SVG uses `Buffer.from` for unicode-safe base64. Mermaid decodes percent-encoded data URL. Image viewer reads via `fs.readBinary` or `fetch`. |
| US-203 | Drawing editor — export (SVG/PNG) | EPIC-007 | 4 toolbar buttons via `createPortal`: theme toggle (Sun/Moon, editor-local darkMode), copy PNG to clipboard (2x, Button spinner feedback), save as file (SVG/PNG popup menu, 2x PNG), open in new tab (SVG→svg-view, PNG→image-view via blob URL). `drawExport.ts` helpers using `exportToSvg`/`exportToBlob` with `exportWithDarkMode` from Excalidraw's `appState.theme`. `ImageViewerModel.dispose()` revokes blob URLs. Empty drawing guard with notification. |
| US-202 | Drawing editor — theme sync & UI polish | EPIC-007 | `theme` prop synced via `settings.use("theme")` + `isCurrentThemeDark()`. Excalidraw's built-in theme toggle hidden (`toggleTheme: false`). Excalidraw manages its own dark/light canvas backgrounds. Investigated custom background via `updateScene` — reverted due to Excalidraw's internal color-space handling conflicting with arbitrary colors. |
| US-201 | Drawing editor — basic Excalidraw integration | EPIC-007 | Content-view editor for `.excalidraw` files. `@excalidraw/excalidraw` package, `DrawViewModel` (ContentViewModel with fingerprint-based dirty detection), `DrawView` (500ms debounced onChange, hidden file actions). Self-hosted woff2 fonts in `assets/excalidraw/fonts/` (496KB, all except Xiaolai CJK). Default font Helvetica. `.excalidraw` → JSON in Monaco languages. `DrawIcon` in quick-add menu, file explorer, and tab icons. `SPECIALIZED_JSON_PATTERNS` updated. |
| US-200 | Graph editor — Ongoing enhancements 3 | EPIC-006 | (1) Legend Selection tab is now first and default. (2) Context menu "Open link" for nodes with markdown links in custom properties — single link shows "Open {property}", multiple links show "Open link..." submenu with open-link icon. Link extraction via `getNodeLinks()`, opening via `openNodeLink()` (simulated `<a>` click for Electron routing). (3) Tooltip suppressed while context menu is open (`isPopupOpen` check + `clearTooltip()` on menu open). |
| US-199 | Graph editor scripting API | EPIC-006 | `page.asGraph()` facade: `nodes`, `links`, `getNode()`, selection (`select()`, `addToSelection()`, `clearSelection()`), relationships (`getNeighborIds()`, `getVisualNeighborIds()`, groups), `search()`, `bfs()`, `getComponents()`. New `graph-editor.d.ts` types (`IGraphEditor`, `IGraphNode`, `IGraphComponent`, `IGraphSearchResult`). MCP resource `notepad://guides/graph` with data format and API reference. Registered in `mcp-http-server.ts`. |
| US-198 | Graph editor — Ongoing enhancements 2 | EPIC-006 | (1) Indexed property display: `key#N` suffix stripped in tooltip and markdown via `stripIndexedSuffix()` in `types.ts`; script updated to use `#` separator. (2) "Open in grid" selection menu action: exports selected nodes as JSON array to grid-json editor page. |
| US-197 | Disable graph grouping | EPIC-006 | Toggle button (violet circle icon) to disable/enable grouping — strips group nodes and membership links from rendering when disabled. Strikethrough visual when enabled, disabled when no groups. Group-related menu items hidden when grouping off (`invisible` property). Empty group cleanup: iterative cascade on `deleteNode`, `deleteSelectedNodes`, `removeFromGroup`. `resetView()` always enabled (full D3 sim restart + BFS reset). "Expand all" hidden when no visibility filter. Legend shows "Search highlighting is active" + "Clear search" button when search active. |
| US-196 | Graph selection enhancements | EPIC-006 | Selection toolbar ("N selected ▾") with popup menu: select children/members/members deep, highlight, copy/open markdown, group, extract/extract with children, delete. Context menu: select children (node), select members/deep (group), selection-aware delete ("Delete N Nodes"), renamed "Delete Link to...". Ctrl+F focuses search. Toolbar auto-grows (min 280px). Extract filters standalone groups. |
| US-195 | Simplify `addEditorPage` + content pattern | | Replaced 3-step `addEditorPage` → `isTextFileModel` → `changeContent` with single `addEditorPage(editor, lang, title, content)` call across 9 files. Removed unused `isTextFileModel` imports from 8 files. |
| US-193 | Multigrouping (Nested Group Hierarchy) | EPIC-006 | Sub-groups, group-in-group, multi-level link routing. Task document created but deferred — single-level grouping covers current needs. |
| US-192 | Graph editor — Ongoing enhancements | EPIC-006 | 5 enhancements: (1) Selection highlights full visual path to real neighbors through group nodes via `getVisualPath()`. (2) Links tab hover shows only selected node's children with green borders; green path through groups. (3) Detail panel stays open when clicking a node. (4) Legend Selection tab "Selected with children" option (processed + real neighbors). (5) Shift key quick highlight for "selected with children". New fields: `selectedLinkKeys`/`hoveredLinkKeys` in `GraphHighlightModel`, `computeSelectedLinkKeys()`/`computeHoveredLinkKeys()`/`hasNodeAt()`/`setAltKeyHighlight()` in `ForceGraphRenderer`. Fixed `selectedKey` tracking in legend (was `selectedCount`). |
| US-191 | Graph connectivity model | EPIC-006 | New `GraphConnectivityModel` — read-only query layer bridging original and preprocessed graphs. Dual adjacency: real (no membership) + processed (visual). Selection uses processed neighbors, hover uses real neighbors. Removed `computeNeighborIds()` from `GraphHighlightModel`, `computeLinkedNodes()`/`getNeighborIdsFromSource()` from `GraphDataModel`. Links tab and Delete Link context menu now show real-data links only. Nested-group-ready API: `getGroupChain()`, `getAllRealMembers()`, `getVisualPath()`, `getMembersWithExternalLinks()`. |
| US-190 | Group management UI | EPIC-006 | Group/ungroup/delete-group via context menu, edit group title, Alt+Click membership toggle, "Remove from Group" on member nodes, "Group Selected" for multi-select (3 cases: all regular, 1 group + regulars, 2+ groups warning). Direction-agnostic membership detection in `GraphGroupModel.rebuild()`. Root node cleared on group deletion. BFS visibility fix: real depth tracking (not showIndex), depth-based initial visibility, focus-first component detection. |
| US-189 | Group link pre-processing | EPIC-006 | `preprocess()` in `GraphGroupModel`: 5 link classification rules (membership hidden, intra-group kept, cross-group split through group, inter-group split through both groups, external kept). Synthetic link deduplication with canonical keys. Per-link distance scaling (`baseDistance / log2(count)`) for multi-count synthetic links. Pipeline wired between `rebuild()` and `GraphVisibilityModel`. Violet coloring (`nodeSpecial`/`borderSpecial`) for root and group nodes — new CSS variables in all 9 themes. Tooltip badges: "ROOT NODE" and "GROUP" labels above title. |
| US-188 | Group node data model & rendering | EPIC-006 | `isGroup` flag on GraphNode. Double-circle rendering (inner 65% filled + outer ring with dark blue groupBorder, state-dependent on select/hover). Level-1 radius (14px), always-visible labels, level-1 font size. `GraphGroupModel` tracks group→members and member→group mappings from links. Tooltip shows "Group · N members". Group nodes filtered out of detail panel. Legend: "Group" row in both Level and Shape tabs with highlight support. New `--color-graph-group-border` CSS variable in all 9 themes. `isGroup` excluded from custom properties and legend level/shape counting. |
| US-187 | Graph editor — Node multiselection | EPIC-006 | Ctrl+Click multi-select with batch edit. Detail panel: "N nodes selected" header, MultiInfoTab (batch level/shape with yellow mixed indicators), PropertiesTab (union of properties, yellow for differing values, _isChanged tracking, status message). Search: [select all]/[add to selection] actions. Collapsed toolbar stays visible with green border when search active. Canvas click collapses expanded panels without selection change. Legend: new "Selection" tab (selected/not-selected radio filters), green chevron when expanded. |
| US-186 | Graph editor — UI polish & highlight rework | EPIC-006 | Tooltip drag fix. Edit panel tabs reordered (Info→Properties→Links). Links tab: removed sub-tabs, always shows all columns with auto-detected widths and sticky ID. Label font scaling by level (14/12/11/10px). Highlighted labels always visible. Selection rework: orange label text for selected, green for hovered+children, no child border highlight. Placeholder colors improved. Expansion note styled as warning. Extracted shared `column-width.ts` utility. |
| US-185 | Graph editor — Architecture refactoring | EPIC-006 | Extracted 6 focused modules from GraphViewModel (1122→713 lines) and ForceGraphRenderer (906→759 lines). New files: `GraphDataModel.ts` (node/link CRUD, legend data), `GraphSearchModel.ts` (search matching), `GraphHighlightModel.ts` (highlight layers, selection/hover, colors), `GraphContextMenu.ts` (menu builders), `GraphIcons.tsx` (shared ShapeIcon/LevelIcon), `shapeGeometry.ts` (shared point generation). Added `effectiveNodeRadius()` to types.ts replacing 6 inline ternaries. Eliminated duplicated SVG icon code between DetailPanel and LegendPanel. |
| US-184 | Graph editor — Legend panel | EPIC-006 | Collapsible bottom-left panel with Level/Shape tabs. Checkbox highlighting (3-layer dimming merge). Free-text description inputs persisted to `options.legend`. Root node shared description across tabs. SVG shape/level icons. `GraphLegendPanel.tsx` component. `legendHighlight` layer in `ForceGraphRenderer`. |
| US-183 | Graph editor — adjustments & fixes | EPIC-006 | Quick-add menu "Force Graph" item. Custom `.fg.json` file icon. Status bar node count (portal). Empty page hint label. Canvas focus fix for popup menu dismissal. MCP documentation for graph editor format. |
| US-180 | Graph editor — polish & enhancements | EPIC-006 | Ctrl+Click badge deep expand (BFS through hidden subtree, visible nodes as barriers). "Expand All" toolbar button with confirmation dialog for >1000 nodes. `expandDeep()`/`expandAll()` on `GraphVisibilityModel`. `ExpandAllIcon` added to icons.tsx. |
| US-182 | Graph settings persistence, expansion UI, root node | EPIC-006 | Physics settings persisted to JSON options (`charge`, `linkDistance`, `collide`). `options.focus` → `options.rootNode` rename. Toolbar "Settings" → "Physics", new "Expansion" tab (ComboSelect root node, expandDepth, maxVisible — deferred apply on reopen). Root node visual: compass star shape, level-1 size, always-visible label. "Set as Root" context menu. `GraphExpansionSettings.tsx` component. |
| US-181 | Graph search enhancement — multi-word, properties, results panel | EPIC-006 | Multi-word AND search across title + custom property names/values. Toolbar restructured with Settings/Results tabs. Scrollable results panel with `highlightText()` highlighting, italic property lines with lighter key opacity. Hidden nodes at 50% opacity in results. Keyboard nav (ArrowUp/Down/Enter/Escape). Click result to reveal (if hidden) and select. Status bar: "matched N visible +K hidden" with clickable bulk reveal. Shared `getCustomProperties()`/`isReservedPropertyKey()`/`formatPropertyValue()` extracted to `types.ts`. `GraphVisibilityModel`: `getHiddenNodes()`/`isNodeVisible()` replace `getHiddenMatchingIds()`. `revealAndSelectNode()` on ViewModel. |
| US-179 | Detail panel — Properties tab (AVGrid) | EPIC-006 | AVGrid with key-value columns (Name, Value) for custom node properties. Apply/Cancel batch workflow. Reserved key validation (id, title, level, shape, _$ system) via `onCellClass` with red highlighting and disabled Apply. Shared `tab-action-row`/`tab-apply-btn`/`tab-cancel-btn` styles. Combined `anyDirty` state blocks panel collapse, tab switching, and node selection. Renamed `onLinksDirtyChange` → `onPanelDirtyChange`. |
| US-178 | Detail panel — Links tab (AVGrid) | EPIC-006 | AVGrid with linked nodes, batch Apply/Cancel workflow. 3 column presets (Default/View/Custom), combobox editors for level/shape. Dirty state protection (blocks panel collapse + node selection). Smart link removal (orphan cleanup). Links tab highlighting: dimming non-linked nodes, external hover from grid focus, green link line between selected and hovered node, auto-expand hidden children. `fontSize: "inherit"` fix for AVGrid cell edit inputs. |
| US-177 | Force tuning panel | EPIC-006 | Redesigned toolbar: gear icon → reset view icon → search input in single row. Gear toggles expandable panel with 3 sliders (Charge -200..0, Distance 10..200, Collide 0..1). Real-time force simulation updates via `ForceParams` instance field on `ForceGraphRenderer`. Toolbar semi-transparent when idle, full opacity on hover/expand/focus-within. Canvas click auto-collapses tuning panel. Double-click node expands detail panel. D3-zoom double-click disabled. Detail panel resets to collapsed on deselect. |
| US-176 | Detail panel — panel shell + Info tab | EPIC-006 | Collapsible overlay at top-right with three visual modes (no-selection/collapsed/expanded). Sticky expand state via `wasExpandedRef`. Tab bar (Info active, Links/Properties placeholders). Info tab: ID rename with validation (updates links, options.focus, visibility model, preserves position), Title commit on blur/Enter, Level icon selector (filled circles), Shape icon selector (SVG shapes). Bottom-left resizer with container clamping (90% max). `onSelectionChanged` callback in renderer. `selectedNode` snapshot in state. `updateNodeProps`/`renameNode` on ViewModel. |
| US-175 | Context menu + basic editing | EPIC-006 | Right-click context menu (Add Node on empty area, Add Child/Delete Node/Delete Link on nodes). Alt+Click toggles links. Three-layer data architecture: sourceData (clean) → visibility-filtered → renderer. Skip-flag serialization pattern. Incremental `updateGraph()` preserves expand/collapse state. Multi-component BFS with `ensureComponentRootsVisible()` for disconnected subgraphs. Position-preserving rebuilds via `updateVisibleData()` with `newNodePositions` hint map. |
| US-174 | Node tooltips (HTML overlay) | EPIC-006 | HTML tooltip on 500ms hover via `ReactDOM.createPortal`. Shows title/id/custom properties (excludes core, D3, system keys). Viewport-clamped positioning with `useLayoutEffect`. `pointer-events: none`. Hides on mouse leave, drag, zoom, data/visibility changes. Stable canvas ref via `useCallback` prevents simulation restart. `refreshColors()` skip optimization when CSS colors unchanged. |
| US-173 | Search with node dimming | EPIC-006 | Search input in graph toolbar with filter-as-you-type. Case-insensitive substring match on `title`/`id`. Non-matching nodes/links/badges/labels dimmed via `ctx.globalAlpha = 0.15`. Match count shown ("N / total"). When visibility filtering hides matching nodes: clickable "(+N hidden)" reveals them along shortest BFS paths to focus via `GraphVisibilityModel.revealPaths()`. Clear button (×) and Escape key. Search recomputed on data changes, expand, and reset. |
| US-172 | Collapse/expand with BFS + options | EPIC-006 | `GraphVisibilityModel` sub-model: BFS discovery order, adjacency map, visibility filtering. Parses `options` (`focus`, `expandDepth`, `maxVisible`). Large graphs (>maxVisible) show closest nodes first. "+" badge on nodes with hidden neighbors — clickable to expand, with hover highlight. "Reset View" toolbar button. Position-preserving `updateVisibleData()` places new nodes near anchor. Shallow copies avoid immer freeze issues. |
| US-171 | Node properties: title, level, shape | EPIC-006 | Added `NodeShape` type (circle, square, diamond, triangle, star, hexagon), `title`/`level`/`shape` to `GraphNode`. Per-node radius by level (1=14px, 5=4px, default=level 5). Shape drawing via `drawShape()`. Labels show `title` (fallback to `id`), always visible for level 1-2 nodes. Per-node `forceCollide` and hit-testing. |
| US-170 | Force Graph viewer — read-only (Phase 1) | EPIC-006 | Content-view editor for `.fg.json` files. D3 force simulation on canvas with zoom, pan, drag, click-to-select, hover-to-highlight. Graph color tokens in all 9 themes. Registered with `isEditorContent` detection (`"type": "force-graph"`). Also added "Open in New Panel" context menu for FileExplorer folders. |
| US-169 | Archive visual indicators — banner + badge | EPIC-005 | Added `getBadge` prop to `TreeView` for rendering badges in level-shift indentation area. `FileExplorer` renders clickable `OpenLinkIcon` badge on archive files (`isArchiveFile`), click calls `pagesModel.openFileAsArchive()`. `NavigationPanel` shows archive content banner: "Archive content" for ZIP (with large-file tooltip), ".asar is read-only" for asar (with save-error tooltip). Banner conditional on `isArchiveRoot`. |
| US-168 | `.asar` browsing — stat override + openFile hook | EPIC-005 | Added `isAsarFile()`, `isAsarPath()` helpers to `file-path.ts`. Added `.asar` to `isArchiveFile()`. `fs.listDirWithTypes()`: override Electron's `isDirectory:true` for `.asar` entries, skip `fileExistsSync` for `.asar` dirs. `fs.stat()`: override `isDirectory` for `.asar` files. `fs.exists()`: use `statSync` for `.asar` paths. `PagesLifecycleModel.openFileAsArchive()`: handle `.asar` without `!` suffix. `NavigationPanel`: single-click `.asar` opens as archive root, double-click navigates into it, file operations + search disabled for `.asar` roots. `FileWatcher`: no-op for `.asar` inner paths. Read-only — Electron's `.asar` patching doesn't support writes. |
| US-167 | File operations inside archives | EPIC-005 | Added `renameFile()`, `mkdir()`, `removeDir()` to ArchiveService. Routed archive paths in `fs.rename()`, `fs.mkdir()`, `fs.removeDir()`. Removed `!isArchiveRoot` guard on `enableFileOperations` in NavigationPanel. All archive file operations (create/rename/delete files and folders) now work via context menu. |
| US-166 | Archive browsing via NavigationPanel | EPIC-005 | Fixed `fpDirname`/`fpJoin` for archive inner paths (forward-slash convention). Added `isArchiveFile()` helper. `navigatePageTo`: replaced sync `fs.existsSync` with async `appFs.exists()`. New `openFileAsArchive()` method on PagesLifecycleModel/PagesModel. FileExplorerModel: "Open as Archive" context menu for archive files, hidden "Show in File Explorer"/"Open in New Window" for archive inner paths. NavigationPanel: double-click archive → change root to `filePath!`, search button hidden for archive roots, `enableFileOperations={!isArchiveRoot}`, `fpSep` fix in `handleMakeRoot`. `newPageModel`: page-editor fallback to Monaco for archive inner paths. `initializeTree` async + `treeViewKey++` fix for expansion state timing. |
| US-165 | ArchiveService + archive path utilities + fs.ts routing | EPIC-005 | Created `archive-service.ts` with jszip: `listEntries`, `listDir`, `readFile`, `writeFile`, `exists`, `deleteFile`, `stat` with per-archive sequential queue. Refactored `fs.ts`: extracted `_decodeBuffer`/`_encodeString`, added archive path routing to `read`, `readFile`, `readBinary`, `write`, `writeBinary`, `exists`, `delete`, `stat`, `listDir`, `listDirWithTypes`. `FileWatcher`: no-op constructor + async `getTextContent` for archive paths. Added `archive-service.ts` to documented `require("fs")` exceptions. Phase 1 complete. |
| US-163 | Migrate TextFileIOModel + themes/index to `app.fs` | EPIC-005 | `renameFile`: removed `require("fs")`, `existsSync`→`await appFs.exists()`, `renameSync`→`await appFs.rename()`. Uses existing `appFs` import. `themes/index.ts` unchanged — documented exception (sync read at startup before `app.fs` init to avoid theme flash). Phase 0 complete. |
| US-162 | Migrate library-service + library-intellisense to `app.fs` | EPIC-005 | library-service: `existsSync`→`fs.exists`, `readdirSync(withFileTypes)`→`fs.listDirWithTypes`, `mkdirSync`→`fs.mkdir`, `copyFileSync`→`fs.copyFile`. Scan methods now async. `nodefs` kept only for `fs.watch()` (documented exception). library-intellisense: `readFileSync`→`await fs.read()`, `registerLibraryFiles` now async. |
| US-161 | Migrate file-tree-builder to `app.fs` | EPIC-005 | Replaced `require("fs")` with `import { fs } from "../../api/fs"`. `readdirSync`+`statSync` → `await fs.listDirWithTypes()` (single call). `readDirectoryItems`, `buildFileTree`, `loadFolderChildren` now async. Cascaded to FileExplorerModel: `buildTree`, `loadChildrenIfNeeded`, `loadChildrenForExpandedPaths`, `ensureParentDirsLoaded`, `applyFilterPaths` now async. Extracted `initializeTree` from sync `setProps` for first-use async flow. |
| US-160 | Migrate FileExplorerModel to `app.fs` | EPIC-005 | Replaced `require("fs")` with `import { fs } from "../../api/fs"`. Migrated 6 sync ops to async: `existsSync`→`exists` (×3), `writeFileSync`→`write`, `mkdirSync`→`mkdir`, `renameSync`→`rename`, `rmdirSync`→`removeDir`, `unlinkSync`→`delete`. All methods already async — clean migration. |
| US-164 | Create `file-path` utility module + migrate `path.*` usage | EPIC-005 | Created `/src/renderer/core/utils/file-path.ts` with 10 exports: archive-aware `fpBasename`, `fpExtname`, `fpDirname`, helpers `isArchivePath`, `parseArchivePath`, `buildArchivePath`, pass-through `fpJoin`, `fpResolve`, `fpRelative`, `fpSep`. Migrated all 21 renderer files from `require("path")` to `fp*` imports. Only `file-path.ts` and `fs.ts` retain direct `path` import. Added "No direct fs/path imports" rule to coding-style.md and review command. |
| US-159 | Extend `app.fs` API with missing operations | EPIC-005 | Added `rename`, `stat`, `copyFile`, `listDirWithTypes`, `removeDir` to FileSystem class + IFileSystem type. New interfaces: `IFileStat`, `IDirEntry`. All methods async with sync internals (matching existing pattern). Prerequisite for Phase 0 migration tasks (US-160–163). |
| US-158 | Browser cascade tab protection — global rate limit | — | Replaced per-tab `PopupRateLimiter` instances with single `globalPopupRateLimiter` singleton in `src/ipc/popup-rate-limiter.ts`. Renderer uses `"tabs"` key, main process uses `"popups"` key. Prevents cascade attacks where each new tab opens more tabs (max 3 in 2s app-wide). Removed `tabRateLimiter` from `BrowserWebviewModel`. Simplified `allowPopups()` to whitelist global limiter. |
| US-157 | Handle wrong editor type error and return it in MCP call | — | `addEditorPage()` throws for page-editor types. MCP `create_page` returns structured error for page-editors with guidance. New `open_url` MCP tool opens URLs in built-in browser (profile/incognito support, smart tab reuse via `openUrlInBrowserTab`). MCP server instructions updated with browser workflow. |
| US-156 | Log item: output.mermaid (renderer + ui.show.mermaid) | EPIC-004 | Inline mermaid diagrams in Log View using `renderMermaidSvg()` + `svgToDataUrl()` from shared `render-mermaid.ts`. `MermaidOutputView` with async rendering (loading/error states), theme-aware (light/dark), CSS hover buttons: Copy image (PNG to clipboard) and Open in Mermaid editor. `Mermaid` helper class (`scripting/api-wrapper/Mermaid.ts`) with `text`, `title` property setters + `openInEditor()`. `ui.show.mermaid()` with two-overload pattern. MCP `output.mermaid` routed via existing generic handler. |
| US-154 | Log item: output.markdown (renderer + ui.show.markdown) | EPIC-004 | Inline rendered markdown in Log View using `MarkdownBlock` (from US-155) with compact mode. `MarkdownOutputView` with CSS hover "Open in Markdown editor" button (top-right corner, opens `md-view` editor). `Markdown` helper class (`scripting/api-wrapper/Markdown.ts`) with `text`, `title` property setters + `openInEditor()`. `ui.show.markdown()` with two-overload pattern. MCP `output.markdown` routed via existing generic handler (no MCP changes needed). |
| US-155 | Extract MarkdownBlock reusable component | EPIC-004 | Extracted `MarkdownBlock` from `MarkdownView` — self-contained markdown rendering component (CSS, ReactMarkdown, code blocks, mermaid, tables, task lists, compact mode, search highlight handle). `MarkdownView` refactored to thin page shell (scroll, minimap, toolbar, search bar). `MarkdownViewModel` simplified — DOM-querying search methods removed, delegated to `MarkdownBlockHandle` via `forwardRef`/`useImperativeHandle`. `onMatchCountChange` callback syncs match count to VM. Reusable for Log View `output.markdown` (US-154). |
| US-153 | Log item: output.text (renderer + ui.show.text) | EPIC-004 | Inline read-only Monaco editor in Log View for syntax-highlighted text blocks. `TextOutputView` creates/disposes Monaco instances per entry (virtualization handles lifecycle). Height auto-fits via `onDidContentSizeChange` + `getContentHeight()` (correct for wrapped lines), capped at `DIALOG_CONTENT_MAX_HEIGHT` (400px). Options: `language` (default "plaintext"), `wordWrap` (default true), `lineNumbers` (default false), `minimap` (default false). `Text` helper class (`scripting/api-wrapper/Text.ts`) with property setters + `openInEditor()`. `ui.show.text()` with two-overload pattern. MCP `output.text` routed via existing generic `output.*` handler. Right gutter (40px) added to Log View via second RenderFlexGrid column for scroll accessibility. |
| US-152 | Log item: output.grid (renderer + ui.show.grid) | EPIC-004 | Inline AVGrid in Log View with column resizing/reordering and cell selection/copy. `GridOutputView` renders data via embedded `AVGrid` with `growToHeight` (400px max). `Grid` helper class (`scripting/api-wrapper/Grid.ts`) with `data`, `columns`, `title` property setters that trigger `updateEntryById()`. `openInEditor()` opens data in Grid editor tab. Per-item auxiliary state (`itemsState` on LogViewModel) persists column widths/order and focus via `host.stateStorage`. `mergeColumnsWithSaved()` preserves saved column state across data updates. MCP `output.grid` accepts `content` (JSON/CSV string) + `contentType` ("json"\|"csv"). Shared `DIALOG_CONTENT_MAX_HEIGHT` constant extracted to `logConstants.ts`. |
| US-151 | Log item: output.progress (renderer + ui.show.progress) | EPIC-004 | First output entry type. `ProgressOutputView` renders progress bar (160px track, blue fill, green when completed) with `CircularProgress` spinner for indeterminate state. `Progress` helper class (`scripting/api-wrapper/Progress.ts`) with getters/setters for `label`, `value`, `max`, `completed` — each setter triggers `updateEntryById()` (debounced JSONL serialization). `completeWithPromise(promise, label?)` for async completion. `ui.show.progress()` with two-overload pattern (simple label or options object). `addEntry()` now supports **upsert** — if `fields.id` matches existing entry, merges in-place. MCP handler routes `output.*` entries with full fields (fixed bug: was passing only `text`). Height cache cleared on upsert. MCP docs updated with "Updating Entries by ID" section. |
| US-150 | Log item: input.select (renderer + ui.dialog.select) | EPIC-004 | New `input.select` dialog type for Log View. `SelectDialogView` uses existing `ComboSelect` component (dropdown with search/filter, keyboard nav). Items as plain `string[]`, single selection via `selected?: string`, `placeholder` support, auto-width via `adjustWithCharWidth`. `ui.dialog.select()` with two-overload pattern. Updated `.d.ts` types, MCP resource, `logTypes.ts` (added `selected`, `buttons` fields to existing stub). |
| US-149 | Log item: input.radioboxes (renderer + ui.dialog.radioboxes) | EPIC-004 | New `input.radioboxes` dialog type for Log View. `RadioboxesDialogView` with `Radio` component (reusable in `components/basic/`). Items as plain `string[]`, single selection via `checked?: string`, `layout: "vertical" | "flex"`, `!` buttons disabled when nothing selected, pre-selected `checked` value honored. `ui.dialog.radioboxes()` with two-overload pattern. Updated `.d.ts` types, MCP resource, `logTypes.ts` (added `layout` field). |
| US-148 | Console forwarding to Log View when `ui` is active | EPIC-004 | When `ui` is accessed, `console.log/info/warn/error` automatically forward to Log View (`log.log/info/warn/error`). Native console always called (additive). `ui.preventConsoleLog/Warn/Error()` to suppress. MCP scripts forward to both `consoleLogs` and shared MCP Log View. `ui.log()` now produces `log.log` (lighter text) distinct from `ui.text()`. `.then()` renamed to `.append()` on StyledLogBuilder (thenable protocol conflict fix). MCP `execute_script` with `ui` uses shared standalone Log View via `mcp-log-state.ts`. |
| US-147 | Log item: input.checkboxes (renderer + ui.dialog.checkboxes) | EPIC-004 | New `input.checkboxes` dialog type for Log View. `CheckboxesDialogView` with `Checkbox` component (reusable in `components/basic/`). Items as `{ label, checked? }[]`, `layout: "vertical" | "flex"`, `!` buttons disabled when nothing checked. `ui.dialog.checkboxes()` with two-overload pattern (string[] shorthand normalized to objects). Updated `.d.ts` types (`ICheckboxItem`), MCP resource, `logTypes.ts` (`CheckboxItem`, updated `RadioboxesEntry` stub with `checked?: string`). |
| US-146 | Standardize ui API — two-overload pattern | EPIC-004 | All `ui.dialog.*` methods now support two calling styles: simple positional form (`confirm("msg", buttons?)`) and full object form (`confirm({ message, buttons? })`). Disambiguation via `isOptionsObject()` — StyledText is always string or array, plain object is always full form. Updated `UiFacade.ts`, both `ui-log.d.ts` copies with overloaded signatures. Also fixed MCP `ui_push` schema in `mcp-http-server.ts` — replaced stale `data: z.any()` with flat `passthrough()` (US-145 leftover). |
| US-145 | Flatten LogEntry structure — remove `data` nesting | EPIC-004 | Refactored `LogEntry` from `{ type, id, data: T, timestamp }` to flat `{ type, id, timestamp, ...fields }`. Log entries use `text` field, dialog entries have fields directly on entry. Deleted unused `LogEntryModel.ts`. Renamed `updateEntryData` → `updateEntryText`. Dialog resolution returns full flat entry. Updated all views, UiFacade, mcp-handler, MCP docs. Added `EditorErrorBoundary` (per-editor-tab crash protection) and `EntryErrorBoundary` (per-log-entry error stub). |
| US-144 | MCP: split API guide into focused resources + server instructions | EPIC-004 | Split monolithic `assets/mcp-api-guide.md` (509 lines) into 3 focused resources: `mcp-res-ui-push.md`, `mcp-res-pages.md`, `mcp-res-scripting.md`. Added `notepad://guides/full` resource that dynamically concatenates all 3. Added MCP server `instructions` (immediate context on connection) with workflow overview and resource pointers. No duplicate content — full guide is assembled from parts at runtime. |
| US-143 | MCP tool: `ui_push` (entry array, batched dialogs, active log page) | EPIC-004 | New `ui_push` MCP tool — AI agents push log entries, dialogs, and output items to a managed Log View page. Active MCP log page tracking in `mcp-handler.ts` (auto-creates, reuses, recreates on close). `sendToRenderer` extended with optional `timeoutMs` (0 = infinite for dialog-containing requests). String shorthand → `log.info`. Batched dialogs via `Promise.all()`. Canceled dialogs return `{ button: null }`. MCP API guide updated with full documentation. Tool named `ui_push` (not `log_push`) for consistency with script `ui` global. Phase 2 of EPIC-004 complete. |
| US-142 | Log View polish: StyledTextBuilder, dialog UX, ScriptRunner fix | EPIC-004 | Fluent `StyledTextBuilder`/`StyledLogBuilder` — logging methods return builders for `.then().color().bold().print()` chaining. Standalone `styledText()` global for dialog labels. `LogViewModel.updateEntryData()` with immediate serialization (race condition fix). Dialog UX: fit-content width, button padding, force scroll-to-bottom (`forceScrollVersion`). Iterative auto-scroll (50/150/300ms). `RenderFlexGrid.preferMinHeightForNewRows` prevents height jumping. Empty line fix (`minHeight: 18`). ScriptRunner: `isBlockCloser` check for `});`/`];` in implicit return logic. |
| US-141 | Script facade: global `ui` variable (logging + dialogs) | EPIC-004 | New `ui` global in script sandbox. `UiFacade` wraps `LogViewModel` for logging (`log/info/warn/error/success/text/clear`) and dialogs (`confirm/buttons/textInput`). Lazy-initialized via `Object.defineProperty` getter — Log View page created on first access, auto-grouped with source page. Log-view module pre-loaded in `ScriptRunner` via `editorRegistry.loadViewModelFactory()`, VM created synchronously via new `acquireViewModelSync()` / `prepareViewModel()` on `IContentHost`. Dialog refactoring prep: renamed `resultButton` → `button`, `result` → `text`, Promise resolves with full `entry.data` (transparent data). Output suppression: accessing `ui` sets `groupedContentWritten = true`. Type definitions in `ui-log.d.ts` (`IUiLog`, `IUiDialog`, `IDialogResult`). |
| US-140 | Log View editor — dialog entries (input.text, confirm, buttons) | EPIC-004 | Three interactive dialog views: `ConfirmDialogView`, `TextInputDialogView`, `ButtonsDialogView` in `items/` folder. Shared building blocks: `DialogContainer` (active/resolved border), `DialogHeader` (optional title), `ButtonsPanel` (`!` prefix required-button support, check icon on result). Refactored rendering to direct state subscription: `LogEntryWrapper` receives `vm + index`, subscribes via `vm.state.use(s => s.entries[index])`. `LogViewContext` provides ViewModel to dialog components. `LogViewModel`: removed model cache/eviction, added `updateEntryAt()` + `dirtyIndices` with 300ms debounced JSONL serialization, refactored `resolveDialog()` to use `state.update()`. |
| US-139 | Log View editor — basic rendering of log entries | EPIC-004 | RenderFlexGrid virtualized rendering with variable row heights. `LogEntryWrapper` (cell root + container with timestamp, accent border), `LogEntryContent` (type router), `LogMessageView` (5 log levels with colored text), `StyledTextView` (plain/styled segment renderer). Auto-scroll to bottom on new entries. Toolbar toggle for timestamps (off by default). Dialog/output stubs. Height cache on LogViewModel. |
| US-136 | Define LogEntry types, models, and LogViewModel | EPIC-004 | Foundation for Log View editor: `logTypes.ts` (LogEntry, StyledText, dialog/output types, type guards), `LogEntryModel` (reactive wrapper with lazy instantiation/eviction), `LogViewModel` (ContentViewModel with JSONL parsing, incremental parse, dialog promises, model cache), placeholder `LogViewEditor` component. Registered `log-view` content-view editor for `.log.jsonl` files. |
| US-138 | Grid editor support for JSONL/NDJSON files | EPIC-004 | Registered `grid-jsonl` content-view editor. Added JSONL parsing (line-by-line `JSON.parse`) and serialization (compact, one object per line). Updated `asGrid()` facade, `PageEditor` type. |
| US-137 | JSONL language support for Monaco | EPIC-004 | Registered `jsonl` language with Monarch tokenizer for `.jsonl`/`.ndjson`/`.log.jsonl` files. Reuses JSON token names for automatic theme colors. Added green `{ }` file icon. Prerequisite for Log View editor. |
| US-135 | Script Library enhancements & example script fixes | EPIC-003 | Fixed example scripts: replaced `Buffer.from()` with browser APIs (`atob`/`btoa`) since Buffer fails in script sandbox. JWT parser strips "Bearer " prefix. format-json sets `page.grouped.language`. Script Library sidebar supports double-click/icon-click to open in tab with NavigationPanel. ScriptPanel "Open in New Tab" button opens selected script (or empty page) in tab with NavPanel rooted at script-panel/. |
| US-134 | Library setup wizard (link/create folder dialog) | EPIC-003 | Setup dialog (`LibrarySetupDialog.tsx`) replaces raw folder pickers in sidebar, settings, and script panel save. Folder input + Browse + "Copy example scripts" checkbox. Bundled example scripts in `assets/script-library/` (base64 encode/decode, sort lines, parse JWT, format JSON, shared helpers module). `copyExampleScripts()` in library-service.ts copies files recursively, skipping existing. All integration points use dynamic `import()`. |
| US-133 | Script panel dropdown & save to library | EPIC-003 | ComboSelect dropdown in script panel toolbar lists saved scripts from `script-panel/{language}/` and `script-panel/all/`. Save button writes modified content back to library (direct overwrite for selected scripts, InputDialog with folder choice for ad-hoc). Ctrl+S shortcut. InputDialog extended with optional radio buttons. State: `selectedScript` + `dirty` flag, persisted to cache. |
| US-132 | Path completion for require("library/...") | EPIC-003 | Monaco `CompletionItemProvider` auto-suggests library folders/files inside `require("library/...")` strings. Folders re-trigger suggestions; files shown without extension. Registered once per language (JS/TS), reads `allFiles` dynamically. Also fixed: `.js` library files with ES module syntax (`export`/`import`) now transpiled via sucrase `imports` transform (scoped to library folder only). Renamed `registerTsExtension()` → `registerLibraryExtensions()`. |
| US-131 | IntelliSense for library modules (addExtraLib + path mapping) | EPIC-003 | New `library-intellisense.ts` in `api/setup/` reads all library `.ts`/`.js` files and registers with Monaco via `addExtraLib()`. Virtual paths `file:///library/{path}`. `paths` compiler option maps `library/*` for module resolution. Subscribes to `LibraryService` state for live updates on file changes. Added `require()` and `preventOutput()` global declarations to `index.d.ts`. |
| US-130 | LibraryService — folder scanning, caching, file watching | EPIC-003 | Lazy-initialized `LibraryService` in `/api/library-service.ts`. Scans `script-panel/` subfolders for file index, recursive `walkDir` for all `.ts`/`.js` files. `fs.watch({ recursive: true })` with 300ms debounce. Calls `scriptRunner.invalidateLibraryCache()` on changes. Subscribes to `script-library.path` setting for re-init. `TModel<T>` + `TGlobalState` for reactive state. |
| US-129 | `require("library/...")` resolution in ScriptContext | EPIC-003 | Patched `require()` in script context to resolve `library/` paths to script library folder. New `library-require.ts` with `.ts` extension handler (sucrase), extension auto-resolution, cache invalidation via dirty flag. Clear error when library not linked. |
| US-128 | Script Library folder setting & sidebar integration | EPIC-003 | New `script-library.path` setting, "Script Library" static entry in sidebar with custom icon, ScriptLibraryPanel component (placeholder when unlinked, FileExplorer when linked), context menu (Change/Open in Explorer/Unlink), Settings page section with Browse/Unlink. |
| US-117 | Fix editor switch buttons for structured JSON editors | — | Structured JSON editors (notebook, todo, link) now embed a `"type"` property in serialized JSON. New `isEditorContent()` hook on `EditorDefinition` uses fast regex to detect content type. `TextFileModel` runs detection on restore, editor change (immediate), and content change (debounced 2.5s). `TextToolbar` merges detected editor into switch options. ViewModels serialize `type` field (`note-editor`, `todo-editor`, `link-editor`). Backward compatible — file extension matching still works. |
| US-126 | Script result handling improvement | — | Phase 1: TextDialog component (Monaco-based multi-purpose dialog) with `app.ui.textDialog()` API. Phase 2: Output suppression — `preventOutput()` global function and automatic `page.grouped.content` write detection via `GroupedPageWrapper`. `ScriptOutputFlags` tracks suppression state. Errors shown in TextDialog when output is suppressed. Added `page.runScript()` for programmatic script execution. |
| US-125 | TypeScript Script Execution via Sucrase | — | Added TypeScript support to script execution using `sucrase` (~275 KB, lazy-loaded). New `transpile.ts` with `transpileIfNeeded()` and `isScriptLanguage()`. `ScriptRunner` methods accept optional `language` parameter. F5 runs `.ts` files, Script Panel uses TypeScript language (accepts both JS/TS transparently), Notebook TS notes executable. MCP `execute_script` tool has new `language` parameter. Quick-add menu has "Script (TS)" option. |
| US-124 | UI Polish & NavPanel Bug Fix | — | External link routing (prefer active browser, reuse empty tab), Image Viewer save button (URL→file), EditLinkDialog image URL clear button, minimap click-to-scroll, auto-hide scrollbars, sidebar folder double-click opens NavPanel tab. **Bug fix:** NavPanel expanded folder state lost on first navigation — race condition where async `restore()` in `addEmptyPage()` overwrote the NavPanel set by `addEmptyPageWithNavPanel()`. Fix: skip `restore()` when creating page with pre-attached NavPanel. |
| US-123 | Lazy-load MCP SDK for faster startup | — | Replaced 4 static `@modelcontextprotocol/sdk` + `zod` imports with dynamic `import()` in `loadSdk()`, called on first `startMcpHttpServer()`. Deferred MCP auto-start by 1.5s after window init. SDK only loads when MCP is actually enabled. |
| US-122 | Phase 6: Test Preview & Browser Facades | EPIC-002 | Tested all preview facades via MCP: `asMarkdown()` (viewMounted, html), `asSvg()` (svg), `asHtml()` (html), `asMermaid()` (svgUrl, loading, error), `asBrowser()` (url, title, navigate, back, forward, reload). Browser facade has async state — url/title getters may return stale values when checked immediately after navigation in same script. All passed. No code changes needed. |
| US-121 | Phase 5: Test Content Editor Facades | EPIC-002 | Tested Notebook, Todo, Link facades via MCP. Notebook: all 11 methods passed. Todo: `addItem()` silently failed without selected list — added 4 new methods (`selectList`, `selectTag`, `setSearch`, `clearSearch`) to TodoEditorFacade and ITodoEditor. Link: all methods passed. Also added editor validation to MCP `create_page` handler (returns error with valid editor list). Created US-117 for switch button fix. |
| US-120 | Phase 4: Test Text & Grid Facades | EPIC-002 | Tested `asText()` and `asGrid()` via MCP. Text: `editorMounted`, `getSelectedText()`, `insertText()`, `replaceSelection()`, `revealLine()`, `setHighlightText()`, `getCursorPosition()` all passed. Grid: `rows`, `columns`, `rowCount`, `editCell()`, `addRows()`, `deleteRows()`, `addColumns()`, `deleteColumns()`, `setSearch()`, `clearSearch()` all passed. No gaps. No code changes needed. |
| US-119 | Phase 3: Test Page Object API | EPIC-002 | Tested `page` properties via MCP: read-only (`id`, `type`, `title`, `modified`, `pinned`, `filePath`), read-write (`content`, `language`, `editor`), `data` storage, `grouped` auto-creation. All passed. No gaps. No code changes needed. |
| US-118 | Phase 2: Test Pages Collection API | EPIC-002 | Tested `app.pages` via MCP: queries (`activePage`, `findPage`, `getGroupedPage`, `isLastPage`, `isGrouped`), lifecycle (`openFile`, `addEmptyPage`, `addEditorPage`, `showAboutPage`, `showSettingsPage`, `showBrowserPage`), navigation (`showPage`, `showNext`, `showPrevious`), layout (`moveTab`, `pinTab`, `unpinTab`, `group`, `ungroup`). All passed. No gaps. No code changes needed. |
| US-116 | Test & Improve app.menuFolders API | EPIC-002 | Tested all `app.menuFolders` methods via MCP: `folders` (returns IMenuFolder[] with id/name/path), `add()` (path-based + virtual with files[], returns string UUID), `find()` (found returns object, not-found returns undefined), `move()` (reorders correctly), `remove()` (removes folder, non-existent is no-op). All passed. No gaps. No code changes needed. |
| US-115 | Test & Improve app.settings API | EPIC-002 | Tested all `app.settings` methods via MCP: `theme` (returns "default-dark"), `get()` (known keys return values, unknown returns undefined), `set()` (round-trip works, value persists), `onChanged` (subscription fires with {key, value}, dispose stops events). All passed. No gaps. No code changes needed. |
| US-114 | Test & Improve app.downloads API | EPIC-002 | Tested all `app.downloads` methods via MCP: `downloads` (returns DownloadEntry[] with id/filename/url/savePath/totalBytes/receivedBytes/status/startTime), `hasActiveDownloads` (false when all completed), `aggregateProgress` (0 when no active), `clearCompleted()` (no throw), `cancelDownload`/`openDownload`/`showInFolder` with invalid ID (all no-ops, no throw). All passed. No gaps. No code changes needed. |
| US-113 | Test & Improve app.recent API | EPIC-002 | Tested all `app.recent` methods via MCP: `load()` (reloads from disk), `files` (returns string[], 100 items capped), `add()` (adds to top, deduplicates, respects cap), `remove()` (removes from list). `clear()` skipped to preserve user data. All passed. No gaps. No code changes needed. |
| US-112 | Test & Improve app.editors API | EPIC-002 | Tested all `app.editors` methods via MCP: `getAll()` (15 editors, correct id/name/category), `getById()` (found + not-found), `resolve()` (JSON/MD/CSV/TXT/unknown all resolve to monaco), `resolveId()`, `getSwitchOptions()` (json→[monaco, grid-json], markdown→[monaco, md-view], plaintext→empty). All passed. No gaps. No code changes needed. |
| US-111 | Test & Improve app.ui API | EPIC-002 | Tested all `app.ui` methods via MCP with user interaction: `confirm()` (default buttons, custom 3-button, dismiss→null), `input()` (default, with prefilled value/selectAll/custom buttons, cancel→null), `password()` (encrypt mode with confirm field, decrypt mode, cancel→null), `notify()` (all 4 types fire-and-forget, click interaction returns "clicked"). All passed. No gaps identified — API is complete. No code changes needed. |
| US-110 | Test & Improve app.window API | EPIC-002 | Tested all `app.window` methods via MCP: `windowIndex` (returns 0), `isMaximized`/`menuBarOpen`/`zoomLevel` (reactive state reads), `maximize()`/`restore()`/`toggleWindow()`/`minimize()` (visual window actions), `toggleMenuBar()` (sidebar toggle), `zoom(delta)`/`resetZoom()` (zoom in/out/reset), `openNew()` (opens new window, returns index). All passed. `close()` skipped (would kill MCP session). No gaps identified — API is complete. No code changes needed. |
| US-109 | Test & Improve app.shell API | EPIC-002 | Tested all `app.shell` methods via MCP: `openExternal()` (opened URL in browser), `encryption.encrypt()`/`decrypt()`/`isEncrypted()` (round-trip, wrong password throws, invalid text throws, detection works), `version.runtimeVersions()` (returns electron/node/chrome), `version.checkForUpdates()` (returns update info). All passed. No gaps identified — API is complete for its scope. No code changes needed. |
| US-108 | Test & Improve app.fs API | EPIC-002 | Tested all `app.fs` methods via MCP `execute_script`: file I/O (read/readFile/readBinary/write/writeBinary), exists/delete, encoding variants (utf-8, utf-8-bom, utf-16le), dialogs (showOpenDialog/showSaveDialog/showFolderDialog with select and cancel), path resolution (resolveDataPath/resolveCachePath/commonFolder), OS integration (showInExplorer/showFolder). All passed. Added two new methods: `listDir(dirPath, pattern?)` and `mkdir(dirPath)` — exposed existing private helpers `_listDirFiles` and `_ensureDir`. Updated `IFileSystem` interface, `assets/editor-types/fs.d.ts`, `assets/mcp-api-guide.md`, and `docs/api/fs.md`. |
| US-107 | MCP Multi-Window Support | EPIC-001 | Added multi-window targeting to all MCP tools via optional `windowIndex` parameter (defaults to first open window for backward compatibility). New `list_windows` tool runs in main process (no IPC) — returns all windows with `status: "open"\|"closed"`, page count, and page metadata. New `open_window` tool reopens closed windows with persisted pages. Closed windows return actionable error suggesting `open_window`. Shared `windowIndexParam` Zod schema across 7 existing tools. Updated `assets/mcp-api-guide.md` with multi-window documentation. |
| US-106 | MCP Title Bar Indicator | EPIC-001 | Push-based MCP status indicator in title bar. Added `eMcpStatusChanged` IPC event broadcast from main process on server start/stop and session connect/disconnect. `Window` class holds reactive `mcpRunning`/`mcpClientCount` state. Green dot + "MCP" label + connection count in `MainPage.tsx`, absolutely positioned below system buttons. Replaced 5s polling in SettingsPage with event subscription. |
| US-105 | MCP API Docs Resource | EPIC-001 | Added `assets/mcp-api-guide.md` — condensed API documentation (~344 lines) exposed as MCP resource `notepad://docs/api-guide`. Registered via `server.registerResource()` in `mcp-http-server.ts`, loaded dynamically from assets using `getAssetPath()`. Gives standalone AI clients (Claude Desktop, ChatGPT, Gemini) scripting API context without project-level docs. |
| US-104 | MCP Settings UI & Polish | EPIC-001 | Added MCP Server section to Settings page (`SettingsPage.tsx`). `McpSection` component with: enable/disable checkbox toggle, port input (disabled when enabled, validated 1024-65535), live status indicator (green dot + client count via `getMcpStatus` polling), URL display with Copy URL button, AI client config JSON snippet with Copy button. Updated `docs/mcp-setup.md` Quick Start for Settings UI. Updated `docs/whats-new.md`. |
| US-103 | MCP Tools & Configuration | EPIC-001 | Added 3 new MCP tools: `create_page` (with content/language/editor/title), `set_page_content` (text-based pages), `get_app_info` (version/pageCount/activePageId). Refined descriptions for all 7 tools. Added `pageId` optional param to `execute_script`. Created `.mcp.json` for Claude Code. Created `docs/mcp-setup.md` user guide with AI client configs (Claude Code, Claude Desktop, ChatGPT, Gemini CLI). |
| US-102 | HTTP MCP Transport | EPIC-001 | Replaced Named Pipe MCP server with Streamable HTTP server (`mcp-http-server.ts`) using `@modelcontextprotocol/sdk`. Per-session McpServer + StreamableHTTPServerTransport on `http://localhost:7865/mcp`. 4 MCP tools: `execute_script`, `list_pages`, `get_page_content`, `get_active_page`. Reuses existing IPC bridge (MCP_EXECUTE/MCP_RESULT). `whenReady` guard for renderer initialization. Bound to 127.0.0.1 for security. `mcp.port` setting added (default 7865). Universal compatibility: Claude Desktop, Claude Code, ChatGPT, Gemini CLI. |
| US-101 | MCP Bidirectional Pipe Protocol | EPIC-001 | Named Pipe server (`mcp-pipe-server.ts`) with JSON-RPC 2.0 protocol for AI agent integration. Renderer-side command handler (`mcp-handler.ts`) dispatches `execute_script`, `get_pages`, `get_page_content`, `get_active_page`. Console capture in ScriptContext (`ConsoleLogEntry`). `ScriptRunner.runWithCapture()` for headless execution. Settings-driven lifecycle via `mcp.enabled`. IPC endpoints `setMcpEnabled`/`getMcpStatus`. Settings `wait()` method for async readiness. |
| US-100 | Mermaid Diagram "Open in Editor" Button | | Added "Open in Editor" button to mermaid diagram hover toolbar in Markdown Preview. Clicking opens the diagram source in a new Mermaid editor tab via `pagesModel.addEditorPage()`. Toolbar replaces single copy button with two-button layout (open + copy). |
| US-072 | Phase 7e: Future Architecture Cleanup | | Deleted `doc/future-architecture/` folder (30+ design/migration planning docs — all content superseded by rewritten architecture docs, diagrams, and user API reference). Deleted 17 completed task folders (US-052 through US-068). Removed migration notice from `active.md` — App Object Model migration is complete. |
| US-071 | Phase 7d: Documentation Automation Skills | | Created `/project:review` command (architecture validation against `doc/`), `/project:document` command (developer docs update in `doc/`), and `/project:userdoc` agent (Sonnet, isolated context, user docs update in `docs/`). Created Epic system: `doc/epics/` with template and active tracker, added Epic column to `completed.md`, updated task template with Epic field. Overhauled CLAUDE.md workflow: auto-task creation for ad-hoc work, mandatory `/project:review` + `/project:document` + `/project:userdoc` on task completion, work discovery flow (active task → active epic → ask user), epic-task linking. |
| US-070 | Phase 7c: API Reference → User Scripting Guide | | Created `docs/api/` with 12 user-facing API reference pages: `index.md` (overview + navigation), `page.md` (IPage + all 10 editor facades), `app.md` (IApp overview + menuFolders), `pages.md` (IPageCollection — queries, lifecycle, navigation, layout), `fs.md` (IFileSystem — file I/O, path resolution, dialogs, OS integration), `settings.md` (ISettings — get/set, change events), `ui.md` (IUserInterface — confirm, input, password, notify), `shell.md` (IShell — openExternal, version, encryption), `window.md` (IWindow — actions, zoom, multi-window), `editors.md` (IEditorRegistry — resolve, switch options), `recent.md` (IRecentFiles), `downloads.md` (IDownloads). All verified against `.d.ts` type definitions. Updated `docs/scripting.md` (added links to API pages, added pages + downloads to services table). Updated `docs/index.md` (added API Reference link). |
| US-069 | Phase 7b: Mermaid Architecture Diagrams | | 5 standalone diagrams in `doc/architecture/diagrams/`: (1) Application Layers — process boundaries + renderer layer dependencies, (2) Bootstrap Sequence — full init timeline with guard flags, (3) Editor System — resolution flow + rendering architecture + content-view switching, (4) ContentViewModel Lifecycle — class hierarchy + acquire/release sequence + multi-consumer ref-counting, (5) Script Execution Flow — F5 → wrappers → facades → auto-release cleanup. |
| US-068 | Phase 7a: Architecture Docs Rewrite | | Full rewrite of 4 docs (`overview.md`, `folder-structure.md`, `state-management.md`, `scripting.md`), significant update of 2 docs (`editors.md`, `pages-architecture.md`), minor update of 1 doc (`browser-editor.md`). All verified against actual source code. Added: Object Model concept, bootstrap sequence, ContentViewModel pattern, 10 scripting facades with auto-release lifecycle, wrapper architecture, type definitions. Fixed: all stale `/store/`/`/features/`/`/core/services/` references. Updated CLAUDE.md (folder structure, key files, doc map). |
| US-067 | Phase 6d: Browser + Read-Only Facades | | `BrowserEditorFacade` (direct model wrap, no ViewModel/ref-counting; url, title, navigate, back, forward, reload). `MarkdownEditorFacade` (viewMounted + html from DOM container). `SvgEditorFacade` (svg source). `HtmlEditorFacade` (html source). `MermaidEditorFacade` (svgUrl, loading, error). 5 new `.d.ts` interfaces, 5 new facade classes. `PageWrapper` updated with `asMarkdown()`, `asSvg()`, `asHtml()`, `asMermaid()`, `asBrowser()`. Browser uses `state.get().type === "browserPage"` check (no instanceof, preserves code splitting). |
| US-066 | Phase 6c: Content Editor Facades (Notebook, Todo, Link) | | `NotebookEditorFacade` (notes CRUD, categories, tags; maps `NoteItem.content.content` → flat `INote.content`). `TodoEditorFacade` (items CRUD, lists, tags; maps `done` → `completed`, `tag: null` → `""`). `LinkEditorFacade` (links CRUD; maps `href` → `url`, computes `pinned`). Added `skipConfirm` parameter to delete methods in `NotebookViewModel` (`deleteNote`) and `TodoViewModel` (`deleteItem`, `deleteList`, `deleteTag`). `NotebookViewModel.addNote()` now returns `NoteItem`. 3 new `.d.ts` interfaces, 3 new facade classes. `PageWrapper` updated with `asNotebook()`, `asTodo()`, `asLink()`. |
| US-065 | Phase 6b: Text + Grid Editor Facades | | `ITextEditor` interface with `editorMounted` property + 6 methods (`getSelectedText`, `revealLine`, `setHighlightText`, `getCursorPosition`, `insertText`, `replaceSelection`). `IGridEditor` + `IColumnInfo` interfaces (rows, columns, editCell, addRows/deleteRows, addColumns/deleteColumns, setSearch/clearSearch). `TextEditorFacade` wraps `TextViewModel`, `GridEditorFacade` wraps `GridViewModel` (maps Column→IColumnInfo). `PageWrapper.asText()` and `asGrid()` acquire ViewModels via `ContentViewModelHost`, return facades, track releases in `releaseList`. Grid editor ID auto-detected from current editor or language. 3 new methods added to `TextViewModel`. |
| US-064 | Phase 6a: Scripting Infrastructure | | `IPage` in `shared/types.ts` renamed to `IPageState` (16 files). `page.data` promoted from `ScriptPanelModel` to `PageModel` base class (all page types). Created `IPage` interface in `api/types/page.d.ts` with content/language/editor/grouped/data + stub `asX()` methods. `IPageInfo` deleted — all returns use `IPage`. `PageEditor`/`Language` types added to `common.d.ts`. Created `PageWrapper`, `PageCollectionWrapper`, `AppWrapper` in `/scripting/api-wrapper/`. Shared `releaseList` flows through all wrappers. `ScriptContext` refactored to use wrappers, returns `{ context, cleanup }`. `ScriptRunner` calls `cleanup()` in finally block. |
| US-063 | Phase 5 — Migration Review | | Audited all remaining old folders. `/store/` deleted (menu-folders→api, link-open-menu→editors/shared, language-mapping→core/utils). `/features/` deleted (tabs/sidebar/navigation→ui). `/setup/` moved to `/api/setup/` with `app.initSetup()`. `/core/services/` deleted (scripting→`/scripting/`, file-watcher→core/utils). Icons extracted to `/components/icons/`. `app.menuFolders` (IMenuFolders) wired. All decisions documented in [10.migration-review.md](../future-architecture/migration/10.migration-review.md). |
| US-062 | NoteItemEditModel — Formal IContentHost | `NoteItemEditModel` now formally `implements IContentHost`. Removed `as unknown as TextFileModel` cast in `NoteItemActiveEditor` and `this as any` in `acquireViewModel`. Fixed `changeLanguage` signature (`string` → `string \| undefined`). `AsyncEditor` prop type widened to `PageModel \| IContentHost`. `FileEditorPage` generic constraint widened to match. Individual content-view editors keep `TextFileModel` prop type unchanged. |
| US-061 | Pure Renderer ViewModels (SVG, HTML, Mermaid) | | `SvgViewModel`, `HtmlViewModel`, `MermaidViewModel` created as `ContentViewModel` wrappers. Factories registered for `"svg-view"`, `"html-view"`, `"mermaid-view"`. SVG/HTML are near-empty ViewModels (pattern consistency + `pageModel` getter for Task 10). MermaidViewModel has real state (`svgUrl`/`error`/`loading`/`lightMode`) — 400ms debounced render moved from `useEffect` to `onContentChanged()` + `lightMode` subscription. All three components use `useContentViewModel` + `useSyncExternalStore`. Parallel imports in `loadModule`. |
| US-060 | LinkViewModel (Link ContentViewModel) | | `LinkEditorModel` refactored to `LinkViewModel extends ContentViewModel`. Factory registered for `"link-view"`. `LinkEditor` uses `useContentViewModel` hook + `useSyncExternalStore`. `swapLayout` prop eliminated from model — component calls `vm.initBrowserSelection()` explicitly. `BrowserBookmarks` switched from direct construction to `acquireViewModel("link-view")` for shared ref-counted instance with BookmarksDrawer; required `language: "json"` in state for registry validation. Selection state caching switched to `host.stateStorage`. Child components updated (`LinkItemList`, `LinkItemTiles`, `PinnedLinksPanel`). `LinkEditorModel.ts` deleted. |
| US-059 | MarkdownViewModel (Markdown ContentViewModel) | | Inline `MarkdownViewModel` extracted from `MarkdownView.tsx` to standalone `MarkdownViewModel extends ContentViewModel`. Factory registered for `"md-view"`. `MarkdownView` uses `useContentViewModel` hook + `useSyncExternalStore`. Read-only preview — no content parsing, `onContentChanged()` only triggers search re-evaluation. `effect()` with deps replaced by `state.subscribe()` + `onContentChanged()`. Container DOM ref kept in reactive state (needed by Minimap). Fixed `containerSrollTop` typo. No child component changes needed (`MarkdownSearchBar`, `CodeBlock` are stateless). |
| US-058 | TodoViewModel (Todo ContentViewModel) | | `TodoEditorModel` refactored to `TodoViewModel extends ContentViewModel`. Factory registered for `"todo-view"`. `TodoEditor` uses `useContentViewModel` hook + `useSyncExternalStore`. `gridModel?.update()` moved to component `useEffect`. Selection state caching switched from direct `fs` to `host.stateStorage` (consistency with GridViewModel). Child components updated (`TodoListPanel`, `TodoItemView`). `TodoEditorModel.ts` deleted. |
| US-057 | NotebookViewModel (Notebook ContentViewModel) | | `NotebookEditorModel` refactored to `NotebookViewModel extends ContentViewModel`. Factory registered for `"notebook-view"`. `NotebookEditor` uses `useContentViewModel` hook + `useSyncExternalStore`. `gridModel?.update()` moved from model to component `useEffect` (React rendering concern). `pageModel` getter for script context access. Debounced save flushed in `onDispose()`. Child components updated (`NoteItemViewModel`, `ExpandedNoteView`, `NoteItemEditModel`). Partial Task 9: `NoteItemEditModel` got `acquireViewModel`/`releaseViewModel`/`stateStorage` via `ContentViewModelHost` — required for embedded grid/markdown editors. `NotebookEditorModel.ts` deleted. |
| US-056 | GridViewModel (Grid ContentViewModel) | | `GridPageModel` refactored to `GridViewModel extends ContentViewModel`. Factory registered for both `"grid-json"` and `"grid-csv"` (same factory, reads editor type from host state). `GridEditor` uses `useContentViewModel` hook + `useSyncExternalStore` (Rules of Hooks fix — cannot call `vm.state.use()` after early return). Auto-focus moved from model to component `useEffect`. `CsvOptions` updated. `GridPageModel.ts` deleted. |
| US-055 | TextViewModel (Monaco ContentViewModel) | | `TextEditorModel` refactored to `TextViewModel extends ContentViewModel`. Factory registered for `"monaco"` via `loadModule` + `await import()`. `TextEditor` uses `useContentViewModel` hook. `TextToolbar` uses `tryGet` + `useOptionalModelState` (no extra ref). External consumers migrated to TextFileModel delegate methods. `ContentViewModelHost.tryGet()` added. Pending ops (revealLine, setHighlightText) work before Monaco mounts. |
| US-053 | TextFileModel implements IContentHost | | `TextFileModel` now implements `IContentHost`. Added `ContentViewModelHost` composition, `acquireViewModel`/`releaseViewModel` delegates, file-based `stateStorage`, and `_vmHost.disposeAll()` in dispose. Purely additive — no consumer changes. |
| US-054 | TextFileModel Decomposition | | Decomposed TextFileModel (~567 lines) into 3 focused submodels: `TextFileIOModel` (file I/O, watchers, cache), `TextFileEncryptionModel` (encrypt/decrypt, password, content mapping), `TextFileActionsModel` (keyboard, scripts, nav panel, compare). Core retains state, lifecycle, and flat API delegates for backward compatibility. Zero consumer changes. |
| US-052 | Content View Models Foundation | | `IContentHost` interface, `ContentViewModel<TState>` abstract base, `ContentViewModelHost` ref-counting helper, `useContentViewModel` hook. `EditorRegistry` extended with module cache + view model factory methods. `EditorModule.createViewModel` optional field. Purely additive — no existing editors modified. |
| US-051 | Window API Consolidation | | Merged window commands, zoom, and dev tools into `app.window` (IWindow). Removed `window-commands.ts`, absorbed into `api/window.ts`. 8 consumers migrated. Created Phase 5 architecture doc `9.content-view-models.md`. |
| US-050 | Phase 4b — Pages API (`app.pages`) | | All page logic moved from `store/pages-store.ts` into 5 submodels under `api/pages/` (Lifecycle, Navigation, Layout, Persistence, Query). `page-actions.ts` absorbed. 29 consumers migrated. Bridge files deleted. `IPageCollection` added to `IApp`. Removed fs.ts 100ms hack. See [migration/8.app-pages.md](../future-architecture/migration/8.app-pages.md). |
| US-049 | Phase 4a — Event Services + Downloads API | | 3-layer bootstrap (`initServices` → `initPages` → `initEvents` → `windowReady`). EventHandler deleted, MainPage simplified. 4 internal services (GlobalEvent, Keyboard, WindowState, RendererEvents). Downloads API (`IDownloads`) absorbed from store. See [migration/8.app-pages.md](../future-architecture/migration/8.app-pages.md). |
| US-048 | Phase 3a — `app.ui` (IUserInterface) | | IUserInterface: wrap dialog functions (confirm, input, password) + absorb alert notification API (notify). `features/dialogs/` moved to `ui/dialogs/`. 11 consumer files (~38 call sites) migrated, old exports removed. |
| US-045 | Phase 3b — `app.shell` (IShell) | | IShell: absorb encryption logic into `api/shell/`, wrap version service IPC, add `openExternal()`. 6 consumer files migrated, `encryption.ts` deleted. |
| US-047 | Phase 2 Revision — Full Absorption into API | | Full absorption: `nodeUtils` file I/O + `filesModel` data/cache/path ops merged into `api/fs.ts`. `windowIndex` added to `IWindow`. `uuid()` replaced with `crypto.randomUUID()`. `files-store.ts` and `node-utils.ts` deleted. `watchFile`/`getFileStats` inlined into `file-watcher.ts`. 18+ consumers updated. |
| US-046 | Phase 1 Revision — Move Logic into API | | `app.settings`: moved logic from `app-settings.ts` to `api/settings.ts` (14 consumers). `app.editors`: confirmed facade pattern correct (no code change). `app.recent`: moved logic from `recent-files.ts` to `api/recent.ts` (4 consumers). Old stores deleted. |
| US-044 | Phase 2 — File System & Window | | `app.fs` (IFileSystem: read/readFile/write/readBinary/writeBinary/exists/delete, resolveDataPath/resolveCachePath, dialogs, showInExplorer/showFolder), `app.window` (IWindow: minimize/maximize/restore/close, isMaximized, zoom/resetZoom/zoomLevel, openNew). Dynamic `import()` in `initServices()`. API reference docs created. |
| US-043 | Phase 1 — Independent Interfaces | | `app.settings` (ISettings wrapping appSettings with onChanged event), `app.editors` (IEditorRegistry — read-only subset of editorRegistry), `app.recent` (IRecentFiles with lazy load()). Dynamic `import()` via `initServices()` in app.ts. API reference docs created. |
| US-042 | Phase 0 — App Object Model Infrastructure | | `/api/` folder with IDisposable, IEvent, IApp types. `app` singleton with async init (version via IPC). Script context wiring. Vite plugin auto-copies .d.ts to assets for Monaco IntelliSense. |
| — | Phase -1: Pre-Migration Complete | Enhanced TComponentModel with effect()/memo()/auto init-dispose. Migrated all page editors (-1.3 through -1.8) and app shell (-1.1, -1.2). Views are now pure render functions. |
| US-041 | Tabs & Navigation Migration to Enhanced Model-View | | Pre-migration -1.2: TabsModel + MenuBarModel → effect()/memo(), refs and state moved to model. PageTabModel skipped (no lifecycle) |
| US-040 | App Shell Migration to Enhanced Model-View | | Pre-migration -1.1: MainPageModel + EventHandlerModel → effect() pattern, auto init/dispose, removed useEffect boilerplate from Views |
| US-039 | Browser Find in Page | | Inline search bar with match counter, Ctrl+F interception via main process + preload script + renderer keydown, webview.findInPage() API |
| US-030 | Browser Download Progress & History | | Toolbar download button with SVG progress ring, Popper popup with download list, main-process DownloadService with IPC broadcasting, native save dialog for reliable path capture, global state shared across windows, last 5 completed persisted. Also fixed external URL routing (left-to-right search, default profile only). |
| US-036 | Register as Default Browser | | HKCU registry management (no admin), Settings page UI, deep-link to Windows Default Apps, separate eOpenExternalUrl event for OS URLs, launcher version info for proper app name/icon |
| US-038 | Switch to electron-builder + NSIS | | electron-builder replaces Forge makers, NSIS installer with custom options page (shortcuts, context menu, file associations, browser registration), AllowSetForegroundWindow for bring-to-front, GitHub Actions with Rust toolchain |
| US-037 | Lightweight Rust Launcher | | 308KB Rust launcher with Named Pipe IPC for instant file/URL/diff opening, pipe server in main process, concurrent connection support |
| US-035 | Miscellaneous Enhancements | | Link Editor hostnames panel, Link/ToDo Editor session state persistence, pinned tab file path tooltip, browser popup/tab spam blocking |
| US-034 | Link Editor Enhancement | | Favicon caching & display, drag-and-drop (links→categories, categories→categories with reparenting), pinned links panel with drag-to-reorder |
| US-028 | Browser Bookmarks (Links Editor Integration) | | Per-profile bookmarks with star button, sliding drawer with full Link Editor, image discovery (meta tags + click tracking + context menu), encrypted bookmarks support via async password dialog, auto-save |
| US-033 | Link Editor | | Structured link manager for `.link.json` files with categories, tags, 5 view modes (list + 4 tile variants), edit dialog, image context menu, per-category and per-tag view mode, quick-add menu |
| US-032 | Browser Integration — Link Opening & Context Menu | | Link open behavior setting (default browser vs internal), smart browser tab routing (right→left→create new), Markdown link context menu (Open in Default/Internal Browser, Incognito), Settings page UI |
| US-031 | Browser Enhancement #1 | | OAuth popup support, toolbar changes, active tab styling, transparent splitter, compact tab extension popup, tab audio mute controls, home button, tabs panel initially collapsed, search engine selector, URL bar suggestions dropdown with search/navigation history |
| US-027 | Browser Profiles & Incognito Mode | | Named browser profiles with isolated sessions (cookies/storage/cache per profile), incognito mode (ephemeral partition), profile management in Settings (add/remove/color/default/clear data), incognito icon in URL bar, profile-colored globe icons on page tabs, quick-add submenu for profiles |
| US-029 | Browser Web Page Context Menu | | Contextual right-click menu for webview content (links, images, text, SVG), View Source/Actual DOM, Open Image/SVG in editor, URL bar with navigate button and Paste and Go, dynamic webview background |
| US-026 | Browser Internal Tabs | | Multi-tab browsing within a browser page, left-side tabs panel, target="_blank"/window.open() open new internal tabs, tab context menu (close/close others/close below), resizable panel, session restore for all tabs |
| US-025 | Basic Browser Editor | | Built-in web browser as page-editor using webview, multi-process architecture (renderer + main + guest preload), favicon/title detection, session restore, protocol blocking |
| US-023 | NavigationPanel File Content Search | | VS Code-style file content search with streaming results, include/exclude patterns, FileExplorer filtering, Monaco highlighting, configurable extensions in Settings |
| US-024 | ToDo List Enhancement — Tags | | Colored tags with filtering, tag assignment on items, predefined color palette, drag-drop warnings |
| US-022 | ToDo Editor | | Structured task list editor for `.todo.json` files with multiple lists, drag-to-reorder, search, comments, virtualized grid |
| US-020 | Markdown View Search | | Ctrl+F search in Markdown Preview with match highlighting, F3/Shift+F3 navigation, match counter, active match background |
| US-019 | Open User Folder in Tab | | Chevron on sidebar folders opens new tab with NavPanel, hover+tooltip for discoverability, built-in items unchanged |
| US-018 | Implement Grouping for Pinned Tabs | | Non-adjacent grouping for pinned+unpinned tabs, script execution works in pinned tabs, grouping preserved through pin/unpin |
| US-017 | Pinning Page Tabs | | Compact icon-only pinned tabs with sticky positioning, drag constraints, window persistence, navigation preservation |
| US-016 | Reusable File Explorer Component | | Tree-based FileExplorer replacing flat lists in sidebar and NavigationPanel, lazy loading, search, file operations, state persistence, navigate up/make root, collapse all |
| US-015 | Document Navigation Panel | | Folder-based nav tree from markdown links, in-tab navigation with auto-preview, pages-store lifecycle refactoring, global keyboard shortcuts fix |
| US-014 | Application Theming | | CSS Custom Properties theming with 6 dark themes, Settings page with theme selector, Monaco integration |
| US-013 | Create HTML Viewer | | Sandboxed iframe preview for HTML files with script execution support |
| US-012 | Markdown View Enhancements | | Code block syntax highlighting, copy-to-clipboard button, inline Mermaid diagrams, shared render-mermaid module |
| US-011 | Mermaid Diagram Viewer | | .mmd/.mermaid preview with light/dark toggle, copy to clipboard, syntax highlighting |
| US-010 | Copy Image to Clipboard | | Copy button in Image Viewer header and SVG View toolbar, Ctrl+C shortcut |
| US-009 | Notebook Editor | | Structured notes editor with categories, tags, search, drag-and-drop, expand-to-overlay |
| US-008 | TypeScript and Dependencies Upgrade | | TypeScript 4.5→5.4, @types/node 16→20, fixed vite compatibility |
| US-007 | About Page and Version Check | | About page with version info + auto update check via GitHub API |
| US-006 | Create Image View Editor | | Image viewer for binary images + SVG preview as content-view |
| US-003 | ContentPageModel Extraction | | Closed - architecture review concluded existing design is correct |
| US-002 | Editor Registry Pattern | | Declarative editor registration in `register-editors.ts` |
| US-001 | Fix Circular Dependencies | | Direct imports instead of barrel exports |
| US-005 | Create User Documentation | | Completed - see `/docs/` folder |
