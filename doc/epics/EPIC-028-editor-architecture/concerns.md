# EPIC-028 — Concerns Log

Architectural concerns surfaced during the design phase. Each row stays open until resolved during mockup work or per-editor walkthrough. Resolutions go in the same row.

## Format

| # | Date raised | Concern (short) | Source / context | Status | Resolution |
|---|-------------|------------------|------------------|--------|------------|

## Open

| # | Date raised | Concern | Source / context | Status | Resolution |
|---|-------------|---------|------------------|--------|------------|

## Resolved

| # | Date raised | Concern | Source / context | Status | Resolution |
|---|-------------|---------|------------------|--------|------------|
| L1 | 2026-05-19 | `addEmptyPage` must instantiate the default editor through the registry. | [`walkthroughs/01-page-lifecycle.md#L1`](walkthroughs/01-page-lifecycle.md) | **Resolved 2026-05-19** | Hardcode `"monaco"` inside `addEmptyPage`. Persephone is a Windows Notepad replacement by design — empty page is and will remain Monaco. No `getDefaultEditor()` helper, no setting. |
| L2 | 2026-05-19 | Persistence-debounce subscription in `PagesModel.attachPage` must hook both editor state and host state. Today's single subscription works because `TextFileModel` IS the editor; after refactor it isn't. | [`walkthroughs/01-page-lifecycle.md#L2`](walkthroughs/01-page-lifecycle.md) | **Resolved 2026-05-19** | `EditorModel.descriptorChanged: Subscription<void>` (A6). Base class auto-fires on state mutation; text-bearing editors forward host-state mutations via the `inheritContentHost`/`extractContentHost` boundary. `attachPage` subscribes to this signal — page layer stays host-agnostic. A1 (`getContentHost` on trait) survives but for the switch-widget rationale only. |
| L3 | 2026-05-19 | `closeFirstPageIfEmpty` heuristic reads `editorState.type === "textFile" && !content && !filePath` directly. Page-collection layer shouldn't know about editor-internal shape. | [`walkthroughs/01-page-lifecycle.md#L3`](walkthroughs/01-page-lifecycle.md) | **Resolved 2026-05-19** | `EditorModel.isFreshEmpty(): boolean` on base (default false), Monaco overrides to return true when its host has no content/filePath and is not modified. `closeFirstPageIfEmpty` becomes a single-line check via the editor. |
| L4 | 2026-05-19 | Fixed-page-ID flows (well-known pages, About, Settings, addEditorPage) and the open-file flow create editor + host together. The page class shouldn't know about hosts. | [`walkthroughs/01-page-lifecycle.md#L4`](walkthroughs/01-page-lifecycle.md) | **Resolved 2026-05-19** | Three-phase model lifecycle (A7): `createEditor → (applyRestoreData OR switchFrom) → restore()`. Page is host-agnostic. Editor builds its own host inside `restore()` with try/catch + empty-host fallback + `ui.notify(...)` error notification. `restore` name kept; semantics evolve (skips host restore when `host.state.restored` is already true after switchFrom). `switchFrom` throws on incompatible old editor. Trait shrinks to just `extractContentHost`. A2 superseded; `createEditorFromFile` and `switchEditorViaContentHost` helpers gone. |
| L5 | 2026-05-19 | `restoreModel` blob shape: today is flat `IEditorState`; after refactor needs `{ editorId, state, host? }`. | [`walkthroughs/01-page-lifecycle.md#L5`](walkthroughs/01-page-lifecycle.md) | **Deferred 2026-05-19** | Deferred to walkthrough 04 — Persistence. Lifecycle's call-site shape (A7's three-phase pattern) is independent of the on-disk format; walkthrough 04 will decide the descriptor shape and the migration shim. |
| L6 | 2026-05-19 | `PageModel.dispose` doesn't guard against Pattern B (model in both `mainEditor` and `secondaryEditors`). | [`walkthroughs/01-page-lifecycle.md#L6`](walkthroughs/01-page-lifecycle.md) | **Resolved 2026-05-19** | Unified-array PageModel redesign (A8): single `editors: EditorModel[]` array + `_mainEditorId: string | null` flag. Pattern B becomes inexpressible — one editor sits in one array with separate "is main" and "contributes panels" flags. `dispose()` is a clean loop. A4 (assertion) dropped. Each editor manages its own panel contributions internally; PageNavigator walks `editors[]` and collects contributions. How contributions are exposed defers to walkthrough 03. |
| L7 | 2026-05-19 | Visibility criterion for editor lifetime in `editors[]`: when should the PageModel detach + dispose an editor? | [`walkthroughs/01-page-lifecycle.md`](walkthroughs/01-page-lifecycle.md) — Q4 resolution | **Resolved 2026-05-19** | Keep iff `(editor.id === _mainEditorId) || editor.contributesPanels()`. Evaluated when `setMainEditor` changes `_mainEditorId` and when an editor's panel contribution changes. Notification mechanism for the second trigger defers to walkthrough 03. |
| C1 | 2026-05-19 | How does `TextChrome` discover host capabilities (file-backed, encryptable, scriptable)? Two candidates: `instanceof TextFileModel` check vs. additional sub-traits on the host (`IFileBacked`, `IEncryptable`, `IScriptable`). | Initial design discussion. Sub-trait approach is more extensible; instanceof is simpler. | **Resolved 2026-05-19** | instanceof. TextChrome (walkthrough 10) branches on concrete host class (`TextFileModel` → full chrome; `NoteItemEditModel` → minimal chrome). A future third host type adds a new branch. Sub-traits considered but rejected as YAGNI — trait machinery only pays off with several independently-composable hosts, and we have two. |
| C2 | 2026-05-19 | Persistence migration: existing session data has `type: "textFile"` and `editor: "link-view"`. New shape stores the editor class directly. | Persistence — initial design. | **Resolved 2026-05-19** | No migration. Treat EPIC-028 as a breaking change; bump major version (3.x → 4.x). Restore implementation must: (1) detect old format and skip the entire restore — app boots with empty page via `checkEmptyPage`; (2) wrap per-page restore in try/catch and `console.warn` + `continue` on failure (unknown editor id, malformed state, etc.); (3) no `ui.notify` spam — restore happens before the user is ready. Walkthrough 04 codifies the detect-and-skip logic alongside the new on-disk shape. |
| C3 | 2026-05-19 | Monaco's `monaco.editor.ITextModel` (undo history, decorations) is owned by the Monaco view-model today and dies on switch-to-Grid. | Switch behavior. | **Resolved 2026-05-19** | Keep current behavior — undo/redo is Monaco-internal and resets on switch. `ITextModel` is created in `MonacoEditor.restore()` from `host.state.content` and disposed in `MonacoEditor.dispose()`. No attachment to `IContentHost`. Cross-editor undo (e.g., undo Monaco edits while in Grid mode) is a potential future epic — out of scope here. |
| C4 | 2026-05-19 | `NoteItemEditModel` decomposition in the new architecture. | Notebook walkthrough. | **Resolved 2026-05-19** | NoteItemEditModel remains a transient IContentHost (lifetime tied to React mount via `useComponentModel`). Embedded editor is a standalone `EditorModel` (Monaco/Grid/Markdown/Mermaid/…) wrapping NoteItemEditModel via the standard IContentHost contract. Initial host injection uses a new public method (likely `setContentHost(host)`) on text-bearing editors — exact shape codified in walkthrough 29. Switching embedded editor type uses standard `switchFrom`. Today's `changeEditor` logic on notes dissolves. Per-note preferred editor type persists in the note's data. |
| C5 | 2026-05-19 | Where the embedded-editor switch widget renders inside Notebook — per-note toolbar, global toolbar that changes meaning, or something else? | Notebook walkthrough. | **Resolved 2026-05-19** | Per-note. Each NoteItem React component renders its own switch widget, reading `embeddedEditor.findCompatibleEditors()` for the option list. Clicking an option triggers the per-note three-phase switch (`createEditor → switchFrom → restore` then replace the embedded editor in the React state). No global notebook-level switch — each note picks its own view. |
| C6 | 2026-05-19 | Compare mode is a flag on `TextFileModel` today (`state.compareMode`). | Compare flow. | **Deferred 2026-05-19 to walkthrough 06** | High-level baseline captured: (1) CompareEditor is host-level — works on two `TextFileModel` content strings, agnostic of which editor type is rendering; (2) Compare is only valid when both pages are grouped AND both have `TextFileModel` hosts; (3) `compareMode` is rendering policy ("render two grouped pages" vs. "render CompareEditor over the pair"), not editor state — suggesting it belongs at the page-pair / groupings level rather than on individual hosts. Walkthrough 06 codifies the final placement. |
| C7 | 2026-05-19 | `TextFileModel` carries several fields that today blur the line between host and view: `temp`, `restored`, `_pendingRevealLine`, `_pendingHighlightText`, `compareMode`, `detectedContentEditor`. Plus chrome refs: `editorToolbarRefFirst/Last`, `editorFooterRefLast`, `editorOverlayRef`. | TextFileModel decomposition. | **Deferred 2026-05-19 to per-property walkthroughs** | Tentative categorization: `temp` stays on host (file-level); `restored` stays on host (used by A7 skip-restore logic); `_pendingRevealLine` and `_pendingHighlightText` move to `MonacoEditor` (Monaco-specific deferred actions — walkthrough 20); `compareMode` deferred to walkthrough 06 (C6); `detectedContentEditor` **absorbed** into `editorRegistry.findEditorsAccepting(host)` — each editor's `accepts()` predicate receives the host and can peek at content for strong content-based matching (e.g., `content.startsWith('{"type":"notebook"')`). No `detectedContentEditor` state field; no background timer; detection happens on-demand when the switch widget queries. **Chrome refs** (`editorToolbarRefFirst/Last`, etc.) move out of TextFileModel into a React component owning them — see C8. Walkthroughs 20 and 06 confirm the other placements. |
| C8 | 2026-05-19 | `TextEditorView` hosts toolbar + script panel + footer + encryption padlock + editor body in one frame, and TextFileModel carries chrome refs (`editorToolbarRefFirst/Last`, etc.) so editors can portal controls into the chrome. After decomposition, where do those refs live and how are they exposed? | Chrome layout. | **Resolved 2026-05-19 (high-level); details to walkthrough 10** | No refs, no portals, no chrome wrapper. Each editor's React view composes shared chrome components directly: `<EditorPageView>`, `<PageToolbar model={model} firstPlace={...} lastPlace={...}>`, `<ScriptPanel model={model}>`, `<PageFooter model={model}>`, `<EditorOverlay model={model}>`. Shared components read `model` (and `model.page`, `model.host`) to decide what to render — e.g., `PageToolbar` renders the encryption padlock when `model.host instanceof TextFileModel && host.encrypted`. PageModel stays framework-agnostic; TextFileModel's `editorToolbarRefFirst/Last`, `editorFooterRefLast`, `editorOverlayRef` deleted. Walkthrough 10 finalizes: shared-component folder location (probably `src/renderer/editors/shared/`), exact slot list on each chrome component, host-instanceof branching internals (C1 resolution applies), naming. |
| C9 | 2026-05-19 | `IContentHost.stateStorage` placement — host or editor? | Editor state persistence. | **Resolved 2026-05-19** | `stateStorage` moves from IContentHost to EditorModel. Each editor exposes its own `stateStorage` keyed by `this.id` (read lazily so id transfers on switchFrom work). The host receives a storage handle via `setStorage(storage)` when the editor adopts it (initial open, switchFrom, setContentHost). Cache file naming: `<editor.id>-host.txt` (host content), `<editor.id>-<editor-type>.json` (editor-specific state), `<editor.id>-script-panel.json` (script panel), etc. On switch, the new editor copies `oldEditor.id`, so cache files survive the transition. Cache cleanup is PAGE-driven, not editor-driven: editor dispose does NOT delete cache files; the page deletes `<editor.id>-*` at "id release" moments — (1) `setMainEditor` when old's id was not transferred to a successor; (2) `reconcileVisibility` for editors that became invisible without a successor; (3) `PageModel.dispose` for all remaining editors. Page-level cache (`<page.id>-*` for sidebar state) is cleaned in `PageModel.dispose`. |
| S1 | 2026-05-19 | View-switch entry point: page method vs. editor method? | [`walkthroughs/02-main-editor-swap.md#S1`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | `PageModel.switchMainEditor(editorId)` — page-level. Notebook per-note switch is a different owner (walkthrough 29), so reusing the name on `EditorModel` would conflate two semantics. Switch widget already has the page in context; `findCompatibleEditors()` on the editor stays for "what can I become" introspection. |
| S2 | 2026-05-19 | File-navigate target resolution: explicit `target` vs. registry resolver. | [`walkthroughs/02-main-editor-swap.md#S2`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | Priority order in `navigatePageTo`: (1) `options.target` if set — throws if not registered; (2) else `editorRegistry.resolveForFile(filePath, language?, mode?)`. Preserves today's link-pipeline override semantics. |
| S3 | 2026-05-19 | `applyRestoreData` shape for file-navigate vs. session-restore. | [`walkthroughs/02-main-editor-swap.md#S3`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | Single phase-2 entry. `applyRestoreData(data: Partial<T>)` accepts whatever subset the caller has — open-file passes `{filePath, pipe, sourceLink, title}`; session-restore passes the full persisted blob. Contract: "remember setup data; don't do I/O yet." `restore()` reads what's been stashed. Matches A7 from walkthrough 01. |
| S4 | 2026-05-19 | Pending `revealLine` / `highlightText` carry — model fires command before React view is mounted (general Persephone pattern). | [`walkthroughs/02-main-editor-swap.md#S4`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | New foundation primitive **`ComponentQueue`** ([`mockups/ComponentQueue.ts`](mockups/ComponentQueue.ts)). Mailbox semantics, FIFO, no coalescing (sender's responsibility — duplicate sends indicate a sender bug). Single consumer at a time. API v1: `send(event)`, `subscribe(handler)`, `use(handler)` (React hook), `dispose()`. Disposed on `EditorModel.dispose()`. Each editor narrows the event union via TS generic. `EditorModel` base gains `readonly queue: ComponentQueue<E>` and a third generic param. `revealLine`/`highlightText` pass through `applyRestoreData` → Monaco's override `queue.send({type, …})` → view's `use` handler drains on mount. Replaces today's `_pendingRevealLine`/`_pendingHighlightText` + ContentViewModelHost-wait-for-view machinery. Future extension `register`/`execute` for view-context queries deferred to walkthrough 12 or 20. |
| S5 | 2026-05-19 | Auto-preview-editor pick on file-navigate; user wants edit-vs-view mode toggle. | [`walkthroughs/02-main-editor-swap.md#S5`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | Add `mode: "edit" | "view"` to `AcceptanceInput`. Per-editor `accepts()` two-rule contract: returns `-1` only when truly incompatible (mode-independent); returns 0–100 priority that mode scales. `resolveForFile(fileName, language?, mode?)` picks the highest priority; default `"edit"` preserves today's behavior. `findEditorsAccepting(host)` stays mode-agnostic (switch widget shows all valid editors). Add `mode?: "edit" | "view"` field to `ILinkData` (real-code change). Explorer sets `"view"`; other entry points default `"edit"`. Future Explorer toggle UI deferred to walkthrough 30. Replaces today's `getPreviewEditor` + `resolve` pair. |
| S6 | 2026-05-19 | Error-path fallback inside `navigatePageTo`. | [`walkthroughs/02-main-editor-swap.md#S6`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | Single try/catch wrapping the three-phase lifecycle (`createEditor → applyRestoreData → restore`). On failure: `ui.notify` + fallback to Monaco with no `filePath` and basename as title. Today's pre-check `appFs.exists` collapses into the catch (file-not-found throws inside `restore`). Distinct from `restore()`'s INTERNAL empty-host fallback (A7) — this catch handles `createEditor` failing too. |
| S7 | 2026-05-19 | Script API `page.editor = X` rewiring + throw policy on `switchMainEditor`. | [`walkthroughs/02-main-editor-swap.md#S7`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | `page.editor = X` (script API) calls `page.switchMainEditor(X)`. `switchMainEditor` throws on: (1) `newEditorId` not registered; (2) current main lacks `CONTENT_HOST_TRAIT`; (3) `newEditorId` not in `findEditorsAccepting(host)`. Silent no-op on: same `editorId` (S10) or no main editor. **Switch widget** trusts its input (built from `findCompatibleEditors`); **script PageWrapper** catches throws and calls `ui.notify(err.message, "error")`; **direct callers** propagate. PageWrapper try/catch lives in walkthrough 12. |
| S8 | 2026-05-19 | Pattern B (Archive demote-on-navigate) under unified-array. | [`walkthroughs/02-main-editor-swap.md#S8`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | Archive's `beforeNavigateAway(newModel)` override survives unchanged — it sets `this.secondaryEditor = ["archive-tree"]` (keep) or `undefined` (drop) based on `newModel.sourceLink?.sourceId`. The Pattern B mechanics simplify on the page side: setter is pure state (A8 from walkthrough 01); archive has one membership in `editors[]`; visibility criterion keeps it via `contributesPanels()`. `_mainEditorId` flips to new main. Same observable behavior, simpler internals — no dual-membership detection, no `_prePromotePanels` bookkeeping, no `queueMicrotask`. |
| S9 | 2026-05-19 | `confirmRelease` semantics on view-switch. | [`walkthroughs/02-main-editor-swap.md#S9`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | `switchMainEditor` does NOT call `confirmRelease` — host transfers to new editor intact (including `modified` flag). `navigatePageTo` keeps its `confirmRelease` because host is being replaced. **Invariant:** `confirmRelease` is only called when the host is about to die. |
| S10 | 2026-05-19 | Same-editor short-circuit + identity field (`type` vs. `editorId`). | [`walkthroughs/02-main-editor-swap.md#S10`](walkthroughs/02-main-editor-swap.md) | **Resolved 2026-05-19** | (a) `switchMainEditor(currentId)` is a silent no-op when `oldEditor.editorId === newEditorId`. (b) Add `EditorModel.editorId: string` (registry key, stable). (c) Remove `IEditorState.type`, `IEditorState.editor`, `EditorDefinition.editorType`, `EditorType`/`EditorView` union types, `isTextFileModel` type guard, `PAGE_TYPE_MIGRATIONS` table. Today's `type === "textFile"` checks → `traits.has(CONTENT_HOST_TRAIT)` (more precise). Specific-class checks → `instanceof` (consistent with C1). Persistence registry lookup `editorType === state.type` → `id === state.editorId`. Migration scope spans walkthroughs 04 / 09 / 12 / 20 / 24 / 30. |
| N1 | 2026-05-19 | Trigger for visibility-criterion re-evaluation on editor panel-list changes outside `setMainEditor` (the explicit deferred from walkthrough 01 / L7). | [`walkthroughs/03-secondary-editors.md#N1`](walkthroughs/03-secondary-editors.md) | **Resolved 2026-05-19** | Option (a) refined. New foundation primitive: `TOneState.subscribe(listener, selector)` overload ([`mockups/TOneState.ts`](mockups/TOneState.ts)) — pure models can observe a slice of another model's state with the same `compareSelection` precision the `use` hook already provides. PageModel.`attach` subscribes per editor on the `secondaryEditor` slice; PageModel.`detach` unsubscribes. Handler `onEditorPanelsChanged(editor)` bumps `state.version` and enforces the visibility criterion. `EditorModel.secondaryEditor` setter stays a fully pure state mutation (A8's intent preserved end-to-end — no editor → page side effect at all). Rejected alternatives: (b) new `panelsChanged: Subscription<void>` primitive on the base class, (c) reusing `descriptorChanged`, (d) tiny setter-side `page.onEditorPanelsChanged(this)` call. |
| N2 | 2026-05-19 | What does PageNavigator subscribe to after `secondaryEditorsVersion` is gone? | [`walkthroughs/03-secondary-editors.md#N2`](walkthroughs/03-secondary-editors.md) | **Resolved 2026-05-19** | Subscribe to `page.state.use()`. The unified `state.version` is bumped on every `attach`/`detach` AND every `onEditorPanelsChanged` (N1) — one subscription covers exactly the events PageNavigator cares about. No mockup change needed. |
| N3 | 2026-05-19 | Iteration source for the rendered panel list — `editors[]` with inline filter, or a derived `panelEditors` getter? | [`walkthroughs/03-secondary-editors.md#N3`](walkthroughs/03-secondary-editors.md) | **Resolved 2026-05-19** | Use `page.panelEditors` getter (already in PageModel mockup from walkthrough 01). Filter logic centralized in PageModel; call sites read clearly. |
| N4 | 2026-05-19 | Self-close pattern for panels — close button on panel header. | [`walkthroughs/03-secondary-editors.md#N4`](walkthroughs/03-secondary-editors.md) | **Resolved 2026-05-19** | Close button = "close the model" gesture. Reuses the N1 visibility-criterion path: panel calls `model.secondaryEditor = undefined`, slice subscription fires, `onEditorPanelsChanged` detaches + disposes via visibility criterion. No new framework API. Panel React components must hide the close button when `model === page.mainEditor` (the today-bug case). Future panel-specific close (rare): panel component filters the model's `secondaryEditor` array directly — not a framework concern. |
| N5 | 2026-05-19 | Replacement for `_notifyMainEditorOfSecondaryChange` duck-type hook (CategoryEditor's sibling-rescan trigger). | [`walkthroughs/03-secondary-editors.md#N5`](walkthroughs/03-secondary-editors.md) | **Resolved 2026-05-19** | Option (a-view). The reactivity is purely view-side — CategoryEditor's view subscribes to `page.state.use()`; useMemo keys on `page?.state.get().version`; scans `page.panelEditors`. Deleted: `CategoryEditorModel.onSecondaryEditorsChanged()`, `_providerVersion` field, `providerVersion` getter, the defensive 50ms `setTimeout` retry, `PageModel._notifyMainEditorOfSecondaryChange()`. No new framework API (rejected (a-model) editor-side subscription, (b) dedicated `editorsChanged` channel, (c) base-class `onPageEditorsChanged()` hook). |
| N6 | 2026-05-19 | Pattern A vs. Pattern B framing under the unified-array model. | [`walkthroughs/03-secondary-editors.md#N6`](walkthroughs/03-secondary-editors.md) | **Resolved 2026-05-19** | Drop the framing. The unified-array model holds editors uniformly in `editors[]`; the `id === _mainEditorId` and `contributesPanels()` flags describe each editor's current role. The today-docs' "two patterns" are just two states of the same single membership relation. `secondary-editors.md` gets rewritten in the final docs pass to use the unified framing — no mockup change. |
| N7 | 2026-05-19 | `close()` confirmation order — insertion order or panels-first / main-last? | [`walkthroughs/03-secondary-editors.md#N7`](walkthroughs/03-secondary-editors.md) | **Resolved 2026-05-19** | Option (b): iterate panel-contributing editors first (insertion order within), then main editor last. Same shape as "Close Other Tabs" — each modified inner editor gets a Save dialog; cancellation on any one aborts the whole close. The main editor going last means cancellation leaves the page (with its content area) visible; closing the main editor is the conceptual commit point that closes the page tab itself. Avoids the raw-insertion-order surprise where the main file gets saved on "Yes" even though a later inner editor's cancel aborts the close. |
| P1 | 2026-05-19 | Persisted descriptor shape — today's flat `Partial<IEditorState>` conflates editor and host; unified `editors[]` needs all editors (main + panel-contributors) covered, not just main. | [`walkthroughs/04-persistence.md#P1`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b). New types in `src/shared/types.ts`: `EditorDescriptor { editorId, id, state, host? }` + `HostDescriptor { kind, state, pipe? }` + `PageDescriptor { id, pinned, modified, mainEditorId, editors[], sidebar? }` + `WindowState.schemaVersion: 4`. Retires `IEditorState`, `EditorType`, `EditorView`, `PageDescriptor.editor`, `PageDescriptor.hasSidebar`. `EditorDescriptor.state` and `HostDescriptor.state` are opaque to the persistence layer (subclass-defined). `sourceLink` lives inside `HostDescriptor.state` (host-owned). Unified `editors[]` mirrors PageModel's unified array (A8). |
| P2 | 2026-05-19 | Old-format detection — today's structural sniff (`data.pages[0]?.type`) needs a robust replacement that distinguishes pre-v3 / v3.x / v4 without a migration table. | [`walkthroughs/04-persistence.md#P2`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b). Add integer `WindowState.schemaVersion: 4`. Restore reads it as the single discriminator — `!== 4` (including missing) → `console.warn` + start empty. No migration shim (per C2). Per-page restore failures stay non-fatal: `console.warn` + `continue`. No `ui.notify` during restore (user not yet ready). Semver/CalVer string rejected — schema bumps are "shape-incompatible" events, not semver-shaped; integer reads cleanly at the check site. |
| P3 | 2026-05-19 | Fold per-page sidebar cache file (`<pageId>-nav-panel.txt`) into the unified WindowState `PageDescriptor`, or keep it separate? | [`walkthroughs/04-persistence.md#P3`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b). Fold. `<pageId>-nav-panel.txt` eliminated. Sidebar metadata (`open`, `width`, `activePanel`) + panel-contributor descriptors all live inside `PageDescriptor.editors[]` + `sidebar?` in openFiles.txt. Single source of truth per window; atomic save (no partial-restore races between cache + main file). Per-editor cache files (`<editor.id>-*` for large host content / decorations / script state per C9) survive — editor-private. PageModel loses: `_saveState`, `_saveStateDebounced`, `_cacheName`, `_skipSave`, `restoreSidebar`, `pendingSecondaryDescriptors`, `_pendingActivePanel`, `restoreSecondaryEditors`, `flushSave`. Sidebar debounce cadence drops from 300ms (PageModel-level) to 500ms (window-level) — imperceptible. |
| P4 | 2026-05-19 | Pipe descriptor location under host split — today on `IEditorState.pipe`; new arch needs it under host (TextFileModel owns the pipe). | [`walkthroughs/04-persistence.md#P4`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b). Move `pipe?: PipeDescriptor` from `EditorDescriptor` to `HostDescriptor`. `TextFileModel.getDescriptor()` packs `{ kind: "textFile", state, pipe }`. No-host editors (PDF, Image, Browser, MCP Inspector, etc.) don't set `host` and never carry a pipe. Clean ownership; matches A1/A7. |
| P6 | 2026-05-19 | `getRestoreData()` / `applyRestoreData()` contract under host split — today's `Partial<IEditorState>` shape conflates layers; new contract needs to thread editor state, host descriptor, and `id` cleanly. | [`walkthroughs/04-persistence.md#P6`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b). Each `EditorModel` subclass defines its own state type. `applyRestoreData(data: RestoreData<S>)` where `RestoreData<S> = Partial<S> & { host?: HostDescriptor; revealLine?: number; highlightText?: string }`. Text-bearing editors destructure `host` to stash on `_pendingHost`; ComponentQueue passthroughs go to `this.queue.send(...)`. `getRestoreData()` returns `EditorDescriptor` and calls `this._host?.getDescriptor()` for text-bearing. `IContentHost.getDescriptor()` + static `fromDescriptor(desc)` factory added (TextFileModel provides both; `fromDescriptor` is sync construction — async I/O deferred to `host.restore()`). `editorRegistry.createEditor(editorId, instanceId?)` accepts an optional instance id — restore passes `desc.id` so identity is set at construction, no post-construction mutation. Rejected (a) passing full `EditorDescriptor` to `applyRestoreData` (leaks identity fields). |
| P7 | 2026-05-19 | Restore failure granularity — per-editor `console.warn + continue` vs. whole-page-skip vs. hybrid. | [`walkthroughs/04-persistence.md#P7`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b). Per-editor `console.warn + continue` inside each page's `Promise.all`. Salvage whatever editors did load — a missing main editor leaves the page sidebar-only (`_mainEditorId = null`); a missing panel-contributor just disappears. A page drops only when ALL editors fail AND no sidebar remains. Warning shape: `[restore] Failed to restore editor <editorId> (id=<uuid>) in page <pageId>: <err.message>`. Rejected (a) whole-page-skip (loses sibling work) and (c) main-fail-skips hybrid (added complexity; the salvage path handles main-missing cleanly). |
| P8 | 2026-05-19 | Empty-pages (no main editor) persistence shape under unified `editors[]`. | [`walkthroughs/04-persistence.md#P8`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | No special branch needed. `mainEditorId: null` + populated `editors[]` (Explorer / Archive panel-contributors) + `sidebar?` present — the P5 restore flow handles it naturally (the `mainEditorId` step is skipped when null; panel-contributors restore like any editor). Edge case: a page with `sidebar` present but `editors.length === 0` after restore is invalid (no such page exists today); restore drops it with a warn. Empty-pages-with-sidebar stops being a special case. |
| P9 | 2026-05-19 | Orphan cache files left behind by failed restores or crashes-between-dispose-and-flush. | [`walkthroughs/04-persistence.md#P9`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (a): do nothing. Asymmetric risk — a sweep that trusts the in-memory model can delete real files when a restore bug drops an editor from `editors[]` while its descriptor is still in openFiles.txt; turning a recoverable hiccup into actual data loss. Not sweeping leaves a few small stale files on disk — harmless drift. Per-editor cleanup stays event-driven via `EditorModel.dispose → fs.deleteCacheFiles(this.id)` (today's path). Mockup adjustments C8 (sweep) and C9 (new `fs.ts` API) both dropped. If disk bloat ever becomes a real problem, revisit with a sweep that double-checks against openFiles.txt directly rather than the live model. |
| P10 | 2026-05-19 | Schema version field to discriminate format breaks across epics. | [`walkthroughs/04-persistence.md#P10`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b): integer `WindowState.schemaVersion: 4` (set to 4 for this epic). Single discriminator used at restore time (per P2). Documented contract: shape-incompatible changes bump the integer; additive optional fields don't. Already encoded in C1's type definitions. |
| P5 | 2026-05-19 | `_pendingActivePanel` under async restore — today's two-step (sidebar cache stash → restore secondaries → resolve pending) needs reshape under unified `editors[]`. | [`walkthroughs/04-persistence.md#P5`](walkthroughs/04-persistence.md) | **Resolved 2026-05-19** | Option (b). Per-page restore uses `Promise.all` over `editors[]` (each does `createEditor → applyRestoreData → restore`); after all editors attach, apply `mainEditorId` and sidebar metadata. `_pendingActivePanel` retired — by the time active-panel resolution runs, all panel-contributing editors are present (or known-failed). Cross-page restores also parallel. Cross-editor restore-time dependencies (e.g., CategoryEditor needing sibling Explorer) handled by view-side `page.state` subscriptions (N5 pattern) — placeholder window microscopic. |
| M1 | 2026-05-19 | Drag IPC payload shape after walkthrough 04's descriptor rewrite — does `PageDragData` carry a `schemaVersion`? | [`walkthroughs/05-multi-window-transfer.md#M1`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b). `PageDragData.page` picks up the new `PageDescriptor` shape automatically from walkthrough 04 / C1; no `schemaVersion` on the drag payload. Same-process same-version means the schema discriminator only lives at file boundaries (openFiles.txt), not in transient IPC messages. |
| M2 | 2026-05-19 | Unify IPC `movePageIn` with bootstrap restore — today they're two separately-maintained paths producing identical outputs. | [`walkthroughs/05-multi-window-transfer.md#M2`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b). `PagesPersistenceModel.restorePage(desc)` becomes the shared entry point for bootstrap restore, IPC `movePageIn`, and `duplicatePage` (with fresh ids). `newEditorModelFromState` and `PAGE_TYPE_MIGRATIONS` in `PagesLifecycleModel` retire — `editorId`-keyed registry lookup in `restorePage` replaces them. One less hand-maintained restore path. |
| M3 | 2026-05-19 | Source-side flush ordering — does the source flush per-editor caches before detach? | [`walkthroughs/05-multi-window-transfer.md#M3`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b). `await page.saveState()` stays in `movePageOut`. The call is lighter than today (no sidebar cache file to write — only per-editor flushes via walkthrough 04 / C7). Establishes the invariant for editor walkthroughs 20–30: `EditorModel.saveState()` is an awaitable flush that drains the editor's own internal debounce, not a fire-and-forget save. |
| M4 | 2026-05-19 | Detach-without-dispose invariant — per-editor cache file survival across transfer depends on `removePage` not calling `page.dispose()`. | [`walkthroughs/05-multi-window-transfer.md#M4`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b): comment-only. Today's `removePage` already does the right thing (splice without dispose); the dispose path lives on `page.onClose` which `detachPage` clears. Add an explanatory comment so future readers don't "clean up" the missing dispose call by mistake. |
| M5 | 2026-05-19 | Editor-id continuity across multi-window transfer — per-editor cache files key on `editor.id`. | [`walkthroughs/05-multi-window-transfer.md#M5`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b): no new mechanism. `editorRegistry.createEditor(id, instanceId)` (walkthrough 04 / C2) is the single route for cache-file id preservation; both bootstrap restore and IPC transfer pass `desc.id` as `instanceId`. `editorRegistry.ts` mockup comment extended to flag the dual use. |
| M6 | 2026-05-19 | Multi-editor (panel-contributor) transfer atomicity. | [`walkthroughs/05-multi-window-transfer.md#M6`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b): per-editor salvage. Page-with-sidebar transfer is naturally atomic at the descriptor level (one `editors[]` array carries main + panel-contributors); restore parallelism + per-editor `console.warn + continue` from walkthrough 04 / P5 + P7 apply equally to IPC. No new logic. |
| M7 | 2026-05-19 | Source window goes empty after last-page-moved — close window. | [`walkthroughs/05-multi-window-transfer.md#M7`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b): close source on last-page-moved (unchanged from today). User dragged a tab OUT — they want fewer windows. Last-window-hides-instead-of-closes in main process (`open-windows.ts` `setCanQuit`) is unaffected. |
| M8 | 2026-05-19 | Pinned-tab cross-window-drag behavior. | [`walkthroughs/05-multi-window-transfer.md#M8`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b): by design. Pinned tabs remain non-cross-window-draggable (today's behavior in `PageTab.handleDragStart` / `handleDragEnd`). Pinned-ness still rides the descriptor when a page IS transferred by other means (e.g., context-menu "Open in New Window"). |
| M9 | 2026-05-19 | Drag payload size budget — IPC bloat risk. | [`walkthroughs/05-multi-window-transfer.md#M9`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b): payload is metadata-only (~50KB worst case). Host content (`content` string) MUST stay in the per-editor cache file (`<editor.id>-host.txt`) and ride M5's cache-file continuity. Invariant logged for walkthrough 20 (Monaco / Text) when defining `HostDescriptor` `state` shape: no large blobs in the descriptor. |
| M10 | 2026-05-19 | Active-page focus after transfer on target — does the moved page auto-activate? | [`walkthroughs/05-multi-window-transfer.md#M10`](walkthroughs/05-multi-window-transfer.md) | **Resolved 2026-05-19** | Option (b): no explicit `showPage` call. The dropped page becomes active implicitly because `s.ordered.push(page)` appends to `ordered` and `query.activePage` returns its last item. Testing will confirm; if a race ever surfaces, add an explicit `showPage(page.id)` after the splice — but don't pre-emptively add it. |
| CK1 | 2026-05-19 | Where does the compare flag live in the new arch — host or pair? | [`walkthroughs/06-compare-mode.md#CK1`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (b). Pair-level `compareGroups: Set<string>` on `PagesModel.state`, keyed by left page id (parallels `leftRight`/`rightLeft`). Single source of truth; `state.use()` covers reactivity; ungroup/removePage/setMainEditor clean atomically. Retires the host-level `TextFileModel.state.compareMode` field. Resolves the C6 "TBD" marker. |
| CK2 | 2026-05-19 | Is CompareEditor a real EditorModel under EPIC-028? | [`walkthroughs/06-compare-mode.md#CK2`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (a). CompareEditor stays a React component rendered by `Pages#PageContent`. Registering it as an EditorModel would break the one-host / one-tab invariants nailed down by walkthroughs 01–05 (CompareEditor needs TWO hosts and spans TWO tabs), and would pollute `findEditorsAccepting(host)` with an entry that only "accepts" when the page is grouped. |
| CK3 | 2026-05-19 | Where does the "both sides are TextFileModel" compatibility check live? | [`walkthroughs/06-compare-mode.md#CK3`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (b). Centralize as `pagesModel.query.canCompare(leftId, rightId): boolean`. Single `instanceof TextFileModel` predicate consumed by toolbar button visibility, `enterCompareMode` precondition, and PageContent render guard. Replaces today's scattered `isTextFileModel(editor)` checks (per S10/C1 the type guard goes away anyway). |
| CK4 | 2026-05-19 | Activation API shape — single pair-level method or two-call pattern? | [`walkthroughs/06-compare-mode.md#CK4`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (b). `pagesModel.layout.enterCompareMode(pageId)` / `exitCompareMode(pageId)` accept either side and resolve the leftId internally — matches today's call-site ergonomics (the toolbar button is on the right page, doesn't know which side is left). Returns boolean (false if `canCompare` precondition fails). Caller passes whichever id it has. |
| CK5 | 2026-05-19 | What do `PageContent` and `Pages` subscribe to after the host-level flag retires? | [`walkthroughs/06-compare-mode.md#CK5`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (b). Add `pagesModel.query.isInCompareMode(pageId): { active: boolean; leftId?: string; rightId?: string }`. Consumers subscribe to `pagesModel.state.use(s => s.compareGroups)` to trigger updates and call the getter to resolve "am I left or right of this pair?" The lookup recurs in PageContent (render decision) AND in toolbar/exit button handlers — centralizing pays off. |
| CK6 | 2026-05-19 | What happens to `compareModeChanged` Subscription and `pagesModel.rerender()` once the flag moves? | [`walkthroughs/06-compare-mode.md#CK6`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (a). Delete both. They existed purely as a bridge from "host state changed" → "pages-layout React re-render" — once `compareGroups` lives on `PagesModel.state`, `state.use()` does the work natively. Removes `compareModeChanged` from `events.ts`, the `rerender: number` field on `PagesModel.state`, and the `rerender()` method. Don't keep as "general-purpose hammer" — violates "no features for hypothetical future requirements." |
| CK7 | 2026-05-19 | What replaces today's `fixCompareMode` sweep for cleanup invariants? | [`walkthroughs/06-compare-mode.md#CK7`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (a). Delete `fixCompareMode`. Three inline cleanups: `PagesLayoutModel.ungroup(pageId)` deletes from `compareGroups`; `PagesLifecycleModel.removePage(page)` drops the entry for the closed page; `PageModel.setMainEditor(newMain)` exits compare when the new main's host isn't `TextFileModel`. Each cleanup sits at the mutating method — impossible to forget. Today's sweep only handled the "ungrouped" case; the new explicit hooks also cover the "host changed shape" case which today's code silently misses. |
| CK8 | 2026-05-19 | `openDiff` rewrite — compose primitives or add a dedicated atomic helper? | [`walkthroughs/06-compare-mode.md#CK8`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (a). Compose existing primitives: `this.model.layout.groupTabs(firstId, secondId, true)` + `this.model.layout.enterCompareMode(firstId)`. Drops today's direct `state.update(s => s.compareMode = true)` calls and the `fixCompareMode()` call. No new one-caller helper; refactor only if a second caller appears. |
| CK9 | 2026-05-19 | Should compare-mode pairs survive a window restart? | [`walkthroughs/06-compare-mode.md#CK9`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (c). Don't persist. Persephone's main window hides to tray (process stays running) so compare mode survives any visible-state change in-memory; only a genuine secondary-window save-on-close → restart loses the flag — rare edge case, acceptable. Cross-window transfer of a compare pair is impossible by design (both pages of the pair can't be dragged simultaneously), so walkthrough 05's IPC-payload concern doesn't apply either. `WindowState` shape stays exactly as walkthrough 04 / P1 defines it; no `compareGroups?` field added; no `PagesPersistenceModel.ts` change. |
| CK10 | 2026-05-19 | How does CompareEditor's exit button reach the new pair-level API? | [`walkthroughs/06-compare-mode.md#CK10`](walkthroughs/06-compare-mode.md) | **Resolved 2026-05-19** | Option (a). Pass `leftPageId` as a third prop to `<CompareEditor>`. `Pages#PageContent` already resolves the leftId when deciding to render the diff — threading it one prop deeper is trivial. Exit button calls `pagesModel.layout.exitCompareMode(leftPageId)`. Avoids adding a global "find page by editor" helper that nothing else uses. |
| GK1 | 2026-05-19 | Group storage shape — parallel `leftRight` / `rightLeft` maps vs. single map + derived reverse vs. richer per-pair record. | [`walkthroughs/07-grouped-pages.md#GK1`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Keep today's parallel `leftRight` / `rightLeft` maps on `PagesModel.state`. Drift is structurally impossible — every mutation site (`group`, `ungroup`) writes both directions atomically. Maps are tiny (a handful of entries); O(1) lookups both ways. Single-map-plus-derived-reverse (b) trades a working pattern for theoretical savings. Richer per-pair record (c) is YAGNI — same reasoning that picked the simplest `compareGroups: Set<leftId>` for CK1. If a future per-pair field appears (split ratio, layout mode), grow into (c) at that point. |
| GK2 | 2026-05-19 | `requireGroupedText` reads `mainEditor?.type !== "textFile"` — the `type` discriminator is retired by S10. | [`walkthroughs/07-grouped-pages.md#GK2`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19; refined 2026-05-20 by walkthrough 08 / T2** | Option (b). Centralize as `pagesModel.query.getTextFileHost(pageId): TextFileModel \| null`. Wraps the host-instanceof lookup once; returns the typed host (or null) so callers can both predicate-check (truthy) AND access the host without a separate read. Originally `hasTextFileHost(pageId): boolean`; walkthrough 08 / T2 refined the signature to return the typed host so the 14 PageTab callsites could reuse the lookup for method calls (`host.saveFile()`, `host.renameFile()`, etc.). The boolean predicate is subsumed by the truthy check — one helper, two use cases. Mirrors walkthrough 06 / CK3's `canCompare` pattern in spirit, though CK3 stays a separate two-id boolean (no single "thing" to return). `requireGroupedText` collapses to the accessor pattern and drops its trailing `as unknown as TextFileModel` cast. Inline `instanceof TextFileModel` (a) leaves scatter; generic `getHost(pageId)` (c) is over-general per C1/C7 (only one host type exists). |
| GK3 | 2026-05-19 | `fixGrouping` body after CK7 retires the `fixCompareMode` trailing call — keep as single sweep or split. | [`walkthroughs/07-grouped-pages.md#GK3`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Keep `fixGrouping` as a single sweep with two remaining responsibilities (swap-direction fix + dangling-group cleanup). Cheap single walk over `pages[]`; shared input; runs from `removePage` and `moveTabByIndex`. Splitting into named helpers (b) adds names without value. Pushing each pass into its single caller (c) loses robustness when a future caller forgets one pass — single sweep is the today's pattern that works. |
| GK4 | 2026-05-19 | `PageTab.closeClick`'s grouped branch calls `fixCompareMode()` between `ungroup` and `showPage` — redundant once CK7 folds compare cleanup into `ungroup`. | [`walkthroughs/07-grouped-pages.md#GK4`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Drop the `pagesModel.fixCompareMode()` call. Resulting flow: `ungroup → showPage`. Behavior identical because CK7's `ungroup` carries the compare-cleanup obligation. Keeping the explicit call as belt-and-suspenders (b) is the cargo-cult pattern walkthrough 06 / CK6 rejected for `compareModeChanged` and `rerender()` — same reasoning applies. |
| GK5 | 2026-05-19 | Multi-window transfer of a grouped page — does the group transfer atomically or dissolve? | [`walkthroughs/07-grouped-pages.md#GK5`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Group dissolves on the source — `fixGrouping` after `removePage` drops the dangling entry; the target receives an ungrouped tab. Cross-window drag is a single-tab gesture by design (no UX affordance for "drag both pages of a group"; simultaneous dual-tab drag is impossible — same reasoning as walkthrough 06 / CK9 cross-window compare-pair transfer). Atomic pair transfer (b) requires `PageDragData` shape changes and synchronized cleanup for no observable user benefit. |
| GK6 | 2026-05-19 | `ImperativeSplitter.widthK` (per-group split ratio) is not persisted today — every group resets to 50/50 on app restart. | [`walkthroughs/07-grouped-pages.md#GK6`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Keep as-is. Splitter ratio persistence is a UX nicety orthogonal to EPIC-028's editor refactor; explicit scope cut for this walkthrough. Safely deferable as additive `WindowState.splitRatios?: Record<leftId, number>` later per P10's "additive optional fields don't bump schemaVersion" contract. No mockup change; no schema bump. Adding it now (b/c) expands scope for tangential UX. |
| GK7 | 2026-05-19 | Two-pages-per-group invariant — does EPIC-028 leave room for N-page groups? | [`walkthroughs/07-grouped-pages.md#GK7`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Permanent design. No N-page generalization. `leftRight` is one-to-one; `GroupContainer` is binary; `ImperativeSplitter` is binary; the diff editor needs exactly two. N>2 would require new splitter mechanics, new tab-grouping affordances, and arbitrary spatial ordering rules — none of which serve any user request. YAGNI applies; matches walkthrough 06 / CK1's `compareGroups: Set<leftId>` simplest-shape reasoning. |
| GK8 | 2026-05-19 | Pin / group interaction — `pinTab` / `unpinTab` don't ungroup; pinned-with-unpinned-partner pairs span both tab sections. | [`walkthroughs/07-grouped-pages.md#GK8`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Preserve today's behavior. Emergent property of parallel-maps storage independent of the pin section; `AppPageManager.findGroupId` still resolves correctly; the splitter spans the container; rendering works. Forcing ungroup on pin (b) or pin-the-partner-too (c) adds bespoke logic for no user benefit. If a user pins one side of a group, they accept the split visual. |
| GK9 | 2026-05-19 | Script API surface (`pages.groupedPage`, `pages.getGroupedPage`, `pages.ungroup`, `pages.groupTabs`) under EPIC-028. | [`walkthroughs/07-grouped-pages.md#GK9`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Keep all four unchanged. Thin `PageCollectionWrapper` delegates over `PagesModel`; implementation underneath unchanged. Tightening to two methods (b) breaks every existing script using `pages.ungroup(pageId)`; no EPIC-028 motivation to break the surface. |
| GK10 | 2026-05-19 | `query.isGrouped(editorId)` resolution under unified `editors[]` from walkthrough 01 / A8. | [`walkthroughs/07-grouped-pages.md#GK10`](walkthroughs/07-grouped-pages.md) | **Resolved 2026-05-19** | Option (a). Works correctly via `findPage` resolution — already updated by walkthroughs 01 / A8 + 03 from `p.id || p.mainEditor?.id || p.secondaryEditors.some(...)` to `p.id || p.editors.some(e => e.id === id)`. `isGrouped` reads the resolved page id; semantics identical to today (the page-id-or-editor-id transparency is preserved). No new change required in 07. |
| T1 | 2026-05-19 | Tab read-surface under host split — today's flat `useOptionalState(editor.state, …)` reads conflate editor and host fields; after the split, `language`/`filePath`/`encrypted`/`encoding`/`temp` move to the host while `title`/`modified`/`secondaryEditor`/favicon/audio stay on the editor. | [`walkthroughs/08-page-tabs.md#T1`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-19** | Option (a). Two parallel `use()` subscriptions in `PageTab`: `editor.state.use(editorSelector)` for editor-owned fields, `editor.contentHost?.state.use(hostSelector)` for host-owned fields (no-op when host is null). Lightest retrofit — both stores already drive re-renders; today's defensive `useOptionalState` shape carries over with one extra `?.` chain. Rejected (b) aggregator-getters (would re-create the flat `IEditorState` we just split) and (c) dedicated `tabView` slice (one-consumer over-engineering). |
| T2 | 2026-05-20 | `isTextFileModel(editor)` retirement — 14 callsites in `PageTab.tsx` (and 1 in `PageTabs.tsx`) read TextFileModel-only fields (`encrypted`, `decrypted`, `withEncryption`, `filePath`) and call TextFileModel-only methods (`saveFile`, `renameFile`, `showEncryptionDialog`, `encryptWithCurrentPassword`, `makeUnencrypted`). The discriminator + type-narrowing pattern must flip from "editor IS TextFileModel" to "editor's host IS TextFileModel." | [`walkthroughs/08-page-tabs.md#T2`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (b1). Refines GK2: replace `hasTextFileHost(pageId): boolean` with `getTextFileHost(pageId): TextFileModel \| null` — single helper, two use cases (truthy check + method access). PageTab callsites cache once per render (`const host = pagesModel.query.getTextFileHost(page.id);`), then read fields via `host?.encrypted` and call methods via `if (host) host.saveFile()`. `requireGroupedText` (GK2 caller) collapses to the accessor pattern and drops its trailing `as unknown as TextFileModel` cast. CK3's `canCompare(leftId, rightId)` stays a separate two-id predicate (no single "thing" to return). Rejected (b2) split-helper + cast-for-method (awkward two-pattern split at one callsite) and (b3) inline `editor.contentHost instanceof TextFileModel` (inconsistent with GK2's centralized predicate family established in walkthroughs 06–07). |
| T3 | 2026-05-20 | Drag payload composition — `PageTab.getDragData` and `PagesPersistenceModel.saveState` both build a `PageDescriptor` inline today. The new shape (walkthrough 04 / P1: `mainEditorId` + `editors[]` + optional `sidebar`) is more elaborate; two inline copies of the composition will drift. | [`walkthroughs/08-page-tabs.md#T3`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a). Add `PageModel.getDescriptor(): PageDescriptor` (B1 mockup — landed in `mockups/PageModel.ts` 2026-05-20). Single source of truth: composes per-editor `getRestoreData()` calls + sidebar metadata (when `pageNavigatorModel` is present); consumed by `PagesPersistenceModel.saveState` (per-window save), `PageTab.getDragData` (cross-window drag payload), and `PagesLifecycleModel.duplicatePage` (with fresh ids). Tab's `getDragData` collapses to `{ ...windowMetadata, page: page.getDescriptor() }`. Mirrors `EditorModel.getRestoreData()` — each layer describes itself; many consumers read the same shape. Rejected (b) inline composition in two callers (drift risk) and (c) free helper in `PagesPersistenceModel` (puts the producer farther from the data). |
| T4 | 2026-05-20 | `editor.noLanguage` and `editor.getIcon` discoverability — `PageTab.tsx:637` branches on `editor?.noLanguage` to render a custom-icon placeholder vs. the language-picker `IconButton`. Should these stay as explicit EditorModel fields, derive from trait presence, or move to a registry descriptor? | [`walkthroughs/08-page-tabs.md#T4`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a). Keep `noLanguage = false` and `getIcon?: () => React.ReactNode` as explicit EditorModel base fields — already in `mockups/EditorModel.ts:419-421`. Per-editor opt-in matches today's pattern; tab reads them as direct properties on `editor` (no registry lookup, no trait coupling). UI flag is independent of structural fact: bundling "show language picker" with `traits.has(CONTENT_HOST_TRAIT)` would couple a UI concern to a structural trait and break the hypothetical case of a text-bearing editor that wants to hide the language picker. Rejected (b) derive `noLanguage` from trait presence (overloads the trait) and (c) move to registry descriptor (per-render lookup for two fields EditorModel already has). No mockup change required. |
| T5 | 2026-05-20 | Audio fields (`_anyTabAudible`, `pageMuted`, `toggleMuteAll`) are BrowserEditor-specific; today's tab reads them via duck-typed `(editor as any)?.toggleMuteAll` and `useOptionalState` selector defaults. Keep duck-typing or formalize as `editor.audio?: AudioCapability` / `AUDIO_TRAIT`? | [`walkthroughs/08-page-tabs.md#T5`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a). Keep duck-typing. BrowserEditor is the only consumer; YAGNI on a formal interface or trait. The duck-typed read is ugly but isolated to one component reading three fields from one editor type. BrowserEditor's state shape declares the fields on its subclass-defined state interface; the tab's selector with defaults reads them safely. Revisit with (b) typed interface or (c) trait registration if a second audio source ever lands. No mockup change required. |
| T6 | 2026-05-20 | `PageTabs.tsx:206` pinned-tab encryption-width branch reads `editor && isTextFileModel(editor) && (editor.encrypted \|\| editor.decrypted)` to decide between `pinnedTabEncryptedWidth` and `pinnedTabWidth`. Same retirement pattern as T2; same flip to host-based access. | [`walkthroughs/08-page-tabs.md#T6`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a-helper). Inline the T2 GK2 helper: `const host = pagesModel.query.getTextFileHost(p.id); const isEnc = host && (host.encrypted \|\| host.decrypted); pinnedLeft += (isEnc ? pinnedTabEncryptedWidth : pinnedTabWidth) + 2;` Single canonical predicate matches T2; widths and `+2` column gap stay inline (no `pinnedWidth(page)` helper — premature factoring for one callsite). Rejected (a-inline) inline `editor.contentHost instanceof TextFileModel` (superseded by T2's helper resolution) and (b) extract math helper (one callsite). No mockup change required. |
| T7 | 2026-05-20 | `PageTab.closeClick`'s grouped branch calls `pagesModel.fixCompareMode()` between `ungroup` and `showPage` — redundant once walkthrough 06 / CK7 folds compare cleanup into `ungroup` and walkthrough 07 / GK4 drops the call from `closeClick`. | [`walkthroughs/08-page-tabs.md#T7`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a). Confirmation. Drop the `pagesModel.fixCompareMode()` call; flow becomes `ungroup → showPage`. CK7's `ungroup` carries the compare-cleanup obligation. No new decision; listed so the implementer doesn't miss the line. No mockup change required. |
| T8 | 2026-05-20 | `PageTab.handleClick`'s Ctrl+click branch calls `pagesModel.groupTabs(activeId, pageId, true)` — does this still work under unified `editors[]` from walkthrough 01 / A8? | [`walkthroughs/08-page-tabs.md#T8`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a). Confirmation. `groupTabs` accepts either page id or editor id and resolves via `findPage`, which was already migrated to the unified `editors[]` shape by walkthroughs 01 / A8 + 03 (confirmed by walkthrough 07 / GK10). Both inputs to the Ctrl+click branch are page ids; resolution is a direct page-id match. No code change in `PageTab.handleClick`. No mockup change required. |
| T9 | 2026-05-20 | `PageTab.handleDragEnd` fires `api.addDragEvent(getDragData())` on drag-out — does it need to `await page.saveState()` first per walkthrough 05 / M3's "saveState is an awaitable flush" invariant? | [`walkthroughs/08-page-tabs.md#T9`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a). Confirmation. No change in the tab. Walkthrough 05 / M3 explicitly puts `await page.saveState()` inside `PagesLifecycleModel.movePageOut` — the source-side flush runs when the IPC event triggers `movePageOut` on the lifecycle layer. `handleDragEnd` stays a pure dispatch. Rejected (b) await inside the tab (duplicates M3's responsibility, splits the invariant across two layers). No mockup change required. |
| T10 | 2026-05-20 | Same-window reorder uses `setTraitDragData(TraitTypeId.PageTab, { key: page.id })` + `getTraitDragData` + `pagesModel.moveTab(data.key, id)`. Page-id payload; trait system is in-process. Does this still resolve under unified `editors[]`? | [`walkthroughs/08-page-tabs.md#T10`](walkthroughs/08-page-tabs.md) | **Resolved 2026-05-20** | Option (a). Confirmation. No change. `pagesModel.moveTab` resolves via `findPage`, already migrated to unified `editors[]` by walkthroughs 01 / A8 + 03 (confirmed by 07 / GK10). Trait system primitives in `/src/renderer/core/traits/dnd.ts` are unchanged by the editor refactor. Rejected (b) page-id-or-editor-id payload (no current consumer; flexibility for no gain). No mockup change required. |
| MI1 | 2026-05-20 | `get_pages` / `get_active_page` flat-row shape exposes `type`, `editor`, `language`, `filePath` directly from `editor.state.get()` today. After S10 + P4 + GK2 / T2 split, `type` has no source, `editor` maps to `editorId`, `language` / `filePath` move under the host. MCP is an external API — wire-compat matters more than for the script API. | [`walkthroughs/13-mcp-integration.md#MI1`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Option (a). Drop `type` from response rows; map `editor` to `editor.editorId`, `language` to `editor.contentHost?.state.get().language` (P4), `filePath` and `content` through `pagesModel.query.getTextFileHost(pageId)` (GK2 / T2). External MCP consumers see the same surface as script-API consumers post-SF5 — zero documented external consumers of the `type` field. `notepad://guides/pages` documentation gets a minor refresh to drop the `type` reference; `editor` field semantics unchanged (was already the registry id). Rejected (b) preserve `type` by deriving (two-shape field, awkward) and (c) full row redesign (breaking; ripples through MCP clients for no win). No mockup change required. |
| MI2 | 2026-05-20 | `setPageContent` / `getPageContent` today gate on `isTextFileModel(editor)` (the function-form predicate from `TextEditorModel.ts`) then read `editor.state.get().content` (read) or call `editor.changeContent(content)` (write). Both points retire under S10 — predicate gone, content lives on the host. | [`walkthroughs/13-mcp-integration.md#MI2`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Option (b). Reuse `pagesModel.query.getTextFileHost(pageId): TextFileModel \| null` helper from GK2 / T2. `getPageContent` truthy-checks the helper for read; `setPageContent` truthy-checks the helper for write and calls `host.changeContent(content)`. Same pattern as PageTab's 14 callsites (T2) and `requireGroupedText` (GK2). Error message in `setPageContent` unchanged (still routes external clients at `execute_script` with `asX` facades for structured editors). Rejected (a) inline `editor.contentHost instanceof TextFileModel` at two sites (inconsistent with the centralized helper family established in walkthroughs 06–08). No mockup change required. |
| MI3 | 2026-05-20 | `create_page` rejects `editorDef.category === "standalone"` editors today (browser-view / pdf-view / image-view / mcp-view / about-view / settings-view) with per-editor hints. The registry mockup explicitly removes the `category` field and adds `hasContentHost: boolean` — does the new gate cover the same set, and does the `log-view` edge case need handling? | [`walkthroughs/13-mcp-integration.md#MI3`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Option (a). Flip `editorDef.category === "standalone"` to `!editorDef.hasContentHost`. New gate matches today's "standalone" set verbatim across the six editors; picks up `log-view` as a new addition (today's `category: "content-view"` was an artifact of the acquireViewModelSync flow that retires under SF2). Add a `log-view` entry to the standalone hints table pointing external clients at `ui_push` and `requireWellKnownPage("mcp-ui-log")`. Behavior change for `create_page({ editor: "log-view" })` documented but non-regressing — zero today-callers in codebase, MCP guides, or external client docs. Rejected (b) special-case log-view as create_page-able (no concrete use case) and (c) add a registry `createPageable: boolean` field (three fields where one does the work). No mockup change required. |
| MI4 | 2026-05-20 | `editor.acquireViewModelSync("log-view") as LogViewModel \| undefined` lives at three sites in `mcp-handler.ts` (`getOrCreateMcpLogViewModel`, `logIncomingRequest`, `showMcpRequestLog`). The whole `acquireViewModelSync` machinery retires under SF2 — LogView becomes its own `EditorModel` subclass directly (no TextFileModel host underneath for log pages). How do the three callsites flip? | [`walkthroughs/13-mcp-integration.md#MI4`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Option (a). Three callsites flip to direct `editor instanceof LogViewEditorModel` checks. The `isTextFileModel(editor)` guard that wrapped today's acquire call goes away at all three sites — under EPIC-028 the log-view page's `mainEditor` IS the LogView editor itself, never a TextFileModel. No helper; cost/benefit doesn't justify centralization for one class at three sites in one file (GK2 / T2's helper paid off across two files and 15 callsites). LogView's final class name (`LogViewEditorModel` placeholder) finalized in walkthrough 23. SF2 already retires the `acquireViewModelSync` machinery; MI4 applies the consequence at three new sites. Rejected (b) `pagesModel.query.getLogViewEditor(pageId)` helper (premature centralization — three callsites in one file is under the threshold). No mockup change required. |
| MI5 | 2026-05-20 | `createPage` return shape today reads `s?.editor` and `s?.language` from `page.mainEditor.state.get()`. After EPIC-028 host split, where do these fields source from? Does `addEditorPage(editor, language, title, content)` signature change? | [`walkthroughs/13-mcp-integration.md#MI5`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Option (a). Mechanical field-mapping mirrors MI1: `editor: page.mainEditor?.editorId` (S10), `language: page.mainEditor?.contentHost?.state.get().language` (P4). `addEditorPage(editor, language, title, content)` signature unchanged per EW2; internals rewrite to the `createEditor + applyRestoreData + restore` three-phase lifecycle. No mockup change required. |
| MI6 | 2026-05-20 | `ui_push` calls `vm.addEntry(type, fields)` and `vm.addDialogEntry(type, fields)` on the acquired `LogViewModel`. Does this API surface preserve when LogView migrates to its own `EditorModel` subclass under EPIC-028? | [`walkthroughs/13-mcp-integration.md#MI6`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Confirmation. `addEntry(type, fields)` and `addDialogEntry(type, fields)` are LogView-internal data mutators; class rename from `LogViewModel` to `LogViewEditorModel` doesn't change call shape. `Promise<LogEntry>` contract on `addDialogEntry` preserved. Walkthrough 23 finalizes the per-class API surface; MCP layer is a thin caller. No mockup change required. |
| MI7 | 2026-05-20 | `ui_push` dialog promises accumulated via `vm.addDialogEntry(...)` stay alive in `pendingDialogs` until the user clicks a button. Does the page-close path need a new dispose contract under EPIC-028 to reject those promises so MCP's `await Promise.all(dialogPromises)` doesn't hang? | [`walkthroughs/13-mcp-integration.md#MI7`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Option (a). No new contract — today's `LogViewModel.onDispose()` at `LogViewModel.ts:72-80` already iterates `pendingDialogs` and resolves each with a sentinel canceled-button entry, then clears the map. The cleanup logic is internal and survives the class rename to `LogViewEditorModel` under EPIC-028 (modulo `onDispose` → `dispose` method-name reshape if walkthrough 23 picks one). MCP's `await Promise.all(dialogPromises)` resolves cleanly when the log page closes because each dialog's `resolve` already fires from the dispose handler. Rejected (b) forward-pointer to walkthrough 23 with a new contract paragraph (would document behavior already implemented). No mockup change required. |
| MI8 | 2026-05-20 | `executeScript` passes `pageModel?.mainEditor` as a context anchor for the script's `page` object. Under EPIC-028, `mainEditor` is now `EditorModel \| null` (null for empty-with-sidebar pages per EW9) and PageWrapper construction drops the `releaseList` parameter per SF3. Does the MCP call site need to change? | [`walkthroughs/13-mcp-integration.md#MI8`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Confirmation. `scriptRunner.runWithCapture(script, pageModel?.mainEditor ?? undefined, language)` carries over verbatim — the `?? undefined` already handles the null case. PageWrapper construction is scripting-internal per SF3; MCP doesn't see the change. No mockup change required. |
| MI9 | 2026-05-20 | `openUrl` delegates to `pagesModel.openUrlInBrowserTab(url, options)`. Browser-view editor migrates to direct-model EditorModel under walkthrough 30. Does the MCP layer need any change? | [`walkthroughs/13-mcp-integration.md#MI9`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Confirmation. `pagesModel.openUrlInBrowserTab(url, options)` page-level method unchanged at the MCP call site. Browser editor's internal migration is walkthrough 30's responsibility; MCP layer is a thin caller. No mockup change required. |
| MI10 | 2026-05-20 | `browser_*` automation commands (Playwright-compatible) are delegated by-prefix to `automation/commands.ts`. The automation layer's `state.type === "browserPage"` gate flips to `editor instanceof BrowserEditorModel` per SF8. Does the MCP dispatcher itself need any change? | [`walkthroughs/13-mcp-integration.md#MI10`](walkthroughs/13-mcp-integration.md) | **Resolved 2026-05-20** | Confirmation. MCP dispatcher's `browser_*` delegation in `mcp-handler.ts` unchanged — by-prefix delegation with dynamic import survives the editor refactor. The automation layer's gate flip per SF8 is walkthrough 30's responsibility (Browser editor rewrite). No MCP-layer or mockup change required. |
| MO1 | 2026-05-20 | `hasSelection` state placement — today on `TextViewModel.state` (ContentViewModel parallel to TextFileModel); TextViewModel dissolves under SF2. Three candidates: (a) one extra field on `MonacoEditor.state`; (b) separate child `TOneState` side-store on the editor; (c) derive on demand via `queue.execute` (rejected — async, can't render). | [`walkthroughs/20-monaco.md#MO1`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (a). Add `hasSelection: boolean` as a field on `MonacoEditorState extends EditorStateBase`. Single reactive surface; chrome's Run-all visibility gate reads via `state.use(s => s.hasSelection)`; Monaco view's selection listener writes via `model.state.update(s => s.hasSelection = v)`. `hasTextSelection()` override on MonacoEditor synchronously returns `state.get().hasSelection`. Rejected (b) separate child `TOneState` (adds a second reactive surface for one field — over-engineering) and (c) derive on demand via `queue.execute` (async, can't drive render). No mockup change required. |
| MO2 | 2026-05-20 | TextViewModel dissolution — where do `setupWheelZoom` / `setupRichPaste` / `setupSelectionListener` live after ContentViewModel + ContentViewModelHost retire under SF2? Today they're instance methods with `addSubscription` + `onDispose` lifecycle. | [`walkthroughs/20-monaco.md#MO2`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (a). Inline `useEffect`s in `MonacoBody.tsx`; each setup function returns a cleanup callback (Monaco's `IDisposable.dispose()` for editor.addAction / onDidChangeCursorSelection; manual removeEventListener for the wheel listener). Idiomatic React; lifecycle owned by the component mount. Rejected (b) view-private class via `useMemo` (re-creates today's ViewModel pattern without any model-side consumers benefiting) and (c) MonacoEditor model methods (wrong scope — Monaco instance dies on every remount, can't live on the model). No mockup change required. |
| MO3 | 2026-05-20 | `monaco.editor.ITextModel` lifecycle — today auto-created by `@monaco-editor/react`'s `<Editor value={...}>`; dies on view unmount, undo history lost on switch. Walkthrough 20 verifies the binding shape under host split. | [`walkthroughs/20-monaco.md#MO3`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (a). No change from today's binding shape — `<Editor value={host.content} onChange={(v) => host.changeContent(v ?? "", true)}>`. The host's reactive `content` flows into Monaco's auto-managed ITextModel; user edits flow back through the `host.changeContent(value, byUser=true)` callback. ITextModel dies on switch-out (e.g., Monaco → Grid); fresh one allocated from `host.content` on switch-back. C3 already locked the undo-loss tradeoff — cross-editor undo is a future epic. No mockup change required. |
| MO4 | 2026-05-20 | Pending operations (`_pendingRevealLine` / `_pendingHighlightText`) under refactor — today fields on `TextFileModel`; consumed by `acquireViewModel` when Monaco mounts. S4 / B1 mockup already covers via `ComponentQueue`. Walkthrough 20 confirms `MonacoQueueEvent` union shape. | [`walkthroughs/20-monaco.md#MO4`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (a). Confirmation — design pinned by S4 / B1. `MonacoQueueEvent = { type: "revealLine"; line } \| { type: "highlightText"; text } \| { type: "focus" }`. `MonacoEditor.applyRestoreData` translates `data.revealLine` / `data.highlightText` into `this.queue.send(...)` per the usage shape at `mockups/ComponentQueue.ts:269-307`. View's `queue.use(handler)` drains on mount; closes over `monacoRef.current` for the imperative Monaco call. Today's `_pendingRevealLine` / `_pendingHighlightText` fields delete from TextFileModel under the host split. No mockup change required. |
| MO5 | 2026-05-20 | Persistence shape — should `hasSelection` (a runtime fact written by view's selection listener) live in the persisted `MonacoEditorState`? Two options: (a) keep with default false, explicitly skip in applyRestoreData; (b) move to derived non-persistent reactive primitive. | [`walkthroughs/20-monaco.md#MO5`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (a). Keep `hasSelection` on `MonacoEditorState` for the simplest reactive subscription (chrome reads via `state.use`); strip during persistence by NOT touching it in `applyRestoreData` (default value at fresh-state construction is `false`). The field roundtrips as `false` on save → restore — acceptable noise (file weight negligible). Mirrors C7's spirit — `temp` / `restored` stay on host as runtime flags even though they're persisted; same shape. No mockup change required. |
| MO6 | 2026-05-20 | `runScript` selection access — today `TextFileActionsModel.runScript` reads selection via synchronous `this.model.getSelectedText()` (TextFileModel sync read through ContentViewModelHost.tryGet("monaco")). The sync-read mechanism dies under SF2. Three candidates: (a) async runScript on host with editor back-reference; (b) caller passes selection string; (c) runScript moves to MonacoEditor. | [`walkthroughs/20-monaco.md#MO6`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (b). Chrome's Run button reads `editor.state.get().hasSelection`, calls `await editor.getSelectedText()` if true, delegates to `host.actions.runScript(scriptText, language)`. F5 keystroke path uses thin `MonacoEditor.runScript(all?)` async wrapper that materializes selection (or full content if `all`) then delegates to `host.actions.runScript(text, language)`. `TextFileActionsModel.runScript` signature becomes `(scriptText: string, language: string) => Promise<void>` — pure data-in, no editor coupling. Host stays selection-unaware; editor stays Monaco-aware; chrome bridges them. The `MonacoEditor.runScript` wrapper is the single Monaco-aware piece — naturally lives on the editor since selection is a view-context query. Rejected (a) gives the host a back-reference to the editor (re-introduces the conflation the epic removes) and (c) couples Run-script to Monaco specifically (Grid/Notebook would need their own Run-script which makes no sense). No mockup change required. |
| MO7 | 2026-05-20 | Monaco focus signal under refactor — today two paths: (1) TextEditorView's 200ms root-focus on `pagesModel.onFocus`; (2) TextViewModel's 0ms `focusEditor()` on the same channel. Walkthrough 10 / TC8 owns the root-focus subscription in chrome; Monaco-specific focus needs a new home. (a) bind `onDidFocusEditorText` Monaco-side only; (b) add `focus` to `MonacoQueueEvent` and have chrome send it after root-focus. | [`walkthroughs/20-monaco.md#MO7`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (b). `MonacoQueueEvent` adds `{ type: "focus" }`; chrome's 200ms `setTimeout(() => root.focus())` is followed by `editor.focus()` (a typed wrapper around `queue.send({type:"focus"})`). To keep chrome editor-agnostic, base `EditorModel` gains a `focus(): void` no-op method; text-bearing editors override to `this.queue.send({type:"focus"})`. View's `queue.use` handler calls `monacoRef.current?.focus()` on the focus event. Reproduces today's two-tier focus precisely (root first, Monaco follows). Non-text-bearing editors (PDF, Image, Browser, …) inherit the no-op default and handle focus internally via their own React widgets. **Mockup edit landed**: `focus(): void { /* override */ }` no-op added to `mockups/EditorModel.ts` between `hasTextSelection?()` and `saveState()`. |
| MO8 | 2026-05-20 | ScriptPanel ownership under TC6 — script panel is host-owned (stays on `TextFileModel.script`). Does any of `ScriptPanel.tsx` reference editor-side state that breaks under the host split? | [`walkthroughs/20-monaco.md#MO8`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Confirmation. Verbatim port — every consumer in `ScriptPanel.tsx` (`this.pageModel.runRelatedScript()`, `this.pageModel.state.get().language`, `pagesModel.openFile(selectedScript)`, `page.createExplorer(scriptPanelDir)`, `page.ensurePageNavigatorModel()`) is host-level or PagesModel-level — nothing reaches into editor state. Rename `model.runRelatedScript()` → `model.actions.runRelatedScript()` (host-internal refactor — `TextFileActionsModel` was already a submodel on TextFileModel). The `ScriptPanel.tsx` file ports as a single React component with no rewrites beyond the prop-type narrowing (`model: TextFileModel` is now the host class, not the dual editor-and-host class). No mockup change required. |
| MO9 | 2026-05-20 | Content-based editor detection retirement — today's `_detectTimer` + `scheduleDetection` + `cancelDetection` + `detectContentEditor` + `state.detectedContentEditor` machinery on `TextFileModel`. Mockup `TextFileModel` and `editorRegistry` notes pin retirement; walkthrough 20 confirms Monaco's `accepts()` doesn't need to detect anything. | [`walkthroughs/20-monaco.md#MO9`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Option (a). Confirmation — all five today-fields/methods delete from `TextFileModel` (`_detectTimer`, `detectContentEditor`, `scheduleDetection`, `cancelDetection`, `state.detectedContentEditor`). Monaco's `accepts()` returns floor priority for any text input (universal fallback); strong-content detection lives in target editors' own `accepts()` per the registry's content-peek contract at `mockups/editorRegistry.ts:62-80` (e.g., Notebook's `content.startsWith('{"type":"notebook"')`). Switch widget calls `editorRegistry.findEditorsAccepting(host)` on demand — chrome render time, context menu open — not on a 2500ms background timer. Each call passes `{ host, fileName, language }`; editors that need to peek at content read `host.state.get().content` inside their predicate. No mockup change required. |
| MO10 | 2026-05-20 | `applyRenamedPath` impact on editor `id` / cache files — today `TextFileModel.applyRenamedPath` updates `filePath` / `title` and recreates pipes; the cache file `<id>-host.txt` survives because `id` doesn't change. Verify under refactor. | [`walkthroughs/20-monaco.md#MO10`](walkthroughs/20-monaco.md) | **Resolved 2026-05-20** | Confirmation. Verbatim port — `MonacoEditor.id` stays unchanged across file renames; `TextFileModel.state.filePath` / `state.title` change inside the host's `io.applyRenamedPath` body. Pipe + cache pipe recreate under the new path; cache file `<editor.id>-host.txt` survives because the editor's id is the cache prefix (C9) and doesn't change. No editor-side code change; the rename is fully a host-internal mutation. No mockup change required. |
| GR1 | 2026-05-20 | Variant handling — Grid is registered three times today (`grid-json` / `grid-csv` / `grid-jsonl`) sharing one VM that discriminates via `host.state.get().editor`. After S10 the host has no `editor` field. One class with three registrations vs three subclasses vs single-id-with-format-on-state. | [`walkthroughs/21-grid.md#GR1`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). One `GridEditor` class with constructor-bound `readonly editorId` + `readonly format`. Three factories each pass a different editorId; format derived via `formatFromEditorId(id)`. Variant is class-baked at construction, never changes. `editorId` declared `abstract readonly` per `mockups/EditorModel.ts:53` — assigning in constructor is permitted by TypeScript's readonly-in-constructor rule. Rejected (b) three subclasses (adds three class definitions for one constant difference) and (c) single-id-with-format-on-state (breaks switch widget UX where users see three distinct Grid options; forces unmotivated script-API migration). No mockup change required. |
| GR2 | 2026-05-20 | Three call sites in today's GridViewModel read `this.host.state.get().editor` to discriminate the variant (`detectCsvDelimiter`, `parseContent`, `getContentToSave`). After GR1's host-state-editor retirement, the discriminator readouts must relocate. | [`walkthroughs/21-grid.md#GR2`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). Read `this.format` (semantic word `"json"|"csv"|"jsonl"`); decouples parsing/serialization logic from registry id prefix string. Tiny `formatFromEditorId(id: GridIds): GridFormat` helper in `src/renderer/editors/grid/util.ts` runs once at construction. Rejected (b) read `this.editorId` directly (saves a helper at the cost of locking parsing/serialization sites to registry id prefix forever). No mockup change required. |
| GR3 | 2026-05-20 | GridViewModel dissolution shape — today's `onInit` body does 5 things (debounce-save subscribe, pageFocused subscribe, csv-options self-watch, initial content load + CSV detection, async restoreState). After SF2 the VM dissolves. Where does each piece go? | [`walkthroughs/21-grid.md#GR3`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). Same shape as MO2 for Monaco — each piece picks its natural home. `GridEditor.restore()` handles variant-aware bootstrap (CSV delimiter detection per GR7 + initial reparse). `adoptHost` subscribes `_hostContentUnsub` (host content → reparseRows) + `_csvOptionsUnsub` (csvDelimiter / csvWithColumns slice → reparseRows). `GridBody.tsx` useEffects handle `pagesModel.onFocus.subscribe` (scroll restore) + mount-time focus. `saveStateDebounced` retires — `descriptorChanged.send()` + window-level debounce per P3 handles persistence. Rejected (b) view-private controller class (re-creates GridViewModel pattern for symmetry's sake) and (c) editor-side `pageFocused` method (gridRef is view-local, can't live on the model). No mockup change required. |
| GR4 | 2026-05-20 | Grid state persistence — today GridViewModel writes `<host.id>-grid-page.json` for columns / focus / search / filters / sortColumn / csv options. Under walkthrough 04 / P6 each editor's state slice rides EditorDescriptor.state. Per-editor cache file vs fold into descriptor. | [`walkthroughs/21-grid.md#GR4`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). Fold into `EditorDescriptor.state`. `<id>-grid-page.json` cache file eliminated. State payload <2KB worst case (columns 5–30 entries × 5 fields ≈ 3KB; filters / focus / search / sort / csv options tiny); within M9's ~50KB per-page metadata budget. View-derived `rows` / `error` stripped from getRestoreData per MO5's pattern. Unifies persistence story: editor state → descriptor; host content → cache file. Rejected (b) keep cache file (contradicts walkthrough 04's unification work) and (c) hybrid (split-state complexity for no benefit). No mockup change required. |
| GR5 | 2026-05-20 | gridRef.state.sortColumn lives on AVGridModel (view-local), not on vm.state. Today's saveState reads it sync; after SF2 the gridRef dies with GridViewModel. How does AVGridModel's sort state sync with editor state? | [`walkthroughs/21-grid.md#GR5`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). Two-way sync in setGridRef callback. On mount: view writes `editor.state.sortColumn` → gridRef. Subscribes gridRef's sortColumn slice → editor.state. Both directions handled in one ref callback, one place. Avoids making `getRestoreData` async. Rejected (b) `queue.execute({type:"getSortColumn"})` (requires `getRestoreData` to become async — non-trivial cascade through `PagesPersistenceModel.saveState` + per-page-descriptor save sites) and (c) editor-side `_gridRef` field (leaks view-side refs into the editor, breaking the SF2 boundary). Focus.selection.rowEnd/colEnd is already on editor.state today — no extra sync needed for focus. No mockup change required. |
| GR6 | 2026-05-20 | Should search / filters / focus carry across switchFrom (Monaco → Grid), or only across restart? | [`walkthroughs/21-grid.md#GR6`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). Per-editor-state-slice covers it. switchFrom only carries the host between editors — each editor's own state slice is independent by construction. When the user switches Monaco → Grid, the new GridEditor instance is freshly created with `defaultGridEditorState` (search: "", filters: [], focus: undefined). No special "clear across switchFrom" logic needed — the absence of carry-over IS the clear. Restart-side persistence works because `applyRestoreData` writes saved fields into the new instance's state. Matches today's observable behavior. Zero new logic — fall-out of the per-editor-state-slice design. No mockup change required. |
| GR7 | 2026-05-20 | CSV delimiter detection — today runs once in GridViewModel.onInit; only for grid-csv variant. After EPIC-028 the VM dissolves. Where does the detection logic land? | [`walkthroughs/21-grid.md#GR7`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). Inside `GridEditor.restore()` after host content loads. Variant-aware bootstrap (only `this.format === "csv"` enters the branch). Only-if-default guard (`!csvDelimiter \|\| csvDelimiter === ","`) prevents overwriting user-chosen CsvOptions setting from a prior session. Runs once per restore — switch-in from another editor (switchFrom) also runs detection because switchFrom is followed by `restore()` per A7. Rejected (b) view-side detection (couples model-state mutation to view lifecycle, double-firing on remount) and (c) generic content-change subscription (would re-detect on every cell edit, clobbering saved choices). No mockup change required. |
| GR8 | 2026-05-20 | `error` field placement — today's GridViewState.error is set by parseContent on failure; view short-circuits to `<EditorError>`. After EPIC-028 it's editor.state vs throw-with-boundary vs view-local React state. | [`walkthroughs/21-grid.md#GR8`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). Stays on `editor.state` for reactive view read via `state.use((s) => s.error)`; stripped from getRestoreData (same pattern as MO5's hasSelection). Editor's `parseContent` method sets it; view reads and renders `<EditorError>` when set, otherwise renders grid. Today's pattern is correct — the error is editor-derived (editor knows it tried to parse and failed); the view just reads and renders. View-derived fields ride state for reactivity but don't make it to disk. Rejected (b) throw with error boundary (parse errors are normal expected cases, not exceptional control flow) and (c) view-local React state (duplicates model-side data into view-side state, requiring editor → view callback for every parse attempt). No mockup change required. |
| GR9 | 2026-05-20 | gridRef survival across switchFrom — today's VM disposes on switch; AVGridModel disposes with view. Under EPIC-028 the editor stays alive on switch-IN; need to confirm gridRef lifecycle works cleanly across switchFrom / dispose paths. | [`walkthroughs/21-grid.md#GR9`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Confirmation. GR5's two-way sync covers the gridRef lifecycle automatically. Switch-in: `GridEditor.restore()` completes, `GridBody` mounts, `setGridRef` fires, sortColumn flows from `editor.state` → gridRef. Switch-out: `GridBody` unmounts, useEffect cleanups run, gridRef nulls, AVGridModel garbage-collects naturally (no `dispose` requirement). Cold restore / switch-in / switch-out all converge on the same setGridRef-mount path. No additional logic. No mockup change required. |
| GR10 | 2026-05-20 | Queue event union — does Grid need anything besides `focus`? Today's restoreState calls `gridRef.models.focus.focusCell(...)` via setTimeout post-mount; script API may also need post-mount cell focus. | [`walkthroughs/21-grid.md#GR10`](walkthroughs/21-grid.md) | **Resolved 2026-05-20** | Option (a). `GridQueueEvent = { type: "focus" } \| { type: "focusCell"; row: number; col: number }`. `focus` for chrome's MO7 root-focus signal; `focusCell` for restore-time + script-API post-mount focus calls (queue buffers until view mounts). `GridQueueRequest = never` — all Grid script API reads are sync against editor.state (no async query path needed, unlike Monaco's `getSelectedText` per MO6). Rejected (b) focus-only with focusCell inlined into setGridRef (special-case for the mount path; breaks on the post-mount script-API path like `await page.asGrid().focusCell(5, 2)`). Matches Monaco's `revealLine`/`highlightText` pattern: model-side fire-and-forget commands into the view. No mockup change required. |
| PV1 | 2026-05-20 | Preview group structure: shared `PreviewEditorBase` vs. four separate classes for `MarkdownEditor` / `SvgEditor` / `HtmlEditor` / `MermaidEditor`? ~80 LOC of mechanical skeleton repeats per class (trait closure / switchFrom / restore / adoptHost / dispose / findCompatibleEditors / getNavigatorTarget / focus). | [`walkthroughs/22-preview-group.md#PV1`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). Four separate classes; no shared base. YAGNI: re-evaluate after walkthroughs 23–29 land when every text-bearing editor is migrated and the actual common surface is provable. Monaco and Grid already broke ground without a base class; premature `PreviewEditorBase` extraction risks customization-point-soup ergonomics (template-method overrides for every concrete difference) or leaves the boilerplate in the subclass anyway. Rejected (b) shared `PreviewEditorBase` (premature abstraction; "common" parts not common enough without distorting the few real differences) and (c) generalized `TextBearingEditorBase` (more speculative; same reasoning amplified). No mockup change required. |
| PV2 | 2026-05-20 | Markdown editor state: which fields ride `getRestoreData` (persist) vs. ride state for reactivity only (stripped per MO5)? Today's VM holds compactMode + searchVisible/searchText/currentMatchIndex/totalMatches + container DOM ref. | [`walkthroughs/22-preview-group.md#PV2`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). Persist `compactMode` only. Search visibility / text / matchIndex / totalMatches ride state for reactivity, stripped from getRestoreData per MO5/GR8 pattern. `compactMode` is the only field with a clear "user-set, sticky" character; search state is conceptually open-find / type / close-find — a transient gesture; persisting it surprises users with a stale query on next open. View-derived (`currentMatchIndex` / `totalMatches`) recomputed by MarkdownBlock's match-counting on every render. Rejected (b) sticky search text (feature not requested; YAGNI) and (c) drop user toggle (`editorConfig.compact` is the embedded-in-notebook context override; user-toggleable `compactMode` is a separate additive override — both still need to exist). No mockup change required. |
| PV3 | 2026-05-20 | Markdown search state location after VM dissolves: editor.state (model-side), view-local React state, or hybrid? | [`walkthroughs/22-preview-group.md#PV3`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). On `MarkdownEditor.state`. Mirrors today's VM-on-model shape (least change); preserves future script-API path (`page.asMarkdown().openSearch("query")` admissible cheaply later); same MO5/GR8 pattern for view-derived fields (ride state for reactivity, strip from persistence). Rejected (b) view-local React state (closes future script-API door for no benefit) and (c) hybrid (adds split-state model where one part is reactive-via-state and the other reactive-via-React; harder to reason about). No mockup change required. |
| PV4 | 2026-05-20 | Markdown scroll position restoration: persist across restart, transient view-local with pagesModel.onFocus restore, or drop entirely? | [`walkthroughs/22-preview-group.md#PV4`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). View-local `scrollTopRef = useRef(0)` inside MarkdownBody + `pagesModel.onFocus` subscription in useEffect. Not persisted across restart — rendered markdown layout can shift between sessions (fonts / window size); feature is for tab-switching, not app-restart. View-local useRef + useEffect-bound subscription is the minimal equivalent; falls out of the SF2 dissolution cleanly. Rejected (b) persist across restart (adds persisted state for a feature that doesn't survive the persistence boundary well) and (c) drop entirely (degrades existing UX without reason). No mockup change required. |
| PV5 | 2026-05-20 | Mermaid render pipeline relocation after ContentViewModel retires: editor-side (model), view-side useEffect, or hybrid (inputs on model, output on view)? | [`walkthroughs/22-preview-group.md#PV5`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). Editor-side. `MermaidEditor._renderTimer` private field + `renderDebounced` private method; `svgUrl / error / loading` on editor.state. Async output IS model-side (`renderMermaid` returns a data URL — conceptually editor-derived state, not view-presentation state); debounce timer lifecycle ties to editor lifecycle (`clearTimeout` in `dispose`); view subscribes via `state.use`. View-side would invite unmounted-ref bugs when the editor switches but view persists. Rejected (b) view-side useEffect (pushes async I/O into the view; breaks editor/view separation) and (c) hybrid (svgUrl read by script API via `page.asMermaid().svgUrl` — must live somewhere reachable from model; view-local React state isn't). No mockup change required. |
| PV6 | 2026-05-20 | Mermaid `lightMode` persistence: persist user override, re-initialize from theme every restore, or promote to global setting? | [`walkthroughs/22-preview-group.md#PV6`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (b). PERSIST per-editor. Default initial value from `isCurrentThemeDark()` on first construct (preserves today's first-open behavior); `applyRestoreData` overrides with saved value if present. Per-diagram user preference is meaningful (printable light-mode diagram on dark-themed app for sharing). Rejected (a) re-init from theme every restore (surprises users who deliberately set light mode on a specific diagram) and (c) global setting (over-globalizes; no evidence per-page is wrong; can be added later if a user asks). Adds `lightMode: boolean` to `MermaidEditorState`'s persisted slice. No mockup change required. |
| PV7 | 2026-05-20 | SVG / HTML editor state shape — extend `EditorStateBase` with named typedef, use base directly in generic param, or add forward-looking fields (zoomLevel, viewMode)? | [`walkthroughs/22-preview-group.md#PV7`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). `type SvgEditorState = EditorStateBase` and `type HtmlEditorState = EditorStateBase` typedefs. Tiny stylistic-symmetry win; future-additive fields land on the named type without rippling through the class generic. Rejected (b) use base directly (saves three LOC at the cost of making future field additions a class-generic change) and (c) add forward-looking fields (YAGNI — zoom-persistence not desired today). No mockup change required. |
| PV8 | 2026-05-20 | Queue event unions for the four preview editors: is `{ type: "focus" }` enough, or do any need additional events (Markdown's scrollToMatch, Mermaid's render-trigger, etc.)? | [`walkthroughs/22-preview-group.md#PV8`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). All four queue events: `{ type: "focus" }` only; queue request `never`. `focus` IS needed because `<TextChrome>`'s TC8 root-focus subscription calls `editor.focus()` — if preview editors no-op it, keyboard navigation breaks (page-focus → no inner focus → tab key doesn't reach the iframe / scroll panel). The base class's no-op MO7 default exists for editors WITHOUT a view-side focus surface (PDF, Image, Browser handle it internally); previews DO have one. `scrollToMatch` is view-internal — match index lives on editor.state per PV3; view's `useEffect(() => blockRef.current?.scrollToMatch(...), [currentMatchIndex])` naturally dispatches when state changes. Rejected (b) add scrollToMatch proactively (speculative; existing useEffect-on-state-change is the right pattern) and (c) skip queue entirely (breaks keyboard navigation). No mockup change required. |
| PV9 | 2026-05-20 | MarkdownEditorFacade DOM peek (`container.innerHTML` for `html` getter): editor.state, private non-state field, or `queue.execute` request/reply? | [`walkthroughs/22-preview-group.md#PV9`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Option (a). Private `_containerRef` field on MarkdownEditor (NOT on state). View `setContainer(el)` callback writes; facade reads `editor.containerInnerHtml` getter. Facade stays sync. DOM refs aren't observable (no consumer subscribes to "container changed" — it's a write-only-from-view, read-from-facade peek); the MO5-derived "ride state for reactivity, strip from persistence" pattern doesn't apply because there's no reactivity. Pure DOM read is sync by nature — wrapping in `queue.execute` (async) introduces artificial latency. `viewMounted` getter reads `this._containerRef !== null` from the same field. Rejected (b) keep on state (DOM ref on state is wrong shape; no subscribers; bypasses immutability assumption) and (c) queue.execute (over-engineered for a sync DOM peek). First example in EPIC-028 of an editor holding a DOM ref outside `state`; sets the pattern for any future "facade needs sync DOM peek" need. No mockup change required. |
| PV10 | 2026-05-20 | Toolbar contributions + facade collapse for preview group: confirmation that walkthroughs 09 / 10 / SF1 / SF2 / SF6 cover the migration, or new design decisions needed per editor? | [`walkthroughs/22-preview-group.md#PV10`](walkthroughs/22-preview-group.md) | **Resolved 2026-05-20** | Confirmation. Walkthroughs 09 / 10 / SF1 / SF2 / SF6 cover the migration; per-editor real-code surface lands in implementation. Toolbar contributions as inline `<PageToolbar>` children — Markdown: compact toggle; Svg: open-draw + copy; Html: none; Mermaid: theme + open-draw + copy. All four facades flip from VM-wrap to EditorModel-wrap; stay sync (no `queue.execute` requests). `force?: boolean` parameter added per SF1. Markdown's DOM peek migrates per PV9 (`_containerRef` private field). No new design decisions. No mockup change required. |
| LV1 | 2026-05-20 | Log View class topology under EPIC-028: direct `LogViewEditor` IS the page's `mainEditor` (with TextFileModel as content host, same shape as Monaco/Grid/Markdown), no-host shape, or hybrid internal-only host? MI4 already locked "LogView IS the mainEditor, never a TextFileModel"; LV1 picks among three readings. | [`walkthroughs/23-log-view.md#LV1`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). `LogViewEditor` IS the mainEditor with TextFileModel as content host + CONTENT_HOST_TRAIT exposed. Mirrors Monaco / Grid / Markdown / Mermaid (uniformity with Tier 5 — LogView is the fifth editor in this shape); preserves switch-to-Monaco (view raw JSONL); reuses TextFileModel's file/pipe/save-restore machinery (~200 LOC saved over no-host). Rejected (b) own-the-file-directly (duplicates host machinery; breaks switch-to-Monaco) and (c) internal-only host (adds opaque branch; CONTENT_HOST_TRAIT is natural exposure point for `findCompatibleEditors`). No mockup change required. |
| LV2 | 2026-05-20 | LogView state slice partitioning: which fields persist via `getRestoreData`, which ride state for reactivity stripped per MO5/GR8, which become private non-state fields? | [`walkthroughs/23-log-view.md#LV2`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Persist `showTimestamps` + `itemsState` only (LV3). Ride state for reactivity (stripped per MO5/GR8): `entries`, `entryCount`, `error` (all recomputable from host content). `forceScrollVersion` retires per LV5. Six private fields (pendingDialogs / nextId / skipNextContentUpdate / lastLineCount / heightCache / dirtyIndices) stay private. Rejected (b) persist `nextId` (redundant with recomputation; can drift if JSONL edited externally between sessions) and (c) global `showTimestamps` (over-globalization; per-page user preference is meaningful). No mockup change required. |
| LV3 | 2026-05-20 | `itemsState` (per-entry aux state — grid columns, focus, scroll positions) persistence shape: fold into `EditorDescriptor.state` (mirrors Grid GR4) vs keep dedicated cache file `<editor.id>-log-view-items.json` (today's `host.stateStorage` path). | [`walkthroughs/23-log-view.md#LV3`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Fold into `EditorDescriptor.state.itemsState`. Mirrors GR4 (Grid's identical decision); size envelope acceptable (<1KB typical, ~50KB worst plausible within M9 budget); unifies persistence story (editor state → descriptor, host content → cache file); IPC drag transfer naturally atomic. Dedicated `<host.id>-log-view-items.json` cache file orphans gracefully per P9 (no sweep). Second instance of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → LogView LV3). Rejected (b) separate cache file (duplicates today-pattern GR4 eliminated) and (c) hybrid size-cap (premature optimization). No mockup change required. |
| LV4 | 2026-05-20 | JSONL parse/serialize lifecycle hooks after `ContentViewModel.onInit` / `onContentChanged` / `onDispose` retire under SF2: where do `loadContent` / `loadContentIncremental` / `appendToContent` / `updateEntryInContent` / `flushDirtyDebounced` land? | [`walkthroughs/23-log-view.md#LV4`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Three-site split: `restore()` calls `loadContent(host.content)` for initial parse; `adoptHost` content subscription calls `loadContentIncremental(content)` (skipNextContentUpdate-guarded per LV6); `dispose()` cancels pendingDialogs + clears dirtyIndices. Preserves today's incremental fast-path (avoids re-parsing entire JSONL on every appendEntry — detects "only new tail lines"). Rejected (b) drop incremental (perf regression for large logs 1000+ entries) and (c) lazy parse (adds complexity for no observed benefit). No mockup change required. |
| LV5 | 2026-05-20 | `forceScrollVersion` (today bumped by `addDialogEntry` to force scroll-to-bottom regardless of position) retirement: replace with queue event `{type:"scrollToBottom"}` (mirrors MO4 revealLine / GR10 focusCell) or keep state counter? | [`walkthroughs/23-log-view.md#LV5`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Replace with `LogQueueEvent.scrollToBottom` queue event. View's `queue.use` drains, calls `scheduleScrollToBottom`. `forceScrollVersion` state field deletes. Third instance of "state counter bump → queue event" pattern (MO4 revealLine, GR10 focusCell, LV5 scrollToBottom); removes a "state field that exists only to bump" smell. Rejected (b) keep state counter (works but inconsistent with MO4/GR10 cross-editor pattern) and (c) direct ref callback (couples editor to view-side ref lifecycle). No mockup change required. |
| LV6 | 2026-05-20 | `skipNextContentUpdate` self-write guard under host content subscription: keep editor-private flag (today's pattern), pass `bySelf` flag through `host.changeContent`, or TOneState change-reason tracking? | [`walkthroughs/23-log-view.md#LV6`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Keep `skipNextContentUpdate` editor-private flag (today's pattern). Race is editor-internal; no other consumer needs to know about self-write. Most editor-specific mechanism in EPIC-028 so far — every other editor either reads host content as ground truth or doesn't write to it; LogView's bidirectional dance is unique to append-only editors. Rejected (b) `bySelf` flag on `host.changeContent` (leaks editor concern into host API for one consumer) and (c) change-reason tracking (over-engineered). No mockup change required. |
| LV7 | 2026-05-20 | Pending dialogs cancellation on dispose — verbatim port from today's `onDispose:73-80` resolving each with sentinel canceled-button entry; behavior across page-close vs switch-out? MI7 already locked for MCP layer; LV7 re-surfaces at per-editor scope. | [`walkthroughs/23-log-view.md#LV7`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Verbatim port. Cancel ALL pendingDialogs on dispose (both page-close AND switch-out cancel uniformly). Switch-out is a deliberate user gesture; the contract "switching destroys the editor state" is uniform across Tier 5. Today's "canceled button = empty string" semantic preserved. Only model-side Promise lifecycle in Tier 5; sets the pattern for future Promise-based interactive editors. Rejected (b) preserve across switch-out (breaks Tier 5 uniformity) and (c) reject Promise on dispose (breaks today's `await ui.dialog.confirm(...)` script API — every caller would need try/catch). No mockup change required. |
| LV8 | 2026-05-20 | LogQueueEvent union shape — minimal (`focus` + `scrollToBottom`) or add proactive events for future script API (scrollToEntry / highlightEntry)? Queue request union (all UiFacade reads are sync today). | [`walkthroughs/23-log-view.md#LV8`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Minimal: `LogQueueEvent = {type: "focus"} \| {type: "scrollToBottom"}`; queue request = `never`. Speculative `scrollToEntry` / `highlightEntry` events have zero current consumers — YAGNI per PV7 / PV8. Add when a real script-API need lands (e.g., "scroll the log to where the error was logged"). Rejected (b) and (c) — premature scaffolding. No mockup change required. |
| LV9 | 2026-05-20 | UiFacade ownership over `LogViewEditor` under SF2 + retirement of `editorRegistry.loadViewModelFactory("log-view")` pre-load step (ScriptRunner.ts:108) + retirement of `acquireViewModelSync` declaration on IContentHost. Four callsites: 3 in mcp-handler.ts + 1 in ScriptContext.ts:249. | [`walkthroughs/23-log-view.md#LV9`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Full retirement. UiFacade constructor accepts `LogViewEditor` directly. `loadViewModelFactory("log-view")` line deletes from `ScriptRunner.ts:108`. `acquireViewModelSync` declaration deletes from `IContentHost.ts:56`; implementations delete from `TextEditorModel.ts:74` + `NoteItemEditModel.ts:331`. `executeUiOnPage` becomes async per EW2; `releaseList.push` retires per SF2. Six wrapper helpers (StyledTextBuilder, Progress, Grid, Text, Markdown, Mermaid) flip constructor arg type — body preserved. **Final SF2 retirement step** — `acquireViewModelSync` machinery fully retires across the codebase as of this walkthrough. Rejected (b) keep async pre-load (dead overhead) and (c) move construction site (refactor for no benefit). No mockup change required. |
| LV10 | 2026-05-20 | `accepts()` predicate shape under registry mockup (collapses today's `acceptFile` + `validForLanguage` + `switchOption` + `isEditorContent` quartet). Filename-strong + content-peek fallback, filename-only, or content-peek-strong? | [`walkthroughs/23-log-view.md#LV10`](walkthroughs/23-log-view.md) | **Resolved 2026-05-20** | Option (a). Filename `.log.jsonl` priority 70 (strong match) + content-peek `"type"\s*:\s*"log\."` against `host.state.content` priority 60 fallback (for `.jsonl` files generated by scripts containing log entries — drives switch-widget visibility per today's `isEditorContent` path). Mode-agnostic. Mirrors today's behavior. Rejected (b) filename-only (drops switch-widget visibility for content-matching `.jsonl` files) and (c) content-peek-strong (file-name-is-authoritative is more intuitive). No mockup change required. |
| LK1 | 2026-05-20 | Class topology — direct `LinkEditor` (with TextFileModel host + CONTENT_HOST_TRAIT) or content-view on top of TextFileModel? Today: TextFileModel IS mainEditor with `LinkViewModel` as a `ContentViewModel` acquired via `useContentViewModel`. | [`walkthroughs/24-link.md#LK1`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). LinkEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT exposed. **Sixth Tier 5 editor** in uniform shape (after Monaco / Grid / Markdown / Mermaid / LogView). Switch-to-Monaco works for raw `.link.json` text edit. Rejected (b) own-the-file-directly (duplicates host machinery; breaks switch-to-Monaco) and (c) internal-only (adds opaque branch for no benefit). No mockup change required. |
| LK2 | 2026-05-20 | State slice partitioning — 16 today fields + 7 private + 2 callback fields under EPIC-028 — which persist via descriptor, which ride state for reactivity, which become private? | [`walkthroughs/24-link.md#LK2`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Three layers — persist 5 (`leftPanelWidth` + `expandedPanel` + `selectedCategory`/`Tag`/`Hostname`); ride-state stripped 11 (`data` + `categories` + `categoriesSize` + `tags` + `tagsSize` + `hostnames` + `hostnamesSize` + `filteredLinks` + `searchText` + `selectedLinkId` + `error`); private 8 (`skipNextContentUpdate` + `lastSerializedData` + `lastFilterState` + `_treeProvider` + `_gridModel` + `_containerElement` + `onLinkOpen?` + `onGetLinkMenuItems?`). `leftPanelWidth` previously forgotten on restart (silent today-bug) gets incidentally fixed by the consolidation. Rejected (b) persist `searchText` + `selectedLinkId` (transient UI state) and (c) persist nothing (regresses today's good behavior). No mockup change required. |
| LK3 | 2026-05-20 | Selection-state cache file (`<host.id>-link-editor.txt` — `expandedPanel` + `selectedCategory` + `selectedTag` + `selectedHostname`) — fold into descriptor.state or keep separate? | [`walkthroughs/24-link.md#LK3`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Fold into `EditorDescriptor.state` per LK2's 5-persisted slice. **Third instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3). Eliminates dedicated cache file; today's `restoreSelectionState` + `selectionRestored` one-shot flag both retire; window-level 500ms debounce per P3 replaces today's 300ms. ~120 bytes typical, 250 bytes worst — well under M9's 50KB budget. Rejected (b) separate cache file (duplicates today-pattern that GR4 + LV3 eliminated) and (c) hybrid split (premature splitting). No mockup change required. |
| LK4 | 2026-05-20 | JSON parse/serialize lifecycle hooks under EPIC-028 / SF2 — today's `onInit` + `onContentChanged` + `onDispose` shape. | [`walkthroughs/24-link.md#LK4`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Three-site split mirrors LV4 — `restore()` initial parse via `loadData(host.content)` + state-subscription setup; `adoptHost` content subscription with `skipNextContentUpdate` guard; `dispose()` flushes pending save. `selectionRestored` one-shot flag retires per LK3 (selection arrives via `applyRestoreData`, not separate async cache read). The state→save subscription happens once in `restore()` (not `adoptHost`, which fires on switch-in too). Rejected (b) tie initial-load to adoptHost (couples concerns; fragile on switchFrom) and (c) lazy parse (adds complexity). No mockup change required. |
| LK5 | 2026-05-20 | `skipNextContentUpdate` self-write guard preserved or replaced under host content subscription? | [`walkthroughs/24-link.md#LK5`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Keep `skipNextContentUpdate` editor-private flag — verbatim port. **Second instance** of self-write-guard pattern (after Log View LV6). Pattern now standardized: any mutate-then-serialize editor that writes back via `host.changeContent` carries this flag. Rejected (b) host-side `bySelf` parameter (leaks editor concern) and (c) change-reason tracking (over-engineered for 2 consumers). No mockup change required. |
| LK6 | 2026-05-20 | Sidebar panel registration — today's view-side `useEffect` writes `model.secondaryEditor = LINK_PANELS` based on `isNavigatorOpen` with a fragile cleanup-function early-return. Who owns this write under EPIC-028? | [`walkthroughs/24-link.md#LK6`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Model-side method `setSidebarPanels(open: boolean)` called by view's useEffect. View reads `isNavigatorOpen` from `page.pageNavigatorModel` (UI concern); model writes its own `secondaryEditor` (model concern) + gates on `mainEditor === model` for demote-safe no-op. View becomes pure dispatcher; demote-survives lives in `beforeNavigateAway` (LK7), not in a cleanup conditional. Rejected (b) model-subscribes-to-page-state (cross-layer coupling) and (c) stay-view-side (doesn't address the smell). No mockup change required. |
| LK7 | 2026-05-20 | `beforeNavigateAway` hook usage — first text-bearing-editor exercise (after Archive's S8 sourceLink-based pattern). For LinkEditor, survival criterion: "the user had the Links sidebar open when they navigated away." | [`walkthroughs/24-link.md#LK7`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Override with `contributesPanels()` discriminator — keep panels if any were registered (sidebar was open at navigate-away time); drop cleanly if sidebar was closed. Replaces today's view-side cleanup early-return ("if still in secondaryEditors[], don't clear panels"). Pattern is **current-state discriminator** — second valid shape alongside Archive's sourceLink-based discriminator. Rejected (b) always-survive (clutters sidebar) and (c) sourceLink-discrimination (Link lacks parent-identity to gate on). **B1 mockup doc-comment refinement NOT landed** — Archive's S8 pattern stays canonical in mockup; LinkEditor override demonstrates second valid shape by example. If a third discriminator shape ever lands in a text-bearing override, revisit the doc comment. |
| LK8 | 2026-05-20 | `onMainEditorChanged` hook usage — first text-bearing-editor exercise. LinkEditor's opportunity is to switch render mode (full panel set in main mode → category+tags-only in standalone-secondary mode), replacing today's `LinkCategorySecondaryEditor.updatePanels` useEffect. | [`walkthroughs/24-link.md#LK8`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Override `onMainEditorChanged(newMain)` — adjust panel list to standalone-secondary shape: drop "link-hostnames" + conditionally drop "link-tags" based on `state.tags.length > 0`. Plus a `state.tags` slice subscription set up in `restore()` to handle the tag-count-crosses-zero case dynamically. View-side `updatePanels` useEffect AND its `vm.state.subscribe` retire entirely. Rejected (b) keep all three panels always (UX regression — Hostnames visible when not main is deliberate today) and (c) move panel-adjust into `beforeNavigateAway` (wrong moment — that fires BEFORE the new main is set). No mockup change required (`onMainEditorChanged` already at `mockups/EditorModel.ts:299-303`). |
| LK9 | 2026-05-20 | TreeProvider integration — today `LinkCategorySecondaryEditor.tsx:37-52` writes `m.treeProvider = vm.treeProvider`, `m.selectionState = …`, `m.selectByHref = …` onto the model via duck-typing (`m: any`); consumed by sibling CategoryEditor's `findTreeProviderHost()`. Under EPIC-028 — typed accessors, trait registration, or keep duck-typing? | [`walkthroughs/24-link.md#LK9`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Typed accessors/methods on LinkEditor class directly — `get treeProvider(): LinkTreeProvider \| null` (lazy via `_treeProvider`), `selectByHref(href: string): void` public method, `selectionState: TOneState<NavigationState>` public field. CategoryEditor's `findTreeProviderHost` flips to `editor instanceof LinkEditor ? editor.treeProvider : (editor instanceof ArchiveEditor ? editor.treeProvider : (editor instanceof ExplorerEditor ? editor.treeProvider : null))` chain (chain grows by one entry per editor with exposed accessor). **3 `(m as any)` casts retire across writer + reader.** Rejected (b) `TREE_PROVIDER_TRAIT` (over-formalization for 1-3 consumers; revisit if a fourth source lands) and (c) keep duck-typing (fails typed-API goal; the workaround was for ContentViewModel/host indirection that retires under SF2). No mockup change required. |
| LK10 | 2026-05-20 | Registry surface — `accepts()` predicate (today's four predicates collapse to one) + queue event union (focus only, or also scrollToLink for future script API?). | [`walkthroughs/24-link.md#LK10`](walkthroughs/24-link.md) | **Resolved 2026-05-20** | Option (a). Filename `.link.json` priority 70 + content-peek (`"type":"link-editor"` AND `"links"`) priority 60 fallback. Same calibration as LV10 / GR10 — Tier 5 priority space coherent. Queue events `{ type: "focus" }` only; queue request = `never`. Minimal queue matches today's UI affordances; no proactive scroll-to-link scaffolding (PV7 / PV8 / LV8 same YAGNI rejection). Rejected (b) filename-only (drops switch-widget visibility for content-matching JSON) and (c) add `scrollToLink` (premature scaffolding). No mockup change required. |
| TD1 | 2026-05-20 | Class topology — direct `TodoEditor` (with TextFileModel host) vs. content-view atop TextFileModel vs. internal-only hybrid. | [`walkthroughs/25-todo.md#TD1`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). `TodoEditor` IS the page mainEditor + HAS a `TextFileModel` content host with CONTENT_HOST_TRAIT exposed. **Seventh Tier 5 editor** in the uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo). Same reasoning as LV1 / LK1 — uniformity with Tier 5, switch-to-Monaco meaningful for `.todo.json` (users may need to hand-edit a corrupted entry), host machinery reuse. Rejected (b) own-the-file-directly (duplicates host machinery; breaks switch-to-Monaco) and (c) internal-only (adds opaque branch for no benefit). No mockup change required. |
| TD2 | 2026-05-20 | State slice partitioning: which of today's 8 state fields persist, which ride state for reactivity, which become private. | [`walkthroughs/25-todo.md#TD2`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). Three layers: **Persist** (3 fields): `leftPanelWidth`, `selectedList`, `selectedTag`. **Ride state, strip from descriptor** (4 fields, MO5/GR8/LV2/LK2 pattern): `data` (derived from `host.content` via `loadData`), `error`, `listCounts` (derived from `data.items`), `filteredItems` (derived from `data + selectedList + selectedTag + searchText`). **Transient UI state** (1 field): `searchText`. **Stay private** (4 fields): `_skipNextContentUpdate`, `_lastSerializedData`, `_lastFilterState`, `_gridModel`. `selectionRestored` retires per TD3. `leftPanelWidth` silent today-bug incidentally fixed (matches LK2 / `leftPanelWidth` — **second instance of this incidental fix**). Rejected (b) persist searchText (transient UI state — stale search on reopen is worse UX) and (c) persist nothing (regresses today's good behavior — users notice when their last selected list is forgotten). No mockup change required. |
| TD3 | 2026-05-20 | Selection-state cache retirement — fold `<host.id>:todo-editor` (selectedList, selectedTag) into descriptor or keep separate cache file. | [`walkthroughs/25-todo.md#TD3`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). Fold into `EditorDescriptor.state` per TD2(a). **Fourth instance of "per-editor cache file → descriptor.state" pattern** (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3). Pattern is now firmly standardized across half of Tier 5. Today's `restoreSelectionState` + `saveSelectionState` + `saveSelectionStateDebounced` + `selectionRestored` one-shot guard + `static cacheName = "todo-editor"` all retire. Three reasons identical to GR4/LV3/LK3: pattern consistency across Tier 5; unifies persistence (one less cache file + one less restore-time async-await path); IPC drag transfer naturally atomic (descriptor carries everything). Rejected (b) keep cache file (duplicates the today-pattern GR4/LV3/LK3 eliminated) and (c) hybrid split (premature splitting). No mockup change required. |
| TD4 | 2026-05-20 | JSON parse/serialize lifecycle hooks under EPIC-028 — three-site split vs. tie-to-adoptHost vs. lazy parse. | [`walkthroughs/25-todo.md#TD4`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). Three-site split mirrors LV4 / LK4: `restore()` initial parse via `loadData(host.content)` + state-subscription setup (`state.subscribe(() => onDataChangedDebounced())`); `adoptHost` content subscription drives re-parse via `loadData(content)` guarded by `skipNextContentUpdate`; `dispose()` flushes pending debounced save + unsubs forwarders + nulls refs + host dispose. **Third Tier 5 editor in this lifecycle shape** (LV4 → LK4 → TD4). The state→save subscription happens once in `restore()` (not in `adoptHost`, which fires on switch-in too — we don't want to re-subscribe). Rejected (b) tie-to-adoptHost (couples concerns; fragile when switchFrom adopts an already-parsed host) and (c) lazy parse (adds complexity). No mockup change required. |
| TD5 | 2026-05-20 | `skipNextContentUpdate` flag under host subscription — keep editor-private flag vs. host-side `bySelf` parameter vs. TOneState change-reason tracking. | [`walkthroughs/25-todo.md#TD5`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). Keep `skipNextContentUpdate` editor-private flag (verbatim port). **Third instance of self-write-guard pattern** in EPIC-028 (Log View LV6 → Link LK5 → Todo TD5). The pattern is now firmly standardized: any append-or-mutate-then-serialize editor that writes back to its host via `host.changeContent` carries this flag. Same reasoning as LV6/LK5 — flag is editor-private; race is editor-internal; today's pattern works. Rejected (b) host-side `bySelf` parameter (leaks editor concern into host API; the consumer count keeps growing but the trade-off doesn't shift) and (c) change-reason tracking (over-engineered for 3 consumers). No mockup change required. |
| TD6 | 2026-05-20 | Sidebar / panel topology: confirm Todo is NOT sidebar-owning — `TodoListPanel` stays inline vs. promote to secondary editor vs. settings-flag opt-in. | [`walkthroughs/25-todo.md#TD6`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). Todo is NOT sidebar-owning. `TodoListPanel` stays inline inside `TodoBody`; no `setSidebarPanels` method, no `beforeNavigateAway` override, no `onMainEditorChanged` override, no TreeProvider duck-typing. **The LK7 / LK8 / LK9 recipe explicitly does NOT apply to Todo.** Confirmed by reading `register-editors.ts:388-422` (no `secondaryEditor` field) + `secondary-editor-registry.ts` (zero `todo` matches) + `TodoEditor.tsx` (TodoListPanel composed inline via `<TodoListPanel pageModel={vm} />`). **Calibration finding:** walkthrough 24's closure pointer to walkthrough 25 as "second sidebar-owning editor" was incorrect; the LK recipe stays at one example (Link) until a true second sidebar-owner lands (likely walkthrough 30 — Archive + Explorer in the no-host group, or walkthrough 26 — Rest Client pending pre-check). Override count: 9 hooks (vs. Link's 11). The two-hook delta documents the "pay-only-when-used" property of `beforeNavigateAway` + `onMainEditorChanged` concretely. Rejected (b) promote to sidebar editor (extends scope without UX justification — today's UI shows no hint of sidebar promotion) and (c) settings-flag opt-in (premature scaffolding). No mockup change required. |
| TD7 | 2026-05-20 | Per-item content height persistence — stay in JSON `data.state[id].contentHeight` vs. move to descriptor vs. move to per-editor cache file. | [`walkthroughs/25-todo.md#TD7`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). `data.state[id].contentHeight` stays in JSON file. Right scope: heights are item properties (a function of item's content — longer comment → taller row), not window properties. Same-file-two-windows want the same measured heights (measuring fresh in each window wastes the first render). **Mirrors Link's per-collection state pattern** (Link folds `categoryViewMode`/`tagViewMode`/`hostnameViewMode`/`pinnedLinks` into `data.state` per-collection; per-window UI state goes to descriptor). Today's pattern works; no bug pressure; the silent "height saved only when piggybacking on another save" is acceptable noise — heights are rough hints to RenderFlexGrid, not load-bearing precision. Rejected (b) move to descriptor (loses cross-window sharing) and (c) per-editor cache file (extra cache file with no advantage). No mockup change required. |
| TD8 | 2026-05-20 | Confirmation dialogs from model mutators — preserve `ui.confirm` / `ui.notify` direct calls vs. ComponentQueue pipe vs. move-to-view. | [`walkthroughs/25-todo.md#TD8`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). Preserve `ui.confirm` / `ui.notify` direct calls from model mutators. Three reasons: `ui` is app-level by design (renderer-process singleton accessible from anywhere; model code already calls it elsewhere — Log View confirm-on-script-cancel, archive overwrite-file); EPIC-028's split is about FILE/EDITOR state ownership, not UI primitive access. Script API symmetry preserved — today's `deleteItem(id, skipConfirm = false)` lets `TodoEditorFacade.deleteItem(id)` pass `true` to bypass dialogs; moving confirmation to view would break this. No leak — `ui.confirm` doesn't pull view-layer concepts into the model; the model decides WHEN to ask, the framework decides HOW to ask. Rejected (b) ComponentQueue pipe (different shape — mailbox vs. RPC) and (c) move-to-view (splits implementation, breaks script API symmetry). No mockup change required. |
| TD9 | 2026-05-20 | Drag-and-drop reordering via `TraitTypeId.TodoItem` — preserve verbatim vs. promote to EditorModel trait vs. inline in view. | [`walkthroughs/25-todo.md#TD9`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). `TraitTypeId.TodoItem` drag system preserved verbatim. Two reasons: **`TraitTypeId` is the drag trait SYSTEM's job, not EditorModel traits** — drag traits (`src/renderer/core/traits/`) describe DATA shapes that can be dragged; editor traits (`model.traits.set(KEY, impl)`) describe CAPABILITY shapes editors expose. The two trait systems are deliberately separate. **Single consumer** — no other editor today consumes `TraitTypeId.TodoItem`; future cross-editor "drag a todo onto a markdown file" handlers are speculative. Today's pattern works; refactoring it adds nothing. Drag-and-drop trait orthogonality now documented as design contract — future editors with reorderable lists (Notebook, possibly Graph) follow the same pattern: drag trait for data shape, editor trait for capability exposure. Rejected (b) promote to `REORDERABLE_LIST_TRAIT` (different trait system; conflation adds confusion) and (c) inline in view (loses cross-editor drop targeting). No mockup change required. |
| TD10 | 2026-05-20 | Registry surface — `accepts()` predicate priorities + queue event union. | [`walkthroughs/25-todo.md#TD10`](walkthroughs/25-todo.md) | **Resolved 2026-05-20** | Option (a). Filename `.todo.json` priority 70 + content-peek (`"type":"todo-editor"` AND `"items"`) priority 60 fallback. Same calibration as LV10 / GR10 / LK10 — Tier 5 priority space coherent across seven editors. Queue events `{ type: "focus" }` only; queue request = `never`. Minimal queue matches today's UI affordances; no scroll-to-item scaffolding (PV7 / PV8 / LV8 / LK10 same YAGNI rejection). Symmetric with Tier 5 siblings — Markdown / Svg / Html / Mermaid / LogView / Link all minimal-queue; Todo follows the pattern. Rejected (b) filename-only (drops switch-widget visibility for content-matching JSON) and (c) add `scrollToItem` (premature scaffolding). No mockup change required. |
| RC1 | 2026-05-20 | Class topology for `RestClientEditor` — IS-mainEditor + TextFileModel host (CONTENT_HOST_TRAIT) vs. own-the-file-directly vs. internal-only host. | [`walkthroughs/26-rest-client.md#RC1`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). `RestClientEditor` IS mainEditor + TextFileModel content host with CONTENT_HOST_TRAIT exposed — **eighth Tier 5 editor** in the uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient). Switch-to-Monaco meaningful for `.rest.json` (users may need to hand-edit corrupted entries or paste curl-exported JSON wholesale). Rejected (b) own-the-file-directly (duplicates host machinery; breaks switch-to-Monaco) and (c) internal-only host (adds opaque branch for no benefit). No mockup change required. |
| RC2 | 2026-05-20 | State slice partitioning: which of today's 8 state fields persist, which ride state for reactivity, which become private? | [`walkthroughs/26-rest-client.md#RC2`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). 2 persisted (`leftPanelWidth` + `selectedRequestId`) / 4 ride-state stripped (`data`/`error`/`response`/`responseTime` — `data` derived from `host.content`; `response`/`responseTime` rebuilt from in-memory `responseCache` on `selectRequest`) / 2 transient (`executing` — never restore mid-flight; `headersJsonInvalid` — transient UI validation) / 3 private (`_skipNextContentUpdate`, `_lastSerializedData`, `_responseCache`). `leftPanelWidth` silent today-bug incidentally fixed — **third instance of this fix** (after Link LK2 / Todo TD2). Rejected (b) persist `executing` (mid-flight state has no restore value) and (c) persist nothing (regresses today's good behavior; response cache keyed by request id would appear empty against a different selection). No mockup change required. |
| RC3 | 2026-05-20 | Selection-state cache (`<host.id>:rest-client` — `{selectedRequestId}`) retirement — fold into `EditorDescriptor.state` or keep separate cache file? | [`walkthroughs/26-rest-client.md#RC3`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Fold into `EditorDescriptor.state`. **Fifth instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3 → Rest Client RC3); pattern now standardized across **five of seven** persisted-Tier-5 editors. Today's `restoreSelectionState` + `saveSelectionState` + `selectionRestored` one-shot guard + `static cacheName = "rest-client"` all retire. Rejected (b) keep separate cache file (duplicates the today-pattern that GR4/LV3/LK3/TD3 eliminated) and (c) hybrid per-field split (premature splitting). No mockup change required. |
| RC4 | 2026-05-20 | JSON parse/serialize lifecycle hooks under EPIC-028 — three-site split (`restore` / `adoptHost` / `dispose`). | [`walkthroughs/26-rest-client.md#RC4`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Three-site split mirrors LV4 / LK4 / TD4 — **fourth Tier 5 editor** in this lifecycle pattern. `restore()` sets up state subscription + initial `loadData(host.content)` + awaits `restoreResponseCache()`; `adoptHost` content subscription with `skipNextContentUpdate` guard; `dispose()` flushes BOTH `onDataChanged` + `saveResponseCache` (incidentally fixes today's lost-response-save bug where pending response-cache writes within the 500ms debounce window get dropped on page close). Rejected (b) single editor-level loadData (couples concerns) and (c) lazy parse (adds complexity). No mockup change required. |
| RC5 | 2026-05-20 | `skipNextContentUpdate` self-write guard — preserve editor-private flag vs. host-side `bySelf` parameter vs. TOneState change-reason tracking. | [`walkthroughs/26-rest-client.md#RC5`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Keep editor-private flag (verbatim port). **Fourth instance** of self-write-guard pattern (LV6 → LK5 → TD5 → RC5); pattern is now **rock-solid standardized** across all mutate-then-serialize editors. With four instances the test for adding host-side machinery is "does it ever pay back?" — answer no, the editor-private flag is two lines of code. Rejected (b) host-side `bySelf` parameter (leaks editor concern into host API) and (c) TOneState change-reason tracking (over-engineered for four consumers). No mockup change required. |
| RC6 | 2026-05-20 | Sidebar topology — confirm Rest Client is NOT sidebar-owning (pre-check: `register-editors.ts:424-458` no `secondaryEditor` field; `secondary-editor-registry.ts` grep zero matches; RequestTree inline). | [`walkthroughs/26-rest-client.md#RC6`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Rest Client is NOT sidebar-owning; `RequestTree` stays inline inside `RestClientBody`; no `setSidebarPanels`, no `beforeNavigateAway`, no `onMainEditorChanged`; override count 9 hooks (same as Todo, vs. Link's 11). **Third non-sidebar-owning Tier 5 editor** (after Grid and Todo) — confirms non-sidebar-owning is the modal shape, sidebar-owning (Link, walkthrough 24) remains the outlier. The LK7+LK8+LK9 recipe stays at one example (Link) until walkthrough 30 lands a true second sidebar-owner. Rejected (b) promote RequestTree to sidebar editor (extends scope without UX justification) and (c) conditional sidebar registration (premature scaffolding). Walkthrough 25's pre-check pointer to RC6 was correct. No mockup change required. |
| RC7 | 2026-05-20 | Response cache (`<host.id>:rest-client-responses` — HTTP response bodies keyed by request id; bytes to MBs per body) — keep as separate per-editor cache file or fold into descriptor or drop entirely? | [`walkthroughs/26-rest-client.md#RC7`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Keep response cache as separate per-editor cache file. **First instance of "split cache-file consolidation by scale"** pattern in EPIC-028 — small UI/selection state (≤ 1KB) folds into descriptor; large payload state (bytes to MBs) stays in a separate per-editor cache file keyed by host id. Folding into descriptor (b) would blow M9's 50KB-per-page budget at the first JSON-API response. Dropping entirely (c) regresses real UX value (response cache is a major Rest Client feature). Today's binary-exclusion gate stays load-bearing (`if (!isBinary) saveResponseCacheDebounced()` — without it a single PDF response could hit hundreds of MB on disk). Cross-window survival comes for free because the cache file is keyed by `<host.id>` and follows the host. **Establishes pattern guidance for future editors with mixed payload sizes:** *write your selection state to the descriptor; write your bulk payload state to a per-editor cache file keyed by host id.* Likely future consumers: Notebook (per-cell outputs), Graph (simulation snapshots if cached). No mockup change required — pattern is editor-internal policy using existing `host.stateStorage` API. |
| RC8 | 2026-05-20 | `ui.confirm` / `ui.notify` split (view does confirms; model does notify-on-validation) under EPIC-028 — preserve verbatim vs. all-view vs. ComponentQueue piped. | [`walkthroughs/26-rest-client.md#RC8`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Preserved verbatim — same shape as TD8. `ui` is app-level by design (`app.ui` namespace accessible from anywhere); today's split (view does delete-confirms, model does send-validation notify) is pragmatic. Send-validation reads a model flag (`headersJsonInvalid`); pushing it to the view splits the flag-read across layers without benefit. Rejected (b) move all dialogs to view (splits send-validation across layers) and (c) ComponentQueue pipe (mailbox-style; confirmation dialogs are RPC-style — different shape). No mockup change required. |
| RC9 | 2026-05-20 | Drag-and-drop — `TraitTypeId.RestRequest` (emit within-tree reordering) + `LINK` trait (accept cross-editor link drops creating new requests). | [`walkthroughs/26-rest-client.md#RC9`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Both preserved verbatim — same orthogonality design contract as TD9 (drag traits via `TraitTypeId.X` are the trait SYSTEM's job; EditorModel traits via `model.traits.set(KEY, impl)` are a separate system; they coexist without conflict). **Rest Client accepting LINK trait drops is the canonical example of the trait system paying its keep** — cross-editor drop from PageNavigator's link list into the request tree is the load-bearing use case that justifies the trait abstraction's complexity. Rejected (b) promote to EditorModel trait (different trait system; conflating them adds confusion) and (c) inline drag handling (loses the trait system's flagship value). No mockup change required. |
| RC10 | 2026-05-20 | Registry `accepts()` predicate + queue event union + scripting facade decision (NO `RestClientEditorFacade.ts` today — first text-bearing Tier 5 editor without a facade). | [`walkthroughs/26-rest-client.md#RC10`](walkthroughs/26-rest-client.md) | **Resolved 2026-05-20** | Option (a). Filename `.rest.json` priority 70 + content-peek (`"type":"rest-client"` AND `"requests"`) priority 60 (mirrors LV10/GR10/LK10/TD10 calibration across **eight** Tier 5 editors); queue events `{ type: "focus" }` only; queue request `never`. **Defer scripting facade** — zero current consumers; adding `RestClientEditorFacade.ts` + `asRestClient(force?)` + declaration is mechanical 4-file touch deferred until real consumer lands. **Documents a deliberate asymmetry:** Rest Client stays the only text-bearing Tier 5 editor without a scripting facade; future readers see "the editor template is uniform, but facades are opt-in per editor based on real consumer demand." Rejected (b) add minimal facade (speculative API surface; adds maintenance with no consumer) and (c) add full facade (even worse for speculation). No mockup change required. |
| NB1 | 2026-05-20 | NotebookEditor topology — does NotebookEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT, mirroring the eight prior Tier 5 editors? | [`walkthroughs/29-notebook.md#NB1`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. NotebookEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **ninth Tier 5 editor** in uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient / Notebook). Non-sidebar-owning (left tree composed inline via `<Splitter>`); 9 hook overrides (matches Grid / Todo / RestClient). Rejected alternatives: (b) hybrid sub-class extending some `NotebookEditorBase` (no benefit — Tier 5 template already carries all uniform machinery), (c) special-case Notebook as a non-Tier-5 editor (would force separate switch + restore + persistence pathways for no observable benefit). |
| NB2 | 2026-05-20 | State slice composition: which of the 13 fields persist vs. ride state vs. transient? | [`walkthroughs/29-notebook.md#NB2`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. 4 persisted (`leftPanelWidth` + `expandedPanel` + `selectedCategory` + `selectedTag`) / 8 ride-state stripped (`data` / `error` / `categories` / `categoriesSize` / `tags` / `tagsSize` / `filteredNotes` / `expandedNoteId`) / 1 transient (`searchText`). `leftPanelWidth` + `expandedPanel` + `selectedCategory` + `selectedTag` silent today-bug incidentally fixed — **fourth instance** of this incidental fix (LK2 → TD2 → RC2 → NB2). Today's NotebookViewModel persists state via TWO mechanisms (`data.state[noteId]` in JSON file; everything else NO persistence today). Descriptor consolidation adds restart survival for free. `expandedNoteId` rides state but strips from getRestoreData (user expects no overlay on restart). `searchText` transient (same shape as TD2 / RC2). |
| NB3 | 2026-05-20 | Per-editor cache file → descriptor.state consolidation — but Notebook today has no UI cache file. | [`walkthroughs/29-notebook.md#NB3`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. Fold UI selection state into `EditorDescriptor.state` — **sixth instance** of "per-editor cache file → descriptor.state" consolidation pattern at the destination (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3 → Rest Client RC3 → Notebook NB3). Notebook is the **new-persistence-via-descriptor variant** of the pattern (no UI cache file today; selection state currently doesn't persist at all — descriptor consolidation incidentally adds restart survival). Pattern now standardized across **six of nine** Tier 5 editors. Today's two implementation variants (LK/TD/RC/etc: cache file → descriptor; NB: no persistence → descriptor) both land at the same destination. Rejected (b) keep no-persistence (silent today-bug; user-visible regression on every restart). |
| NB4 | 2026-05-20 | Three-site lifecycle split (today's onInit decomposes into restore + adoptHost + dispose). | [`walkthroughs/29-notebook.md#NB4`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. Today's `onInit` (5 statements: state subscribe + initial loadData) splits cleanly into `restore()` (initial parse + state subscribe) + `adoptHost()` (host content subscription) + `dispose()` (flush + unsubscribe + host dispose). **Fifth Tier 5 editor** in this lifecycle shape (LV4 → LK4 → TD4 → RC4 → NB4). |
| NB5 | 2026-05-20 | Self-write-guard pattern — keep `skipNextContentUpdate` editor-private flag? | [`walkthroughs/29-notebook.md#NB5`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. Keep `skipNextContentUpdate` editor-private flag — **fifth instance** of self-write-guard pattern (LV6 → LK5 → TD5 → RC5 → NB5). With five instances, the host-side `bySelf` parameter (option b) and TOneState change-reason tracking (option c) are both **architecturally retired** as future-rejected paths; pattern fully solidified across all mutate-then-serialize editors. |
| NB6 | 2026-05-20 | NoteItemEditModel decomposition — resolves **C4**. What's the final shape? | [`walkthroughs/29-notebook.md#NB6`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. NoteItemEditModel becomes a **lightweight transient IContentHost** (~70 LOC down from ~375). Strips: `_vmHost: ContentViewModelHost` field; `acquireViewModel` / `releaseViewModel` / `prepareViewModel` / `acquireViewModelSync` methods (LV9 already retired acquireViewModelSync at IContentHost interface and TextFileModel; NB6 finishes the cleanup on NoteItemEditModel — completes LV9's "(also touched by walkthrough 29)" deferred work); portal refs (`editorToolbarRefFirst/Last`, `editorFooterRefLast`); `editor: NoteEditorModel` sub-class (Monaco-specific selection/height/highlight machinery relocates into the embedded MonacoEditor); `type = "textFile"` discriminator; compatibility properties (`noLanguage`, `getIcon`, `filePath`, `title`, `encrypted`, `decrypted`). Implements new IContentHost interface (`setStorage` no-op since storage backed by `notebook.data.state[id]`; `dispose` no I/O; `getDescriptor` returns minimal `{ kind: "noteItem", state: { noteId } }` for interface completeness). **`src/renderer/editors/base/ContentViewModelHost.ts` deletes entirely from the codebase** — last consumer dissolves (TextFileModel ditched `_vmHost` in earlier walkthroughs; NB6 ditches NoteItemEditModel's `_vmHost`, leaving zero consumers). Rejected (b) keep `_vmHost` ref-counting (nothing references it post-EPIC-028 — embedded editors are standalone EditorModels), (c) split NoteItemEditModel into separate host + adapter classes (adds complexity for no observable benefit). |
| NB7 | 2026-05-20 | Per-note embedded EditorModel + per-note switch widget placement — resolves **C5**. | [`walkthroughs/29-notebook.md#NB7`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. Each NoteItemView holds `embeddedEditor: EditorModel` (Monaco / Grid / Markdown / Mermaid / SVG / Html / etc.) wrapping the per-note NoteItemEditModel via the standard IContentHost contract. Per-note switch widget (NoteItemView-local) reads `embeddedEditor.findCompatibleEditors()` for the option list; on user click triggers per-note three-phase switch (`createEditor → switchFrom → restore` then replace embedded editor in NoteItemViewModel state); `notebookEditor.updateNoteEditor(noteId, newEditorId)` writes new editor id to `note.content.editor`. **First and only consumer of `switchEditorViaContentHost` at a second-level (non-page) owner** — demonstrates the EPIC-028 contract is fully composable (nothing in the switch protocol requires the wrapper to be a PageModel specifically). **`EditorConstructorArgs.initialHost: IContentHost \| undefined` becomes the canonical injection mechanism** — cleaner than C4's tentative `setContentHost()` separate-call shape (keeps host adoption at the only place the host can be adopted: construction). Top-level page-owned editors construct without `initialHost` and create their host inside `restore()`; nested editors construct WITH `initialHost: noteItemEditModel` and skip the construction branch. Rejected (b) global notebook toolbar switch widget (violates C5 — user can be working with multiple notes of different views simultaneously), (c) right-click-menu-only switch (discoverability matters; per-note view choice is primary affordance). |
| NB8 | 2026-05-20 | NoteItemEditModel stateStorage backed by `notebook.data.state[id]` (IN THE FILE, NOT a cache file). | [`walkthroughs/29-notebook.md#NB8`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. Preserve verbatim. Per-note state (Grid column widths, search filters in a note rendered as Markdown with search, etc.) lives **inside the notebook JSON file**, NOT in a separate cache file. Rationale: per-note state is logically per-note (survives cross-window page transfer via M5/P6 instanceId pathway; survives cross-machine sync because JSON file moves whole). A cache file would orphan when notebook moves. Same logical pattern as TD7's `data.state[id].contentHeight` but generalized — arbitrary string keys. **Symmetry with non-notebook editors:** standalone Grid uses descriptor; nested Grid uses `notebook.data.state[noteId][name]` — both pathways converge on durable serialization at different storage locations. Rejected (b) per-note separate cache file (orphans when notebook moves; breaks cross-window survival; adds dozens of tiny cache files). |
| NB9 | 2026-05-20 | Drag-and-drop traits — three active systems (`TraitTypeId.Note` + `TraitTypeId.NotebookCategory` + `LINK` accept). | [`walkthroughs/29-notebook.md#NB9`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. Preserve all three trait systems verbatim: (1) `TraitTypeId.Note` (emit by NoteItemView for cross-category drag); (2) `TraitTypeId.NotebookCategory` (emit + accept by category tree for nesting categories); (3) `LINK` trait (accept by category tree for cross-editor link drops from PageNavigator → creates new note). Drag traits orthogonal to EditorModel traits. **Notebook is the richest trait consumer in Tier 5** — three active trait types vs. Link/RestClient's two. Documents the architectural property that drag traits scale to per-editor needs without requiring framework changes. Rejected (b) merge Note + NotebookCategory into single trait (payload shapes differ — Note carries `{ noteId }`, Category carries `{ category }`; merging forces discriminated union with no benefit). |
| NB10 | 2026-05-20 | Registration via accepts() + queue events + scripting facade decision. | [`walkthroughs/29-notebook.md#NB10`](walkthroughs/29-notebook.md) | **Resolved 2026-05-20** | Option (a) confirmed. Filename `.note.json` priority 70 + content-peek (`"type":"note-editor"` AND `"notes"`) priority 60 — drops today's quartet (`acceptFile` / `validForLanguage` / `switchOption` / `isEditorContent`) per standard collapse. Mirrors LV10/GR10/LK10/TD10/RC10 across all nine Tier 5 editors. Queue event union: `{ type: "focus" }` only. Queue request: `never`. **Defer scripting facade** — matches Rest Client's RC10 deferral. **Second instance of "text-bearing Tier 5 editor without a scripting facade"** — with two instances, asymmetry documented as **intentional**: scripting facades are opt-in per editor, not required by the Tier 5 template. Adding `NotebookEditorFacade.ts` + `api/types/notebook-editor.d.ts` + `page.asNotebook(force?)` is mechanical 4-file touch deferred until a real consumer lands. Rejected (b) ship facade now (no consumer; YAGNI), (c) reject content-peek calibration (matches all eight prior calibrations). |
| NH1 | 2026-05-20 | BrowserEditorModel class shape — third generic (`BrowserQueueEvent`) per S4 / B1. | [`walkthroughs/30-no-host-group.md#NH1`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. `BrowserQueueEvent = { type: "focus" }` only. Minimal queue, matches Tier-5 calibration across eight prior editors (Grid GR10 / Todo TD10 / RC10 / NB10) plus Browser as ninth registered consumer of the minimal-queue pattern. Keyboard shortcuts route via `globalKeyDown` subscription per today's pattern (direct method calls into `webview` sub-model); queue stays reserved for chrome-to-editor wakeups like focus restoration after switch. Rejected (b) larger union (premature scaffolding; today's direct-call shape works), (c) skip third generic (inconsistent with rest of Tier-5; the generic is free when defaulted to `{ focus }`). |
| NH2 | 2026-05-20 | No CONTENT_HOST_TRAIT — confirm Browser as canonical no-host shape. | [`walkthroughs/30-no-host-group.md#NH2`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Confirm + no mockup change. Absence of CONTENT_HOST_TRAIT IS the marker (the negative of presence). `contentHost` getter inherits base-class default returning `null` (B2 from walkthrough 08). `findCompatibleEditors()` registry probe returns `[]` — switch widget hidden per PT10 (`findCompatibleEditors().length >= 2 && includes(editorId)` evaluates to false). Browser is end-of-the-line for the switch protocol. Rejected (b) `NO_CONTENT_HOST_TRAIT` marker (redundant declaration; no machinery would consume it). |
| NH3 | 2026-05-20 | State slice partitioning — 32 fields total (10 persisted + 22 transient). | [`walkthroughs/30-no-host-group.md#NH3`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) + (c) confirmed. Keep today's 10-field persisted boundary (`url` / `pageTitle` / `tabs` / `activeTabId` / `tabsPanelWidth` / `profileName` / `isIncognito` / `isTor` / `searchEngineId` / `lastSearchQuery`) AND promote `bookmarksWidth` to persisted — **fifth instance of `leftPanelWidth`-equivalent silent-today-bug incidental fix** (LK2 → TD2 → RC2 → NB2 → NH3 (c)). Pattern: any field that lived on VM-state-but-never-persisted gets a free persistence upgrade when descriptor folding runs. Rejected (b) promote `pageMuted` — transient gesture to silence one browsing session; persisting would surprise users who don't realize they muted browser at last close. |
| NH4 | 2026-05-20 | `BrowserBookmarks.acquireViewModel("link-view")` retirement (SF2 + LV9 + NB6 retired the machinery). | [`walkthroughs/30-no-host-group.md#NH4`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Embedded LinkEditor via `EditorConstructorArgs.initialHost` (NB7 pathway) — `new LinkEditor({ initialHost: this.textFileHost })`. **Browser is the second consumer of `initialHost`** after Notebook NB7 — confirms the property NB7 established (host adoption at construction, not post-construction). The `acquireViewModel` / `releaseViewModel` quartet now fully retired across the entire codebase — `BrowserBookmarks.init()` was the final remaining external consumer outside of NoteItemEditModel (which dropped it in NB6). LinkEditor's `restore()` adopts the already-restored host without re-reading (host's `state.restored === true`). Rejected (b) direct LinkViewModel construction (loses unified LinkEditor abstraction), (c) service locator pattern (speculative; one consumer today). |
| NH5 | 2026-05-20 | `<LinkEditor model={textModel} swapLayout toolbarRef…>` retirement (portal-refs retired by walkthroughs 09 / 10). | [`walkthroughs/30-no-host-group.md#NH5`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Render embedded LinkEditor through its View component — `<bookmarks.linkEditor.View />` per NB7's per-note rendering pattern. Drawer-specific UI (close button, splitter, slide-in animation) stays in `BookmarksDrawer.tsx` as plain JSX; the LinkEditor body is the only piece that renders LinkEditor-specific chrome. The `swapLayout` prop also retires — LinkEditor's TextFileModel host is hidden behind LinkEditor's own surface; no second editor to swap to inside the drawer. **Symmetric with NB7's per-note rendering pattern** — embedded LinkEditor inside Browser is no different from embedded Monaco inside Notebook. Rejected (b) keep portal-ref shape (adds machinery for one consumer), (c) `embedded?: boolean` prop (Browser drawer doesn't need separate layout). |
| NH6 | 2026-05-20 | Sub-model preservation — webview / urlBar / bookmarksUI / target (2300 LOC across four sub-models). | [`walkthroughs/30-no-host-group.md#NH6`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Preserve all four sub-models verbatim. Sub-model boundary is **orthogonal to the EditorModel boundary**. Today's split is orthogonal organizational concerns (webview lifecycle, URL bar UX, bookmarks UX, automation adapter); each has its own state subscriptions and disposal logic; today's pattern (each sub-model holds reference to parent BrowserEditorModel and subscribes to its state) works. Rejected (b) flatten into BrowserEditorModel (1075-LOC class → 2316-LOC monolith with no benefit; harder to test), (c) promote sub-models to base-class hooks (over-fits EPIC-028 to one editor; sub-model patterns are editor-internal policy). |
| NH7 | 2026-05-20 | Bookmarks lifecycle — lazy + opt-in (timer-based silent preload + on-demand init). | [`walkthroughs/30-no-host-group.md#NH7`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Preserve lifecycle verbatim — only the construction-of-LinkEditor pieces change (NH4 / NH5). Today's lazy lifecycle (300ms-timer-based silent preload + lazy on-demand init via user gesture) is good Persephone behavior; avoids blocking page open on bookmarks parsing (parsing happens after 300ms; password prompt happens on first user gesture). The EPIC-028 changes are surgical — replace `acquireViewModel` with `initialHost` construction; replace portal refs with `<View />` invocation. Rejected (b) eager-construct LinkEditor at BrowserEditorModel construction (wastes memory when bookmarks aren't configured), (c) move lazy-init into `restore()` chain (would move lazy plumbing into framework lifecycle for one consumer). |
| NH8 | 2026-05-20 | `restore` / `getRestoreData` / `applyRestoreData` already EPIC-028-shaped — wrap return in EditorDescriptor per P6. | [`walkthroughs/30-no-host-group.md#NH8`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Wrap partial state in `EditorDescriptor` — `return { editorId: this.editorId, id: this.id, state: { ...10 fields } }`. Mechanical envelope change; no behavioral change. The `currentUrls` merge logic (`data.tabs = s.tabs.map((t) => ({ ...t, url: this.currentUrls.get(t.id) || t.url }))`) carries verbatim inside the `state` field. Re-assigning fresh tab IDs in `applyRestoreData` carries verbatim. Rejected (b) keep raw partial state (breaks walkthrough 04 / P1's descriptor contract; would diverge Browser from EPIC-028 persistence contract). |
| NH9 | 2026-05-20 | Tor reconnect on restore + window-close cleanup — preserve verbatim? | [`walkthroughs/30-no-host-group.md#NH9`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. No EPIC-028 changes to Tor flow. Today's manual-reconnect UX is intentional — user chooses to restart Tor with its perf/privacy implications. The `applyRestoreData → show-reconnect-overlay` path stays; the `dispose → stop Tor` path stays; the `windowClosing → stop Tor` path stays. Tor flow is a sub-flow inside the existing override list; doesn't touch any EPIC-028 lifecycle hook directly. Rejected (b) auto-reconnect on restart (violates user expectation; UX regression). |
| NH10 | 2026-05-20 | Registry `accepts()` + standalone category retirement per MI3 + S10. | [`walkthroughs/30-no-host-group.md#NH10`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. `accepts({ mode }) => -1` + drop `editorType: "browserPage"` (per S10) + drop `category: "standalone"` (per MI3 — replaced by `!hasContentHost` flag derived from `editor.contentHost == null`). Browser doesn't open files — today's behavior is users must explicitly create a Browser page (menu, hotkey, script). The `accepts()` returns `-1` unconditionally keeps this exact behavior; Browser invisible in switch widget per NH2. Rejected (b) accept `.html` files in view mode (would change today's file-open routing; not a goal of EPIC-028). |
| CP1 | 2026-05-20 | Confirm CompareEditor stays a React component, not a registered EditorModel (CK2). | [`walkthroughs/30-no-host-group.md#CP1`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. CK2's resolution holds — CompareEditor stays a React component; the Monaco diff editor lifecycle stays in `CompareEditorModel` (a `TComponentModel`). Today's code already enforces this — CompareEditor uses `TComponentModel`, not `EditorModel`; is not in `editorRegistry`; has no `editorId` / `accepts()` / `getRestoreData()`. CompareEditor is a **transient view of two already-existing TextFileModel hosts**; the `compareGroups` set in `pagesModel.state` is the persistent representation. Rejected (b) promote CompareEditor to registered EditorModel (would require manufacturing synthetic page id / editor id / descriptor — none meaningful for a transient render). |
| CP2 | 2026-05-20 | Activation reads `pagesModel.state.compareGroups`, not `model.state.compareMode` (CK1). | [`walkthroughs/30-no-host-group.md#CP2`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Per CK1 — read from `pagesModel.state.compareGroups: Set<string>` keyed by left page id. Pages.tsx subscribes to `pagesModel.state.use((s) => s.compareGroups)`; reactivity covered by single subscription on the unified pages state. `compareModeChanged` Subscription retires; `fixCompareMode` deletes (cleanup inlined into `ungroup` / `removePage` / `setMainEditor` per CK7). Consolidating to `compareGroups` removes the today-duplicate truth state on TextFileModel. Rejected (b) keep `compareMode` flag on TextFileModel for backward compat (would leave orphaned state for no consumer). |
| CP3 | 2026-05-20 | Exit compare via `pagesModel.exitCompareMode(leftPageId)` (CK4 + CK10). | [`walkthroughs/30-no-host-group.md#CP3`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. CompareEditor receives `leftPageId` as third prop (CK10) + exit button calls `pagesModel.exitCompareMode(leftPageId)` (CK4 — `enterCompareMode` / `exitCompareMode` accept either side, resolve `leftId` internally, return boolean). Prop is a thin string; passing it from `PageContent` (which already has pageId in scope) is one line. Replaces today's `model.setCompareMode(false); groupedModel.setCompareMode(false);`. Rejected (b) derive leftPageId from `pagesModel.query.findPageForEditor(model)` (adds new query helper for single call site; brittle if same TextFileModel referenced from two pages). |
| CP4 | 2026-05-20 | Internal `CompareEditorModel` (TComponentModel for DiffEditor) — preserve. | [`walkthroughs/30-no-host-group.md#CP4`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Preserve verbatim. `TComponentModel` and `EditorModel` are different abstractions — TComponentModel is React-mount-scoped state (lifecycle tied to React mount via `useComponentModel`); EditorModel is page-scoped persistent editor. They cohabit fine. The DiffEditor + change subscription lifecycle stays exactly as today: `editorDidMount` captures the Monaco `IStandaloneDiffEditor`, sets up the modified-side `onDidChangeModelContent` subscription that writes back to `groupedModel.changeContent(...)`. Rejected (b) inline `CompareEditorModel` as `useEffect` + `useRef` (loses typed model class; harder to test; CompareEditor is already small at 113 LOC). |
| CP5 | 2026-05-20 | Migration scope verification — confirm walkthrough 06 / CK migration scope is complete. | [`walkthroughs/30-no-host-group.md#CP5`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Migration scope is complete per walkthrough 06; no additions needed. CK9 already established that persistence isn't part of the contract ("persistence dropped — main window's tray-hide design covers in-memory survival"), so no migration shim is needed. This walkthrough only touches `CompareEditor.tsx` (accept `leftPageId` prop; exit via `exitCompareMode`) and `Pages.tsx` (read from `pagesModel.state.compareGroups`); the broader real-code surface (`PagesModel.ts` add `compareGroups`; `PagesLayoutModel.ts` add `enterCompareMode`/`exitCompareMode`, remove `fixCompareMode`; `PagesQueryModel.ts` add `canCompare`/`isInCompareMode`; `PagesLifecycleModel.openDiff` rewrite; `PageModel.setMainEditor` cleanup; `events.ts` delete `compareModeChanged`; `TextToolbar.tsx` flip Compare button; `TextEditorModel.ts` + `TextFileActionsModel.ts` drop `compareMode` field + `setCompareMode`) is carried by walkthrough 06 / CK. Rejected (b) add migration shim for old persisted `compareMode` flags (per CK9 no persisted state to migrate). |
| EX1 | 2026-05-20 | Explorer as a no-host secondary-only EditorModel under unified-array (A8). | [`walkthroughs/30-no-host-group.md#EX1`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. A8's unified `editors[]` array + `_mainEditorId: string \| null` flag covers both sidebar-owning-mainEditor (Link) and sidebar-only-EditorModel (Explorer). Explorer simply never has `editor.id === _mainEditorId`, only ever satisfies the visibility criterion via `contributesPanels() === true`. **Explorer is the canonical example of secondary-only-EditorModel** — confirms the unified-array contract is general enough to cover both shapes via a single visibility criterion `(editor.id === _mainEditorId) \|\| editor.contributesPanels()`. Rejected (b) promote Explorer to special "secondary-only" subclass (adds machinery to distinguish what the visibility criterion already covers). |
| EX2 | 2026-05-20 | Constructor signature: `(rootPath?: string)` → `(state)` to align with EPIC-028 factory convention. | [`walkthroughs/30-no-host-group.md#EX2`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Align with `(state)` convention — `new ExplorerEditorModel(new TComponentState({ ...getDefaultExplorerEditorModelState(), rootPath: folderPath }))`. Explorer is the only no-host editor outside the registry; its constructor diverging from the rest of EPIC-028 would be surprising. The state-bake-in shape works fine — lifecycle callers (`PagesLifecycleModel.addEmptyPageWithNavPanel`) are already calling `getDefaultExplorerEditorModelState()` today; adding `rootPath: folderPath` to the destructure is a one-line change. Rejected (b) keep `(rootPath?)` shape (divergence with no upside), (c) hybrid `(state)` + factory helper (adds helper for single caller — only `addEmptyPageWithNavPanel` constructs Explorer outside bootstrap restore). |
| EX3 | 2026-05-20 | State slice partitioning — typed extras vs. underscore-prefixed extras in persistence shape. | [`walkthroughs/30-no-host-group.md#EX3`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (c) confirmed. Drop underscore prefix; use typed nested shape — `interface ExplorerEditorModelState extends IEditorState { rootPath: string; treeState?: TreeProviderViewSavedState; selectedHref?: string \| null; searchState?: FileSearchState; }`. Under EPIC-028 the `EditorDescriptor.state` is the editor's own typed shape (T generic), so typed extras are first-class. Drops two `as any` casts from today's `ExplorerEditorModel.ts:161-178` (today uses `eslint-disable-next-line @typescript-eslint/no-explicit-any`). Matches Tier-5 typed-state convention (Grid, Link, Todo all have proper typed shapes for ride-state). Rejected (a) keep underscore prefix (today-artifact unnecessarily preserved), (b) promote to fields on `state` (forces reactivity for fields that don't need it; would force re-renders on save). |
| EX4 | 2026-05-20 | Persistence shape — typed extras stay as private fields + TOneStates at runtime; merged into descriptor at save. | [`walkthroughs/30-no-host-group.md#EX4`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Stay as private fields + TOneStates at runtime; `getRestoreData()` merges them into the returned `EditorDescriptor.state`. Pattern: **persistence shape ≠ runtime shape** — `getRestoreData()` is a typed bridge between them. Same pattern Grid uses for sortColumn (lives on `gridRef` at runtime, lives in `descriptor.state` at persistence). Explorer keeps `treeState` + `searchState` as private fields (not naturally reactive — treeState only flows on save, searchState only on visibility change), `selectionState` + `revealVersion` as TOneStates (reactive — secondary view subscribes to them). Rejected (b) promote to fields on `state` (see EX3 — reactivity for no consumer), (c) underscore-prefixed (see EX3 — keeps `as any` cast). |
| EX5 | 2026-05-20 | `beforeNavigateAway` no-op + `onMainEditorChanged` highlight — second consumer of LK8 hook (but NOT LK7). | [`walkthroughs/30-no-host-group.md#EX5`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) + (c) confirmed. **Drop the no-op `beforeNavigateAway` override** (base class default suffices); document Explorer as **second consumer of LK8** (`onMainEditorChanged`) but NOT LK7 (`beforeNavigateAway`). **Reframes walkthrough 24's "LK7 + LK8 recipe" as two SEPARABLE hooks** — they're independent capabilities that Link happens to consume together; Explorer demonstrates LK8 without LK7. Different membership patterns: Link is sidebar-OWNING-mainEditor (transitions in/out of mainEditor role → needs `beforeNavigateAway` to clean up panels); Explorer is sidebar-ONLY-EditorModel (never mainEditor; always survives navigation → no `beforeNavigateAway` needed). Today's no-op override is explicit "I always survive" documentation; under EPIC-028 the base-class default IS that contract. Rejected isolated (b) keep no-op override for documentation (explicit no-op is noise). |
| EX6 | 2026-05-20 | `setPage` override fits unified-array N1 lifecycle (publishes panels on first attach). | [`walkthroughs/30-no-host-group.md#EX6`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Preserve verbatim. `setPage(page)` override is part of EditorModel base; setting `secondaryEditor` on first attach is a pure state mutation; N1's slice subscription handles the rest (triggers `onEditorPanelsChanged(this)` on next event-loop turn, bumps `state.version`). Same path Link uses to publish its panels. Today's order (publish on attach) is correct — Explorer is constructed already-restored (no second-phase restore needed for panel publication). Rejected (b) move into `restore()` instead (would create a window where Explorer is attached but doesn't yet publish panels; UI gap). |
| EX7 | 2026-05-20 | Self-close via `pageNavigatorModel.close()` — sidebar-wide vs. per-panel close-button gesture. | [`walkthroughs/30-no-host-group.md#EX7`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (b) confirmed. Keep today's behavior — close button closes the **whole sidebar** (PageNavigator-level gesture), NOT just the Explorer tree. N4's "close the model" affordance applies to **panel header close buttons** (per-panel close), which is a different UX gesture from Explorer's current sidebar-wide close. Explorer's "Close Panel" button in its header IS a sidebar-wide affordance (`model.page?.pageNavigatorModel?.close()` closes the navigator panel entirely). If a per-panel close gesture lands later (user demand surfaces), it would be a separate affordance reused via N4. Rejected (a) switch to N4 — close button calls `model.secondaryEditor = undefined` (would change UX), (c) hybrid (speculative; today's single-button-closes-all UX works). |
| EX8 | 2026-05-20 | `treeProvider` typed accessor — joins Link's LK9 typed-instanceof chain over three editors (Link + Archive + Explorer). | [`walkthroughs/30-no-host-group.md#EX8`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Full migration. `findTreeProviderHost` becomes typed `instanceof` chain over `LinkEditor → ArchiveEditor → ExplorerEditor` (Explorer is the third typed source — joins Link as text-bearing host + Archive as no-host sidebar-owning + Explorer as no-host secondary-only). **Confirms LK9's recipe across three architectural shapes simultaneously**; the `(editor as any).treeProvider` cast retires across all consumers. Rejected (b) partial migration (leaves `as any` cast for one editor), (c) promote `treeProvider` to base-class field on EditorModel (over-fits base class for trait that only three editors expose; the `instanceof` chain at the helper site stays clean). |
| EX9 | 2026-05-20 | Search panel via `secondaryEditor = ["explorer", "search"]` array — array form under unified-array N1. | [`walkthroughs/30-no-host-group.md#EX9`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Preserve array form verbatim. `secondaryEditor` setter takes `string \| string[] \| undefined`; N1's slice subscription fires on any change to the array; panels list updates per `editor.contributesPanels()`. **Explorer is the canonical multi-panel example** — one EditorModel publishes two panels (`["explorer", "search"]`) with related lifecycles (search opens within Explorer's rootPath; closing Explorer also closes search). Confirms the array form works under unified-array N1 / N3 (`panelEditors` getter iterates `editors[]` and inspects each `secondaryEditor`). Rejected (b) split into `ExplorerTreeModel` + `ExplorerSearchModel` (scatters related state across two models; Explorer-internal coordination would need cross-model plumbing). |
| EX10 | 2026-05-20 | Direct construction site (`PageModel.createExplorer` → `addEmptyPageWithNavPanel` inlining per EW5). | [`walkthroughs/30-no-host-group.md#EX10`](walkthroughs/30-no-host-group.md) | **Resolved 2026-05-20** | Option (a) confirmed. Per EW5 — inline 4-line construction (`new ExplorerEditorModel(state)` + `await explorer.restore()` + `page.secondaryEditors.push(explorer)` + `explorer.setPage(page)`) into `PagesLifecycleModel.addEmptyPageWithNavPanel(folderPath)`. **Removes `PageModel.createExplorer` method** — Explorer construction is a page-lifecycle concern (which page should have an explorer with what root), not a page-model concern (PageModel shouldn't know about specific editor types). `addEmptyPageWithNavPanel` is the only construction site outside bootstrap restore; inlining is clean. Today's `PageModel.ts:532` migration shim ("Migrate old format: rootPath at top level → create ExplorerEditorModel descriptor") moves to the descriptor migration path per walkthrough 04 / C2 — no migration; old format triggers detect-and-skip; user re-opens explorer manually. Rejected (b) keep thin helper for one caller (no upside), (c) promote Explorer to registered editor (semantically empty — no `accepts()`, no switch-widget visibility, no main-editor slot). |
