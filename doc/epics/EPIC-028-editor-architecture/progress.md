# EPIC-028 — Analysis Progress

Working checklist for the design phase. Mark items as we go.

**Status legend:** `[ ]` not started · `[~]` in progress · `[x]` complete

See [README.md](README.md) for the workflow and walkthrough templates.

---

## Foundation mockups

- [x] **IContentHost** — [`mockups/IContentHost.ts`](mockups/IContentHost.ts)
- [x] **traits** — [`mockups/traits.ts`](mockups/traits.ts)
- [x] **EditorModel base** — [`mockups/EditorModel.ts`](mockups/EditorModel.ts)
- [x] **TextFileModel** (now an `IContentHost`, not an `EditorModel`) — [`mockups/TextFileModel.ts`](mockups/TextFileModel.ts)
- [x] **editorRegistry** — [`mockups/editorRegistry.ts`](mockups/editorRegistry.ts)
- [x] **PageModel switch helper** — [`mockups/PageModel.ts`](mockups/PageModel.ts)
- [x] **ComponentQueue** (model → view mailbox primitive, S4 — walkthrough 02) — [`mockups/ComponentQueue.ts`](mockups/ComponentQueue.ts)
- [x] **TOneState** (selective `subscribe(listener, selector)` for pure models, N1 — walkthrough 03) — [`mockups/TOneState.ts`](mockups/TOneState.ts)
- [x] **PersistenceTypes** (`EditorDescriptor` / `HostDescriptor` / `PipeDescriptor` / `PageDescriptor` / `WindowState` with `schemaVersion`, walkthrough 04 — P1) — [`mockups/PersistenceTypes.ts`](mockups/PersistenceTypes.ts)

---

## Tier 1 — Page core

- [x] **01 — Page lifecycle** ([`walkthroughs/01-page-lifecycle.md`](walkthroughs/01-page-lifecycle.md)) — *Done 2026-05-19. Mockups updated.*
  - Sources: `src/renderer/api/pages/PageModel.ts`, `src/renderer/api/pages/PagesLifecycleModel.ts` (attachPage, detachPage, removePage, checkEmptyPage), `src/renderer.tsx` (bootstrap restore)
  - Scope: create, restore, focus, close, dispose; bootstrap flow; empty-page auto-create
  - Resolved: L1 (hardcode `"monaco"`), L2 (A6 — descriptorChanged), L3 (A3 — isFreshEmpty), L4 (A7 — three-phase lifecycle + A1 — trait shrunk), L6 (A8 — unified `editors[]`), L7 (visibility criterion). L5 deferred to 04. A2, A4 dropped.
  - Mockups touched: `traits.ts`, `EditorModel.ts`, `PageModel.ts`, `editorRegistry.ts`.

- [x] **02 — Main editor swap** ([`walkthroughs/02-main-editor-swap.md`](walkthroughs/02-main-editor-swap.md)) — *Done 2026-05-19. Mockups updated.*
  - Sources: `PageModel.setMainEditor`, `PagesLifecycleModel.navigatePageTo`, today's `TextFileModel.changeEditor`
  - Scope: view-switch (same content, different editor) vs. file-navigate (different content) — two distinct ops in the new architecture
  - Resolved: S1 (page-level `switchMainEditor`), S2 (`target` > `resolveForFile`), S3 (single phase-2 `applyRestoreData`), S4 (new ComponentQueue primitive), S5 (`mode: edit|view` on accepts + ILinkData), S6 (single try/catch fallback to Monaco), S7 (throw + script PageWrapper catches), S8 (Pattern B absorbed by unified-array; archive override unchanged), S9 (`confirmRelease` only when host dies), S10 (add `editorId`, remove `type`/`editor` fields)
  - Mockups touched: `ComponentQueue.ts` (new), `EditorModel.ts` (queue field + 3rd generic + dispose), `editorRegistry.ts` (mode param)
  - Migration scope handed off: persistence registry-id (walkthrough 04), PageWrapper validations (walkthrough 12), Compare's `isTextFileModel` (walkthrough 09), text-bearing type-guards (walkthrough 20), archive/explorer instance-checks (walkthroughs 24 / 30)

- [x] **03 — Secondary editors & PageNavigator** ([`walkthroughs/03-secondary-editors.md`](walkthroughs/03-secondary-editors.md)) — *Done 2026-05-19. Mockups updated.*
  - Sources: `PageModel` (`secondaryEditors[]`, `addSecondaryEditor`, `removeSecondaryEditor`, `notifyMainEditorChanged`), `src/renderer/ui/navigation/PageNavigatorModel.ts`, `PageNavigator.tsx`, `secondary-editor-registry.ts`, [`doc/architecture/secondary-editors.md`](../../architecture/secondary-editors.md)
  - Scope: panel registration; `beforeNavigateAway` / `onMainEditorChanged`; Pattern A vs. Pattern B; the panels-changed notification (L7 trigger from walkthrough 01)
  - Resolved: N1 (TOneState selector overload + per-editor slice subscription in `attach`/`detach`; `EditorModel.secondaryEditor` setter stays fully pure), N2 (PageNavigator subscribes to `page.state` — `state.version` covers both attach/detach and panel-list changes), N3 (`page.panelEditors` getter — filter rule centralized in PageModel), N4 (close button = "close model" gesture; reuses N1 visibility-criterion path; no new API; panel components hide the button when their model is mainEditor), N5 (CategoryEditor view subscribes to `page.state.use()`; model-side `onSecondaryEditorsChanged` + `_providerVersion` + defensive 50ms retry all deleted; no new framework API), N6 (drop Pattern A/B framing — unified-array makes them states, not patterns; pure docs concern logged for final docs pass), N7 (panel editors first, main last in `close()` — cancellation leaves the page visible; closing main is the conceptual commit point).
  - Foundation mockup added: `TOneState.ts` (selector overload on `subscribe`).
  - Mockups touched: `TOneState.ts` (new), `PageModel.ts` (`_editorSubs` field, slice-subscribe in `attach`/`detach`, new `onEditorPanelsChanged` handler, `close()` panel-first ordering, defensive sub-drain in `dispose`).
  - Deferred to walkthrough 04: secondary-editor persistence shape (`secondaryModelDescriptors[]` blob), `_pendingActivePanel` restore timing — both fold into the unified page descriptor.
  - Migration scope handed off: CategoryEditor view rewire (walkthrough 24), panel React components hide close-button when `model === page.mainEditor` (touched in walkthroughs 24, 30 alongside each editor).

- [x] **04 — Persistence** ([`walkthroughs/04-persistence.md`](walkthroughs/04-persistence.md)) — *Done 2026-05-19. Mockups updated.*
  - Sources: `PagesPersistenceModel.ts`, `EditorModel.getRestoreData/applyRestoreData`, `TextFileModel` persistence, sidebar cache storage
  - Scope: round-trip across app restart; new descriptor shape (`EditorDescriptor` / `HostDescriptor` / unified `PageDescriptor.editors[]`); `editorId`-keyed restore; pipe descriptor moves to host; fold sidebar cache into WindowState; orphan-cache sweep dropped; schema version field; per-editor salvage on restore failure
  - Resolved: P1 (new `EditorDescriptor` + `HostDescriptor` + unified `PageDescriptor.editors[]`), P2 (integer `schemaVersion: 4` as single discriminator; no migration shim), P3 (fold sidebar cache file into PageDescriptor — eliminates `<pageId>-nav-panel.txt`), P4 (pipe descriptor moves under HostDescriptor), P5 (parallel `Promise.all` restore retires `_pendingActivePanel`), P6 (`RestoreData<S>` shape + `createEditor(id, instanceId?)` for cache-id continuity), P7 (per-editor `console.warn + continue` salvage), P8 (empty-pages-with-sidebar stop being a special case), P9 (no cleanup sweep — asymmetric risk; per-editor dispose remains the cleanup path), P10 (integer schema version field). C8 / C9 dropped per P9.
  - Mockups touched: `PersistenceTypes.ts` (new), `editorRegistry.ts` (optional `instanceId`), `EditorModel.ts` (`editorId` field + `getRestoreData` returns `EditorDescriptor` + `RestoreData<S>` shape, retires the shared `IEditorState`), `IContentHost.ts` (`getDescriptor` + static `fromDescriptor` contract), `PageModel.ts` (sidebar cache machinery removed; new `saveState` iterates editors; `dispose` no longer deletes page-level cache).
  - Absorbs deferrals: L5 (walkthrough 01), S10 / C2 (walkthrough 02), walkthrough 03's `secondaryModelDescriptors[]` shape + `_pendingActivePanel` timing.
  - Migration scope handed off: real-code rewrite of `src/shared/types.ts`, `PagesPersistenceModel.ts`, `PageModel.ts` (cleanup); `EditorModel` subclasses adopt `editorId` + override `getRestoreData()` per editor (walkthroughs 20–30).

- [x] **05 — Multi-window transfer** ([`walkthroughs/05-multi-window-transfer.md`](walkthroughs/05-multi-window-transfer.md)) — *Done 2026-05-19. Mockups updated.*
  - Sources: `PagesLifecycleModel.movePageIn`/`movePageOut`, `PageDescriptor` IPC payload, `src/main/open-windows.ts`, `PageTab.handleDragEnd`, `src/main/drag-model.ts`
  - Scope: drag tab between windows; cache-file survival via `detachPage` (no dispose); descriptor reconstruction with new editor classes
  - Resolved: M1 (drag payload picks up walkthrough 04's new `PageDescriptor` shape; no `schemaVersion` field — same-process same-version), M2 (unify with bootstrap restore — `PagesPersistenceModel.restorePage(desc)` is the shared entry point for bootstrap, `movePageIn`, and `duplicatePage`), M3 (`await page.saveState()` stays in `movePageOut` — now per-editor only; establishes "saveState is an awaitable flush" invariant for editor walkthroughs), M4 (detach-without-dispose invariant — comment-only; behavior already correct), M5 (`createEditor(id, instanceId)` preserves cache-file id for both bootstrap and IPC — single mechanism), M6 (per-editor salvage on transfer mirrors walkthrough 04 / P7), M7 (close source window on last-page-moved — unchanged), M8 (pinned tabs stay non-cross-window-draggable — unchanged), M9 (payload ~50KB worst-case; host content stays in per-editor cache, not the IPC blob), M10 (no explicit `showPage` — the `ordered.push` already activates the dropped page; testing will confirm).
  - Mockups touched: `editorRegistry.ts` (C1 — `instanceId` comment extended to cover IPC transfer alongside session restore), `PageModel.ts` (C2 — `saveState()` comment lists both callers).
  - Migration scope handed off: `PageDragData` carries the new `PageDescriptor` shape (walkthrough 04 type change); `PageTab.getDragData()` rewrite; `movePageIn` / `movePageOut` / `duplicatePage` rewrites in `PagesLifecycleModel`; `restorePage` promoted to public on `PagesPersistenceModel` (C3 — real-code only, no mockup file).

- [x] **06 — Compare mode** ([`walkthroughs/06-compare-mode.md`](walkthroughs/06-compare-mode.md)) — *Done 2026-05-19. Mockups updated.*
  - Sources: today's `TextFileModel.compareMode`, `TextFileActionsModel.setCompareMode`, `PagesLayoutModel.group` + `fixCompareMode`, `src/renderer/editors/compare/`, `Pages.tsx`, `AppPageManager.tsx`, `compareModeChanged` Subscription, `PagesLifecycleModel.openDiff`
  - Scope: where the `compareMode` flag lives in the new architecture (resolves C6); activation/render/exit API; relation to grouped pages and cleanup obligations
  - Resolved: CK1 (pair-level `compareGroups: Set<string>` on `PagesModel.state` keyed by left page id), CK2 (CompareEditor stays a React component — not a registered EditorModel), CK3 (`pagesModel.query.canCompare(leftId, rightId)` centralizes the `instanceof TextFileModel` predicate), CK4 (`enterCompareMode(pageId)` / `exitCompareMode(pageId)` accept either side, resolve leftId internally, return boolean), CK5 (`pagesModel.query.isInCompareMode(pageId): { active, leftId?, rightId? }` derived getter, consumers subscribe via `state.use(s => s.compareGroups)`), CK6 (`compareModeChanged` Subscription + `pagesModel.rerender` field + `rerender()` method all retired), CK7 (`fixCompareMode` deleted; cleanup inlined into `ungroup` / `removePage` / `setMainEditor`), CK8 (`openDiff` composes `groupTabs + enterCompareMode`), CK9 (persistence dropped — main window's tray-hide design covers in-memory survival), CK10 (CompareEditor receives `leftPageId` as third prop).
  - Mockups touched: `TextFileModel.ts` (C1 — `compareMode` comments now point to `PagesModel.state.compareGroups`; `setCompareMode` listed in "REMOVED" block).
  - Migration scope handed off: real-code rewrite of `PagesModel.ts` (add `compareGroups` field, remove `rerender`), `PagesLayoutModel.ts` (add `enterCompareMode`/`exitCompareMode`, remove `fixCompareMode`, fold cleanup into `ungroup`), `PagesQueryModel.ts` (or equivalent — add `canCompare`/`isInCompareMode` helpers), `PagesLifecycleModel.ts` (`openDiff` rewrite, `removePage` cleanup hook), `PageModel.ts` (`setMainEditor` cleanup hook), `events.ts` (delete `compareModeChanged`), `Pages.tsx` (`PageContent` + `Pages` reads), `TextToolbar.tsx` (toolbar button calls `enterCompareMode`), `CompareEditor.tsx` (accept `leftPageId` prop, exit calls `exitCompareMode`), `TextEditorModel.ts` + `TextFileActionsModel.ts` (drop `compareMode` field + `setCompareMode` method).

- [x] **07 — Grouped pages** ([`walkthroughs/07-grouped-pages.md`](walkthroughs/07-grouped-pages.md)) — *Done 2026-05-19. No mockup adjustments required.*
  - Sources: `PagesLayoutModel.group`/`ungroup`/`groupTabs`/`fixGrouping`, `PagesQueryModel.isGrouped`/`getGroupedPage`/`getLeftGroupedPage`/`groupedPage`, `PagesModel.state.leftRight`/`rightLeft`, `PagesPersistenceModel.saveState`/`restoreState` (groupings), `AppPageManager` + `GroupContainer` + `ImperativeSplitter`, `PageTab.handleClick`/`closeClick`, `PagesLifecycleModel.openDiff`/`requireGroupedText`/`movePageIn`/`movePageOut`, `PageCollectionWrapper` script API
  - Scope: side-by-side page rendering; the `leftRight`/`rightLeft` parallel maps; activation/cleanup primitives; rendering/DOM machinery; relation to compare mode (06) and multi-window transfer (05)
  - Resolved: GK1 (keep parallel `leftRight`/`rightLeft` maps — drift impossible, O(1) both ways), GK2 (centralize `requireGroupedText`'s discriminator as `pagesModel.query.getTextFileHost(pageId): TextFileModel | null` — signature refined 2026-05-20 by walkthrough 08 / T2 from the original `hasTextFileHost: boolean` to return the typed host; mirrors CK3's `canCompare` in spirit), GK3 (`fixGrouping` stays a single sweep with two remaining responsibilities after CK7's `fixCompareMode` deletion), GK4 (`PageTab.closeClick` drops the redundant `fixCompareMode()` call), GK5 (group dissolves on multi-window transfer — single-tab gesture by design, dual-tab drag impossible), GK6 (splitter `widthK` stays non-persisted — deferable as additive `WindowState.splitRatios?` later per P10), GK7 (two-pages-per-group is permanent design — no N-page generalization), GK8 (pin/group interaction preserved — emergent benign behavior), GK9 (script API surface unchanged — thin delegates), GK10 (`isGrouped(editorId)` resolves correctly via the already-updated `findPage`).
  - Mockups touched: none. `WindowState.groupings?: [string, string][]` already in `PersistenceTypes.ts` from walkthrough 04 / P1.
  - Migration scope handed off: real-code only — add `PagesQueryModel.getTextFileHost(pageId): TextFileModel | null` helper (GK2 — signature refined by walkthrough 08 / T2); rewrite `PagesLifecycleModel.requireGroupedText` to use it (GK2); delete the trailing `this.fixCompareMode()` call inside `PagesLayoutModel.fixGrouping` (already absorbed by walkthrough 06 / CK7); delete the `pagesModel.fixCompareMode()` call inside `PageTab.closeClick`'s grouped branch (GK4). `AppPageManager` / `GroupContainer` / `ImperativeSplitter` / `PagesPersistenceModel` (groupings part) / script wrappers / `group`/`ungroup`/`groupTabs` carry over verbatim.

- [x] **Tier 1 second pass** — revisited walkthroughs 01-07 against the final Tier 1 mockup shape (2026-05-19). Each walkthrough now has a "Second-pass review" section.
  - **No decision drift.** Every L / S / N / P / M / CK / GK concern survives untouched against later walkthrough decisions. All resolutions still hold.
  - **Some in-doc mockup snippets are stale** relative to the live `mockups/*.ts` files — most visibly the A8 PageModel sketch in walkthrough 01, which predates 03 / N1's slice-subscribe bookkeeping, 04 / C7's sidebar-cache removal, and 05 / C2's `saveState()` dual-caller doc. The live mockups are the source of truth; the walkthrough sketches are best read as design rationale.
  - **Stability signal positive.** The last two walkthroughs (06, 07) produced zero foundation-mockup changes (just one TextFileModel doc-comment touch in 06). Tier 1 is design-ready; Tier 2 can begin without reshaping foundation primitives.
  - **No new concerns raised.** All Tier 1 design tradeoffs and forward-pointers to Tier 2+ are already captured in the originating walkthroughs.

---

## Tier 2 — UI surfaces

- [x] **08 — Page tabs** ([`walkthroughs/08-page-tabs.md`](walkthroughs/08-page-tabs.md)) — *Done 2026-05-20. Mockups updated (B1, B2). GK2 helper signature refined.*
  - Sources: `src/renderer/ui/tabs/PageTabs.tsx`, `PageTab.tsx`
  - Scope: tab strip; drag/pin/group; drag-to-new-window; context menu; reading title/modified/language/filePath/encrypted/audio across the editor-and-host split
  - Resolved: T1 (two parallel `use()` subs — `editor.state.use()` + `editor.contentHost?.state.use()`), T2 (retire `isTextFileModel(editor)` for the GK2 helper `pagesModel.query.getTextFileHost(pageId): TextFileModel | null` across 14 callsites — refines GK2 from `hasTextFileHost: boolean` into a typed accessor that subsumes both the predicate and the host-method-access paths), T3 (factor `PageModel.getDescriptor(): PageDescriptor` — B1 mockup), T4 (keep `noLanguage` + `getIcon` as explicit EditorModel base fields), T5 (keep audio fields duck-typed — one consumer), T6 (pinned-tab encryption-width branch uses the same GK2 helper as T2 — inline at one callsite), T7 (drop `fixCompareMode()` from `closeClick` — confirmation via 06 / CK7 + 07 / GK4), T8 (Ctrl+click `groupTabs` works under unified `editors[]` — confirmation via 07 / GK10), T9 (`handleDragEnd` stays pure dispatch — `movePageOut` carries the saveState flush per 05 / M3), T10 (same-window reorder trait payload page-id-keyed — confirmation).
  - Mockups touched: `PageModel.ts` (B1 — `getDescriptor(): PageDescriptor` single source of truth for the new descriptor shape from walkthrough 04 / P1; consumed by `PagesPersistenceModel.saveState`, `PageTab.getDragData`, `PagesLifecycleModel.duplicatePage`), `EditorModel.ts` (B2 — `contentHost: IContentHost | null` accessor; base returns null, text-bearing subclasses override).
  - Cross-walkthrough refinement: GK2's helper renamed from `hasTextFileHost(pageId): boolean` to `getTextFileHost(pageId): TextFileModel | null` (signature refinement noted in walkthroughs 02 / 06 / 07 status sections + concerns.md). One helper, two use cases (truthy check + host-method access); `requireGroupedText` drops its trailing `as unknown as TextFileModel` cast as a side benefit. CK3's `canCompare(leftId, rightId)` stays a separate two-id predicate (no host to return).
  - Migration scope handed off: real-code only — add `PagesQueryModel.getTextFileHost(pageId)` helper; rewrite all 14 PageTab + 1 PageTabs callsites to use the helper; rewrite `PageTab.getDragData` to call `page.getDescriptor()`; rewrite `PagesPersistenceModel.saveState` to call `page.getDescriptor()` per page; add `contentHost` getter override to every text-bearing EditorModel subclass (Monaco, Grid, Markdown, Mermaid, Svg, Html, Link, Todo, RestClient, Log, Notebook's embedded editors, Graph, Draw — walkthroughs 20–29); update `requireGroupedText` to use the accessor and drop its trailing cast (was GK2 scope; signature change here). `PageTabs.tsx` scroll/add-page-split-button machinery, the trait-based same-window reorder (`TraitTypeId.PageTab`), `pagesModel.{moveTab,pinTab,unpinTab,closeOtherPages,closeToTheRight,duplicatePage,groupTabs,ungroup,showPage}`, `api.addDragEvent` IPC contract, and audio duck-typing carry over verbatim.

- [x] **09 — Page-level toolbar** ([`walkthroughs/09-page-toolbar.md`](walkthroughs/09-page-toolbar.md)) — *Done 2026-05-20. Mockups updated (B1 new file, B2 + B3 on EditorModel).*
  - Sources: `src/renderer/ui/app/RenderEditor.tsx`, `src/renderer/editors/text/TextEditorView.tsx`, `TextToolbar.tsx`, `src/renderer/editors/base/EditorToolbar.tsx`
  - Scope: page-level toolbar host (`<PageToolbar>`) auto-renders NavPanel button on the left + switch widget on the right; editor contributions sit as inline children; `RenderEditor`'s category branch retires; portal refs (`editorToolbarRefFirst`/`Last`) deleted in favor of inline composition; six editors collapse their inline NavPanel IconButton blocks to a single `getNavigatorTarget()` override; text-host-specific buttons (Compare, Run, Run-all, Show-resources) defer placement details to walkthrough 10's `<TextChrome>` (NavPanel exits `<TextChrome>` to `<PageToolbar>` per PT5 reframe).
  - Resolved: PT1 (each editor's view composes shared chrome components directly per C8 — TextEditorView wrapper dissolves), PT2 (switch widget auto-rendered inside `<PageToolbar>` on the right via `editor.findCompatibleEditors()`), PT3 (portal refs deleted; editor views inline-compose `<PageToolbar model>{contributions}</PageToolbar>`), PT4 (`RenderEditor` category branch retires — single uniform path via `<AsyncEditor>`), PT5 (NavPanel is NOT text-only — REFRAMED by user; auto-render in `<PageToolbar>` via new `EditorModel.getNavigatorTarget()` accessor — B3; eliminates 6 per-editor inline IconButton blocks across Text/PDF/Image/Video/Archive/Category), PT6 (Compare lives in walkthrough 10's `<TextChrome>` — host-aware via CK3/CK4), PT7 (Run-script in `<TextChrome>` via host language; Run-all additionally gated by `editor.hasTextSelection?.()` Monaco-only optional method — B2), PT8 (Show-resources in `<TextChrome>` via host language === "html"), PT9 (walkthrough 09 retires `editorToolbarRefFirst`/`Last` only; footer + overlay refs defer to walkthrough 10), PT10 (switch widget visibility: `findCompatibleEditors().length >= 2 && includes(editorId)`).
  - Mockups touched: `PageToolbar.tsx` (new — B1), `EditorModel.ts` (B2 — `hasTextSelection?()` + B3 — `getNavigatorTarget()`).
  - Cross-walkthrough refinement: NavPanel button reclassified from text-host-specific to page-level uniform affordance (user feedback on PT5). `<TextChrome>` shrinks to Compare + Run + Run-all + Show-resources only.
  - Migration scope handed off: real-code only — `RenderEditor.tsx` collapses to a single `<AsyncEditor>` path (PT4); `TextEditorView.tsx` dissolves (focus management becomes `useEditorFocus(model)` hook or absorbs into walkthrough 10's `<TextChrome>`; keyboard routing migrates per-editor); `TextToolbar.tsx` deleted as a separate component (text-host-specific actions migrate into walkthrough 10's `<TextChrome>`; portal `<div ref={…} />` markers removed); `TextEditorModel.ts:159-178` + `NoteItemEditModel.ts:193-244` drop the four portal ref fields/setters (toolbar pair in 09; footer + overlay in 10); six editors (Text, PDF, Image, Video, Archive, Category) override `getNavigatorTarget()` and delete the inline NavPanel IconButton block; ten editor views (Grid, Markdown, Mermaid, SVG, Todo, Link, LogView, Draw, Graph, Notebook + per-note) rewrite `createPortal` toolbar contributions to inline children of `<PageToolbar>`; Monaco finalizes `hasTextSelection?()` override in walkthrough 20; switch widget `model.changeEditor(v)` replaces with `page.switchMainEditor(v)` per S1.

- [x] **10 — TextChrome** ([`walkthroughs/10-text-chrome.md`](walkthroughs/10-text-chrome.md)) — *Done 2026-05-20. Mockups updated (B1 new file, B2 on IContentHost). B3 dropped.*
  - Sources: `src/renderer/editors/text/TextEditorView.tsx`, `TextToolbar.tsx` (remaining four buttons after walkthrough 09), `TextFooter.tsx`, `ScriptPanel.tsx`, `ActiveEditor.tsx`, `note-editor/NoteItemToolbar.tsx`, `NotebookEditor.tsx` (overlay portal consumer); resolves C1 (host-capability discovery — TextFileModel vs. NoteItemEditModel) and the rest of C8 (no portal refs; each editor composes shared chrome directly)
  - Scope: dissolution of `TextEditorView` as a separate wrapper; new shared `<TextChrome>` component each text-bearing editor's view composes around its body; relocation of Compare / Run / Run-all / Show-resources from `TextToolbar.tsx` into `<TextChrome>`; relocation of script-toggle / encoding / per-editor footer status from `TextFooter.tsx` into `<TextChrome>`'s footer row; retirement of the remaining two portal refs (`editorFooterRefLast` + `editorOverlayRef` per PT9); migration of focus management + keyboard routing out of TextEditorView; host-instanceof branching for full chrome (TextFileModel) vs. minimal chrome (NoteItemEditModel — no script panel, no footer, no overlay, no Compare); Notebook overlay relocates from page-level portal to Notebook-inline composition; `ActiveEditor.tsx` dissolves (per S10 — `state.editor` retired); encrypted-fallback to Monaco becomes per-editor view guard (no new UI per TC11)
  - Concerns (in walkthrough doc): TC1 (chrome shape — single `<TextChrome>` wrapper vs. separately-composed pieces), TC2 (host-capability discovery — re-confirm C1 instanceof), TC3 (`TextEditorView` dissolution — per-editor compose vs. AsyncEditor auto-wrap), TC4 (toolbar contribution order in row), TC5 (minimal-chrome contract for NoteItemEditModel), TC6 (script panel ownership — host vs. editor vs. chrome), TC7 (`<EditorOverlay>` shape — Notebook-inline), TC8 (focus management migration), TC9 (`handleKeyDown` migration), TC10 (footer portal retirement + `footerContributions` prop), TC11 (encryption indicator — no new UI)
  - Recommendations on file: TC1 (a) single `<TextChrome>` wrapper with `toolbarContributions` + `footerContributions` props; TC2 (a) confirm C1 instanceof; TC3 (a) per-editor module composes; TC4 (a) text-host buttons → editor contributions → spacer → Show-resources → switch; TC5 (a) minimal branch drops script panel + footer + overlay + Compare; TC6 (a) script panel stays host-owned; TC7 (c) Notebook renders its own overlay inline (no `<EditorOverlay>` chrome piece); TC8 (b) `<TextChrome>` owns the focus subscription on its root; TC9 (a) chrome's `onKeyDown` delegates to `host.handleKeyDown?.(e)`; TC10 (a) `footerContributions` prop on `<TextChrome>`; TC11 (a) no new UI — encrypted fallback to Monaco stays a per-editor view guard
  - Mockup adjustments proposed: B1 (new `mockups/TextChrome.tsx` — single shared chrome wrapper component); B2 dropped (no `IContentHost` capability surface — confirm C1 instanceof); B3 dropped (no `EditorModel.script?` accessor — script panel stays host-owned)

---

## Tier 3 — Special page shapes

- [x] **11 — Empty pages & well-known pages** ([`walkthroughs/11-empty-and-well-known-pages.md`](walkthroughs/11-empty-and-well-known-pages.md)) — *Done 2026-05-20. Zero mockup adjustments — verification-and-tie-down walkthrough.*
  - Sources: `src/renderer/api/pages/well-known-pages.ts`, `PagesLifecycleModel.addEmptyPage`, `addEmptyPageWithNavPanel`, `addEditorPage`, `requireWellKnownPage`, `showAboutPage`/`showSettingsPage`/`showStorybookPage`, `closeFirstPageIfEmpty`
  - Scope: four page shapes — empty Monaco (`addEmptyPage`), sidebar-only (`addEmptyPageWithNavPanel`), singleton-editor with fixed id (About/Settings/Storybook), well-known singletons (mcp-ui-log/mcp-server-log); `addEditorPage` script API rewrite post-S10 (no more `state.editor` flag); singleton dedup via `addPage`'s `findPage` collision check; persistence of `mainEditorId: null` pages (P8 verification)
  - Resolved: EW1 (`addEmptyPage` → async `createEditor("monaco") + restore()` — confirms L1 + A7), EW2 (`addEditorPage` → async `createEditor(editorId) + applyRestoreData({title, host: {kind: "textFile", state: {content, language}}}) + restore()` — replaces today's `newTextFileModel + s.editor = X` shape via P6's canonical phase-2 entry), EW3 (`requireWellKnownPage` → `createEditor(def.editor, def.id)` propagates well-known id to editor instance id via M5/P6's `instanceId` param — cache files survive restarts), EW4 (page-id-and-editor-id overlap in well-known — same string in two namespaces by explicit design; `findPage` resolves either way per 01 / A8 + 03 + 07 / GK10), EW5 (Explorer creation stays direct construction — secondary-only editor not in main editor registry; today's `page.createExplorer` dissolves into lifecycle method), EW6 (language goes through `applyRestoreData({host: {state: {language}}})` — same lane as content per P6), EW7 (`WellKnownPageDef` shape stays minimal — `{id, title, editor, language}`; future fields added when concrete need lands per YAGNI), EW8 (keep explicit `findPage` check in `requireWellKnownPage` to short-circuit before the editor module's dynamic-import on hot path; About/Settings/Storybook keep implicit-via-`addPage` dedup), EW9 (empty-with-sidebar persistence already covered by P8 + P5 — verification only), EW10 (`isFreshEmpty()` Monaco override checks `host.content === "" && filePath === undefined && !modified && editor.title === ""` — title check is the discriminator that distinguishes `addEmptyPage()` default-title from `addEditorPage("monaco", "js", "Custom Title")` user-set title; override body finalized in walkthrough 20).
  - Mockup adjustments: none landed. Every concern collapsed to "confirms earlier resolution" — L1, L3, P6, P8, M5, S10 already cover the design space.
  - Migration scope handed off: real-code only — async-ify `addEmptyPage` + `addEditorPage` + `requireWellKnownPage` in `PagesLifecycleModel.ts`; inline Explorer direct-construction into `addEmptyPageWithNavPanel` (PageModel's today-`createExplorer` dissolves); rewrite to `createEditor + applyRestoreData + restore` shape across the four entry points; `well-known-pages.ts` gets a comment block documenting the singleton-id namespace-overlap design choice; About/Settings/Storybook lifecycle flows unchanged beyond per-page-module EditorModel migration (walkthrough 30); Monaco `isFreshEmpty()` body finalized in walkthrough 20 with EW10's four-condition checklist.

---

## Tier 4 — Cross-cutting

- [x] **12 — Scripting facades** ([`walkthroughs/12-scripting-facades.md`](walkthroughs/12-scripting-facades.md)) — *Done 2026-05-20. Mockup updated (B1).*
  - Sources: `src/renderer/scripting/api-wrapper/PageWrapper.ts`, `PageCollectionWrapper.ts`, 13 *Facade.ts files (TextEditorFacade, GridEditorFacade, NotebookEditorFacade, TodoEditorFacade, LinkEditorFacade, MarkdownEditorFacade, SvgEditorFacade, HtmlEditorFacade, MermaidEditorFacade, GraphEditorFacade, DrawEditorFacade, BrowserEditorFacade, McpInspectorFacade); `ScriptContext.ts` (script teardown / releaseList); `AppWrapper.ts` (event-channel subscription tracking)
  - Scope: `page.asX()` new shape — direct `mainEditor instanceof X` with opt-in `force?: boolean` switch using the UI switch widget's compatibility source; retirement of `ContentViewModelHost` ref-counting (`acquireViewModel` / `releaseViewModel` / `useContentViewModel`); `set editor(v)` setter rewires to `page.switchMainEditor(v).catch(ui.notify)` per S7; `PageWrapper.type` removed entirely (zero consumers); `EditorView` union retires from internal types and re-lives in `api/types/page.d.ts` for script-API IntelliSense; `releaseList` constructor parameter drops from `PageWrapper` / `PageCollectionWrapper` but stays alive in `ScriptContext` + `AppWrapper` for event-channel subscriptions; ComponentQueue request/reply extension (`execute` / `register`) for view-context probes (`getSelectedText` / `getCursorPosition` / `insertText` / `replaceSelection`) — S4's deferred future lands here
  - Resolved: SF1 (asX `force?: boolean` opt-in switch — uses `editor.findCompatibleEditors()` same as UI switch widget; throw-by-default; 11 ViewModel-backed facades get the parameter; Browser/McpInspector stay throw-only), SF2 (ContentViewModelHost / acquireViewModel / releaseViewModel / useContentViewModel all retire — each EditorModel IS its own viewmodel), SF3 (refined after verification — `releaseList` drops from PageWrapper + PageCollectionWrapper only; stays alive in ScriptContext + AppWrapper for AppWrapper.events subscription cleanup per AutoloadRunner contract), SF4 confirmation (S7 already resolved — fire-and-forget setter with `ui.notify` catch), SF5 (drop `type` getter entirely — zero consumers in codebase grep), SF6 (extend ComponentQueue with request/reply half symmetric with send/subscribe — B1 mockup; script-API impact: TextEditorFacade query methods become async, one extra await), SF7 confirmation (mechanical translation — `state.editor` → `editorId`, `state.language` → `contentHost.state.language`), SF8 confirmation (`state.type === "X"` → `instanceof BrowserEditorModel`/`McpInspectorEditorModel`; drops `as unknown as` cast), SF9 (base `EditorModel.modified` getter survives unchanged; text-bearing override delegates to host — already correct at `mockups/EditorModel.ts:373`), SF10 (EditorView retires from `src/shared/types.ts`; re-lives in `api/types/page.d.ts` as script-API-only union; value↔id mapping verified 1:1 verbatim so setter is direct passthrough; two-place maintenance touch when new editor lands)
  - Mockups touched: `ComponentQueue.ts` (B1 — second generic `Req = never`; `execute(req): Promise<unknown>` + `register(handler)` request/reply pair with mailbox semantics symmetric to existing `send` / `subscribe`; `dispose` rejects pending requests with disposal error; `pendingRequestCount` inspection helper; Monaco usage shape sketched for walkthrough 20).
  - Migration scope handed off: `PageWrapper.ts` rewrite (host-split getters, `force?: boolean` on 11 facade methods, `instanceof` gates, `switchMainEditor + ui.notify` setter, drop `type` getter, drop `releaseList` parameter); `PageCollectionWrapper.ts` (drop `releaseList`, `addEditorPage(editorId: string, ...)` covered by walkthrough 11 / EW2); all 11 ViewModel-backed facades flip from wrapping ViewModel to wrapping EditorModel subclass directly (finalized per editor in walkthroughs 20–29); `TextEditorFacade` collapses to thin Promise-returning delegates over `editor.queue.execute(...)`; Browser + McpInspector facades drop `as unknown as` cast; `ScriptContext.ts` stops passing `releaseList` to PageWrapper / PageCollectionWrapper, Log View viewmodel cleanup rewires under walkthrough 13; `AppWrapper.ts` constructor unchanged, stops passing `releaseList` to PageCollectionWrapper; `src/shared/types.ts` deletes `EditorView` union; `api/types/page.d.ts` adds `EditorView` union + deletes `readonly type: string` from `IPage` + updates facade signatures with `force?: boolean` and `Promise<>` returns; `ContentViewModelHost.ts` + `useContentViewModel.ts` files deleted; per-editor confirmation in walkthroughs 20–29 (each subclass declares own state shape; text-bearing editors override `modified` getter; Monaco finalizes `MonacoQueueRequest` union + `register` dispatcher in walkthrough 20).

- [x] **13 — MCP integration** ([`walkthroughs/13-mcp-integration.md`](walkthroughs/13-mcp-integration.md)) — *Done 2026-05-20. Zero mockup adjustments — verification-and-tie-down walkthrough.*
  - Sources: `src/renderer/api/mcp-handler.ts`, `src/main/mcp-http-server.ts`, `src/renderer/api/pages/well-known-pages.ts`
  - Scope: per-method translation under EPIC-028 — flat editor-state fields (`type`, `editor`, `language`, `filePath`) split across editor/host per S10 + P4; `isTextFileModel(editor)` predicate retires (GK2 / T2 helper takes over); `acquireViewModelSync("log-view")` retires per SF2 (LogView becomes its own EditorModel subclass per walkthrough 23); registry `category === "standalone"` filter flips to `!hasContentHost`; `addEditorPage` internals already covered by EW2; well-known-page restore already covered by EW3
  - Resolved: MI1 (drop `type` wire field; map `editor` to `editorId`, `language` / `filePath` to host); MI2 (reuse GK2 / T2 helper `getTextFileHost(pageId)` for read/write gates); MI3 (`!hasContentHost` flag replaces `category === "standalone"`; add `log-view` hint to the rejection table); MI4 (direct `instanceof LogViewEditorModel` checks at three sites); MI5 (`createPage` return-shape mapping mechanical per S10 + P4); MI6 (confirmation — `addEntry` / `addDialogEntry` API surface preserved); MI7 — recommendation overridden by user (no new dispose contract needed; today's `LogViewModel.onDispose()` at `LogViewModel.ts:72-80` already cancels pending dialogs); MI8 (confirmation — `executeScript` mainEditor arg unchanged per SF3); MI9 (confirmation — `openUrlInBrowserTab` unchanged); MI10 (confirmation — `browser_*` delegation unchanged).
  - Mockup adjustments: none. Every concern collapsed to mechanical translation of upstream resolutions (S10, P4, GK2 / T2, SF2, SF3, SF8, EW2, EW3) or "today already does it" (MI7).
  - Migration scope handed off: real-code only — rewrite `src/renderer/api/mcp-handler.ts` per MI1+MI2+MI3+MI4+MI5 (drop `type` from `getPages` / `getActivePage` rows; route `getPageContent` / `setPageContent` through `pagesModel.query.getTextFileHost(pageId)`; flip `category === "standalone"` to `!hasContentHost`; add `log-view` hint entry; replace three `editor.acquireViewModelSync("log-view")` sites with `editor instanceof LogViewEditorModel`); `notepad://guides/pages` documentation refresh to drop the `type` field reference; LogViewModel's `onDispose` cleanup logic carries over to `LogViewEditorModel` (rename only — walkthrough 23 owns the migration); `mcp-http-server.ts` unchanged (transport layer below the host split); `automation/commands.ts` rewrites under walkthrough 30 (Browser editor — SF8's gate flip applied at the automation layer).

---

## Tier 5 — Editors

- [x] **20 — Monaco / Text** ([`walkthroughs/20-monaco.md`](walkthroughs/20-monaco.md)) — *Done 2026-05-20. One tiny mockup edit landed (MO7).*
  - Sources: `src/renderer/editors/text/` — `TextEditorModel.ts`, `TextEditor.tsx`, `TextEditorView.tsx`, `ActiveEditor.tsx`, `TextToolbar.tsx`, `TextFooter.tsx`, `ScriptPanel.tsx`, `TextFileIOModel.ts`, `TextFileEncryptionModel.ts`, `TextFileActionsModel.ts`, `paste-rich-text.ts`
  - Scope: most complex text-bearing editor; sets the template; ITextModel survival across switch (C3); MonacoEditor subclass + MonacoEditorState + MonacoQueueEvent / MonacoQueueRequest unions; TextViewModel + ContentViewModel + ContentViewModelHost dissolution (SF2); TextEditorView + TextToolbar + TextFooter + ActiveEditor retirement (walkthroughs 09 / 10 / TC11); script-panel host-ownership confirmation (TC6); content-based editor detection retirement (mockup TextFileModel notes); runScript selection access path under SF6
  - Resolved: MO1 (a) `hasSelection` on `MonacoEditor.state` — single reactive surface for chrome's Run-all visibility gate; MO2 (a) inline `useEffect`s in `MonacoBody.tsx` — ContentViewModel + ContentViewModelHost retire per SF2; MO3 (a) `@monaco-editor/react`'s `<Editor>` props unchanged (host's content reactive, ITextModel auto-managed, undo dies on switch per C3); MO4 (a) confirmation — `MonacoQueueEvent` carries revealLine/highlightText per S4 / B1; MO5 (a) keep `hasSelection: false` in saved state (skip in applyRestoreData); MO6 (b) chrome materializes selection then calls `host.actions.runScript(text, language)`; F5 path uses thin `MonacoEditor.runScript(all?)` async wrapper that materializes selection then delegates to host (`TextFileActionsModel.runScript(scriptText, language)` becomes pure data-in, no editor coupling); MO7 (b) `MonacoQueueEvent.focus` + base `EditorModel.focus()` no-op — chrome's 200ms root-focus is followed by `editor.focus()` typed wrapper; text-bearing editors override; MO8 confirmation — ScriptPanel verbatim port (rename `model.runRelatedScript()` → `model.actions.runRelatedScript()` only); MO9 confirmation — detection machinery (`_detectTimer` + 4 methods + 1 state field) deletes from `TextFileModel`; switch widget calls `findEditorsAccepting(host)` on demand with registry content-peek; MO10 confirmation — `applyRenamedPath` verbatim port, `MonacoEditor.id` unchanged.
  - Mockups touched: `EditorModel.ts` (MO7 — `focus(): void { /* override */ }` no-op added between `hasTextSelection?()` and `saveState()`; text-bearing editors override to `this.queue.send({type:"focus"})`).
  - Migration scope handed off: real-code only — new `src/renderer/editors/monaco/` folder (`MonacoEditor.ts` class + `MonacoBody.tsx` view + `index.ts` module export); `TextFileModel` host class relocates to a content-host folder (likely `src/renderer/api/content/TextFileModel.ts` — final path during implementation) with submodels (`TextFileIOModel`, `TextFileEncryptionModel`, `TextFileActionsModel`, `ScriptPanel`) following; eight files delete (`TextEditor.tsx`, `TextEditorView.tsx`, `ActiveEditor.tsx`, `TextToolbar.tsx`, `TextFooter.tsx`, `src/renderer/editors/base/ContentViewModel.ts`, `ContentViewModelHost.ts`, `useContentViewModel.ts`); `TextFileModel` loses `_detectTimer` + 4 detection methods + `state.detectedContentEditor` (MO9); `TextFileActionsModel.runScript(scriptText: string, language: string)` signature change (MO6); F5 keystroke routing for `runScript` shifts from `actions.handleKeyDown` to chrome's `onKeyDown` calling `editor.runScript()` (MO6); `register-editors.ts` registers `monaco` instead of `textFile`; `editorRegistry.ts` strips per registry mockup (delete `validateForLanguage` / `getSwitchOptions` / `detectContentEditor` / `getPreviewEditor`); `TextEditorFacade` thins to SF6 shape (4 async + 2 sync methods over MonacoEditor); `paste-rich-text.ts` verbatim; persistence migration zero per C2 + P2.

- [x] **21 — Grid** ([`walkthroughs/21-grid.md`](walkthroughs/21-grid.md)) — *Done 2026-05-20. Zero mockup adjustments — template-confirmation walkthrough.*
  - Sources: `src/renderer/editors/grid/` — `GridViewModel.ts`, `GridEditor.tsx`, `utils/grid-utils.ts`, `components/ColumnsOptions.tsx`, `components/CsvOptions.tsx`
  - Scope: first non-Monaco exercise of the Tier 5 template; three registry ids (`grid-json` / `grid-csv` / `grid-jsonl`) sharing one `GridEditor` class with constructor-bound format discriminator; GridViewModel + ContentViewModel + ContentViewModelHost dissolution (SF2); per-editor cache file (`<id>-grid-page.json`) eliminated in favor of EditorDescriptor.state ride (GR4); gridRef sortColumn two-way sync via setGridRef callback (GR5); CSV delimiter detection moves to editor's variant-aware bootstrap inside `restore()` (GR7); error field stays on editor.state stripped from descriptor (GR8); queue events `focus` + `focusCell`, GridQueueRequest never (GR10)
  - Resolved: GR1 (a) one `GridEditor` class with constructor-bound `editorId` + `format` — three registry factories, one class; GR2 (a) variant discriminator readouts read `this.format` (semantic word, decoupled from registry id); GR3 (a) GridViewModel.onInit body splits across `GridEditor.restore()` (variant bootstrap + initial reparse) + `adoptHost` (host content + csv-options slice subscriptions) + `GridBody.tsx` useEffects (pagesModel.onFocus scroll restore + mount focus); `saveStateDebounced` retires; GR4 (a) fold Grid state into `EditorDescriptor.state` — eliminates `<id>-grid-page.json`; payload <2KB worst case, within M9 budget; GR5 (a) two-way sortColumn sync via setGridRef callback — view writes saved → ref on mount, subscribes ref slice → editor.state; both directions in one ref callback; GR6 (a) per-editor-state-slice covers switchFrom — new GridEditor instance starts with `defaultGridEditorState`; no special "clear across switch" logic; GR7 (a) CSV delimiter detection inside `GridEditor.restore()` with only-if-default guard; GR8 (a) `state.error` stays on editor.state stripped from getRestoreData (MO5 pattern); GR9 confirmation — GR5's two-way sync covers gridRef lifecycle automatically; GR10 (a) `GridQueueEvent = focus \| focusCell`; `GridQueueRequest = never`.
  - Mockups touched: **none.** Grid lands all eight pieces of the walkthrough 20 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) without any base-mockup touch. Tier 5 template stability confirmed on the first non-Monaco editor.
  - Migration scope handed off: real-code only — new `src/renderer/editors/grid/GridEditor.ts` (class + GridEditorState + GridQueueEvent + formatFromEditorId helper); new `GridBody.tsx` (AVGrid + FilterBar view with parse-error short-circuit); new `GridEditorView.tsx` (composes `<TextChrome>` + `<GridBody>` + toolbar/footer contributions); `GridViewModel.ts` deletes; today's `GridEditor.tsx` deletes (body splits between GridBody + GridEditorView); `register-editors.ts` three factory swaps (`() => new GridEditor(state, "grid-csv")` style); `editorRegistry.ts` three Grid `accepts()` predicates (content-peek + filename rules); `GridEditorFacade.ts` flips from VM-wrap to EditorModel-wrap (stays sync); `showCsvOptions(anchor, vm)` → `showCsvOptions(anchor, editor: GridEditor)` signature touch; `utils/grid-utils.ts` + `components/ColumnsOptions.tsx` verbatim; `<id>-grid-page.json` cache files orphaned on first boot post-upgrade, cleaned by per-editor `fs.deleteCacheFiles(id)` on future dispose; persistence migration zero per C2 + P2.

- [x] **22 — Preview group: Markdown / Svg / Html / Mermaid** ([`walkthroughs/22-preview-group.md`](walkthroughs/22-preview-group.md)) — *Done 2026-05-20. Zero mockup adjustments — second template-confirmation walkthrough (after Grid).*
  - Sources: `src/renderer/editors/markdown/` (`MarkdownViewModel.ts`, `MarkdownView.tsx`, `MarkdownBlock.tsx`), `svg/` (`SvgViewModel.ts`, `SvgView.tsx`), `html/` (`HtmlViewModel.ts`, `HtmlView.tsx`), `mermaid/` (`MermaidViewModel.ts`, `MermaidView.tsx`, `render-mermaid.ts`)
  - Scope: second non-Monaco group; four sibling content-views over `TextFileModel`, no edit surface, one registry id each. Light editors (Svg / Html state ≈ `EditorStateBase` only; Mermaid carries async render pipeline + `lightMode`; Markdown carries search + compact-mode + scroll). Stress-tests the Tier 5 template on near-empty editor state slices — and the template holds.
  - Resolved: PV1 (a) — four separate classes; no shared `PreviewEditorBase` (YAGNI; re-evaluate after walkthroughs 23–29 land); PV2 (a) — Markdown `compactMode` persisted only; search visibility/text/matchIndex/totalMatches ride state for reactivity, stripped from getRestoreData per MO5; PV3 (a) — search state lives on `MarkdownEditor.state` (preserves future script-API path `page.asMarkdown().openSearch(...)`); PV4 (a) — Markdown scroll position view-local (`useRef` + `pagesModel.onFocus` useEffect); not persisted across restart; PV5 (a) — Mermaid render pipeline on editor (`_renderTimer` private field + `renderDebounced` private method; `svgUrl/error/loading` on state); PV6 (b) — Mermaid `lightMode` PERSISTS (per-editor user choice survives restart); default initialized from `isCurrentThemeDark()` on first construct; PV7 (a) — `SvgEditorState = HtmlEditorState = EditorStateBase` typedefs (symmetric naming, zero added fields); PV8 (a) — all four queue events `{ type: "focus" }`; queue request `never`; PV9 (a) — `MarkdownEditor._containerRef` private DOM ref field (NOT on state); view's `setContainer(el)` callback writes; facade reads `editor.containerInnerHtml` getter; facade stays sync; PV10 confirmation — toolbar contributions inline children of `<PageToolbar>`; facades wrap editor directly, all sync; `force?: boolean` parameter added per SF1.
  - Mockups touched: **none.** Preview group lands all eight pieces of the walkthrough 20 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) without any base-mockup touch. Tier 5 template stability confirmed across the complexity axis (Monaco complex → Grid medium → Preview light).
  - Migration scope handed off: real-code only — 8 new files (4× `XxxEditor.ts` + 4× `XxxEditorView.tsx`) + 4 new `XxxBody.tsx` (preview surface — MarkdownBlock host, BaseImageView host, iframe host); 4 `XxxViewModel.ts` files delete; 4 `XxxView.tsx` files delete; `register-editors.ts` four factory swaps (`() => new MarkdownEditor(state)`, etc.); 4 facades flip from VM-wrap to EditorModel-wrap (stays sync); `PageWrapper.asMarkdown/asSvg/asHtml/asMermaid` add `force?: boolean` parameter per SF1; `MarkdownBlock.tsx` + `CodeBlock.tsx` + `rehypeHighlight.ts` + `render-mermaid.ts` + `BaseImageView` + `FindBar` carry over verbatim. Persistence migration zero per C2 + P2 (preview VMs never wrote per-editor cache files). First example in EPIC-028 of an editor holding a DOM ref outside `state` (MarkdownEditor's `_containerRef` per PV9) — sets the pattern for any future "facade needs sync DOM peek" need.

- [x] **23 — Log View** ([`walkthroughs/23-log-view.md`](walkthroughs/23-log-view.md)) — *Done 2026-05-20. Zero mockup adjustments — third template-confirmation walkthrough in a row (after Grid + Preview group). Final SF2 retirement step: `acquireViewModelSync` machinery fully removed from codebase.*
  - Sources: `src/renderer/editors/log-view/` — `LogViewModel.ts`, `LogViewEditor.tsx`, `LogViewContext.ts`, `LogEntryWrapper.tsx`, `LogMessageView.tsx`, `LogEntryContent.tsx`, `StyledTextView.tsx`, `items/*.tsx` (9 dialog/output items), `logTypes.ts`, `logConstants.ts`; consumers in `mcp-handler.ts` (3 callsites), `ScriptContext.ts` (1 callsite), `ScriptRunner.ts` (1 pre-load line), `UiFacade.ts` + wrapper helpers (StyledTextBuilder, Progress, Grid, Text, Markdown, Mermaid)
  - Resolved: LV1 (a) `LogViewEditor` IS the page mainEditor with TextFileModel content host + CONTENT_HOST_TRAIT exposed (fifth Tier 5 editor in this shape); LV2 (a) persist `showTimestamps` + `itemsState`; `entries`/`entryCount`/`error` ride state for reactivity stripped per MO5/GR8; six private fields stay private; `forceScrollVersion` retires per LV5; LV3 (a) `itemsState` folds into `EditorDescriptor.state` (mirrors Grid GR4 — second instance of "per-editor cache file → descriptor.state" consolidation); LV4 (a) three-site lifecycle split: `restore()` initial parse + `adoptHost` content subscription + `dispose()` pendingDialogs cancellation; LV5 (a) `forceScrollVersion` retires in favor of `LogQueueEvent.scrollToBottom` (third instance of "state counter bump → queue event" pattern after MO4 revealLine + GR10 focusCell); LV6 (a) `skipNextContentUpdate` editor-private flag preserved (most editor-specific mechanism in EPIC-028 so far — unique to append-only editors); LV7 (a) verbatim port of pendingDialogs cancellation on dispose; uniform across page-close AND switch-out (only model-side Promise lifecycle in Tier 5; sets pattern for future Promise-based interactive editors); LV8 (a) minimal queue: `{type:"focus"} | {type:"scrollToBottom"}`; request `never`; LV9 (a) full `acquireViewModelSync` retirement — declaration deletes from `IContentHost.ts:56`, implementations from `TextEditorModel.ts:74` + `NoteItemEditModel.ts:331`; `loadViewModelFactory("log-view")` pre-load line deletes from `ScriptRunner.ts:108`; **final SF2 cleanup step** — machinery fully retires across codebase; LV10 (a) `accepts()` predicate: filename `.log.jsonl` priority 70 + content-peek `"type"\s*:\s*"log\."` priority 60 fallback; mode-agnostic.
  - Mockups touched: **none.** Tier 5 template carries Log View end-to-end across all eight pieces (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT). Stability holds across the most divergent editor yet — complex (Monaco) → medium (Grid) → light (Preview) → append-only Promise-based (LogView).
  - Migration scope handed off: real-code only — new `LogViewEditor.ts` (class + LogViewEditorState + LogQueueEvent) + new `LogViewEditorView.tsx` (composes `<TextChrome>` + `<LogBody>` + `<LogToolbarBits>`); today's `LogViewEditor.tsx` renames to `LogBody.tsx` (absorbs auto-scroll machinery + virtual grid render; drops portal-based toolbar per walkthrough 09 / 10); `LogViewModel.ts` deletes (state + setters + private fields + JSONL parse/serialize + entry mutators all absorb into `LogViewEditor.ts`); 16 view files (LogViewContext, LogEntryWrapper, LogMessageView, LogEntryContent, StyledTextView, items/*) carry over verbatim with type rename `LogViewModel` → `LogViewEditor` in `useLogViewModel()` return type and constructor args; `register-editors.ts` log-view registration swaps from VM-based to EditorModel-based factory + drops `acceptFile`/`validForLanguage`/`switchOption`/`isEditorContent` quartet in favor of single `accepts()` per LV10; `UiFacade.ts` constructor accepts `LogViewEditor` (method bodies preserved per MI6); six wrapper helpers flip constructor arg type; `ScriptContext.ts:208-269` `executeUiOnPage` becomes async per EW2, drops `acquireViewModelSync` + `releaseList.push` per LV9, `instanceof LogViewEditor` replaces `state.editor === "log-view"` + `isTextFileModel` predicates; `ScriptRunner.ts:108` drops `loadViewModelFactory("log-view")` line; `mcp-handler.ts` three callsites flip to `editor instanceof LogViewEditor` direct checks per MI4 + LV9; `IContentHost.ts:56` declaration deletes; `TextEditorModel.ts:74` + `NoteItemEditModel.ts:331` implementations delete (final SF2 cleanup — also touched by walkthrough 29 for NoteItemEditModel); new `api/types/log-view-editor.d.ts` for script-API IntelliSense (`page.asLog(force?)`); persistence migration zero per C2 + P2 (today's `<host.id>-log-view-items.json` cache files orphan gracefully per P9).

- [x] **24 — Link** ([`walkthroughs/24-link.md`](walkthroughs/24-link.md)) — *Done 2026-05-20. Zero mockup adjustments — fourth template-confirmation walkthrough in a row (after Grid, Preview group, Log View). **First sidebar-owning editor in Tier 5.** First text-bearing-editor exercise of `beforeNavigateAway` + `onMainEditorChanged` hooks deferred from walkthrough 03.*
  - Sources: `src/renderer/editors/link-editor/` — `LinkViewModel.ts`, `LinkEditor.tsx`, `LinkTreeProvider.ts`, `linkTypes.ts`, `linkTraits.ts`, three in-editor panels + three secondary panel wrappers
  - Resolved: LK1 (a) LinkEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT (sixth Tier 5 editor in uniform shape); LK2 (a) state slice partitioning: 5 persisted (`leftPanelWidth` + `expandedPanel` + `selectedCategory`/`Tag`/`Hostname`) / 11 ride-state stripped / 8 private; `leftPanelWidth` previously forgotten on restart (silent today-bug) incidentally fixed by consolidation; LK3 (a) fold selection-state cache into `EditorDescriptor.state` — **third instance** of cache-file → descriptor pattern (Grid GR4 → Log View LV3 → Link LK3); today's `restoreSelectionState` + `selectionRestored` one-shot flag both retire; LK4 (a) three-site lifecycle split: `restore()` initial parse + state-subscription setup + `adoptHost` content subscription with self-write guard + `dispose()` flush; mirrors LV4; LK5 (a) keep `skipNextContentUpdate` editor-private flag (verbatim port) — **second instance** of self-write-guard pattern after LV6; pattern now standardized for mutate-then-serialize editors; LK6 (a) sidebar panel registration becomes `setSidebarPanels(open)` model method called from view's useEffect; view reads `isNavigatorOpen` (UI concern), model writes own `secondaryEditor` (model concern) + gates on `mainEditor === model` for demote-safe no-op; LK7 (a) override `beforeNavigateAway` with `contributesPanels()` discriminator — **first text-bearing-editor exercise** of the hook; pattern is "current-state discriminator" (second valid shape alongside Archive's S8 sourceLink-based); replaces today's view-side cleanup early-return ("if still in secondaryEditors[], don't clear panels"); LK8 (a) override `onMainEditorChanged(newMain)` to drop "link-hostnames" + conditionally drop "link-tags" based on `state.tags.length > 0` (plus a `state.tags` subscription in `restore()` for tag-count-crosses-zero case); **first text-bearing-editor exercise** of the hook; replaces today's `LinkCategorySecondaryEditor.updatePanels` useEffect; LK9 (a) typed accessors on LinkEditor class (`get treeProvider(): LinkTreeProvider \| null`, `selectByHref(href)`, `selectionState: TOneState<NavigationState>`); `findTreeProviderHost` flips to `instanceof` chain (LinkEditor → ArchiveEditor → ExplorerEditor); **three `(m as any)` casts retire across writer + reader**; LK10 (a) filename `.link.json` priority 70 + content-peek (`"type":"link-editor"` AND `"links"`) priority 60 fallback (mirrors LV10/GR10 calibration); queue events `{ type: "focus" }` only, request `never`.
  - Mockups touched: **none.** Tier 5 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) carries LinkEditor end-to-end across the first sidebar-owning editor — the most lifecycle-heavy editor in Tier 5 so far. Two lifecycle-hook overrides (`beforeNavigateAway` + `onMainEditorChanged`) — already in `mockups/EditorModel.ts:292-303` since walkthrough 03 for exactly this purpose — fit alongside the existing override list without base-class machinery growth. B1 LK7 doc-comment refinement NOT landed (Archive's S8 sourceLink pattern stays canonical in mockup; LinkEditor demonstrates second valid shape by example).
  - Migration scope handed off: real-code only — new `LinkEditor.ts` (class + LinkEditorState + LinkQueueEvent) + new `LinkEditorView.tsx` (composes `<TextChrome>` + `<LinkBody>` + `<LinkToolbarBits>` + `<LinkFooterBits>`); today's `LinkEditor.tsx` renames to `LinkBody.tsx`; `LinkViewModel.ts` deletes (state + setters + private fields + JSON parse/serialize all absorb into `LinkEditor.ts`); `LinkCategorySecondaryEditor.tsx` lines 37-52 (duck-typed `m.treeProvider = vm.treeProvider` block) + lines 57-71 (`updatePanels` useEffect) both delete entirely (replaced by typed accessors + `onMainEditorChanged` model-side); `LinkTagsSecondaryEditor.tsx` + `LinkHostnamesSecondaryEditor.tsx` + 7 view files (LinksList, LinksTiles, LinkItemList, LinkItemTiles, PinnedLinksPanel, EditLinkDialog, LinkTooltip) + 3 in-editor panel files (LinkCategoryPanel, LinkTagsPanel, LinkHostnamesPanel) all flip `vm: LinkViewModel` prop type to `model: LinkEditor`; `LinkTreeProvider.ts` constructor arg renames `vm` → `editor`; `register-editors.ts` link-view registration swaps to EditorModel factory + drops `acceptFile`/`validForLanguage`/`switchOption`/`isEditorContent` quartet in favor of single `accepts()` per LK10; `LinkEditorFacade.ts` flips from VM-wrap to EditorModel-wrap + adds `force?: boolean` per SF1; `findTreeProviderHost` (in CategoryEditor) replaces `(editor as any).treeProvider` with typed `instanceof` chain; new `api/types/link-editor.d.ts` declaration file; persistence migration zero per C2 + P2 (today's `<host.id>-link-editor.txt` cache files orphan gracefully per P9).

- [x] **25 — Todo** ([`walkthroughs/25-todo.md`](walkthroughs/25-todo.md)) — *Done 2026-05-20. Zero mockup adjustments — fifth template-confirmation walkthrough in a row (after Grid, Preview group, Log View, Link). **First non-sidebar-owning Tier 5 editor since walkthrough 23 (Log View)** — confirms the template covers both sidebar topologies uniformly. **Calibration finding:** walkthrough 24's closure pointer to walkthrough 25 as "second sidebar-owning editor" was incorrect; Todo registers no secondary editors and renders TodoListPanel inline.*
  - Sources: `src/renderer/editors/todo/` — `TodoViewModel.ts`, `TodoEditor.tsx`, `todoTypes.ts`, `todoColors.ts`, `components/TodoListPanel.tsx`, `components/TodoItemView.tsx`
  - Resolved: TD1 (a) TodoEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **seventh Tier 5 editor** in uniform shape; TD2 (a) state slice partitioning: 3 persisted (`leftPanelWidth` + `selectedList` + `selectedTag`) / 4 ride-state stripped (`data`/`error`/`listCounts`/`filteredItems`) / 1 transient (`searchText`) / 4 private; `leftPanelWidth` silent today-bug incidentally fixed — **second instance of this incidental fix** after Link LK2; TD3 (a) fold selection-state cache into `EditorDescriptor.state` — **fourth instance** of cache-file → descriptor pattern (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3); today's `restoreSelectionState` + `saveSelectionState` + `selectionRestored` one-shot guard + `static cacheName` all retire; TD4 (a) three-site lifecycle split mirrors LV4/LK4 — third Tier 5 editor in this lifecycle shape; TD5 (a) keep `skipNextContentUpdate` editor-private flag — **third instance** of self-write-guard pattern (LV6 → LK5 → TD5); pattern now firmly standardized across all mutate-then-serialize editors; TD6 (a) Todo is NOT sidebar-owning — corrects walkthrough 24's closure prediction; `TodoListPanel` stays inline inside `TodoBody`; LK7/LK8/LK9 recipe explicitly does NOT apply; override count 9 hooks (vs. Link's 11) documents the "pay-only-when-used" property of `beforeNavigateAway` + `onMainEditorChanged` concretely; TD7 (a) per-item heights stay in JSON `data.state[id].contentHeight` — same precedent as Link LK2's `data.state` per-collection split (heights are item properties not window properties); TD8 (a) `ui.confirm`/`ui.notify` direct calls from model mutators preserved (`ui` is app-level; script API symmetry preserved via `skipConfirm=true`); TD9 (a) `TraitTypeId.TodoItem` drag system preserved verbatim — drag traits orthogonal to EditorModel traits (drag traits describe DATA shapes; editor traits describe CAPABILITY shapes); orthogonality now documented as design contract; TD10 (a) filename `.todo.json` priority 70 + content-peek (`"type":"todo-editor"` AND `"items"`) priority 60 (mirrors LV10/GR10/LK10 calibration); queue events `{ focus }` only, request `never`.
  - Mockups touched: **none.** Tier 5 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) carries TodoEditor end-to-end across the **first non-sidebar-owning Tier 5 editor since walkthrough 23 (Log View)** — confirms the template covers both sidebar topologies with a uniform shell and pay-only-when-used lifecycle hooks.
  - Migration scope handed off: real-code only — new `TodoEditor.ts` (class + TodoEditorState + TodoQueueEvent) + new `TodoEditorView.tsx` (composes `<TextChrome>` + `<TodoBody>` + `<TodoToolbarBits>` + `<TodoFooterBits>`); today's `TodoEditor.tsx` renames to `TodoBody.tsx`; `TodoViewModel.ts` deletes (state + setters + private fields + JSON parse/serialize + item / list / tag CRUD all absorb into `TodoEditor.ts`); `components/TodoListPanel.tsx` + `components/TodoItemView.tsx` flip `pageModel: TodoViewModel` → `model: TodoEditor` (method calls preserved verbatim); `register-editors.ts` todo-view registration swaps to EditorModel factory + drops `acceptFile`/`validForLanguage`/`switchOption`/`isEditorContent` quartet in favor of single `accepts()` per TD10; `TodoEditorFacade.ts` flips from VM-wrap to EditorModel-wrap + adds `force?: boolean` per SF1; existing `api/types/todo-editor.d.ts` updated to reflect `page.asTodo(force?): TodoEditorFacade`. `RenderFlexGrid` / `Splitter` / `Panel` / `Input` / `Textarea` / `IconButton` / `WithMenu` / `Dot` / trait DnD utilities all carry over verbatim. Persistence migration zero per C2 + P2 (today's `<host.id>-todo-editor.txt` orphans gracefully per P9).

- [x] **26 — Rest Client** ([`walkthroughs/26-rest-client.md`](walkthroughs/26-rest-client.md)) — *Done 2026-05-20. Zero mockup adjustments — sixth template-confirmation walkthrough in a row (after Grid, Preview group, Log View, Link, Todo). **Eighth Tier 5 editor** in the uniform shape; **third non-sidebar-owning** (after Grid and Todo). **New pattern surfaced and resolved:** RC7's split-cache-file consolidation by scale.*
  - Sources: `src/renderer/editors/rest-client/` — `RestClientViewModel.ts`, `RestClientEditor.tsx`, `restClientTypes.ts`, `httpConstants.ts`, `RequestBuilder.tsx`, `KeyValueEditor.tsx`, `ResponseViewer.tsx`, `multipartBuilder.ts`, `parseClipboardRequest.ts`, `serializeRequest.ts`, `open-in-rest-client.ts`
  - Resolved: RC1 (a) RestClientEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **eighth Tier 5 editor** in uniform shape; RC2 (a) state slice: 2 persisted (`leftPanelWidth` + `selectedRequestId`) / 4 ride-state stripped (`data`/`error`/`response`/`responseTime`) / 2 transient (`executing` + `headersJsonInvalid`) / 3 private; `leftPanelWidth` silent today-bug incidentally fixed — **third instance** of this incidental fix (after Link LK2 / Todo TD2); RC3 (a) fold selection-state cache into `EditorDescriptor.state` — **fifth instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3 → Rest Client RC3); pattern now standardized across **five of seven** persisted-Tier-5 editors; today's `restoreSelectionState` + `saveSelectionState` + `selectionRestored` + `static cacheName` all retire; RC4 (a) three-site lifecycle split mirrors LV4 / LK4 / TD4 — **fourth Tier 5 editor** in this lifecycle shape; incidentally fixes today's lost-response-save bug (dispose now flushes saveResponseCache too); RC5 (a) keep `skipNextContentUpdate` editor-private flag — **fourth instance** of self-write-guard pattern (LV6 → LK5 → TD5 → RC5); pattern now rock-solid standardized; RC6 (a) Rest Client is NOT sidebar-owning — `register-editors.ts:424-458` no `secondaryEditor` field + `secondary-editor-registry.ts` grep zero matches + `RequestTree` composed inline inside `RestClientEditor.tsx` via `<Splitter>`; LK7/LK8/LK9 recipe does NOT apply; **third non-sidebar-owning Tier 5 editor** (after Grid and Todo); override count 9 hooks (same as Todo, vs. Link's 11); RC7 (a) keep response cache as separate per-editor cache file — **first instance of "split cache-file consolidation by scale"** pattern; small UI/selection state → descriptor; large payload state (bytes to MBs per HTTP response body) → per-editor cache file keyed by host id; folding into descriptor (b) would blow M9's 50KB-per-page budget at the first JSON-API response; today's binary-exclusion gate stays load-bearing; cross-window survival comes for free; establishes pattern guidance for future editors with mixed payload sizes (Notebook outputs, possibly Graph snapshots); RC8 (a) `ui.confirm` / `ui.notify` split (view does confirms; model does notify-on-validation) preserved verbatim — same shape as TD8; RC9 (a) `TraitTypeId.RestRequest` (emit within-tree) + `LINK` trait (accept cross-editor link drops from PageNavigator) preserved verbatim — drag traits orthogonal to EditorModel traits; LINK accept is the **canonical example** of the trait system paying its keep; RC10 (a) filename `.rest.json` priority 70 + content-peek priority 60 (mirrors LV10/GR10/LK10/TD10 across **eight** Tier 5 editors); queue events `{ focus }` only, request `never`; **defer scripting facade** — adding one is mechanical 4-file touch; Rest Client stays the only text-bearing Tier 5 editor without a `XxxEditorFacade.ts` (documents deliberate asymmetry).
  - Mockups touched: **none.** Tier 5 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) carries RestClientEditor end-to-end across the **third non-sidebar-owning Tier 5 editor** (after Grid and Todo) plus the new split-cache pattern (RC7) — confirms the template covers both sidebar topologies AND both payload-size patterns (small UI state folds into descriptor; large payload state stays in per-editor cache file). The split-cache pattern is editor-internal policy using existing `host.stateStorage` API; no new framework primitive needed.
  - Migration scope handed off: real-code only — new `RestClientEditor.ts` (class + RestClientEditorState + RestClientQueueEvent) + new `RestClientEditorView.tsx` (composes `<TextChrome>` + `<RestClientBody>`); today's `RestClientEditor.tsx` renames to `RestClientBody.tsx`; `RestClientViewModel.ts` deletes (state + setters + private fields + JSON parse/serialize + request/header/formData/formDataEntry CRUD + `sendRequest` + `pasteRequest` + `responseCache` machinery all absorb into `RestClientEditor.ts`); 3 component files (`RequestBuilder.tsx` + `ResponseViewer.tsx` + `KeyValueEditor.tsx`) flip `vm: RestClientViewModel` → `model: RestClientEditor`; `register-editors.ts` rest-client registration swaps to EditorModel factory + drops `acceptFile`/`validForLanguage`/`switchOption`/`isEditorContent` quartet in favor of single `accepts()` per RC10; **NO new facade or `api/types/rest-client-editor.d.ts`** per RC10 (Rest Client stays the only text-bearing Tier 5 editor without a scripting facade); `open-in-rest-client.ts` + `multipartBuilder.ts` + `parseClipboardRequest.ts` + `serializeRequest.ts` + `httpConstants.ts` + `restClientTypes.ts` all verbatim. `content/resolvers.ts:150-153` (the resolver dispatch to `openInRestClient` for `data.target === "rest-client"`) unchanged. `tree-context-menus.tsx:43` ("Open in Rest Client" menu entry) unchanged. Persistence migration zero per C2 + P2 (today's `<host.id>:rest-client` selection cache orphans gracefully per P9; today's `<host.id>:rest-client-responses` response cache **survives forward verbatim** since the cache name stays the same and host id propagates via M5/P6's `instanceId` pathway).

- [~] **27 — Graph** — **SKIPPED for design phase** (2026-05-20). Structurally similar to other Tier 5 editors already walked (own-state JSON editor with sub-models). Concerns + resolutions are expected to mirror the standardized set: RC1-class shape (uniform Tier 5 with CONTENT_HOST_TRAIT if host-backed, else lands in walkthrough 30's no-host group), RC3-class cache→descriptor consolidation, RC5-class self-write-guard, RC4-class three-site lifecycle split, RC9-class drag traits preserved verbatim, possibly RC7-class split-cache for force-graph simulation snapshots. **Will investigate first-principles during implementation** — read `src/renderer/editors/graph/` end-to-end then either match existing patterns or surface novel concerns at PR time.

- [~] **28 — Draw** — **SKIPPED for design phase** (2026-05-20). Excalidraw-backed canvas editor with large embedded state (the embedded library scene + assets + history). Same reasoning as walkthrough 27 — expected to fit Tier 5 template with RC7-class split-cache pattern likely (Excalidraw scene blobs scale beyond M9's 50KB descriptor budget). **Will investigate first-principles during implementation** — read `src/renderer/editors/draw/` end-to-end then either match existing patterns or surface novel concerns at PR time.

- [x] **29 — Notebook + note-level switching** ([`walkthroughs/29-notebook.md`](walkthroughs/29-notebook.md))
  - Sources: `src/renderer/editors/notebook/` — `NotebookViewModel.ts`, `NotebookEditor.tsx`, `NoteItemViewModel.ts`, `note-editor/NoteItemEditModel.ts`
  - Scope: stress-test for `switchEditorViaContentHost` at a second owner; resolves C4 and C5
  - **Done 2026-05-20.** All ten concerns (NB1-NB10) RESOLVED.
  - Resolved: NB1 (a) NotebookEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **ninth Tier 5 editor** in uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient / Notebook); NB2 (a) state slice: 4 persisted (`leftPanelWidth` + `expandedPanel` + `selectedCategory` + `selectedTag`) / 8 ride-state stripped / 1 transient (`searchText`); **fourth instance** of `leftPanelWidth`-equivalent silent-today-bug incidental fix (LK2 → TD2 → RC2 → NB2); NB3 (a) fold UI selection state into `EditorDescriptor.state` — **sixth instance** of pattern at the destination (Grid GR4 → LV3 → LK3 → TD3 → RC3 → NB3); Notebook is the **new-persistence-via-descriptor variant** (no UI cache file today; selection state currently doesn't persist at all — descriptor consolidation incidentally adds restart survival); pattern now standardized across **six of nine** Tier 5 editors; NB4 (a) three-site lifecycle split — **fifth Tier 5 editor** (LV4 → LK4 → TD4 → RC4 → NB4); NB5 (a) `skipNextContentUpdate` self-write guard — **fifth instance** (LV6 → LK5 → TD5 → RC5 → NB5); pattern fully solidified; host-side `bySelf` (option b) and TOneState change-reason tracking (option c) both architecturally retired; NB6 (a) NoteItemEditModel decomposition **resolves C4** — lightweight transient IContentHost (~70 LOC down from ~375); strips `_vmHost: ContentViewModelHost` field + `acquireViewModel/releaseViewModel/prepareViewModel/acquireViewModelSync` methods (LV9 already retired acquireViewModelSync at IContentHost interface and TextFileModel; NB6 finishes the cleanup on NoteItemEditModel — completes LV9's "(also touched by walkthrough 29)" deferred work) + portal refs + `NoteEditorModel` Monaco sub-class + `type = "textFile"` discriminator + compatibility properties; **`src/renderer/editors/base/ContentViewModelHost.ts` deletes entirely from the codebase** — last consumer dissolves (TextFileModel ditched `_vmHost` in earlier walkthroughs; NB6 ditches NoteItemEditModel's `_vmHost`, leaving zero consumers); NB7 (a) per-note embedded EditorModel + per-note switch widget **resolves C5** — each NoteItemView holds `embeddedEditor: EditorModel`; per-note switch widget reads `findCompatibleEditors()` and triggers per-note three-phase switch (`createEditor → switchFrom → restore` then replace embedded editor in NoteItemViewModel state); **first and only consumer of `switchEditorViaContentHost` at a second-level owner** — demonstrates the EPIC-028 contract is fully composable; **`EditorConstructorArgs.initialHost: IContentHost \| undefined` becomes the canonical injection mechanism** — cleaner than C4's tentative `setContentHost()` separate-call shape (keeps host adoption at the only place the host can be adopted: construction); NB8 (a) NoteItemEditModel stateStorage backed by `notebook.data.state[id]` IN THE JSON FILE (NOT a cache file) — per-note state survives cross-window/cross-machine; **standalone Grid uses descriptor; nested Grid uses `notebook.data.state[noteId][name]`** — both pathways converge on durable serialization; NB9 (a) three trait systems verbatim (`TraitTypeId.Note` emit + `TraitTypeId.NotebookCategory` emit+accept + `LINK` accept) — Notebook is the **richest trait consumer in Tier 5** (three active trait types vs. Link/RC's two); drag traits orthogonal to EditorModel traits; NB10 (a) filename `.note.json` priority 70 + content-peek priority 60 across nine Tier 5 editors; queue `{ focus }` only, request `never`; **defer scripting facade** — **second instance of deferred-facade** (after RC10); documents asymmetry as intentional: scripting facades are opt-in per editor.
  - Mockups touched: **none.** Tier 5 template + IContentHost mockup carry NotebookEditor + NoteItemEditModel end-to-end. `EditorModel.ts` mockup unchanged (`EditorConstructorArgs.initialHost` already exists from walkthrough 04 / P6); `IContentHost.ts` mockup unchanged (post-NB6 NoteItemEditModel exactly implements the sketched interface); `editorRegistry.ts` mockup unchanged. **Seventh template-confirmation walkthrough in a row** (Grid + Preview group + Log View + Link + Todo + Rest Client + Notebook). Template + interface now confirmed stable across nine editors with CONTENT_HOST_TRAIT, two host implementations (TextFileModel + NoteItemEditModel), two switch scopes (page-level + per-note), and three sidebar topologies (sidebar-owning Link / non-sidebar-owning Grid-Todo-RC-Notebook / future-no-host group).
  - Migration scope handed off: real-code only — 2 new files (`NotebookEditor.ts` + `NotebookEditorView.tsx`) + 1 renamed (`NotebookEditor.tsx` → `NotebookBody.tsx`); 2 file deletes (`NotebookViewModel.ts` + `ContentViewModelHost.ts`); heavy edits to `NoteItemEditModel.ts` (~70 LOC down from ~375 — strips view-model machinery + portal refs + NoteEditorModel sub-class + compatibility properties; implements new IContentHost) and `NoteItemViewModel.ts` (adds `_embeddedEditor: EditorModel \| null` + `embeddedEditor` getter + `switchNoteEditor` method + dispose hook) and `NoteItemView.tsx` (adds per-note `<EditorSwitchWidget>` + renders `<embeddedEditor.View>`); `register-editors.ts` notebook-view registration swaps to EditorModel factory + drops `acceptFile`/`validForLanguage`/`switchOption`/`isEditorContent` quartet in favor of single `accepts()` per NB10; **NO new facade or `api/types/notebook-editor.d.ts`** per NB10. Verbatim port: `notebookTypes.ts` (NoteItem, NoteContent, NotebookData, NoteItemState), `ExpandedNoteView.tsx`, `TagsListView.tsx`, `category-tree.tsx`, `note-editor/NoteItemActiveEditor.tsx`, `note-editor/NoteItemToolbar.tsx`, `note-editor/MiniTextEditor.tsx`. Persistence migration zero per C2 — today's `.note.json` file format is **unchanged** (`data.notes[].content.editor` still stores per-note editor id; `data.state[id]` still carries per-note state); old notebook files open in the new editor with no transformation. **C4 and C5 in concerns.md are now FULLY EXERCISED** by NB6 / NB7 — they remain marked "Resolved 2026-05-19" with the original high-level resolutions, and NB6 / NB7 codify the implementation-level specifics they pointed at.

- [x] **30 — No-host editors** ([`walkthroughs/30-no-host-group.md`](walkthroughs/30-no-host-group.md)) — *Done 2026-05-20. All 25 concerns (NH1–NH10, CP1–CP5, EX1–EX10) RESOLVED. Zero mockup adjustments — eighth template-confirmation walkthrough in a row (Grid + Preview group + Log View + Link + Todo + Rest Client + Notebook + No-host group). Per user direction, covers three editors in depth (Browser / Compare / Explorer); other nine deferred for first-principles investigation at implementation time (same skip-rationale as walkthroughs 27 / 28). **First walkthrough covering three architecturally distinct shapes in a single doc.***
  - Sources: `src/renderer/editors/browser/` (BrowserEditorModel + 4 sub-models + BrowserBookmarks + BookmarksDrawer), `compare/CompareEditor.tsx`, `explorer/ExplorerEditorModel.ts` + `ExplorerSecondaryEditor.tsx` + `SearchSecondaryEditor.tsx`; consumer sites in `Pages.tsx`, `PageModel.createExplorer`, `PagesLifecycleModel.addEmptyPageWithNavPanel`
  - Scope: editors WITHOUT `CONTENT_HOST_TRAIT` — three architecturally distinct shapes covered: (1) Browser as no-host page mainEditor with embedded LinkEditor for bookmarks (second consumer of `EditorConstructorArgs.initialHost` after Notebook NB7); (2) Compare as not-an-EditorModel React component composed over two grouped TextFileModel hosts (verifies walkthrough 06 / CK1–CK10); (3) Explorer as no-host secondary-only EditorModel (second consumer of `onMainEditorChanged` hook after Link LK8, but NOT a consumer of `beforeNavigateAway` — reframes LK7 + LK8 as separable hooks). Three concern blocks: NH1–NH10 (Browser) + CP1–CP5 (Compare) + EX1–EX10 (Explorer). Other 9 editors (PDF / image / archive / video / settings / about / mcp-inspector / storybook / category) deferred — same skip-rationale as walkthroughs 27 / 28 (structurally similar to one of the three covered; investigated first-principles during implementation).
  - Resolved (2026-05-20):
    - **Browser (NH1–NH10):** NH1 (a) `BrowserQueueEvent = { focus }` only; NH2 (a) confirm no-host shape — absence of CONTENT_HOST_TRAIT IS the marker; NH3 (a+c) keep 10-field persisted boundary, promote `bookmarksWidth` to persisted (**fifth instance of `leftPanelWidth`-equivalent incidental fix** after LK2 / TD2 / RC2 / NB2); NH4 (a) embedded LinkEditor via `EditorConstructorArgs.initialHost` (NB7 pathway — Browser is **second consumer of `initialHost`** after Notebook); the `acquireViewModel/releaseViewModel/prepareViewModel/acquireViewModelSync` quartet now **fully retired across entire codebase** — Browser was final external consumer; NH5 (a) render embedded LinkEditor through `<bookmarks.linkEditor.View />` per NB7's pattern; drop `swapLayout` + portal-ref props; NH6 (a) preserve all four sub-models verbatim — sub-model boundary orthogonal to EditorModel boundary; NH7 (a) preserve lazy bookmarks lifecycle verbatim; NH8 (a) wrap return in `EditorDescriptor` (P6 mechanical envelope); NH9 (a) preserve Tor manual-reconnect flow verbatim; NH10 (a) `accepts() => -1` + drop `editorType` (S10) + drop `category: standalone` (MI3).
    - **Compare (CP1–CP5):** CP1 (a) confirm CompareEditor stays React component (CK2); CP2 (a) activation reads `pagesModel.state.compareGroups` (CK1); CP3 (a) `leftPageId` third prop + `exitCompareMode(leftPageId)` (CK4 + CK10); CP4 (a) preserve internal `CompareEditorModel` (TComponentModel) verbatim; CP5 (a) confirm walkthrough 06 / CK migration scope complete; no migration shim needed per CK9.
    - **Explorer (EX1–EX10):** EX1 (a) confirm no-host secondary-only EditorModel under unified-array (A8) — single visibility criterion covers both Link's sidebar-owning-mainEditor shape AND Explorer's sidebar-only shape; EX2 (a) constructor flips to `(state)` shape per EPIC-028 factory convention; EX3 (c) drop underscore-prefix; typed nested shape in `EditorDescriptor.state` — drops two `as any` casts; EX4 (a) persistence shape ≠ runtime shape — `getRestoreData()` is typed bridge; EX5 (a+c) **drop `beforeNavigateAway` no-op override; reframe LK7 + LK8 as separable hooks** — Explorer is second consumer of LK8 (`onMainEditorChanged`) but NOT LK7 (`beforeNavigateAway`); EX6 (a) `setPage` override fits N1 slice-subscribe lifecycle; EX7 (b) keep today's sidebar-wide close-button gesture (different from N4's per-panel close); EX8 (a) `treeProvider` typed `instanceof` chain over Link → Archive → Explorer (**three consumers of LK9 recipe across three architectural shapes**); EX9 (a) preserve `secondaryEditor = ["explorer", "search"]` array form — Explorer is **canonical multi-panel example**; EX10 (a) per EW5 — inline construction into `addEmptyPageWithNavPanel`; delete `PageModel.createExplorer` method.
  - Mockups touched: **none.** Browser lands as no-host EditorModel under existing `EditorModel<S, R, E>` mockup; Compare requires no mockup involvement (CK2 confirmed React-component status); Explorer lands under unified-array (A8) + slice-subscribe (N1) + LK9 typed-accessor + base-class default for unused hooks. **Eighth template-confirmation walkthrough in a row** (Grid + Preview group + Log View + Link + Todo + Rest Client + Notebook + No-host group). **First walkthrough covering three architecturally distinct shapes in a single doc.** Tier 5 template + IContentHost mockup + unified-array (A8) + slice-subscribe (N1) + N4 close-button + LK7/LK8 separable hooks + LK9 typed-accessor + B2 contentHost getter + PT10 switch-widget visibility now confirmed stable across: (a) nine text-bearing editors with CONTENT_HOST_TRAIT; (b) two host implementations (TextFileModel + NoteItemEditModel); (c) two switch scopes (page-level + per-note); (d) three sidebar topologies (sidebar-owning Link / non-sidebar-owning Grid-Todo-RC-Notebook / secondary-only Explorer); (e) three no-host shapes (page-mainEditor Browser / not-an-EditorModel Compare / secondary-only Explorer).
  - Net real-code surface: edited files only across three folders. **Browser:** `BrowserEditorModel.ts` (third generic + EditorDescriptor wrap + promote `bookmarksWidth`), `BrowserBookmarks.ts` (`initialHost` construction, drop `acquireViewModel/releaseViewModel`), `BookmarksDrawer.tsx` (drop portal-refs + `swapLayout`; render `<linkEditor.View />`), `register-editors.ts` (drop `editorType` + `category`; add `accepts() => -1`). **Compare:** `CompareEditor.tsx` (accept `leftPageId` prop; exit via `exitCompareMode`), `Pages.tsx` (read from `pagesModel.state.compareGroups`). **Explorer:** `ExplorerEditorModel.ts` (constructor flip, typed extras, drop no-op override), `PageModel.ts` (delete `createExplorer`), `PagesLifecycleModel.ts` (inline construction into `addEmptyPageWithNavPanel`), plus the LK9 chain update at the `findTreeProviderHost` consumer site. **Umbrella note** documents the 9 deferred editors (PDF / image / archive / video / settings / about / mcp-inspector / storybook / category) with expected fits — each pattern instance of one of the three shapes.

---

## Updates

- 2026-05-20 — **Walkthrough 30 (No-host editors) complete.** All 25 concerns (NH1–NH10, CP1–CP5, EX1–EX10) RESOLVED. **Zero mockup adjustments** — eighth template-confirmation walkthrough in a row (Grid + Preview group + Log View + Link + Todo + Rest Client + Notebook + No-host group). **First walkthrough covering three architecturally distinct shapes in a single doc** — three concern blocks under one umbrella for Browser (no-host page mainEditor with embedded LinkEditor) + Compare (not an EditorModel) + Explorer (no-host secondary-only EditorModel). **EPIC-028 design phase is now complete** — all 30 walkthroughs landed (or deferred per documented skip-rationale per walkthroughs 27 / 28). Final outcomes: **Browser (NH1–NH10):** NH1 (a) `BrowserQueueEvent = { focus }` only — Browser is ninth registered consumer of the minimal-queue pattern; NH2 (a) confirm no-host shape — absence of CONTENT_HOST_TRAIT IS the marker (the negative of presence); `contentHost` getter inherits base-class default returning `null` per B2; `findCompatibleEditors()` returns `[]` — switch widget hidden per PT10; NH3 (a) + (c) keep 10-field persisted boundary, promote `bookmarksWidth` to persisted (**fifth instance of `leftPanelWidth`-equivalent silent-today-bug incidental fix** after LK2 / TD2 / RC2 / NB2); NH4 (a) embedded LinkEditor via `EditorConstructorArgs.initialHost` (NB7 pathway) — Browser is **second consumer of `initialHost`** after Notebook NB7; **the `acquireViewModel/releaseViewModel/prepareViewModel/acquireViewModelSync` quartet is now FULLY RETIRED across the entire codebase** — Browser's `BrowserBookmarks.init()` was the final remaining external consumer outside of NoteItemEditModel (which dropped it in NB6); the chain: SF2 (walkthrough 12) declared retirement → LV9 (walkthrough 23) retired `acquireViewModelSync` at `IContentHost.ts` interface + `TextEditorModel.ts:74` implementation → NB6 (walkthrough 29) retired the quartet at `NoteItemEditModel.ts:331` + deleted `ContentViewModelHost.ts` entirely → NH4 finishes by retiring the last external consumer; NH5 (a) render `<bookmarks.linkEditor.View />`; drop `swapLayout` + portal-ref props (`toolbarRefFirst` / `toolbarRefLast` / `footerRefLast`); symmetric with NB7's per-note rendering pattern; NH6 (a) preserve all four sub-models verbatim (webview / urlBar / bookmarksUI / target — 2300 LOC) — sub-model boundary is orthogonal to EditorModel boundary; NH7 (a) preserve lazy bookmarks lifecycle verbatim — only the construction-of-LinkEditor pieces change; NH8 (a) wrap return in `EditorDescriptor` per P6 — mechanical envelope; NH9 (a) preserve Tor manual-reconnect flow verbatim (intentional UX — user chooses when to restart Tor with its perf/privacy implications); NH10 (a) `accepts({mode}) => -1` + drop `editorType: "browserPage"` (S10) + drop `category: "standalone"` (MI3). **Compare (CP1–CP5):** CP1 (a) confirm CompareEditor stays React component (CK2) — not an EditorModel; internal `CompareEditorModel` is `TComponentModel` (React-mount-scoped state) for Monaco DiffEditor lifecycle; CP2 (a) activation reads `pagesModel.state.compareGroups: Set<string>` (CK1) — Pages.tsx subscribes via `pagesModel.state.use((s) => s.compareGroups)`; `compareModeChanged` Subscription retires; `fixCompareMode` deletes (cleanup inlined per CK7); CP3 (a) `leftPageId` third prop + `pagesModel.exitCompareMode(leftPageId)` (CK4 + CK10) — replaces today's `model.setCompareMode(false); groupedModel.setCompareMode(false);`; CP4 (a) preserve internal `CompareEditorModel` (TComponentModel) verbatim; TComponentModel and EditorModel cohabit fine — different abstractions; CP5 (a) confirm walkthrough 06 / CK migration scope complete; no migration shim per CK9 (persistence dropped — main window's tray-hide design covers in-memory survival). **Explorer (EX1–EX10):** EX1 (a) confirm no-host secondary-only EditorModel under unified-array (A8) — Explorer is the **canonical example of secondary-only-EditorModel** (never has `editor.id === _mainEditorId`; only satisfies visibility criterion via `contributesPanels() === true`); single visibility criterion `(editor.id === _mainEditorId) || editor.contributesPanels()` covers both sidebar-owning and sidebar-only shapes; EX2 (a) constructor flips to `(state)` per EPIC-028 factory convention — `new ExplorerEditorModel(new TComponentState({...getDefaultExplorerEditorModelState(), rootPath: folderPath}))`; last EditorModel constructor outside the registry now aligns with the in-registry shape; EX3 (c) drop underscore-prefix; typed nested shape in `EditorDescriptor.state` — `interface ExplorerEditorModelState extends IEditorState { rootPath: string; treeState?: TreeProviderViewSavedState; selectedHref?: string | null; searchState?: FileSearchState; }`; drops two `as any` casts from today's `ExplorerEditorModel.ts:161-178`; EX4 (a) persistence shape ≠ runtime shape — `getRestoreData()` is typed bridge; runtime carriers stay as today (private fields for treeState/searchState; TOneState for selectionState/revealVersion); same pattern Grid uses for sortColumn; EX5 (a) + (c) **drop the no-op `beforeNavigateAway` override (base default suffices); reframe LK7 + LK8 as two SEPARABLE hooks** — Explorer is **second consumer of LK8** (`onMainEditorChanged`) after Link but NOT LK7 (`beforeNavigateAway`); different membership patterns (sidebar-OWNING-mainEditor for Link transitions in/out of mainEditor role → needs LK7 to clean up panels; sidebar-ONLY-EditorModel for Explorer never mainEditor; always survives → no LK7 needed); confirms LK8 hook is generic across two membership patterns; pay-only-when-used extends to per-hook granularity, not just per-feature; EX6 (a) `setPage` override fits N1 slice-subscribe lifecycle verbatim — setting `secondaryEditor` on first attach is pure state mutation; N1's slice subscription handles the rest; EX7 (b) keep today's sidebar-wide close-button gesture (PageNavigator-level) — different from N4's per-panel close affordance which applies to panel header close buttons; EX8 (a) **`treeProvider` typed `instanceof` chain over `LinkEditor → ArchiveEditor → ExplorerEditor`** — Explorer is the **third typed source** joining Link as text-bearing host + Archive as no-host sidebar-owning + Explorer as no-host secondary-only; **confirms LK9's recipe across three architectural shapes simultaneously**; the `(editor as any).treeProvider` cast retires across all consumers; EX9 (a) preserve `secondaryEditor = ["explorer", "search"]` array form — Explorer is **canonical multi-panel example** (one EditorModel publishes two panels with related lifecycles: search opens within Explorer's rootPath; closing Explorer closes search); confirms `secondaryEditor: string | string[] | undefined` array-form contract via concrete consumer; EX10 (a) per EW5 — inline 4-line construction (`new ExplorerEditorModel(state)` + `await explorer.restore()` + `page.secondaryEditors.push(explorer)` + `explorer.setPage(page)`) into `PagesLifecycleModel.addEmptyPageWithNavPanel(folderPath)`; removes `PageModel.createExplorer` method (page-lifecycle concern, not page-model concern). **Headline observations:** (1) **Browser is the second consumer of `EditorConstructorArgs.initialHost`** (after Notebook NB7) — with two consumers the canonical-injection-mechanism property is now confirmed across two distinct embedding patterns (per-note-variable in Notebook vs. drawer-fixed in Browser); the property NB7 established (host adoption at construction, not post-construction) holds across both; (2) **Explorer is the second consumer of `onMainEditorChanged` (LK8)** (after Link) — but NOT a consumer of `beforeNavigateAway` (LK7); **reframes walkthrough 24's "LK7 + LK8 recipe" as two SEPARABLE hooks**; future text-bearing editors that own a sidebar can choose any subset of LK7 + LK8 + LK9 hooks based on their membership pattern; (3) **Compare is the canonical "not an EditorModel" pattern** — CompareEditor stays a React component over two pre-existing TextFileModel hosts; documents the architectural property that not every page-shape needs to be an EditorModel — transient views over already-existing content can stay as React components when their lifecycle is tied to React mount (not page identity); (4) **Three architecturally distinct shapes in one walkthrough** — first time EPIC-028 covers three editor families in a single walkthrough; established the no-host-group pattern (umbrella walkthrough touching multiple editors with shared no-host classification but distinct internal structures: page-mainEditor / not-an-EditorModel / secondary-only-EditorModel); (5) **`acquireViewModel` quartet retires from codebase entirely** (NH4) — Browser was the final remaining external consumer; with NB6 retiring NoteItemEditModel's quartet + LV9 retiring the IContentHost interface + TextFileModel implementation + NH4 retiring the last external consumer (BrowserBookmarks), the entire ref-counted view-model machinery is now **fully gone**; (6) **Fifth instance of `leftPanelWidth`-equivalent silent-today-bug incidental fix** (NH3 (c) — `bookmarksWidth`; after LK2 → TD2 → RC2 → NB2) — pattern fully solidified; any field that lived on VM-state-but-never-persisted gets a free persistence upgrade when descriptor folding runs; (7) **LK9's typed-accessor recipe extends to three editors across three architectural shapes** (EX8) — confirms the recipe is fully general across the host/no-host axis AND the sidebar-topology axis; (8) **No-host editors are two-to-three hooks lighter than text-bearing editors** — Browser ~8 hooks, Explorer ~7 hooks (after EX5 drops no-op override), Compare 0 hooks (not an EditorModel); versus Grid/Todo/RC's 9 hooks and Link's 11; hook count cleanly tracks EditorModel surface area each editor consumes; (9) **Explorer is the canonical multi-panel example** (EX9) — `secondaryEditor: string | string[] | undefined` array-form contract documented via concrete consumer; (10) **EPIC-028 design phase is now COMPLETE** — all 30 walkthroughs landed (`[x]` for 28; `[~]` SKIPPED for walkthroughs 27 / 28 — Graph and Draw — with documented skip-rationale; investigated first-principles during implementation); ready for implementation phase; no further design-phase walkthroughs needed; (11) **Tier 5 template stability confirmed across the full editor matrix** — (a) nine text-bearing editors with CONTENT_HOST_TRAIT; (b) two host implementations (TextFileModel + NoteItemEditModel); (c) two switch scopes (page-level + per-note); (d) three sidebar topologies (sidebar-owning Link / non-sidebar-owning Grid-Todo-RC-Notebook / secondary-only Explorer); (e) three no-host shapes (page-mainEditor Browser / not-an-EditorModel Compare / secondary-only Explorer). Net real-code surface: 0 new files / 0 file deletes (Compare's `CompareEditor.tsx`, `BrowserEditorModel.ts`, `ExplorerEditorModel.ts`, `BrowserBookmarks.ts`, `BookmarksDrawer.tsx`, `Pages.tsx`, `PageModel.ts`, `PagesLifecycleModel.ts`, `register-editors.ts` all edited in place). **Walkthroughs 27 (Graph) + 28 (Draw) remain skipped for design phase — same documented skip-rationale.** Umbrella note in closure documents nine deferred no-host editors (PDF / image / archive / video / settings / about / mcp-inspector / storybook / category) with expected fits — each pattern instance of one of the three covered shapes (or Archive as the sidebar-owning no-host special case). **Next: EPIC-028 design phase closes; implementation phase opens.** No further design-phase walkthroughs queued.

- 2026-05-20 — **Walkthrough 30 (No-host editors) initial draft written.** Per user direction, covers three editors in depth (Browser / Compare / Explorer); the other nine no-host editors (PDF / image / archive / video / settings / about / mcp-inspector / storybook / category) are deferred for first-principles investigation during implementation — same skip-rationale as walkthroughs 27 / 28. Three concern blocks proposed: **NH1–NH10** for Browser, **CP1–CP5** for Compare, **EX1–EX10** for Explorer. **First walkthrough covering three architecturally distinct shapes in a single doc.** Walkthrough 30 entry in dashboard flipped `[ ]` → `[~]` (drafted). Headline proposals: (1) **Browser is the second consumer of `EditorConstructorArgs.initialHost`** (after Notebook NB7) — the bookmarks drawer's embedded LinkEditor wraps the same `TextFileModel` via `new LinkEditor({ initialHost: this.textFileHost })`; confirms the property NB7 established (host adoption at construction); applies across two distinct embedding patterns (per-note-variable in Notebook vs. drawer-fixed in Browser); (2) **Explorer is the second consumer of `onMainEditorChanged`** (after Link LK8) — but NOT a consumer of `beforeNavigateAway` (Explorer's no-op override drops per EX5 (a)); reframes walkthrough 24's "LK7 + LK8 recipe" as two SEPARABLE hooks — Explorer demonstrates LK8 without LK7 (different membership pattern: sidebar-OWNING-mainEditor for Link vs. sidebar-ONLY-EditorModel for Explorer); (3) **Compare verifies walkthrough 06 / CK1–CK10 end-to-end** — no new resolution needed; CompareEditor stays a React component per CK2 (uses internal `CompareEditorModel` (TComponentModel) for Monaco DiffEditor lifecycle); migration scope already complete in walkthrough 06 / CK; (4) **Fifth instance of `leftPanelWidth`-equivalent silent-today-bug incidental fix** (NH3 (c) — `bookmarksWidth` joins persisted set; after LK2 / TD2 / RC2 / NB2) — pattern now standardized across five Tier-5 and no-host editors; (5) **`acquireViewModel` retires from the codebase** (NH4) — Browser was the final remaining consumer outside of NoteItemEditModel (which dropped it in NB6); the `acquireViewModel/releaseViewModel/prepareViewModel/acquireViewModelSync` quartet is now fully retired across **all consumers** per the SF2 + LV9 + NB6 + NH4 chain; (6) **Three architecturally distinct shapes in one walkthrough** — first time EPIC-028 covers three editor families in a single walkthrough; established the no-host-group pattern (umbrella walkthrough touching multiple editors with shared no-host classification but distinct internal structures: page-mainEditor / not-an-EditorModel / secondary-only-EditorModel); (7) **No-host editors are two-to-three hooks lighter than text-bearing editors** — Browser ~8 hooks, Explorer ~7 hooks (after EX5 drops the no-op override), Compare 0 hooks (not an EditorModel); versus Grid/Todo/RC's 9 hooks and Link's 11; (8) **Constructor convention alignment** — Explorer's `(rootPath?: string)` constructor flips to `(state)` per EX2; matches the rest of EPIC-028's factory convention; the only no-host editor outside the registry now aligns with the in-registry shape; (9) **EX3 typed-extras decision** — drops two `as any` casts from `ExplorerEditorModel.ts:161-178`; underscore-prefix shape (`_treeState`, `_selectedHref`, `_searchState`) retires in favor of typed nested shape in `EditorDescriptor.state`; (10) **EX8 — LK9 typed-accessor chain extends to three editors** — `findTreeProviderHost` becomes typed `instanceof` chain over `LinkEditor → ArchiveEditor → ExplorerEditor`; Explorer is the third typed source; LK9's recipe now confirmed across two no-host consumers (Archive + Explorer) plus one text-bearing host (Link); (11) **EX9 — Explorer is the canonical multi-panel example** — one EditorModel publishes two panels (`["explorer", "search"]`) with related lifecycles (search inherits Explorer's rootPath; closing Explorer closes search); confirms the `secondaryEditor: string \| string[] \| undefined` array form under unified-array N1. Mockup forecast: **zero changes** — all three editors land under existing mockups (`EditorModel<S, R, E>` for Browser/Explorer; no mockup involvement for Compare per CK2). **Eighth template-confirmation walkthrough in a row** if recommendations stand (Grid + Preview group + Log View + Link + Todo + Rest Client + Notebook + No-host group) — Tier 5 template + IContentHost mockup + unified-array (A8) + slice-subscribe (N1) + N4 close-button + LK7/LK8 separable hooks + LK9 typed-accessor + B2 contentHost getter + PT10 switch-widget visibility now confirmed stable across: (a) nine text-bearing editors with CONTENT_HOST_TRAIT; (b) two host implementations (TextFileModel + NoteItemEditModel); (c) two switch scopes (page-level + per-note); (d) three sidebar topologies (sidebar-owning Link + non-sidebar-owning Grid-Todo-RC-Notebook + secondary-only Explorer); (e) three no-host shapes (page-mainEditor Browser + not-an-EditorModel Compare + secondary-only Explorer). Net real-code surface: 0 new files / 0 file deletes; edited files only across three folders. Awaiting user review concern-by-concern.

- 2026-05-20 — **Walkthrough 29 (Notebook + note-level switching) complete.** All ten concerns (NB1–NB10) RESOLVED; **zero mockup adjustments** — seventh template-confirmation walkthrough in a row (Grid + Preview group + Log View + Link + Todo + Rest Client + Notebook). **Resolves C4 (NoteItemEditModel decomposition) and C5 (per-note switch widget placement)** — the last two `Cn` concerns referencing walkthrough 29. **Ninth Tier 5 editor** in the uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient / Notebook). **First and only consumer of `switchEditorViaContentHost` at a second-level owner** — demonstrates the EPIC-028 contract is fully composable; nothing in the switch protocol requires the wrapper to be a PageModel specifically. Final outcomes: (NB1) NotebookEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — ninth Tier 5 editor in uniform shape; (NB2) state slice: 4 persisted (`leftPanelWidth` + `expandedPanel` + `selectedCategory` + `selectedTag`) / 8 ride-state stripped / 1 transient (`searchText`); **fourth instance** of `leftPanelWidth`-equivalent silent-today-bug incidental fix (LK2 → TD2 → RC2 → NB2); (NB3) fold UI selection state into `EditorDescriptor.state` — **sixth instance** of pattern at the destination (Grid GR4 → LV3 → LK3 → TD3 → RC3 → NB3); Notebook is the **new-persistence-via-descriptor variant** (no UI cache file today; selection state currently doesn't persist at all — descriptor consolidation incidentally adds restart survival); pattern now standardized across **six of nine** Tier 5 editors; (NB4) three-site lifecycle split — **fifth Tier 5 editor** (LV4 → LK4 → TD4 → RC4 → NB4); (NB5) `skipNextContentUpdate` self-write guard — **fifth instance** (LV6 → LK5 → TD5 → RC5 → NB5); pattern fully solidified; host-side `bySelf` (option b) and TOneState change-reason tracking (option c) both architecturally retired; (NB6) NoteItemEditModel decomposition **resolves C4** — lightweight transient IContentHost (~70 LOC down from ~375); strips `_vmHost: ContentViewModelHost` field + `acquireViewModel/releaseViewModel/prepareViewModel/acquireViewModelSync` methods (LV9 already retired acquireViewModelSync at IContentHost interface and TextFileModel; NB6 finishes the cleanup on NoteItemEditModel — completes LV9's "(also touched by walkthrough 29)" deferred work) + portal refs + `NoteEditorModel` Monaco sub-class + `type = "textFile"` discriminator + compatibility properties; **`src/renderer/editors/base/ContentViewModelHost.ts` deletes entirely from the codebase** — last consumer dissolves (TextFileModel ditched `_vmHost` in earlier walkthroughs; NB6 ditches NoteItemEditModel's `_vmHost`, leaving zero consumers); net codebase loses ~80 LOC of ref-counting infrastructure with no remaining consumers; (NB7) per-note embedded EditorModel + per-note switch widget **resolves C5** — each NoteItemView holds `embeddedEditor: EditorModel`; per-note switch widget reads `findCompatibleEditors()` and triggers per-note three-phase switch (`createEditor → switchFrom → restore` then replace embedded editor in NoteItemViewModel state); **first and only consumer of `switchEditorViaContentHost` at a second-level owner**; **`EditorConstructorArgs.initialHost: IContentHost \| undefined` becomes the canonical injection mechanism** — cleaner than C4's tentative `setContentHost()` separate-call shape (keeps host adoption at the only place the host can be adopted: construction); top-level page-owned editors construct without `initialHost` and create their TextFileModel inside `restore()`; nested editors construct WITH `initialHost: noteItemEditModel` and skip the construction branch; (NB8) NoteItemEditModel stateStorage backed by `notebook.data.state[id]` IN THE JSON FILE (NOT a cache file) — per-note state (Grid column widths, search filters, etc.) lives INSIDE the notebook JSON; **standalone Grid uses descriptor; nested Grid uses `notebook.data.state[noteId][name]`** — both pathways converge on durable serialization that survives restart, window transfer, and cross-machine sync; per-note state survives cross-window page transfer via M5/P6 instanceId pathway and cross-machine sync because JSON file moves whole; (NB9) three trait systems verbatim (`TraitTypeId.Note` emit + `TraitTypeId.NotebookCategory` emit+accept + `LINK` accept) — Notebook is the **richest trait consumer in Tier 5** (three active trait types vs. Link/RC's two); drag traits orthogonal to EditorModel traits; (NB10) filename `.note.json` priority 70 + content-peek priority 60 across nine Tier 5 editors; queue `{ focus }` only, request `never`; **defer scripting facade** — **second instance of deferred-facade** (after RC10); documents asymmetry as **intentional**: scripting facades are opt-in per editor, not required by the Tier 5 template. **Headline observations:** (1) Tier 5 template now confirmed stable across **nine editors** with the same ~80-LOC CONTENT_HOST_TRAIT + adoptHost + switchFrom + restore + dispose skeleton; PV1's "re-evaluate after walkthroughs 23–29" reaches its end point — common surface is provable across nine consumers; (2) **four patterns now fully standardized** across Tier 5 — per-editor cache file → descriptor.state (6 instances), self-write-guard flag (5 instances), three-site lifecycle split (5 instances), and `leftPanelWidth`-equivalent silent-today-bug incidental fix (4 instances); future text-bearing editors default to all four patterns without re-litigation; (3) **`ContentViewModelHost.ts` deletes entirely from the codebase** — the last vestige of the old content-view-model subsystem dissolves with NB6; net codebase loses the ref-counting machinery + the `acquireViewModel/releaseViewModel/prepareViewModel/acquireViewModelSync` quartet across both `IContentHost.ts` interface and both implementations; (4) **first and only consumer of `switchEditorViaContentHost` at a second-level owner** (per-note level scoped to a single note's content) — the EPIC-028 contract is now demonstrably composable; nothing requires the wrapper to be a PageModel specifically; this property unlocks future per-cell editor patterns in any aggregate editor (Notebook today; future per-row Grid cells; future per-step workflow cells) without framework changes; (5) **`EditorConstructorArgs.initialHost` becomes the canonical injection mechanism for pre-existing hosts** — supersedes C4's tentative `setContentHost()` separate-call shape; keeps host adoption at the only place the host can be adopted (construction); applies uniformly across top-level page-owned editors (no `initialHost` → host created in `restore()`) and nested per-note editors (with `initialHost` → host adoption skipped in `restore()`); (6) **scripting facade asymmetry now intentional across two instances** (RC10 + NB10) — Rest Client and Notebook both deferred facades; with two instances the pattern is documented as deliberate design property: scripting facades are opt-in per editor, not required by the Tier 5 template; (7) **Notebook is the richest trait consumer in Tier 5** — three active trait systems simultaneously (Note + NotebookCategory + LINK); documents that drag traits scale to per-editor needs without requiring framework changes; (8) **two host implementations + two switch scopes + three sidebar topologies** all confirmed stable under the same Tier 5 template — comprehensive validation of the EPIC-028 contract across the full editor matrix; (9) **C4 and C5 in concerns.md are now FULLY EXERCISED** by NB6 / NB7 — the implementation-level specifics they pointed at are now codified; concerns.md `Cn` series is fully resolved (no open Cn concerns remain). Net real-code surface: 2 new files (`NotebookEditor.ts` + `NotebookEditorView.tsx`) + 1 renamed (`NotebookEditor.tsx` → `NotebookBody.tsx`); 2 file deletes (`NotebookViewModel.ts` + `ContentViewModelHost.ts`); heavy edits to `NoteItemEditModel.ts` (~70 LOC down from ~375) and `NoteItemViewModel.ts` (adds `_embeddedEditor: EditorModel \| null` + `embeddedEditor` getter + `switchNoteEditor` method + dispose hook) and `NoteItemView.tsx` (adds per-note `<EditorSwitchWidget>` + renders `<embeddedEditor.View>`); `register-editors.ts` notebook-view registration swaps to EditorModel factory + drops `acceptFile`/`validForLanguage`/`switchOption`/`isEditorContent` quartet in favor of single `accepts()` per NB10; **NO new facade or `api/types/notebook-editor.d.ts`** per NB10. Persistence migration zero per C2 — today's `.note.json` file format is **unchanged** (`data.notes[].content.editor` still stores per-note editor id; `data.state[id]` still carries per-note state); old notebook files open in the new editor with no transformation. **Walkthrough 30 (No-host editors) is next** — first walkthrough in the no-host group; pre-check needed for each of 12 editors (PDF / image / archive / video / browser / settings / about / mcp-inspector / storybook / compare / category / explorer); Explorer is special — registered as secondary-only editor, would be the **second sidebar-owning editor** in EPIC-028 and validate Link's LK7+LK8+LK9 recipe across two consumers.

- 2026-05-20 — **Walkthroughs 27 (Graph) and 28 (Draw) SKIPPED for design phase.** User decision: both editors have structurally similar shapes to the eight prior Tier 5 editors already walked; re-litigating the standardized concerns (RC1-class uniform shape, RC3-class cache→descriptor, RC5-class self-write-guard, RC4-class three-site lifecycle, RC9-class drag traits verbatim, possibly RC7-class split-cache for Draw's embedded scene blobs) would add no design signal. Both flipped from `[ ]` → `[~]` (skipped) in the dashboard. **Will investigate first-principles during implementation** — read each editor's source end-to-end then either match existing patterns or surface novel concerns at PR time. Proceeding directly to walkthrough 29 (Notebook + note-level switching) — the design-novel walkthrough that resolves C4 and C5 (the only remaining open Cn concerns) and exercises `switchEditorViaContentHost` at a second-level owner.

- 2026-05-20 — **Walkthrough 29 (Notebook + note-level switching) initial draft written.** NB1-NB10 proposed for review. Tier 5 per-editor walkthrough — Notebook editor (`.note.json` files) **plus** the second-level per-note switch mechanism (resolves C4 + C5). **Notebook is structurally the most distinctive Tier 5 editor remaining** — it owns two `IContentHost` implementations in play simultaneously: `TextFileModel` (the `.note.json` file on disk, wrapped by NotebookEditor) and `NoteItemEditModel` (per-note in-memory host, one per note item, wrapped by an embedded EditorModel chosen per-note). **Only place in EPIC-028 where `switchEditorViaContentHost` operates at a second-level (non-page) host**, and only place where one editor (Notebook) contains many nested EditorModel instances (one per note). Headline proposals: (NB1) NotebookEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **ninth Tier 5 editor** in uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient / Notebook); non-sidebar-owning (left tree inline via `<Splitter>`); 9 hook overrides (matches Grid / Todo / RC); (NB2) state slice: 4 persisted / 8 ride-state stripped / 1 transient; `leftPanelWidth` + `expandedPanel` + `selectedCategory` + `selectedTag` silent today-bug incidentally fixed — **fourth instance of `leftPanelWidth`-equivalent incidental fix** (LK2 → TD2 → RC2 → NB2); (NB3) fold UI selection state into `EditorDescriptor.state` — **sixth instance** of pattern at the destination (Grid GR4 → LV3 → LK3 → TD3 → RC3 → NB3); Notebook is the **new-persistence-via-descriptor variant** (no UI cache file today; selection state currently doesn't persist at all — descriptor consolidation incidentally adds restart survival for free); pattern now standardized across six Tier 5 editors; (NB4) three-site lifecycle split — **fifth Tier 5 editor** in this lifecycle shape (LV4 → LK4 → TD4 → RC4 → NB4); (NB5) `skipNextContentUpdate` self-write guard — **fifth instance** of pattern (LV6 → LK5 → TD5 → RC5 → NB5); pattern fully solidified across all mutate-then-serialize editors; host-side `bySelf` parameter (option b) and TOneState change-reason tracking (option c) both **architecturally retired** as future-rejected paths; (NB6) **NoteItemEditModel decomposition — resolves C4.** Lightweight transient IContentHost (~70 LOC down from ~375); strips `_vmHost: ContentViewModelHost` field + `acquireViewModel/releaseViewModel/prepareViewModel/acquireViewModelSync` methods (LV9 already retired acquireViewModelSync at IContentHost interface and TextFileModel; NB6 finishes the cleanup on NoteItemEditModel) + portal refs + `NoteEditorModel` Monaco sub-class + `type = "textFile"` discriminator + compatibility properties (`noLanguage`, `getIcon`, `filePath`, `title`, `encrypted`, `decrypted`); implements new IContentHost interface (`setStorage` no-op + `dispose` + `getDescriptor`); **`src/renderer/editors/base/ContentViewModelHost.ts` deletes entirely from the codebase** — last consumer dissolves (TextFileModel ditched `_vmHost` in earlier walkthroughs; NB6 ditches NoteItemEditModel's `_vmHost`, leaving zero consumers); (NB7) **per-note embedded EditorModel + per-note switch widget — resolves C5.** Each NoteItemView holds `embeddedEditor: EditorModel` (Monaco / Grid / Markdown / Mermaid / SVG / Html / etc.) wrapping the per-note NoteItemEditModel; per-note switch widget (NoteItemView-local) reads `embeddedEditor.findCompatibleEditors()` for the option list and on user click triggers per-note three-phase switch (`createEditor → switchFrom → restore` then replace embedded editor in NoteItemViewModel state); **first and only consumer of `switchEditorViaContentHost` at a second-level (non-page) owner** — demonstrates that the EPIC-028 contract is fully composable; nothing in the switch protocol requires the wrapper to be a PageModel specifically; `EditorConstructorArgs.initialHost: IContentHost \| undefined` becomes the **canonical injection mechanism** (cleaner than C4's tentative `setContentHost()` separate-call shape — keeps host adoption at the only place the host can be adopted: construction); top-level page-owned editors construct without `initialHost` and create their TextFileModel inside `restore()`; nested editors construct WITH `initialHost: noteItemEditModel` and skip the construction branch; (NB8) NoteItemEditModel stateStorage backed by `notebook.data.state[id]` IN THE JSON FILE (NOT a cache file) — per-note state (Grid column widths, search filters, etc.) lives INSIDE the notebook JSON; **standalone Grid uses descriptor; nested Grid uses `notebook.data.state[noteId][name]` — both pathways converge on durable serialization that survives restart, window transfer, and cross-machine sync**; per-note state survives cross-window page transfer (M5/P6 instanceId pathway) and cross-machine sync (JSON file moves whole); cache file would orphan when notebook moves; (NB9) three trait systems verbatim — **richest trait consumer in Tier 5** (`TraitTypeId.Note` emit + `TraitTypeId.NotebookCategory` emit+accept + `LINK` accept); drag traits orthogonal to EditorModel traits; documents the architectural property that drag traits scale to per-editor needs without requiring framework changes; (NB10) filename `.note.json` priority 70 + content-peek priority 60 (mirrors LV10/GR10/LK10/TD10/RC10 across nine Tier 5 editors); queue `{ focus }` only, request `never`; **defer scripting facade** — **second instance of deferred-facade** (after RC10); documents asymmetry as intentional: scripting facades are opt-in per editor, not required by the Tier 5 template; adding `NotebookEditorFacade.ts` is mechanical 4-file touch deferred until a real consumer lands. Mockup forecast: **zero changes** — `EditorModel.ts` mockup unchanged (`EditorConstructorArgs.initialHost` field already exists from walkthrough 04 / P6); `IContentHost.ts` mockup unchanged (the post-NB6 NoteItemEditModel exactly implements the sketched interface); `editorRegistry.ts` mockup unchanged (`findEditorsAccepting(host)` works at both page-level and note-level scope). **Seventh template-confirmation walkthrough in a row** (Grid + Preview group + Log View + Link + Todo + Rest Client + Notebook) — Tier 5 template + IContentHost mockup now confirmed stable across: (a) nine editors with `CONTENT_HOST_TRAIT`; (b) two host implementations (TextFileModel + NoteItemEditModel); (c) two switch scopes (page-level + per-note); (d) three sidebar topologies (sidebar-owning Link / non-sidebar-owning Grid-Todo-RC-Notebook / future-no-host walkthrough 30 group). Net real-code surface: 2 new files (`NotebookEditor.ts` + `NotebookEditorView.tsx`) + 1 renamed (`NotebookEditor.tsx` → `NotebookBody.tsx`); 2 file deletes (`NotebookViewModel.ts` + `ContentViewModelHost.ts`); heavy edits to `NoteItemEditModel.ts` (~70 LOC down from ~375) and `NoteItemViewModel.ts` (adds `_embeddedEditor` + `switchNoteEditor` + dispose hook) and `NoteItemView.tsx` (adds per-note `<EditorSwitchWidget>`); `register-editors.ts` notebook-view registration swaps to EditorModel factory + drops quartet predicates in favor of single `accepts()`; **NO new facade or `api/types/notebook-editor.d.ts`** per NB10. Persistence migration zero per C2 — today's `.note.json` file format is **unchanged** (`data.notes[].content.editor` still stores per-note editor id; `data.state[id]` still carries per-note state); old notebook files open in the new editor with no transformation. Walkthrough 30 (No-host editors) is next — first walkthrough in the no-host group; pre-check needed for each of 12 editors (PDF / image / archive / video / browser / settings / about / mcp-inspector / storybook / compare / category / explorer); Explorer is special — registered as secondary-only editor, would be the **second sidebar-owning editor** in EPIC-028 and validate Link's LK7+LK8+LK9 recipe across two consumers. Awaiting user review concern-by-concern.

- 2026-05-20 — **Walkthrough 26 (Rest Client) complete.** All ten concerns (RC1–RC10) RESOLVED; **zero mockup adjustments** — sixth template-confirmation walkthrough in a row (Grid + Preview group + Log View + Link + Todo + Rest Client). **Eighth Tier 5 editor** in the uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient); **third non-sidebar-owning** Tier 5 editor (after Grid and Todo) — confirms non-sidebar-owning is the modal shape, sidebar-owning (Link) remains the outlier. **First instance of "split cache-file consolidation by scale" pattern** (RC7) — establishes guidance for future editors with mixed payload sizes. Final outcomes: (RC1) RestClientEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — eighth Tier 5 editor in uniform shape; (RC2) state slice: 2 persisted (`leftPanelWidth` + `selectedRequestId`) / 4 ride-state stripped (`data`/`error`/`response`/`responseTime`) / 2 transient (`executing` + `headersJsonInvalid`) / 3 private; `leftPanelWidth` silent today-bug incidentally fixed — **third instance of this incidental fix** (after Link LK2 / Todo TD2); (RC3) **fifth instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3 → Rest Client RC3); pattern now standardized across **five of seven** persisted-Tier-5 editors; today's `restoreSelectionState` + `saveSelectionState` + `selectionRestored` + `static cacheName` all retire; (RC4) three-site lifecycle split mirrors LV4 / LK4 / TD4 — **fourth Tier 5 editor** in this lifecycle shape; incidentally fixes today's lost-response-save bug (dispose now flushes saveResponseCache too); (RC5) **fourth instance** of self-write-guard pattern (LV6 → LK5 → TD5 → RC5) — pattern now **rock-solid standardized** across all mutate-then-serialize editors; with four instances the host-side `bySelf` parameter (option b) and TOneState change-reason tracking (option c) are now both retired as future-rejected paths; (RC6) Rest Client is NOT sidebar-owning — pre-check confirmed; LK7/LK8/LK9 recipe does NOT apply; override count 9 hooks (same as Todo, vs. Link's 11); the 9/9 hook stability across Grid / Todo / RestClient documents pay-only-when-used as a **structural** property, not an implementation detail; (RC7) **first instance** of "split cache-file consolidation by scale" pattern — small UI/selection state (≤ 1KB) folds into descriptor; large payload state (bytes to MBs per HTTP response body) stays in a separate per-editor cache file keyed by host id; folding into descriptor (b) would blow M9's 50KB-per-page budget at the first JSON-API response; today's binary-exclusion gate stays load-bearing (`if (!isBinary)` prevents PDF/image responses hitting hundreds of MB on disk); cross-window survival comes for free because the cache file is keyed by `<host.id>`; establishes pattern guidance for future editors with mixed payload sizes (Notebook + cell outputs, possibly Graph + simulation snapshots); (RC8) `ui.confirm`/`ui.notify` split preserved verbatim — same shape as TD8; (RC9) `TraitTypeId.RestRequest` + `LINK` trait both preserved verbatim — Rest Client accepting cross-editor link drops from PageNavigator is the **canonical example** of the trait system paying its keep; the trait abstraction earns its complexity here; (RC10) filename `.rest.json` priority 70 + content-peek priority 60 across eight Tier 5 editors; minimal queue `{ focus }`, request `never`; **defer scripting facade** — Rest Client stays the only text-bearing Tier 5 editor without a `XxxEditorFacade.ts` + `asX()` accessor; documents deliberate asymmetry. **Headline observations:** (1) Tier 5 template carries cleanly across **eight editors** with the same ~80-LOC CONTENT_HOST_TRAIT + adoptHost + switchFrom + restore + dispose skeleton (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient); PV1's "re-evaluate after walkthroughs 23–29" continues to apply — common surface keeps growing more provable; (2) **three patterns now fully standardized** across Tier 5 — per-editor cache file → descriptor.state (5 instances), self-write-guard flag (4 instances), and three-site lifecycle split (4 instances); the next text-bearing editors should default to all three patterns without re-litigation; (3) **new pattern surfaced and resolved this walkthrough** — RC7's split-cache-file consolidation by scale; not a refinement of the existing cache→descriptor pattern but an orthogonal axis: *small UI state → descriptor; bulk payload state → cache file keyed by host id*; needed because the descriptor's 50KB budget can't hold MB-scale HTTP responses, but the cache file mechanism scales naturally; documents the editor-internal policy approach (binary-exclusion gate stays editor-owned) without forcing framework support; (4) **the 9/9 hook stability across Grid / Todo / RestClient** documents pay-only-when-used as a structural property — three non-sidebar-owning editors with identical override count, two-hook delta vs. Link is the entire architectural difference; (5) **scripting facade asymmetry documented as intentional** — Rest Client stays the only text-bearing Tier 5 editor without a facade; Tier 5 template doesn't require one; adding later is mechanical; future readers see "editor template uniform, facades opt-in"; (6) **`selectionRestored` one-shot flag retirement is now third instance** (Link LK3 → Todo TD3 → Rest Client RC3) — pattern is fully general: any "one-shot async cache restore" guard retires when its cache file folds into descriptor; (7) **async HTTP execution lifecycle (sendRequest)** carries verbatim under EPIC-028 with `nodeFetch` + binary file streaming via `fs.createReadStream` + multipart builder dynamic-import + base64 binary response encoding + responseCache write with binary-exclusion gate; setting demonstrates that significant editor-internal async lifecycles fit cleanly inside the Tier 5 template without framework support beyond the existing CONTENT_HOST_TRAIT shape. Net real-code surface: 2 new files (`RestClientEditor.ts` + `RestClientEditorView.tsx`) + 1 renamed (`RestClientEditor.tsx` → `RestClientBody.tsx`); 1 file deletes (`RestClientViewModel.ts`); 3 component files (`RequestBuilder.tsx` + `ResponseViewer.tsx` + `KeyValueEditor.tsx`) flip `vm: RestClientViewModel` → `model: RestClientEditor`; `register-editors.ts` factory swap + drop quartet predicates in favor of single `accepts()`; **NO new facade or declaration file** per RC10; `open-in-rest-client.ts` + `multipartBuilder.ts` + `parseClipboardRequest.ts` + `serializeRequest.ts` + `httpConstants.ts` + `restClientTypes.ts` all verbatim; `content/resolvers.ts` + `tree-context-menus.tsx` unchanged. Persistence migration zero per C2 + P2 (today's `<host.id>:rest-client` selection cache orphans gracefully per P9; today's `<host.id>:rest-client-responses` response cache survives forward verbatim — cache name stays the same, host id propagates via M5/P6's `instanceId` pathway). **Walkthrough 27 (Graph) is next** — pre-check needed to determine whether Graph follows the no-host topology (would land in walkthrough 30's no-host group) or has retrofitted a TextFileModel host since EPIC-012's content-pipe rollout. If host-backed, Graph would become the **ninth Tier 5 editor** in the uniform shape and a possible second consumer of RC7's split-cache pattern (simulation snapshots could ride the bulk-payload cache file).

- 2026-05-20 — **Walkthrough 26 (Rest Client) initial draft written.** RC1–RC10 proposed for review. Tier 5 per-editor walkthrough — Rest Client editor (`.rest.json` files). **Pre-check confirmed Rest Client is NOT sidebar-owning** — `register-editors.ts:424-458` has no `secondaryEditor` field; `secondary-editor-registry.ts` grep for `rest-client`/`RestClient` returns zero matches; `RequestTree` is composed inline inside `RestClientEditor.tsx` via `<Splitter>` exactly as Todo composes `TodoListPanel`. So Rest Client lands in the **non-sidebar-owning shape** (eighth Tier 5 editor in uniform shape; third non-sidebar-owning after Grid and Todo). Headline proposals: (RC1) RestClientEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **eighth Tier 5 editor** in uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo / RestClient); (RC2) state slice: 2 persisted (`leftPanelWidth` + `selectedRequestId`) / 4 ride-state stripped (`data`/`error`/`response`/`responseTime`) / 2 transient (`executing` + `headersJsonInvalid`) / 3 private; `leftPanelWidth` silent today-bug incidentally fixed — **third instance** of this incidental fix (after Link LK2 / Todo TD2); (RC3) fold selection-state cache into `EditorDescriptor.state` — **fifth instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3 → Rest Client RC3); pattern now standardized across five of seven persisted-Tier-5 editors; today's `restoreSelectionState` + `saveSelectionState` + `selectionRestored` + `static cacheName` all retire; (RC4) three-site lifecycle split mirrors LV4 / LK4 / TD4 — **fourth Tier 5 editor** in this lifecycle shape; incidentally fixes today's lost-response-save bug (dispose now flushes saveResponseCache too); (RC5) keep `skipNextContentUpdate` editor-private flag — **fourth instance** of self-write-guard pattern (LV6 → LK5 → TD5 → RC5); pattern now rock-solid standardized across all mutate-then-serialize editors; (RC6) Rest Client is NOT sidebar-owning — pre-check confirmed; `RequestTree` stays inline inside `RestClientBody`; no `setSidebarPanels`, no `beforeNavigateAway`, no `onMainEditorChanged`; override count 9 hooks (same as Todo, vs. Link's 11); third non-sidebar-owning Tier 5 editor (after Grid and Todo) — confirms non-sidebar-owning is the modal shape; (RC7) keep response cache as separate per-editor cache file — **first instance of "split cache-file consolidation by scale"** pattern; size envelope 3+ orders of magnitude larger than selection state (bytes to MBs per HTTP response body); folding into descriptor would blow M9's 50KB-per-page budget at the first JSON-API response; today's binary-exclusion gate stays load-bearing; establishes pattern guidance for future editors with mixed payload sizes (Notebook outputs, possibly Graph snapshots); (RC8) `ui.confirm`/`ui.notify` split (view does confirms; model does notify-on-validation) preserved verbatim — same shape as TD8; (RC9) `TraitTypeId.RestRequest` (emit within-tree) + `LINK` trait (accept cross-editor link drops) preserved verbatim — drag traits orthogonal to EditorModel traits; LINK accept is the **canonical example** of the trait system paying its keep — Rest Client accepting cross-editor link drops from PageNavigator is the load-bearing use case justifying the trait abstraction; (RC10) filename `.rest.json` priority 70 + content-peek (`"type":"rest-client"` AND `"requests"`) priority 60 (mirrors LV10/GR10/LK10/TD10 calibration across eight editors); minimal queue `{ focus }`, request `never`; **defer scripting facade** — adding `RestClientEditorFacade.ts` + `asRestClient(force?)` is mechanical 4-file touch deferred until a real consumer lands; documents deliberate asymmetry: Rest Client is the only text-bearing Tier 5 editor without a facade. Mockup forecast: **zero changes** — Tier 5 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) carries RestClientEditor end-to-end; the split-cache pattern (RC7) is editor-internal policy using existing per-editor cache-file API; no new framework primitive needed. Net real-code surface: new `RestClientEditor.ts` + new `RestClientEditorView.tsx`; today's `RestClientEditor.tsx` renames to `RestClientBody.tsx`; `RestClientViewModel.ts` deletes; 3 component files (`RequestBuilder.tsx` + `ResponseViewer.tsx` + `KeyValueEditor.tsx`) flip `vm: RestClientViewModel` → `model: RestClientEditor`; `register-editors.ts` rest-client registration swaps to EditorModel factory + drops quartet predicates; **no new facade or declaration file** per RC10; `open-in-rest-client.ts` + `multipartBuilder.ts` + `parseClipboardRequest.ts` + `serializeRequest.ts` + `httpConstants.ts` + `restClientTypes.ts` all verbatim. Persistence migration zero per C2 + P2 (today's `<host.id>:rest-client` selection cache orphans gracefully per P9; today's `<host.id>:rest-client-responses` response cache survives forward verbatim since cache name stays the same and host id propagates via M5/P6's `instanceId` pathway). Walkthrough 27 (Graph) is next — pre-check needed to determine whether Graph follows no-host topology (would land in walkthrough 30's no-host group) or has a TextFileModel host (would make it the ninth Tier 5 editor).

- 2026-05-20 — **Walkthrough 25 (Todo) complete.** All ten concerns (TD1–TD10) RESOLVED; **zero mockup adjustments** — fifth template-confirmation walkthrough in a row (Grid + Preview group + Log View + Link + Todo). **First non-sidebar-owning Tier 5 editor since walkthrough 23 (Log View)** — confirms the Tier 5 template covers both sidebar topologies uniformly with a single shell and pay-only-when-used lifecycle hooks. Final outcomes: (TD1) TodoEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **seventh Tier 5 editor** in uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo); (TD2) state slice: 3 persisted / 4 ride-state stripped / 1 transient / 4 private; `leftPanelWidth` silent today-bug incidentally fixed — **second instance of this incidental fix** (after Link LK2); (TD3) **fourth instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3) — pattern now firmly standardized across half of Tier 5; today's `restoreSelectionState` + `saveSelectionState` + `selectionRestored` + `static cacheName` all retire; (TD4) three-site lifecycle split mirrors LV4 / LK4 — third Tier 5 editor in this lifecycle shape; (TD5) **third instance** of self-write-guard pattern (LV6 → LK5 → TD5) — pattern now firmly standardized across all mutate-then-serialize editors; (TD6) **calibration finding** — walkthrough 24's closure pointer to walkthrough 25 as "second sidebar-owning editor" was incorrect; Todo registers NO secondary editors and renders TodoListPanel inline (confirmed by reading `register-editors.ts:388-422` + `secondary-editor-registry.ts` grep + `TodoEditor.tsx` render tree); the LK7 / LK8 / LK9 recipe (`beforeNavigateAway` + `onMainEditorChanged` + TreeProvider integration) explicitly does NOT apply; LK recipe stays at one example (Link) until a true second sidebar-owner lands; override count 9 hooks (vs. Link's 11) — the two-hook delta documents the "pay-only-when-used" property of `beforeNavigateAway` + `onMainEditorChanged` concretely; (TD7) per-item heights (`data.state[id].contentHeight`) stay in JSON file — mirrors Link's `data.state` per-collection split (item heights are item properties not window properties); (TD8) `ui.confirm` / `ui.notify` direct calls from model mutators preserved — `ui` is app-level by design; EPIC-028's split is about FILE/EDITOR state ownership not UI primitive access; script API symmetry via `skipConfirm=true` parameter preserved; (TD9) `TraitTypeId.TodoItem` drag system preserved verbatim — **drag traits orthogonal to EditorModel traits** (drag traits describe DATA shapes; editor traits describe CAPABILITY shapes); orthogonality now documented as design contract for future editors with reorderable lists (Notebook, possibly Graph); (TD10) filename `.todo.json` priority 70 + content-peek priority 60 (mirrors LV10/GR10/LK10 calibration across seven editors); minimal queue `{ focus }` only, request `never`. **Headline observations:** (1) Tier 5 template carries cleanly across **both sidebar topologies** — sidebar-owning (Link with 11 overrides) and non-sidebar-owning (Todo with 9 overrides); the two-hook delta is the entire difference; (2) **two patterns now fully standardized** across Tier 5 — per-editor cache file → descriptor.state (4 instances) and self-write-guard flag (3 instances); the next text-bearing editors should default to these patterns without re-litigation; (3) Tier 5 class repetition count grows to **seven editors** with the same ~80-LOC CONTENT_HOST_TRAIT + adoptHost + switchFrom + restore + dispose skeleton; PV1's "re-evaluate after walkthroughs 23–29" continues to apply — common surface keeps growing more provable; (4) **calibration finding from walkthrough 24's closure** caught and resolved at first re-read; demonstrates the value of first-principles per-editor verification (reading `register-editors.ts` + `secondary-editor-registry.ts` + the editor's render tree) rather than carrying over predictions from prior walkthroughs; (5) `selectionRestored` one-shot flag retires — **second instance** of this retirement (after Link LK3); confirms the pattern is general (any "one-shot async cache restore" guard retires when its cache file folds into descriptor). Net real-code surface: 2 new files (`TodoEditor.ts` + `TodoEditorView.tsx`) + 1 renamed (`TodoEditor.tsx` → `TodoBody.tsx`); 1 file deletes (`TodoViewModel.ts`); 2 component files (`TodoListPanel.tsx` + `TodoItemView.tsx`) flip prop type `pageModel: TodoViewModel` → `model: TodoEditor`; `register-editors.ts` factory swap + drop quartet predicates in favor of single `accepts()`; `TodoEditorFacade.ts` flips from VM-wrap to EditorModel-wrap + adds `force?: boolean` per SF1; existing `api/types/todo-editor.d.ts` updated for `page.asTodo(force?)`. Persistence migration zero per C2 + P2 (today's `<host.id>-todo-editor.txt` cache files orphan gracefully per P9). **Walkthrough 26 (Rest Client) is next** — before drafting, pre-check whether Rest Client's collection panel is sidebar-registered (would make it a second sidebar-owner and validate the LK6+LK7+LK8+LK9 recipe across editors) or rendered inline (would make it the eighth Tier 5 editor in Todo's non-sidebar-owning shape).
- 2026-05-20 — **Walkthrough 25 (Todo) initial draft written.** TD1–TD10 proposed for review. Tier 5 per-editor walkthrough — Todo editor (`.todo.json` files). **Topology correction:** walkthrough 24's closure pointer to walkthrough 25 as "second sidebar-owning editor" was wrong. Reading `register-editors.ts:388-422` (no `secondaryEditor` field) + `secondary-editor-registry.ts` (zero `todo` matches) + `TodoEditor.tsx` (TodoListPanel composed inline via `<TodoListPanel pageModel={vm} />`) confirms Todo registers NO secondary editors and renders its lists/tags panel inline inside the editor body. So the LK7 / LK8 / LK9 recipe (`beforeNavigateAway` + `onMainEditorChanged` + TreeProvider integration) does NOT apply here — TD6 explicitly flags this as a calibration finding; the recipe stays at one example (Link) until a true second sidebar-owner lands (likely walkthrough 30 — Archive + Explorer in the no-host group). Headline proposals: (TD1) `TodoEditor` IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **seventh Tier 5 editor** in uniform shape (Monaco / Grid / Markdown / Mermaid / LogView / Link / Todo); (TD2) state slice: 3 persisted (`leftPanelWidth` + `selectedList` + `selectedTag`) / 4 ride-state stripped (`data`/`error`/`listCounts`/`filteredItems`) / 1 transient (`searchText`) / 4 private; `leftPanelWidth` silent today-bug incidentally fixed (matches LK2); (TD3) fold selection-state cache into `EditorDescriptor.state` — **fourth instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3 → Todo TD3); pattern now firmly standardized across half of Tier 5; today's `restoreSelectionState` + `selectionRestored` one-shot guard + `static cacheName = "todo-editor"` all retire; (TD4) three-site lifecycle split mirrors LV4 / LK4 — third Tier 5 editor in this lifecycle shape; (TD5) keep `skipNextContentUpdate` editor-private flag (verbatim port) — **third instance** of self-write-guard pattern (Log View LV6 → Link LK5 → Todo TD5); pattern now firmly standardized; (TD6) Todo is NOT sidebar-owning — corrects walkthrough 24's closure prediction; `TodoListPanel` stays inline inside `TodoBody`; no `setSidebarPanels`, no `beforeNavigateAway` override, no `onMainEditorChanged` override; override count 9 hooks (vs. Link's 11) documents the "pay-only-when-used" property concretely; (TD7) per-item heights stay in JSON `data.state[id].contentHeight` (mirrors Link's `data.state` per-collection split — item heights are item properties not window properties); (TD8) `ui.confirm` / `ui.notify` direct calls from model mutators preserved (script API uses `skipConfirm=true`); `ui` is app-level, EPIC-028's split is about FILE / EDITOR state ownership not UI primitives; (TD9) `TraitTypeId.TodoItem` drag system preserved verbatim — drag traits orthogonal to EditorModel traits (drag traits describe DATA shapes; editor traits describe CAPABILITY shapes); (TD10) filename `.todo.json` priority 70 + content-peek priority 60 (mirrors LV10/GR10/LK10 calibration); queue `{ focus }` only, request `never`. Mockup forecast: **zero changes** — Tier 5 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) carries TodoEditor end-to-end across the **first non-sidebar-owning Tier 5 editor since walkthrough 23 (Log View)** — confirms the template covers both sidebar topologies with a uniform shell. Net real-code surface: new `TodoEditor.ts` (class + TodoEditorState + TodoQueueEvent) + new `TodoEditorView.tsx`; today's `TodoEditor.tsx` renames to `TodoBody.tsx`; `TodoViewModel.ts` deletes; `components/TodoListPanel.tsx` + `components/TodoItemView.tsx` flip `pageModel: TodoViewModel` → `model: TodoEditor`; `register-editors.ts` todo-view registration swaps to EditorModel factory + drops `acceptFile`/`validForLanguage`/`switchOption`/`isEditorContent` quartet in favor of single `accepts()` per TD10; `TodoEditorFacade.ts` flips from VM-wrap to EditorModel-wrap + adds `force?: boolean` per SF1; existing `api/types/todo-editor.d.ts` updated for `page.asTodo(force?)`. Persistence migration zero per C2 + P2 (today's `<host.id>-todo-editor.txt` orphans gracefully per P9). Walkthrough 26 (Rest Client) is next — pre-check whether Rest Client's collection panel is sidebar-registered (would make it a second sidebar-owner and validate the LK6+LK7+LK8+LK9 recipe) or rendered inline (would make it the eighth Tier 5 editor in the same shape as Todo). Awaiting user review concern-by-concern.
- 2026-05-20 — **Walkthrough 23 (Log View) complete.** All ten concerns (LV1–LV10) RESOLVED; **zero mockup adjustments** — third template-confirmation walkthrough in a row (Grid + Preview group + Log View). Tier 5 template stability now confirmed across the **most divergent axis examined yet**: complex (Monaco: ITextModel + selection queries + ComponentQueue request/reply) → medium (Grid: AVGrid + variant-multiplexing + two-way sortColumn sync) → light (Preview: three of four near-empty state slices) → **append-only Promise-based** (Log View: JSONL self-write loop + Promise-based dialog handshake + the only model-side Promise lifecycle in EPIC-028). The eight-piece template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) slots into LogView cleanly despite its unique characteristics. Headline resolutions: (LV1) `LogViewEditor` IS the page mainEditor with TextFileModel as content host + CONTENT_HOST_TRAIT exposed — fifth editor in this Tier 5 shape after Monaco / Grid / Markdown / Mermaid; preserves switch-to-Monaco (view raw JSONL); reuses TextFileModel's file/pipe/save-restore machinery (~200 LOC saved over no-host); rejected no-host + hybrid-internal alternatives; (LV2) persist `showTimestamps` + `itemsState` only; `entries`/`entryCount`/`error` ride state for reactivity but get stripped from getRestoreData per MO5/GR8 (all recomputable from host content on restore); six private fields (pendingDialogs / nextId / skipNextContentUpdate / lastLineCount / heightCache / dirtyIndices) stay private; `forceScrollVersion` retires per LV5; (LV3) `itemsState` folds into `EditorDescriptor.state` — mirrors Grid GR4's identical decision; size envelope acceptable (<1KB typical, ~50KB worst plausible within M9 budget); dedicated `<host.id>-log-view-items.json` cache file eliminated; **second instance of "per-editor cache file → descriptor.state" consolidation pattern** (Grid GR4 → Log View LV3); (LV4) JSONL parse/serialize hooks split three ways — `restore()` does initial parse via `loadContent`, `adoptHost` content subscription drives incremental re-parse via `loadContentIncremental` (LV6-guarded), `dispose()` cancels pending dialogs + clears dirtyIndices; preserves today's incremental fast-path (avoids re-parsing entire JSONL on every appendEntry); (LV5) `forceScrollVersion` state-counter retires in favor of `LogQueueEvent.scrollToBottom` queue event — **third instance of "state counter bump → queue event" pattern** (MO4 revealLine, GR10 focusCell, LV5 scrollToBottom); view's `queue.use` handler drains and fires `scheduleScrollToBottom`; removes a "state field that exists only to bump" smell; (LV6) `skipNextContentUpdate` editor-private flag preserved (today's pattern works; race is editor-internal); **most editor-specific mechanism in EPIC-028 so far** — every other editor either reads host content as ground truth or doesn't write to it; LogView's bidirectional dance (editor mutates entries → serializes to JSONL → writes to host → subscription would re-parse if not guarded) is unique to append-only editors; (LV7) verbatim port of pendingDialogs cancellation on dispose (today's `onDispose:73-80` iterates `pendingDialogs` and resolves each with sentinel canceled-button entry `{type: "", id, timestamp: 0}`); uniform across page-close AND switch-out (switch-out is a deliberate user gesture; the "switching destroys editor state" contract is uniform across Tier 5); **only model-side Promise lifecycle in EPIC-028 Tier 5** — today's sentinel-resolve approach preserves the script-side `await ui.dialog.confirm(...)` contract uniformly across all dispose scenarios; sets the pattern for future Promise-based interactive editors; (LV8) minimal queue event union: `{type: "focus"} | {type: "scrollToBottom"}`; queue request = `never` (all UiFacade reads are sync against editor.state); rejected proactive `scrollToEntry` / `highlightEntry` events per YAGNI; (LV9) **final SF2 retirement step** — `acquireViewModelSync` machinery fully retires across the codebase as of this walkthrough; declaration deletes from `IContentHost.ts:56` + implementations from `TextEditorModel.ts:74` + `NoteItemEditModel.ts:331` (also touched by walkthrough 29 for NoteItemEditModel); `loadViewModelFactory("log-view")` async pre-load line deletes from `ScriptRunner.ts:108`; UiFacade constructor accepts `LogViewEditor` directly; `executeUiOnPage` becomes async per EW2 (single `await` per `addEditorPage` call); `releaseList.push` retires per SF2; six wrapper helpers (StyledTextBuilder, Progress, Grid, Text, Markdown, Mermaid) flip constructor arg type with body preserved; (LV10) `accepts({host, fileName, language})` predicate: filename `.log.jsonl` priority 70 strong match + content-peek `"type"\s*:\s*"log\."` against `host.state.content` priority 60 fallback (drives switch-widget visibility for `.jsonl` files generated by scripts containing log entries — replaces today's `isEditorContent` path); mode-agnostic. Class name finalizes as `LogViewEditor` (not MI4's `LogViewEditorModel` placeholder) — matches Monaco/Grid/Markdown naming; today's `LogViewEditor.tsx` React component file renames to `LogBody.tsx` per the Tier 5 template (no name collision because `LogViewEditor.ts` is a class file). Net real-code surface: 2 new files (`LogViewEditor.ts` + `LogViewEditorView.tsx`) + 1 renamed (`LogViewEditor.tsx` → `LogBody.tsx`); 1 file deletes (`LogViewModel.ts`); 16 view files (`LogViewContext`, `LogEntryWrapper`, `LogMessageView`, `LogEntryContent`, `StyledTextView`, `items/*`) carry over verbatim with type rename `LogViewModel` → `LogViewEditor`; `register-editors.ts` factory swap + drop quartet predicates in favor of single `accepts()`; `UiFacade.ts` + 6 wrapper helpers flip constructor arg type; `ScriptContext.ts` async-ifies + drops releaseList push + `instanceof LogViewEditor` replaces dual predicates; `ScriptRunner.ts:108` line deletes; `mcp-handler.ts` three callsites flip to direct instanceof; `IContentHost.ts:56` declaration deletes; `TextEditorModel.ts:74` + `NoteItemEditModel.ts:331` implementations delete (final SF2 cleanup); persistence migration zero per C2 + P2. **Walkthrough 24 (Link) is next** — first sidebar-owning editor in Tier 5; introduces `beforeNavigateAway` / `onMainEditorChanged` lifecycle hook exercise (deferred from walkthrough 03); resolves today's "demote survives as secondary" bug.
- 2026-05-20 — **Walkthrough 23 (Log View) initial draft written.** LV1–LV10 proposed for review. Tier 5 per-editor walkthrough; Log View is unique in the Tier 5 set so far — **append-only** (entries materialize from JSONL lines parsed out of host content) and **interactive** (dialog entries return Promises that resolve when the user clicks a button — the only editor in EPIC-028 with a model-side Promise-based handshake to the user). It is also the final consumer of `acquireViewModelSync` machinery — 4 callsites across 2 files (3 in `mcp-handler.ts` per MI4, 1 in `ScriptContext.ts:249`); walkthrough 23 retires the `acquireViewModelSync` declaration on `IContentHost` and its implementations on `TextFileModel` + `NoteItemEditModel` as part of SF2's final cleanup. Headline proposals: (LV1) `LogViewEditor` IS the page mainEditor with TextFileModel as content host + CONTENT_HOST_TRAIT exposed — fifth editor in Tier 5 with this shape (Monaco / Grid / Markdown / Mermaid / Log View); preserves switch-to-Monaco (view raw JSONL) and reuses TextFileModel's file/pipe/save-restore machinery; rejected no-host + hybrid-internal alternatives; (LV2) only `showTimestamps` + `itemsState` persist via getRestoreData; `entries / entryCount / error` ride state for reactivity but get stripped (recomputed from host content on restore); six private fields stay private; `forceScrollVersion` retires per LV5; (LV3) `itemsState` folds into `EditorDescriptor.state` — mirrors Grid GR4 decision (size envelope <1KB typical, ~50KB worst plausible within M9 budget); dedicated `<host.id>-log-view-items.json` cache file eliminated; second instance of "per-editor cache file → descriptor.state" consolidation after Grid; (LV4) JSONL parse/serialize hooks split three ways — `restore()` does initial parse, `adoptHost` content subscription drives incremental re-parse (LV6-guarded), `dispose()` cancels pending dialogs; preserves today's `loadContentIncremental` fast-path (avoids re-parsing entire JSONL on every appendEntry); (LV5) `forceScrollVersion` state-counter retires in favor of `LogQueueEvent.scrollToBottom` queue event — third instance of "state counter bump → queue event" pattern after MO4 (revealLine) + GR10 (focusCell); view's `queue.use` handler drains and fires `scheduleScrollToBottom`; removes a "state field that exists only to bump" smell; (LV6) `skipNextContentUpdate` editor-private flag preserved (today's pattern works — race is editor-internal; alternatives `bySelf` flag on host.changeContent or TOneState change-reason tracking both leak the concern into TextFileModel for one consumer); (LV7) pendingDialogs cancellation on dispose — verbatim port from today's `onDispose:73-80` (MI7 already confirmed for MCP layer; LV7 surfaces and re-confirms the page-close-vs-switch-out subtlety — both cancel uniformly, switch-out is a deliberate user gesture); (LV8) minimal queue event union: `{type: "focus"} | {type: "scrollToBottom"}`; queue request = `never` (all UiFacade reads are sync against editor.state); rejected proactive `scrollToEntry` / `highlightEntry` events per YAGNI; (LV9) full retirement of `acquireViewModelSync` machinery + `prepareViewModel("log-view")` pre-load step — UiFacade constructor accepts `LogViewEditor` directly; `loadViewModelFactory("log-view")` line deletes from ScriptRunner; `acquireViewModelSync` declaration deletes from `IContentHost.ts:56` + implementations from `TextEditorModel.ts:74` + `NoteItemEditModel.ts:331` (final SF2 retirement); `executeUiOnPage` becomes async per EW2; `releaseList.push` retires per SF2; six wrapper helpers (StyledTextBuilder, Progress, Grid, Text, Markdown, Mermaid) flip constructor arg type — body preserved; (LV10) `accepts({host, fileName, language})` predicate: filename `.log.jsonl` priority 70 strong match + content-peek `"type"\s*:\s*"log\."` priority 60 fallback (mirrors today's `acceptFile + isEditorContent` for non-`.log.jsonl` JSONL files with log entries); mode-agnostic. Mockup forecast: **zero changes** — Tier 5 template carries Log View end-to-end despite its unique append-only / Promise-based characteristics. If accepted, third template-confirmation walkthrough in a row (Grid + Preview group + Log View) — template stability across the most diverse axis examined yet: complex (Monaco) → medium (Grid) → light (Preview) → append-only-Promise-based (Log View). Class name finalizes from MI4's `LogViewEditorModel` placeholder to `LogViewEditor` (matches `MonacoEditor` / `GridEditor` / `MarkdownEditor` naming); today's `LogViewEditor.tsx` React component file renames to `LogBody.tsx` per the Tier 5 template (no name collision). Awaiting user review concern-by-concern.
- 2026-05-20 — **Walkthrough 22 (Preview group — Markdown / Svg / Html / Mermaid) complete.** All ten concerns (PV1–PV10) RESOLVED; **zero mockup adjustments** — second template-confirmation walkthrough (after Grid). Four sibling editors covered in one walkthrough land all eight pieces of the Tier 5 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) without any base-mockup touch. Stability signal strongly positive: **template holds across the complexity axis** — Monaco (complex: ITextModel + selection queries + script panel) → Grid (medium: AVGrid + variant-multiplexing + two-way sortColumn sync) → Preview (light: three of four have near-empty state, Svg/Html literally equal `EditorStateBase`). Headline resolutions: (PV1) four separate classes; no shared `PreviewEditorBase` extraction yet — ~80 LOC of mechanical skeleton repeats per class, but YAGNI says re-evaluate after walkthroughs 23–29 land when every text-bearing editor is migrated and the actual common surface is provable rather than speculative; Monaco and Grid already broke ground without a base class; (PV2) Markdown's `compactMode` is the only persisted field — search visibility / text / matchIndex / totalMatches ride state for reactivity but get stripped from getRestoreData per MO5/GR8 (search state is conceptually open-find / type / close-find, a transient gesture; persisting it surprises users on next open); (PV3) Markdown search state lives on `MarkdownEditor.state` — mirrors today's VM-on-model shape, preserves future script-API path (`page.asMarkdown().openSearch("query")` admissible cheaply later); (PV4) Markdown scroll position stays view-local (`scrollTopRef = useRef(0)` + `pagesModel.onFocus` useEffect inside MarkdownBody) — not persisted across restart (rendered markdown layout can shift between sessions; feature is for tab-switching not app-restart); (PV5) Mermaid render pipeline relocates to editor — `_renderTimer` private field + `renderDebounced` private method; `svgUrl / error / loading` on editor.state; debounce-timer lifecycle ties to editor lifecycle (`clearTimeout` in `dispose`); view-side debounce would invite unmounted-ref bugs on switchFrom; (PV6) Mermaid `lightMode` PERSISTS — per-editor user choice survives restart (a user might want a dark-themed app but a printable light-mode diagram for sharing); initial value defaults from `isCurrentThemeDark()` on first construct, overridden by `applyRestoreData` if a saved value exists; (PV7) `SvgEditorState = HtmlEditorState = EditorStateBase` typedefs — symmetric naming, zero added fields; future-additive fields land on the named type without rippling through the class generic; (PV8) all four queue events: `{ type: "focus" }` only — `<TextChrome>`'s TC8 200ms root-focus subscription calls `editor.focus()`; preview editors DO have a view-side focus surface (scroll panel / BaseImageView root / iframe), so the base class's MO7 no-op default doesn't fit; queue request `never` across the group (no view-context queries); Markdown's scrollToMatch is view-internal (useEffect-on-state-change dispatches naturally); (PV9) `MarkdownEditor._containerRef` private DOM ref field (NOT on `state`) — DOM nodes aren't observable; putting them on state breaks the MO5 "ride state for reactivity" assumption (no consumer subscribes to "container changed"); view calls `model.setContainer(el)`; facade reads `editor.containerInnerHtml` getter; facade stays sync (queue.execute wrapping for a synchronous DOM peek is over-engineered); **first example in EPIC-028 of an editor holding a DOM ref outside `state`** — sets the pattern for any future "facade needs sync DOM peek" need; (PV10) confirmation only — walkthroughs 09 / 10 / SF1 / SF2 / SF6 already cover toolbar contributions (inline `<PageToolbar>` children: Markdown compact toggle, Svg open-draw + copy, Html none, Mermaid theme + open-draw + copy), facade flip (VM-wrap → EditorModel-wrap), and `force?: boolean` parameter; per-editor real-code surface lands in implementation. Net real-code surface: 8 new files (4× `XxxEditor.ts` + 4× `XxxEditorView.tsx`) + 4 new `XxxBody.tsx` (preview surface) + 4 `XxxViewModel.ts` files delete + 4 `XxxView.tsx` files delete + `register-editors.ts` four factory swaps + four facades flip from VM-wrap to EditorModel-wrap (stays sync) + `PageWrapper.asMarkdown/asSvg/asHtml/asMermaid` add `force?` parameter. `MarkdownBlock` + `CodeBlock` + `rehypeHighlight` + `render-mermaid` + `BaseImageView` + `FindBar` carry over verbatim. Persistence migration zero per C2 + P2. Walkthrough 23 (Log View) is next — append-only render path; uniform editor (no Category-A/B carve-out); resolves `acquireViewModelSync("log-view")` retirement at three MCP-handler sites.
- 2026-05-20 — **Walkthrough 22 (Preview group — Markdown / Svg / Html / Mermaid) initial draft written.** PV1–PV10 proposed for review. Second non-Monaco exercise of the Tier 5 template — four sibling editors (Markdown, Svg, Html, Mermaid) covered in one walkthrough because they share the same shape (content-view over `TextFileModel`, no edit surface, one registry id each, no AVGrid-equivalent imperative ref to round-trip). The group exists to stress-test the Tier 5 template on **light editors**: three of four have a near-empty editor state slice (Svg / Html state literally equals `EditorStateBase`; Mermaid carries an async render pipeline + `lightMode`; Markdown carries search + compact-mode + scroll machinery). Headline proposals: (PV1) four separate classes; no shared `PreviewEditorBase` extraction yet — the ~80 LOC of mechanical skeleton (trait closure / switchFrom / restore / dispose / findCompatibleEditors / getNavigatorTarget / focus) repeats across each class, but YAGNI says re-evaluate after walkthroughs 23–29 land when the full common surface is provable rather than speculative; Monaco and Grid already broke ground without a base class, and a premature `TextBearingEditorBase` risks customization-point-soup ergonomics; (PV2) Markdown's `compactMode` is the only persisted field — search visibility / text / currentMatchIndex / totalMatches ride state for reactivity but get stripped from getRestoreData per MO5/GR8 pattern (search state is conceptually open-find / type / close-find, a transient gesture; persisting it surprises the user on next open); (PV3) Markdown search state lives on `MarkdownEditor.state` — mirrors today's VM-on-model shape, preserves future script-API path (`page.asMarkdown().openSearch("query")` admissible cheaply later); (PV4) Markdown scroll position stays view-local (`useRef` + `pagesModel.onFocus` useEffect inside MarkdownBody) — not persisted across restart (rendered markdown layout can shift between sessions; the feature is for tab-switching not app-restart); (PV5) Mermaid render pipeline relocates to editor — `_renderTimer` private field + `renderDebounced` private method; `svgUrl / error / loading` on editor.state; debounce-timer lifecycle ties to editor lifecycle (`clearTimeout` in `dispose`); identical shape to today's VM, just moved up; view-side debounce would invite unmounted-ref bugs on switchFrom; (PV6) Mermaid `lightMode` PERSISTS — per-editor user choice survives restart (a user might want a dark-themed app but a printable light-mode diagram for sharing); initial value defaults from `isCurrentThemeDark()` on first construct, overridden by `applyRestoreData` if a saved value exists; (PV7) `SvgEditorState = HtmlEditorState = EditorStateBase` typedefs — symmetric naming, zero added fields; future-additive fields land on the named type without rippling through the class generic; (PV8) all four queue events: `{ type: "focus" }` only — `<TextChrome>`'s TC8 200ms root-focus subscription calls `editor.focus()`; preview editors DO have a view-side focus surface (scroll panel / BaseImageView root / iframe), so the base class's MO7 no-op default doesn't fit; queue request `never` across the group (no view-context queries needed); (PV9) `MarkdownEditor._containerRef` private DOM ref field (NOT on `state`) — DOM nodes aren't observable and putting them on state breaks the MO5 "ride state for reactivity" assumption (no consumer subscribes to "container changed"); view calls `model.setContainer(el)`; facade reads `editor.containerInnerHtml` getter; facade stays sync (queue.execute wrapping for a synchronous DOM peek is over-engineered); (PV10) confirmation only — walkthroughs 09 / 10 / SF1 / SF2 / SF6 already cover toolbar contributions (inline children of `<PageToolbar>`), facade flip (VM-wrap → EditorModel-wrap), and `force?: boolean` parameter; per-editor real-code surface lands in implementation. Mockup forecast: **zero changes** — Tier 5 template carries all four editors end-to-end. Second template-confirmation walkthrough (after Grid). Net real-code surface: 8 new files (`XxxEditor.ts` + `XxxEditorView.tsx` per editor) + 4 new `XxxBody.tsx` (preview surface) + 4 `XxxViewModel.ts` files delete + 4 `XxxView.tsx` files delete + `register-editors.ts` four factory swaps + four facades flip from VM-wrap to EditorModel-wrap (stays sync) + `PageWrapper.asMarkdown/asSvg/asHtml/asMermaid` add `force?` parameter. `MarkdownBlock` + `CodeBlock` + `rehypeHighlight` + `render-mermaid` + `BaseImageView` + `FindBar` carry over verbatim. Persistence migration zero per C2 + P2. Awaiting user review concern-by-concern.
- 2026-05-20 — **Walkthrough 21 (Grid) complete.** All ten concerns (GR1–GR10) RESOLVED; **zero mockup adjustments** — first template-confirmation walkthrough for the Tier 5 template laid out in walkthrough 20's Closure. Grid is the first non-Monaco editor to exercise the eight-piece template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) and lands every piece without any base-mockup touch. Stability signal strongly positive: Tier 5 template holds across genuinely different editors (Monaco's view-local ITextModel + ComponentQueue request/reply pattern vs Grid's AVGridModel + two-way sortColumn sync + variant-formatted serialization). Headline resolutions: (GR1) one `GridEditor` class with three registry factories — `readonly editorId` and `readonly format` set in constructor; eliminates the three-VM-share-one-class shape today required because `host.state.editor` retired under S10; (GR2) variant discriminator readouts at the three parsing/serialization sites read `this.format` (semantic word `"json"|"csv"|"jsonl"`) decoupled from registry id prefix; (GR3) GridViewModel's 5-piece onInit splits cleanly — model-side init (variant bootstrap + reparse) into `GridEditor.restore()`; reactive subscriptions (host content + csv-options slice) into `adoptHost`; view-side init (pagesModel.onFocus + mount focus) into `GridBody.tsx` useEffects; `saveStateDebounced` retires entirely because descriptorChanged + window-level debounce per P3 covers it; (GR4) `<id>-grid-page.json` cache file ELIMINATED — Grid's structural state (columns / focus / search / filters / sortColumn / csv options) folds into EditorDescriptor.state; payload <2KB worst case within M9's metadata-only budget; view-derived `rows` / `error` stripped from getRestoreData per MO5; unifies persistence story (editor state → descriptor; host content → cache file); (GR5) gridRef sortColumn two-way sync via setGridRef callback — view writes saved → ref on mount, subscribes ref slice → editor.state; avoids making getRestoreData async (rejected `queue.execute({type:"getSortColumn"})` path); (GR6) per-editor-state-slice means switchFrom carries the host only — new GridEditor instance starts with `defaultGridEditorState`; no special clear-across-switch logic; restart-side persistence via applyRestoreData works as designed; (GR7) CSV delimiter detection inside `GridEditor.restore()` after host content loads with only-if-default guard — variant-aware bootstrap, runs once per restore, doesn't clobber CsvOptions-popover user choice; (GR8) `state.error` stays on editor.state for reactive view read via `state.use((s) => s.error)`; stripped from getRestoreData like MO5's `hasSelection`; (GR9) gridRef lifecycle across switchFrom is confirmation — GR5's two-way sync covers cold-restore / switch-in / switch-out; AVGridModel has no `dispose` requirement, naturally garbage-collects when GridBody unmounts; (GR10) `GridQueueEvent = focus | focusCell` (focus from chrome's MO7 root-focus; focusCell for restore-time + script-API post-mount calls); `GridQueueRequest = never` — all Grid script API reads sync against editor.state. Net real-code surface: new `GridEditor.ts` + `GridBody.tsx` + `GridEditorView.tsx`; `GridViewModel.ts` + today's `GridEditor.tsx` delete; `register-editors.ts` three factory swaps; `GridEditorFacade.ts` flips from VM-wrap to EditorModel-wrap (stays sync); `showCsvOptions(anchor, vm)` → `showCsvOptions(anchor, editor: GridEditor)` signature touch; `utils/grid-utils.ts` + `components/ColumnsOptions.tsx` verbatim. Walkthrough 22 (Preview group — Markdown / Svg / Html / Mermaid) is next; expected to be the lightest Tier 5 walkthrough (simple preview-only editors, no AVGrid-equivalent state to round-trip, no format variants per editor).
- 2026-05-20 — **Walkthrough 21 (Grid) initial draft written.** GR1–GR10 proposed for review. First non-Monaco exercise of the Tier 5 template — Grid registers three ids (`grid-json` / `grid-csv` / `grid-jsonl`) sharing one editor class. Key proposals: (GR1) one `GridEditor` class with `editorId` set in constructor + `format: "json"|"csv"|"jsonl"` instance field — three registry factories, one class definition, format-as-constant; (GR2) variant discriminator readouts (three sites in today's GridViewModel — detectCsvDelimiter / parseContent / getContentToSave) flip from `this.host.state.get().editor` to `this.format` (semantic word, decoupled from registry id); (GR3) GridViewModel.onInit body splits across `GridEditor.restore()` (host content load + variant-aware CSV delimiter bootstrap + initial reparse), `adoptHost` (host content slice subscription for live reparse + csv-options slice subscription for reload-on-change), and `GridBody.tsx` useEffects (pagesModel.onFocus scroll restore + mount-time focus); saveStateDebounced retires entirely (descriptorChanged + window-level debounce per P3 handles it); (GR4) per-editor `<id>-grid-page.json` cache file ELIMINATED — all Grid state (columns / focus / search / filters / sortColumn / csvDelimiter / csvWithColumns) folds into `EditorDescriptor.state`; view-derived `rows` / `error` stripped from getRestoreData per MO5 pattern; payload <2KB per Grid editor, within M9's ~50KB metadata budget; (GR5) gridRef sortColumn two-way sync via setGridRef callback — view writes saved sortColumn → gridRef on mount, then subscribes gridRef.state.sortColumn slice → editor.state.sortColumn; both directions handled in one ref callback; avoids making getRestoreData async; (GR6) search/filter/focus persist via descriptor across restart but DON'T carry across switchFrom (each editor has its own state slice; new GridEditor instance starts with `defaultGridEditorState` — no special "clear across switch" logic needed); (GR7) CSV delimiter detection moves into `GridEditor.restore()` after host content loads — variant-aware bootstrap path, only-if-default guard prevents overwriting user-chosen CsvOptions setting; (GR8) `error` field stays on editor.state for reactive view read via `state.use((s) => s.error)`; stripped from getRestoreData like MO5's `hasSelection`; view short-circuits to `<EditorError>` when set; (GR9) gridRef survival across switchFrom — confirmation only; two-way sync from GR5 covers the full lifecycle (cold restore + switch-in + switch-out all natural); (GR10) GridQueueEvent = `focus | focusCell` (focus from chrome's 200ms root-focus per MO7; focusCell for restore-time + script-API post-mount focus calls); GridQueueRequest = never (all Grid script API reads are sync against editor.state). Mockup forecast: **zero changes** — Grid fits the walkthrough 20 template end-to-end. Net real-code surface: new `GridEditor.ts` + `GridBody.tsx` + `GridEditorView.tsx`; `GridViewModel.ts` + today's `GridEditor.tsx` delete; `register-editors.ts` three factory swaps; `GridEditorFacade.ts` flips from VM-wrap to EditorModel-wrap (stays sync); `showCsvOptions(anchor, vm)` → `showCsvOptions(anchor, editor)` signature touch; `<id>-grid-page.json` cache file gone (orphaned on first boot post-upgrade, cleaned by per-editor `fs.deleteCacheFiles` on future dispose). Awaiting user review concern-by-concern.
- 2026-05-20 — **Walkthrough 20 (Monaco / Text) complete.** All ten concerns (MO1–MO10) resolved; one tiny mockup edit landed — `focus(): void { /* override */ }` no-op added to base `EditorModel` (MO7), with text-bearing editors overriding to `this.queue.send({type:"focus"})`. Tier 5 opener confirms the template holds. Net real-code surface is sizable but every change is mechanical translation of upstream Tier 1–4 resolutions: (1) new `src/renderer/editors/monaco/` folder (`MonacoEditor.ts` + `MonacoBody.tsx` + `index.ts`); (2) `TextFileModel` host class relocates to a content-host folder with submodels following (IO, encryption, actions, script panel — all four submodels carry over with `this.host` typed `TextFileModel` instead of today's dual `this.model`); (3) eight files delete — `TextEditor.tsx` (TextViewModel + view dissolves into MonacoBody), `TextEditorView.tsx` + `TextToolbar.tsx` + `TextFooter.tsx` (walkthroughs 09 / 10 absorbed all four into `<PageToolbar>` + `<TextChrome>`), `ActiveEditor.tsx` (per S10 / TC11 — encryption read-only handled inline by `<Editor options.readOnly={host.encrypted}>`), `ContentViewModel.ts` + `ContentViewModelHost.ts` + `useContentViewModel.ts` (SF2 retires the ref-counting system); (4) `TextFileModel` loses `_detectTimer` + 4 detection methods + `state.detectedContentEditor` (MO9 — switch widget calls `editorRegistry.findEditorsAccepting(host)` on demand with content peek per registry mockup); (5) `TextFileActionsModel.runScript` signature flips to `(scriptText: string, language: string) => Promise<void>` (MO6 — pure data-in, host stays selection-unaware); F5 keystroke routes through chrome's `onKeyDown` calling `editor.runScript()` (thin Monaco-side async wrapper that materializes selection via `queue.execute({type:"getSelectedText"})` then delegates to host); (6) `register-editors.ts` registers `monaco` instead of `textFile`; (7) `editorRegistry.ts` strips `validateForLanguage` / `getSwitchOptions` / `detectContentEditor` / `getPreviewEditor` per registry mockup; (8) `TextEditorFacade` thins to SF6 shape — 4 async methods (`getSelectedText` / `getCursorPosition` / `insertText` / `replaceSelection` via `queue.execute`) + 2 sync methods (`revealLine` / `setHighlightText` via `queue.send`); (9) persistence migration zero (C2 + P2 + walkthrough 04 / P10 — major version bump with detect-old-format-and-start-empty). Stability signal positive: one tiny base-class method addition, no structural foundation changes. Tier 5 walkthroughs 21–29 reuse the template laid out in walkthrough 20's Closure (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT). Walkthrough 21 (Grid) is next — exercises the template for the first time on a non-Monaco editor.
- 2026-05-20 — **Walkthrough 20 (Monaco / Text) initial draft written.** MO1–MO10 proposed for review. Tier 5 opener — first per-editor walkthrough; sets the template for editors 21–29. Key proposals: (MO1) `hasSelection` as one extra field on `MonacoEditor.state` — cleanest reactive surface for chrome's Run-all visibility gate (PT7); (MO2) TextViewModel + ContentViewModel + ContentViewModelHost dissolve entirely — setup functions (`setupWheelZoom` / `setupRichPaste` / `setupSelectionListener`) become inline `useEffect` cleanups inside `MonacoBody.tsx`; (MO3) `@monaco-editor/react`'s `<Editor>` props unchanged — ITextModel lifecycle continues to ride the component mount; C3 already locked the undo-loss-on-switch tradeoff; (MO4) confirmation — `MonacoQueueEvent` carries `revealLine` / `highlightText` events per S4 / B1 mockup usage shape; `_pendingRevealLine` / `_pendingHighlightText` fields delete; (MO5) keep `hasSelection: false` in saved descriptor; explicitly skip in `applyRestoreData`; (MO6) `runScript` selection materialization — chrome's Run button reads `editor.state.get().hasSelection`, calls `await editor.getSelectedText()` if true, delegates to `host.actions.runScript(text, language)`; F5 keystroke path uses thin `MonacoEditor.runScript(all?)` async wrapper that does the same dance; host stays selection-unaware; (MO7) `MonacoQueueEvent.focus` for chrome → Monaco focus signal — chrome's 200ms root-focus also sends `editor.focus()`; each text-bearing editor's union declares `"focus"`; tiny mockup edit lands `focus(): void` no-op on base `EditorModel`; (MO8) confirmation — `ScriptPanel.tsx` verbatim port under host ownership (TC6); only `model.runRelatedScript()` → `model.actions.runRelatedScript()` rename; (MO9) confirmation — `_detectTimer` + `scheduleDetection` + `cancelDetection` + `detectContentEditor` + `state.detectedContentEditor` all delete; switch widget calls `editorRegistry.findEditorsAccepting(host)` on demand (registry's `accepts()` can peek at content per `mockups/editorRegistry.ts:62-80`); (MO10) confirmation — `applyRenamedPath` verbatim port. Mockup forecast: zero or one tiny edit (single `focus(): void` no-op on base `EditorModel` if MO7 lands (b)). Net real-code surface is sizable: new `src/renderer/editors/monaco/` folder (MonacoEditor + MonacoBody + index), `TextFileModel` host class relocates to a content-host folder (likely `src/renderer/api/content/` — final path during implementation) with submodels following, six files delete (TextEditor + TextEditorView + ActiveEditor + TextToolbar + TextFooter + ContentViewModel + ContentViewModelHost + useContentViewModel), editor registry strip-downs per registry mockup. Awaiting user review concern-by-concern.
- 2026-05-20 — **Walkthrough 13 (MCP integration) complete.** MI1–MI10 all resolved; zero mockup adjustments landed. Smallest Tier 4 walkthrough as predicted — every concern collapsed to either mechanical translation of upstream resolutions (S10 + P4 + GK2 / T2 + SF2 + SF3 + SF8 + EW2 + EW3) or "today's code already does it" (MI7 — `LogViewModel.onDispose()` at `LogViewModel.ts:72-80` already cancels pending dialogs). Net real-code surface is `mcp-handler.ts` only: `getPages` / `getActivePage` row shapes drop `type`, source `editor` from `editorId`, `language` from `contentHost.state.language`, `filePath` / `content` through `pagesModel.query.getTextFileHost(pageId)`; `getPageContent` + `setPageContent` route through the same helper for the host gate and write; `createPage` flips standalone filter to `!hasContentHost` and gains a `log-view` hint entry; three `editor.acquireViewModelSync("log-view") as LogViewModel | undefined` sites collapse to `editor instanceof LogViewEditorModel` direct checks (SF2 consequence at three new sites). `mcp-http-server.ts` unchanged (transport below the host split). `automation/commands.ts` rewrites for `browser_*` carry over to walkthrough 30. `notepad://guides/pages` documentation gets a small refresh to drop the `type` field reference. **Tier 4 cross-cutting complete.** Walkthroughs 12 (Scripting facades) and 13 (MCP integration) both done; design is stable across both. Tier 5 (per-editor walkthroughs) begins next with walkthrough 20 — Monaco / Text.
- 2026-05-20 — Walkthrough 13 / MI8 + MI9 + MI10 resolved (confirmations). **MI8 — `executeScript` mainEditor argument under host split**: `scriptRunner.runWithCapture(script, pageModel?.mainEditor ?? undefined, language)` carries over verbatim — the `?? undefined` already handles the null case for empty-with-sidebar pages (EW9). PageWrapper construction is scripting-internal per SF3; MCP doesn't see the change. **MI9 — `openUrl` browser-tab flow**: `pagesModel.openUrlInBrowserTab(url, options)` page-level method unchanged at the MCP call site; Browser editor's internal migration is walkthrough 30's responsibility. **MI10 — `browser_*` automation command translation**: MCP dispatcher's by-prefix delegation in `mcp-handler.ts` unchanged; automation layer's `state.type === "browserPage"` gate flips to `editor instanceof BrowserEditorModel` per SF8 (walkthrough 30 owns the rewrite). No mockup change required for any of the three.
- 2026-05-20 — Walkthrough 13 / MI7 resolved (recommendation overridden by user). **MI7 — Dialog promise lifecycle under script teardown**: option (a) — no new dispose contract needed. Verified — today's `LogViewModel.onDispose()` at `src/renderer/editors/log-view/LogViewModel.ts:72-80` already iterates `pendingDialogs` and resolves each with a sentinel canceled-button entry (`{ type: "", id, timestamp: 0 }`), then clears the map. The cleanup logic is internal to LogViewModel and survives the class rename to `LogViewEditorModel` under EPIC-028 (modulo any `onDispose` → `dispose` method-name reshape walkthrough 23 picks). MCP's `await Promise.all(dialogPromises)` in `handleUiPush` resolves cleanly when the log page closes because each dialog's `resolve` already fires from the existing dispose handler. The previous forward-pointer framing in walkthrough 13 misread today's behavior — the contract is already met. Rejected (b) forward-pointer to walkthrough 23 (would document behavior already implemented). No mockup change required.
- 2026-05-20 — Walkthrough 13 / MI4 resolved. **MI4 — Log View VM acquisition retirement**: option (a) — three callsites in `mcp-handler.ts` (`getOrCreateMcpLogViewModel`, `logIncomingRequest`, `showMcpRequestLog`) flip from `isTextFileModel(editor) && editor.acquireViewModelSync("log-view") as LogViewModel | undefined` to direct `editor instanceof LogViewEditorModel` checks. The `isTextFileModel(editor)` guard goes away at all three sites — under EPIC-028 the log-view page's `mainEditor` IS the LogView editor itself, never a TextFileModel. No helper; cost/benefit doesn't justify centralization for one class at three sites in one file (GK2 / T2's helper paid off across two files and 15 callsites). LogView's final class name (`LogViewEditorModel` placeholder used here) finalized in walkthrough 23. SF2 already retires the `acquireViewModelSync` machinery; MI4 lands the consequence at three new sites. Rejected (b) `pagesModel.query.getLogViewEditor(pageId)` helper (premature centralization — three callsites in one file is under the GK2 / T2 threshold). No mockup change required.
- 2026-05-20 — Walkthrough 13 / MI3 resolved. **MI3 — `create_page` "standalone" filter under registry simplification**: option (a) — flip `editorDef.category === "standalone"` to `!editorDef.hasContentHost`. The new gate matches today's "standalone" set verbatim across six editors (`browser-view`, `pdf-view`, `image-view`, `mcp-view`, `about-view`, `settings-view`); picks up `log-view` as a new addition (today's `category: "content-view"` was an artifact of the acquireViewModelSync flow that retires under SF2). Add a `log-view` hint entry to the standalone rejection table pointing external clients at `ui_push` and `requireWellKnownPage("mcp-ui-log")`. Behavior change for `create_page({ editor: "log-view" })` documented but non-regressing — zero today-callers in the codebase, MCP guides, or external client docs. Rejected (b) special-case log-view as create_page-able (no concrete use case) and (c) new `createPageable: boolean` registry field (three fields where one does the work — `hasContentHost` already encodes the right distinction). No mockup change required.
- 2026-05-20 — Walkthrough 13 / MI2 resolved. **MI2 — `setPageContent` / `getPageContent` gate translation**: option (b) — reuse `pagesModel.query.getTextFileHost(pageId): TextFileModel | null` helper from GK2 / T2 at both callsites. `getPageContent` truthy-checks the helper for the read path; `setPageContent` truthy-checks the helper for the write path and calls `host.changeContent(content)` on the typed result. Same canonical pattern as PageTab's 14 callsites (T2), `requireGroupedText` (GK2), and `setPageContent`'s today gate. The function-form `isTextFileModel(editor)` predicate retires (S10) and the content write path moves from `editor.changeContent(content)` (today, on `TextEditorModel`) to `host.changeContent(content)` (after EPIC-028, on `TextFileModel` as `IContentHost`). Error message in `setPageContent` unchanged. Rejected (a) inline `editor.contentHost instanceof TextFileModel` at two sites (inconsistent with the centralized helper family established in walkthroughs 06–08). No mockup change required.
- 2026-05-20 — Walkthrough 13 / MI1 resolved. **MI1 — `get_pages` / `get_active_page` wire-shape under host split**: option (a) — drop `type` field from response rows; map `editor` to `editor.editorId` (S10), `language` to `editor.contentHost?.state.get().language` (P4), `filePath` and `content` through `pagesModel.query.getTextFileHost(pageId)` (GK2 / T2). External MCP consumers see the same surface as script-API consumers post-SF5 — zero documented external consumers of the `type` field. `notepad://guides/pages` documentation gets a minor refresh to drop the `type` reference; existing `editor` field semantics carry over verbatim (it was already the registry id under today's mapping). Rejected (b) preserve `type` by deriving (two-shape field, awkward) and (c) full row redesign (breaking; ripples through MCP clients for no win). No mockup change required.
- 2026-05-20 — **Walkthrough 13 (MCP integration) initial draft written.** MI1–MI10 proposed for review. Tier 4 closer. Key proposals: (MI1) drop `type` field from `get_pages` / `get_active_page` wire shape (mirrors SF5 — zero documented external consumers; `editor` field maps to `editorId`, `language` / `filePath` source from host per P4 + GK2); (MI2) reuse `pagesModel.query.getTextFileHost(pageId)` helper from GK2 / T2 for `getPageContent` / `setPageContent` gates — two more callsites for an existing helper; (MI3) flip registry filter `editorDef.category === "standalone"` to `!editorDef.hasContentHost`; behavior change `create_page({ editor: "log-view" })` joins the rejection set under EPIC-028 (zero today-callers); add `log-view` hint pointing external clients at `ui_push` and `requireWellKnownPage`; (MI4) Log View VM acquisition retirement — `editor.acquireViewModelSync("log-view") as LogViewModel | undefined` flips to `editor instanceof LogViewEditorModel` at three sites (`getOrCreateMcpLogViewModel`, `logIncomingRequest`, `showMcpRequestLog`) per SF2; direct `instanceof` check, no helper (cost / benefit doesn't justify centralization for one class at three sites in one file); (MI5) `createPage` return-shape mapping mirrors MI1 — `editor` from `editorId`, `language` from `contentHost.state.language`; (MI6) confirmation — `addEntry(type, fields)` and `addDialogEntry(type, fields)` LogView-internal methods preserved across class rename; (MI7) forward-pointer to walkthrough 23 — `LogViewEditorModel.dispose()` rejects pending dialog promises with "page closed before dialog response" error, symmetric with ComponentQueue dispose-rejects-pending per SF6 / B1; (MI8) confirmation — `executeScript` `pageModel?.mainEditor` argument carries over verbatim, PageWrapper construction is scripting-internal per SF3; (MI9) confirmation — `openUrlInBrowserTab(url, options)` page-level call unchanged, Browser editor migration is walkthrough 30's responsibility; (MI10) confirmation — `browser_*` delegation unchanged, automation layer's `state.type === "browserPage"` gate flips to `editor instanceof BrowserEditorModel` per SF8 (walkthrough 30 owns the rewrite). No mockup adjustments proposed — every concern is mechanical translation of upstream resolutions (S10, P4, GK2 / T2, SF2, SF8, EW2, EW3) or forward-pointer to walkthrough 23. Smallest Tier 4 walkthrough; net real-code surface is in `mcp-handler.ts` only. Awaiting user review concern-by-concern.
- 2026-05-20 — Walkthrough 12 / B1 mockup refined (post-completion). User-flagged that the request/reply channel should expose a parallel React hook to match the existing fire-and-forget `use()` hook. Added `useRequest(handler)` to `mockups/ComponentQueue.ts` — wraps `useEffect(() => this.register(handler), [this])` per the same handler-stability convention as `use()` (consumer's responsibility for `useCallback` if closure references reactive values; Monaco's typical closure over `monacoRef.current` doesn't need it since refs don't trigger re-renders). View code collapses from explicit `useEffect(() => model.queue.register(...), [model])` to one-line `model.queue.useRequest((req) => { switch (req.type) {...} })`. Monaco usage sketch in the mockup footer updated accordingly. SF6 resolution text in walkthrough 12 updated to list both `register` (programmatic) and `useRequest` (hook) as view-side entry points alongside `execute` model-side.
- 2026-05-20 — **Walkthrough 12 (Scripting facades) complete.** SF1–SF10 all resolved; one mockup adjustment landed — B1 (`mockups/ComponentQueue.ts` extended with request/reply half: second generic `Req = never`, `execute(req): Promise<unknown>` + `register(handler)` symmetric with existing fire-and-forget `send` / `subscribe`, `dispose` rejects pending requests). Net effect: script API surface preserves IPage semantics with three real script-author-visible changes — (1) `asX(force?: boolean)` opt-in switch replaces today's implicit shadow-ViewModel promotion (same compatibility source as UI switch widget); (2) TextEditorFacade query methods become async (one extra `await` per call — Promise-returning via ComponentQueue.execute); (3) `page.type` field gone (zero codebase consumers; scripts branch on `page.editor` if needed). Architecturally large under the hood: `ContentViewModelHost` + `acquireViewModel` / `releaseViewModel` / `useContentViewModel` all retire — each EditorModel IS its own viewmodel under EPIC-028; `EditorView` literal union retires from `src/shared/types.ts` and re-lives in `api/types/page.d.ts` as script-API-only IntelliSense surface (value↔registry-id mapping verified 1:1 verbatim so setter is direct passthrough); `releaseList` plumbing simplifies — drops from PageWrapper + PageCollectionWrapper but stays alive in ScriptContext + AppWrapper for AppWrapper.events subscription cleanup per AutoloadRunner's "scripts share one ScriptContext, subscriptions tracked in releaseList" contract. ComponentQueue now has two parallel channels (fire-and-forget + request/reply) which lands S4's "future extension deferred to walkthrough 12 or 20" — keeps walkthrough 20 focused on Monaco internals rather than primitive design. Tier 4 cross-cutting walkthrough 13 (MCP integration) begins next; walkthrough 13's `ui_push` Log View cleanup naturally inherits SF3's releaseList-stays-in-ScriptContext shape.
- 2026-05-20 — Walkthrough 12 / SF10 resolved. **SF10 — `EditorView` literal-string union retirement**: option (b) — retires from `src/shared/types.ts` (internal `editorId: string` per S10); re-lives in `api/types/page.d.ts` as script-API-only literal union for IntelliSense. Verified value↔id mapping is 1:1 verbatim (each `EditorView` literal matches a `editorRegistry.register({ id: "..." })` entry exactly) so setter is direct passthrough — no translation step. `page.editor = "grid-json"` directly invokes `switchMainEditor("grid-json")`. Two-place maintenance touch when a new editor lands (registry + EditorView union); pays off at script-author keystroke. No generic `"grid"` shortcut — exact registry id required at setter; auto-resolve from language happens only via `asGrid(true)` per SF1. Rejected (a) plain `string` (kills IntelliSense) and (c) re-export internal type (re-couples script API to internal types). No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF9 resolved. **SF9 — `PageWrapper.modified` getter under host split**: option (a) — base `EditorModel.modified: boolean` getter survives unchanged (already at `mockups/EditorModel.ts:373`, reads `state.get().modified` from `EditorStateBase.modified` field); text-bearing subclasses override to delegate to host (`return this._host?.state.get().modified ?? false`). Non-text editors inherit the base — defaults to false; editors that need a "modified" notion write to their own state. PageWrapper unchanged — `this.model.modified` continues to work. Centralizing avoids host-lookup scatter across PageTab (walkthrough 08 / T1's `editor.state.use()`), save-on-close ordering (N7), and the script API. Base field's "dead-when-overridden" state is acceptable leak (defaults to false, unread). Rejected (b) PageWrapper inlines `this.model.contentHost?.state.get().modified ?? false` (pushes host-lookup to every consumer). No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF8 resolved (confirmation). **SF8 — Browser / McpInspector facade gates**: mechanical translation. `state.get().type !== "browserPage"` → `!(this.model instanceof BrowserEditorModel)`; same flip for McpInspector. TypeScript narrows after the instanceof check so the `as unknown as BrowserEditorModel` / `as unknown as McpInspectorEditorModel` cast drops as a side benefit. No `force?: boolean` parameter on these two — SF1 stays throw-only for direct-model facades (no switch path to/from Browser / McpInspector pages). No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF7 resolved (confirmation). **SF7 — `asGrid()` editor-id resolution under S10**: mechanical translation. `this.model.state.get().editor` → `this.model.editorId` (S10); `this.model.state.get().language` → `this.model.contentHost?.state.get().language` (P4 — language host-owned). Short-circuit (`currentEditor === "grid-json"`) becomes `id === "grid-json"`. Throw branch unchanged. Same translation applies to any facade method reading `state.get().editor` or `state.get().language` — walkthroughs 20–29 verify per editor. No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF6 resolved + B1 mockup landed. **SF6 — `ComponentQueue.register` / `.execute` for view-context queries**: option (a) — extend `ComponentQueue` with request/reply half symmetric with the existing fire-and-forget `send` / `subscribe` mailbox. Lands S4's "future extension deferred to walkthrough 12 or 20" inside walkthrough 12 — keeps walkthrough 20 focused on Monaco specifics. **B1 mockup**: `mockups/ComponentQueue.ts` extended — second generic `Req = never`; private `_pendingRequests` + `_requestHandler` fields; `execute(req): Promise<unknown>` queues if no handler, resolves on `register` drain (same mailbox semantics as `send`); `register(handler)` drains pending FIFO and returns unregister function; `dispose` rejects pending requests with "ComponentQueue disposed before request was handled" so awaiting scripts don't hang. Single-handler-at-a-time discipline mirrors `subscribe`. Stale-result risk noted: `execute` while view unmounted resolves against the NEW view's state at remount; senders that care add `Promise.race` timeout. Real-code consequence: walkthrough 20 finalizes Monaco's `MonacoQueueRequest` union (`getSelectedText` / `getCursorPosition` / `insertText` / `replaceSelection`); TextEditorFacade collapses to thin Promise-returning delegates over `editor.queue.execute(...)`. **Script-API impact**: `TextEditorFacade.getSelectedText` / `getCursorPosition` / `insertText` / `replaceSelection` become async (return `Promise`); today they're sync — scripts pay one extra await. `revealLine` / `setHighlightText` stay sync (fire-and-forget via `send`, not `execute`). Acceptable breaking change. Rejected (b) separate `ComponentBridge` primitive (two primitives where one mailbox suffices) and (c) defer to walkthrough 20 (lands primitive design in an editor-specific walkthrough).
- 2026-05-20 — Walkthrough 12 / SF5 resolved. **SF5 — `PageWrapper.type` getter under S10**: option (b) — drop the `type` getter entirely. Verification grep across the repo (outside `doc/`) returned zero `page.type` consumers — only the type declaration at `src/renderer/api/types/page.d.ts:35`. Clean removal with no script breakage risk. Scripts that need to discriminate page shape branch on `page.editor` (the editorId string) — `if (page.editor === "browserPage")` replaces hypothetical `if (page.type === "browserPage")` (no existing script does this today). Real-code: delete `get type()` from `PageWrapper`; delete `readonly type: string` from `IPage` in `src/renderer/api/types/page.d.ts`. Rejected (a) derive type at wrapper (preserves a contract no script consumes — three `instanceof` branches for dead UX) and (c) per-EditorModel `pageWrapperType?: string` field (registry-side concept for zero callers). The "is this text-backed?" abstraction is genuinely useful but no current consumer — re-add as `page.hasContentHost: boolean` (or similar) when a concrete consumer appears. No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF4 resolved (confirmation). **SF4 — `set editor(value)` setter error handling**: S7 (walkthrough 02) already pinned this down — `switchMainEditor` throws on (1) `newEditorId` not registered, (2) current main lacks `CONTENT_HOST_TRAIT`, (3) `newEditorId` not in `findEditorsAccepting(host)`; PageWrapper catches and calls `ui.notify(err.message, "error")`. Setter is fire-and-forget (assignment is sync, switch is async — matches today's `model.changeEditor(v)` shape; no regression). Missing `model.page` (detached editor) → silent no-op, confirm during real-code rewrite. No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF3 resolved (refined after verification). **SF3 — `releaseList` constructor parameter on PageWrapper**: option (a) — drop `releaseList` from `PageWrapper` + `PageCollectionWrapper` constructors only; keep alive in `ScriptContext` and `AppWrapper`. Initial draft proposed dropping `releaseList` entirely, but user-flagged-and-verified grep across `src/renderer/scripting/` surfaced three consumers (not one): (1) PageWrapper `asX()` push sites — 11 of them, retire under SF2; (2) AppWrapper event-channel subscriptions via `wrapEventChannel` / `createEventsProxy` — push `() => sub.unsubscribe()` for every `app.events.X.subscribe(handler)` call, load-bearing for autoload script reload/teardown per `AutoloadRunner.ts:21-25` ("all scripts share one ScriptContext — event subscriptions tracked in a single releaseList"); (3) `initializeUiFacade` Log View viewmodel — retires under SF2; new cleanup shape lands in walkthrough 13 (MCP / ui_push). Net effect: `ScriptContext.releaseList: Array<() => void>` field unchanged; `AppWrapper` constructor signature unchanged; `PageWrapper(model, outputFlags?)` and `PageCollectionWrapper(pages)` drop the parameter; `AppWrapper` builds its `PageCollectionWrapper` without passing the list. If a future PageWrapper method ever needs script-end cleanup, re-thread at that one well-understood site. Rejected (b) keep threading through PageWrapper / PageCollectionWrapper for forward-compat (preemptive parameter for hypothetical consumer). No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF2 resolved. **SF2 — `ContentViewModelHost` retirement**: confirmation. Direct consequence of SF1's design — under explicit-force semantics the page IS the editor at all times, so the shadow-ViewModel machinery has no home. Retired: `model.acquireViewModel(editorId)`, `model.releaseViewModel(editorId)`, `ContentViewModelHost` (`src/renderer/editors/base/ContentViewModelHost.ts`), per-host viewmodel registry / ref-count machinery, `useContentViewModel` React hook (`src/renderer/editors/base/useContentViewModel.ts`). Each EditorModel subclass owns its viewmodel-equivalent state directly; walkthroughs 20–29 confirm per editor. No mockup change required.
- 2026-05-20 — Walkthrough 12 / SF1 resolved (user-refined design). **SF1 — `asX()` behavior when page isn't already X**: option (d) — opt-in switch via `force?: boolean` parameter, refining the initial proposed option set. Default throws (predictable, no hidden UI side effects); `force = true` attempts the switch using `editor.findCompatibleEditors()` — the SAME compatibility source the UI switch widget reads per PT2 / PT10 / S1, so "can the script switch?" exactly matches "would the user see this option in the widget?" Three layered throws give precise error messages (editor-not-attached → content-shape-incompatible → compatibility-list-miss → switch-failed). Scope: eleven ViewModel-backed facades gain `force?: boolean` (`asText`, `asGrid`, `asNotebook`, `asTodo`, `asLink`, `asMarkdown`, `asSvg`, `asHtml`, `asMermaid`, `asGraph`, `asDraw`); two direct-model facades stay throw-only (`asBrowser`, `asMcpInspector` — no switch path to/from those page shapes). Mirrored on `IPage.d.ts` method signatures. Trade-off: scripts that today rely on `page.asGrid()` implicitly promoting a JSON-in-Monaco page must update to `page.asGrid(true)` — clean break that forces script authors to acknowledge the visible UI side effect. Rejected (a) direct narrow only (loses the implicit-promote ergonomics entirely), (b) auto-switch by default (hidden UI side effect, fails the predictability bar), and (c) per-facade hybrid (collapses to (b) for ViewModel-backed and (a) for direct-model — same net result with more rules). No mockup change required.
- 2026-05-20 — **Walkthrough 12 (Scripting facades) initial draft written.** SF1–SF10 proposed for review. Tier 4 opener. Key proposals: (SF1) `asX()` auto-switches via `page.switchMainEditor(targetId)` when the page isn't already that editor — matches today's `acquireViewModel` intent; (SF2) `ContentViewModelHost` + `acquireViewModel`/`releaseViewModel`/`useContentViewModel` retire as a direct consequence — each EditorModel IS its own viewmodel; (SF3) drop `releaseList` constructor parameter from `PageWrapper`/`PageCollectionWrapper`; (SF4) `set editor(v)` rewires to `page.switchMainEditor(v).catch(err => ui.notify(err.message, "error"))` per S7 — confirmation; (SF5) `PageWrapper.type` derives at the wrapper layer (three `instanceof` branches + `editorId` fallback) — preserves the `IPage.type` script-API contract verbatim under S10; (SF6) extend `ComponentQueue` with `register(handler)` + `execute(req)` request/reply primitive for view-context queries — Monaco uses it for `getSelectedText`/`getCursorPosition`/`insertText`/`replaceSelection` (S4's "future extension deferred to walkthrough 12 or 20" lands here); (SF7) `asGrid()` editor-id resolution rewrites to read `this.model.editorId` + `this.model.contentHost.state.language` — mechanical translation; (SF8) Browser/McpInspector facades flip `state.get().type !== "..."` checks to `instanceof BrowserEditorModel`/`McpInspectorEditorModel` — drops the `as unknown as` cast as side benefit; (SF9) base `EditorModel.modified` getter survives; text-bearing override delegates to `host.state.modified`; centralizes a value needed by PageTab + save-on-close + script API; (SF10) `EditorView` literal-string union retires from `src/shared/types.ts` (internal `editorId: string` per S10) but lives on as a script-API-only literal union in `api/types/page.d.ts` — script authors keep IntelliSense on `page.editor = "..."`. One mockup adjustment proposed: B1 (`ComponentQueue.ts` — add `register`/`execute` request/reply API per SF6). Awaiting user review concern-by-concern.
- 2026-05-19 — initial list created. Foundation mockups complete; Tier 1 starts next.
- 2026-05-19 — Walkthrough 01 (Page lifecycle) complete. Mockups updated: `traits.ts` (A1 shrink), `EditorModel.ts` (A3/A6/A7 + secondaryEditor side-effect removal), `PageModel.ts` (A8 unified-array redesign), `editorRegistry.ts` (A7 — dropped `createEditorFromFile`). Tier 1 second-pass step added to workflow (README) and progress.
- 2026-05-19 — Walkthrough 02 (Main editor swap) complete. S1–S10 resolved. New foundation primitive `ComponentQueue` (general model→view mailbox, replaces today's pending-fields patterns). Mockups updated: `ComponentQueue.ts` (new), `EditorModel.ts` (`queue` field + 3rd generic + dispose-clears-queue), `editorRegistry.ts` (`mode: edit|view` on `AcceptanceInput` + `resolveForFile` param; `findEditorsAccepting` mode-agnostic). Key decisions: page-level `switchMainEditor` is the view-switch entry; `confirmRelease` invariant ("only when host dies"); `IEditorState.type`/`.editor` fields slated for removal across walkthroughs 04 / 09 / 12 / 20 / 24 / 30 (replaced by `EditorModel.editorId` + `instanceof` + `traits.has(CONTENT_HOST_TRAIT)`).
- 2026-05-19 — Walkthrough 03 (Secondary editors & PageNavigator) initial analysis written. N1–N7 proposed for review: N1 (setter calls `page.onEditorPanelsChanged` to keep visibility criterion reactive; preferred over per-editor state subscriptions or a new Subscription primitive), N2 (PageNavigator subscribes to `page.state` — `version` already covers both attach/detach and panel-list changes), N3 (iterate `page.panelEditors` getter), N4 (self-close gesture works via N1 — no extra API), N5 (CategoryEditor's cross-editor reactivity via a `page.state` subscription set up inside `setPage`; rejected dedicated `editorsChanged` channel as single-caller over-design), N6 (drop Pattern A/B framing — unified-array makes them states, not patterns), N7 (close-order: panel-contributing editors first, main last — preserves today's user-visible behavior). Persistence restore (`pendingSecondaryDescriptors`, `_pendingActivePanel`) bounces to walkthrough 04.
- 2026-05-19 — Walkthrough 03 / N1 resolved with a refined option (a). New foundation mockup `TOneState.ts` adds a selector overload to `TOneState.subscribe(listener, selector)` — pure models can now observe a slice of another model's state with the same `compareSelection` precision the `use` hook already provides. PageModel.attach subscribes per editor on the `secondaryEditor` slice and unsubscribes in detach. `EditorModel.secondaryEditor` setter stays a fully pure state mutation (A8 intent preserved end-to-end — no editor → page side effect at all). Mockup-adjustments renumbered: B1 = TOneState overload (foundation), B2 = setter unchanged, B3 = attach/detach sub bookkeeping, B4 = `onEditorPanelsChanged` handler, B5 = PageNavigator subscribes to page.state, B6 = close() order, B7 = drop Pattern A/B framing. N2–N7 await review.
- 2026-05-19 — Walkthrough 03 (Secondary editors & PageNavigator) complete. N1–N7 resolved. Key decisions: panels-changed trigger via `TOneState.subscribe(listener, selector)` on the `secondaryEditor` slice (setter stays pure, N1); PageNavigator and CategoryEditor view both consume `page.state.use()` (N2 + N5 — same signal); `page.panelEditors` getter centralizes the filter rule (N3); close button = "close model" reusing N1's visibility-criterion path with no new API (N4); Pattern A/B framing dropped — they're states of the same single membership (N6); `close()` iterates panel editors first, main editor last for cancellation correctness (N7). Mockups updated: `TOneState.ts` (new — selector overload), `PageModel.ts` (`_editorSubs` field, slice-subscribe in `attach`/`detach`, new `onEditorPanelsChanged` handler, `close()` panel-first ordering, defensive sub-drain in `dispose`; "What's gone" comment block updated). `EditorModel.ts` unchanged — A8's pure setter from walkthrough 01 already correct. Deferred to walkthrough 04: secondary-editor persistence shape, `_pendingActivePanel` restore timing.
- 2026-05-19 — Walkthrough 04 (Persistence) initial analysis written. P1–P10 proposed for review. Key proposals: new `EditorDescriptor` + `HostDescriptor` types replace flat `IEditorState` (P1); single `schemaVersion: 4` field discriminates old-vs-new (P2, P10); fold per-page sidebar cache file into the unified `PageDescriptor.editors[]` + `sidebar?` block (P3 — eliminates `<pageId>-nav-panel.txt`); pipe descriptor moves under `HostDescriptor` (P4); parallel per-editor restore via `Promise.all` retires `_pendingActivePanel` (P5); `createEditor(id, instanceId?)` handles cache-file-id continuity (P6); per-editor salvage on restore failure preserves siblings (P7); empty-pages naturally fit (P8); opportunistic orphan-cache sweep on bootstrap (P9). Absorbs L5 / S10 / C2 / walkthrough 03 deferred items. Awaiting user review concern-by-concern.
- 2026-05-19 — Walkthrough 04 (Persistence) complete. P1–P10 resolved. Key decisions: new `EditorDescriptor { editorId, id, state, host? }` + `HostDescriptor { kind, state, pipe? }` + unified `PageDescriptor.editors[]` + `mainEditorId` retire today's flat `IEditorState` shape (P1); integer `WindowState.schemaVersion: 4` is the single old-format discriminator (P2, P10) — no migration shim per C2; fold sidebar cache file into PageDescriptor's `sidebar?` block, retire `<pageId>-nav-panel.txt` entirely (P3); pipe descriptor relocates from editor to host (P4); parallel `Promise.all` restore retires `_pendingActivePanel` (P5); `editorRegistry.createEditor(id, instanceId?)` preserves cache-file id at construction (P6); per-editor `console.warn + continue` salvages siblings on failed restore (P7); empty-pages stop being a special case (P8); orphan cache cleanup intentionally NOT implemented — asymmetric risk where a restore bug could trigger real data loss outweighs the disk-space drift (P9); future shape bumps increment the integer (P10). Foundation mockup added: `PersistenceTypes.ts`. Mockups touched: `PersistenceTypes.ts` (new), `editorRegistry.ts` (optional `instanceId`), `EditorModel.ts` (abstract `editorId` + `getRestoreData` returns `EditorDescriptor` + `RestoreData<S>` shape; retires shared `IEditorState`), `IContentHost.ts` (`getDescriptor` + static `fromDescriptor` contract), `PageModel.ts` (sidebar cache machinery removed; `saveState` iterates editors; `dispose` no longer deletes page-level cache). C8 (sweep) and C9 (new fs API) both dropped per P9.
- 2026-05-19 — Walkthrough 05 (Multi-window transfer) initial analysis written. M1–M10 proposed for review. Key proposals: drag IPC payload picks up the new `PageDescriptor` shape automatically from walkthrough 04 / C1; no `schemaVersion` on the drag payload (same-process, same-version) — M1. Unify `movePageIn` with bootstrap restore via a shared `PagesPersistenceModel.restorePage(desc)` entry point — M2. Keep `await page.saveState()` in `movePageOut` (now per-editor only, no sidebar file) — M3. Detach-without-dispose invariant gets a comment in `removePage` (no code change) — M4. Per-editor cache file survival via `editorRegistry.createEditor(id, instanceId)` (already covered by walkthrough 04 / C2) — M5. Per-editor salvage on transfer mirrors walkthrough 04 / P7 — M6. Source closes when last page moves out (unchanged) — M7. Pinned tabs stay non-cross-window-draggable (unchanged) — M8. Payload size ~50KB worst case; host content stays in per-editor cache file, not the IPC blob — M9. No auto-focus on target after transfer — M10. Awaiting user review concern-by-concern.
- 2026-05-19 — Walkthrough 05 (Multi-window transfer) complete. M1–M10 resolved. Key decisions: drag IPC payload carries the new `PageDescriptor` shape (from walkthrough 04 / C1) and no `schemaVersion` — same-process same-version means the schema discriminator only needs to live at file boundaries (M1); `PagesPersistenceModel.restorePage(desc)` becomes the shared restore entry point consumed by bootstrap restore, IPC `movePageIn`, and `duplicatePage`, dropping `newEditorModelFromState` + `PAGE_TYPE_MIGRATIONS` from `PagesLifecycleModel` (M2); `await page.saveState()` stays in `movePageOut` — lighter than today (no sidebar file), establishes "saveState is an awaitable flush, not fire-and-forget" invariant for editor walkthroughs 20–30 (M3); detach-without-dispose is comment-only since today's `removePage` already does the right thing (M4); editor-id continuity uses the existing `editorRegistry.createEditor(id, instanceId)` from walkthrough 04 / C2 — single mechanism, two consumers (bootstrap + IPC) (M5); multi-editor transfer (page-with-sidebar) is naturally atomic at the descriptor level, per-editor salvage from walkthrough 04 / P7 applies (M6); close source window on last-page-moved unchanged (M7); pinned tabs stay non-cross-window-draggable (M8); payload is metadata-only (~50KB worst case) — host content stays in per-editor cache file via M5's id continuity, never riding the IPC blob (M9); the dropped page is activated implicitly by `s.ordered.push(page)` since `query.activePage` reads the last item of `ordered` — no explicit `showPage` call, testing will confirm (M10). Mockups touched: `editorRegistry.ts` (C1 — `instanceId` comment expanded to mention IPC transfer alongside session restore), `PageModel.ts` (C2 — `saveState()` comment lists both callers — `handleBeforeQuit` and `movePageOut`). C3 (promote `restorePage` to public on `PagesPersistenceModel`) logged for real-code implementation only; `PagesPersistenceModel.ts` isn't a foundation mockup. Net effect: walkthrough 05 mostly inherits walkthrough 04's mechanisms — the new `PageDescriptor` shape and `createEditor(id, instanceId)` route do the heavy lifting; transfer becomes a thin wrapper around the shared `restorePage(desc)` helper.
- 2026-05-19 — Walkthrough 06 (Compare mode) initial analysis written. CK1–CK10 proposed for review. Key proposals: move the compare flag off `TextFileModel.state.compareMode` and onto `PagesModel.state.compareGroups: Set<string>` keyed by left page id (CK1); CompareEditor stays a React component rendered by `Pages#PageContent`, NOT a registered EditorModel — it has two hosts and spans two tabs, both of which break walkthrough 01–05 invariants (CK2); centralize the `instanceof TextFileModel` precondition as `pagesModel.query.canCompare(leftId, rightId)` (CK3); `enterCompareMode(pageId)` / `exitCompareMode(pageId)` accept either side and resolve the leftId internally (CK4); both `PageContent` and `Pages` read from `pagesModel.state.use()` via `query.isInCompareMode(pageId)` (CK5); retire `compareModeChanged` Subscription + `pagesModel.rerender()` field/method — both are dead code once the flag lives next to its consumer (CK6); retire `fixCompareMode` sweep, fold cleanup into `ungroup`/`removePage`/`setMainEditor` explicitly (CK7); `openDiff` collapses to `groupTabs + enterCompareMode` (CK8); persist as `WindowState.compareGroups?: string[]` — additive optional, no schema bump (CK9); CompareEditor receives `leftPageId` prop, exit button calls `exitCompareMode(leftPageId)` (CK10). Two mockup adjustments: `PersistenceTypes.ts` (add optional `compareGroups?: string[]` to `WindowState`) and `TextFileModel.ts` (drop "C6 — TBD" markers, point to walkthrough 06). Awaiting user review concern-by-concern.
- 2026-05-19 — Walkthrough 06 (Compare mode) complete. CK1–CK10 resolved. Key decisions: pair-level `compareGroups: Set<string>` on `PagesModel.state` keyed by left page id replaces the host-level `TextFileModel.state.compareMode` flag — single source of truth, naturally cleaned by ungroup/removePage/setMainEditor (CK1); CompareEditor stays a React component rendered by `Pages#PageContent` — registering it as an EditorModel would break the one-host / one-tab invariants from walkthroughs 01–05 and pollute `findEditorsAccepting` with a context-dependent "accepts" answer (CK2); compatibility check centralized as `pagesModel.query.canCompare(leftId, rightId)` consumed by toolbar visibility, `enterCompareMode` precondition, and PageContent render guard (CK3); `enterCompareMode(pageId)` / `exitCompareMode(pageId)` accept either side and resolve leftId internally so callers don't need to know orientation, return boolean (CK4); `pagesModel.query.isInCompareMode(pageId): { active, leftId?, rightId? }` derived getter consumed by PageContent and toolbar handlers; subscribers wire `state.use(s => s.compareGroups)` for reactivity (CK5); `compareModeChanged` Subscription + `pagesModel.rerender` field + `rerender()` method ALL retired — they existed purely as a bridge between host state and page-level React re-render, dead code once flag lives on PagesModel (CK6); `fixCompareMode` sweep deleted, three inline cleanups land at the mutating methods (`ungroup`, `removePage`, `setMainEditor`) — covers the "host changed shape" case today's sweep silently misses (CK7); `openDiff` collapses to `groupTabs + enterCompareMode` composition (CK8); persistence dropped — Persephone's main window hides to tray instead of closing so compare mode survives in-memory; rare secondary-window save-on-close → restart loses the flag, acceptable; cross-window transfer of a compare pair is impossible by design (both pages can't be dragged simultaneously) so walkthrough 05's IPC concerns don't apply — `WindowState` shape unchanged (CK9); CompareEditor receives `leftPageId` as a third prop and exit button calls `exitCompareMode(leftPageId)` (CK10). Mockups touched: `TextFileModel.ts` (C1 — `compareMode` comments now point to `PagesModel.state.compareGroups` instead of the "C6 — TBD" markers; `setCompareMode` method listed in "REMOVED" block). Net effect: a feature that today requires a host-level field, a global Subscription, a `rerender` counter, a sanity-sweep, and per-host setters collapses into a single pair-level Set with three explicit cleanup hooks. No persistence wiring, no new foundation mockup, no schema bump.
- 2026-05-19 — Walkthrough 07 (Grouped pages) initial analysis written. GK1–GK10 proposed for review. Key proposals: keep today's parallel `leftRight`/`rightLeft` maps on `PagesModel.state` — drift is structurally impossible since every mutation site writes both, YAGNI applies to single-map/derived-reverse variants (GK1); `requireGroupedText` discriminator `mainEditor?.type !== "textFile"` flips to a centralized `pagesModel.query.hasTextFileHost(pageId)` helper mirroring CK3's `canCompare` pattern (GK2); `fixGrouping` keeps both remaining responsibilities (swap-direction fix + dangling-group cleanup) as a single sweep — splitting adds names without value (GK3); `PageTab.closeClick`'s grouped branch drops the `pagesModel.fixCompareMode()` call since CK7 already folds compare cleanup into `ungroup` (GK4); multi-window transfer of a grouped page keeps today's behavior (group dissolves on source via `fixGrouping`, target receives an ungrouped tab) — single-tab gesture is the explicit UX, dual-tab drag is impossible by design (GK5); `ImperativeSplitter.widthK` persistence stays out — UX nicety orthogonal to EPIC-028; safely deferable as additive `WindowState.splitRatios?` later per P10 (GK6); two-pages-per-group invariant is permanent design — no N-page generalization (GK7); pin/group interaction stays as-is — emergent benign behavior with no clear user benefit to change (GK8); script API (`pages.groupedPage` / `getGroupedPage` / `ungroup` / `groupTabs`) unchanged — no EPIC-028 motivation to break (GK9); `isGrouped(editorId)` resolves correctly under unified `editors[]` via the already-updated `findPage` (walkthrough 01 / A8 + 03), no new change required (GK10). No mockup adjustments required — `WindowState.groupings?: [string, string][]` already in `PersistenceTypes.ts` from walkthrough 04. Net effect: grouped pages are page-level layout state, mostly orthogonal to the editor refactor; only GK2's discriminator flip + GK4's CK7-driven simplification are real-code changes. Awaiting user review concern-by-concern.
- 2026-05-19 — **Tier 1 second pass complete.** Re-read walkthroughs 01–07 against the final mockup shape; each got a "Second-pass review" section. Findings: (1) no decision drift — every L1–L7 / S1–S10 / N1–N7 / P1–P10 / M1–M10 / CK1–CK10 / GK1–GK10 resolution holds; (2) some in-doc mockup sketches drifted relative to live `mockups/*.ts` files (most visibly walkthrough 01's A8 PageModel sketch, which predates 03's slice-subscribe bookkeeping, 04's sidebar-cache removal, 05's `saveState` dual-caller doc, and 06's `setMainEditor` compare cleanup hook). Sketches now annotated as "design rationale; live mockups are source of truth"; (3) stability signal positive — walkthroughs 06 and 07 produced essentially zero foundation-mockup changes (one TextFileModel doc-comment touch in 06; none in 07), satisfying the README workflow's "last 2–3 walkthroughs produce zero mockup changes" stability criterion; (4) no new concerns surfaced. Tier 1 is design-ready. Tier 2 (UI surfaces — page tabs, page toolbar, TextChrome) can begin without further reshaping of foundation primitives.
- 2026-05-20 — **Walkthrough 10 (TextChrome) complete.** TC1–TC11 all resolved; two mockup adjustments landed — B1 (new `mockups/TextChrome.tsx` — single shared chrome wrapper with `toolbarContributions` + `footerContributions` slot props, host-instanceof branching internal per TC2, full chrome for TextFileModel + minimal chrome for NoteItemEditModel per TC5, host-owned script panel per TC6, focus subscription + onKeyDown delegation on outer panel per TC8 + TC9), B2 (optional `handleKeyDown?(e): void` added to `IContentHost` interface — repurposed from originally-dropped B2 slot — so chrome's TC9 delegation is type-safe without instanceof cast). B3 dropped (no `EditorModel.script?` accessor needed — script panel stays host-owned). Net effect: `TextEditorView.tsx` + `TextToolbar.tsx` + `TextFooter.tsx` + `ActiveEditor.tsx` + `NoteItemToolbar.tsx` all dissolve; the four portal refs fully retire across both `TextEditorModel` and `NoteItemEditModel` (toolbar pair retired by walkthrough 09; footer + overlay retired here); 13 text-bearing editors each export a React module composing `<TextChrome>` inline around their body; five editors' `createPortal(…, editorFooterRefLast)` blocks rewrite to pass JSX through `footerContributions` (Grid row count, Todo counts, Link status, Graph node count, Notebook note count); Notebook's expanded-note overlay relocates from page-level portal to inline composition over its own body via TC7; encrypted-content fallback to Monaco becomes a one-line per-editor view guard or `<TextChrome>` short-circuit (exact placement decided in walkthrough 20). C1 + C8 fully resolved by Tier 2 — host-capability discovery uses instanceof, no portal refs, no chrome wrapper, each editor composes shared chrome directly. **Tier 2 complete.** Three Tier 2 walkthroughs done (08 / 09 / 10); design is stable. Tier 3 (Special page shapes — empty/well-known pages, walkthrough 11) begins next.
- 2026-05-20 — **Walkthrough 11 (Empty pages & well-known pages) complete.** EW1–EW10 all resolved; zero mockup adjustments landed. Net effect: four distinct page shapes (empty Monaco, sidebar-only, singleton-editor fixed id, well-known singleton) ride the foundation primitives without any new infrastructure. Every concern collapsed to "confirms earlier resolution" — L1 (hardcode monaco for empty page), L3 (`isFreshEmpty()` override pattern), P6 (`createEditor(id, instanceId?)` + `applyRestoreData(RestoreData<S>)` canonical phase-2 entry), P8 (empty-with-sidebar persistence), M5 (instance-id continuity for the well-known case), S10 (`state.editor` retirement + `editorId` field on EditorModel) cover the design space. Real-code surface: async-ify four `PagesLifecycleModel` entry points (`addEmptyPage`, `addEmptyPageWithNavPanel`, `addEditorPage`, `requireWellKnownPage`); rewrite them to the `createEditor + applyRestoreData + restore` pattern; today's `page.createExplorer` PageModel method dissolves into `addEmptyPageWithNavPanel`'s inline direct-import; today's "single TextFileModel with `s.editor = X` flag" anti-pattern in `addEditorPage` replaced by per-class registry construction; About / Settings / Storybook singleton flows unchanged beyond per-page-module migration in walkthrough 30. **Tier 3 complete** (single-walkthrough tier; no second pass required). Tier 4 (cross-cutting) begins next with walkthrough 12 — scripting facades.
- 2026-05-20 — Walkthrough 11 / EW10 resolved. **EW10 — `closeFirstPageIfEmpty` heuristic via `isFreshEmpty()` discrimination**: option (a) — four-condition check. Monaco's `isFreshEmpty()` override checks `host.state.content === ""` AND `host.state.filePath === undefined` AND `host.state.modified === false` AND `editor.state.title === ""` (default title). The title check is the discriminator: `addEmptyPage()` produces default title → fresh-empty → close-eligible; `addEditorPage("monaco", "js", "Custom Title")` produces user-set title → not fresh-empty → keep. Rejected (b) skip title check (would auto-close user's titled page on file-open) and (c) add `pristine: boolean` flag flipped on any user mutation (introduces state field for what's derivable from existing state). Override body finalized in walkthrough 20 (Monaco). Signature already exists on EditorModel mockup per L3. No mockup change required.
- 2026-05-20 — Walkthrough 11 / EW9 resolved. **EW9 — Persistence of empty-with-sidebar pages**: confirmed (verification-only). `addEmptyPageWithNavPanel`'s output round-trips via walkthrough 04 / P8 ("empty-pages-with-sidebar stop being a special case") + P5's `Promise.all` per-editor restore. `PageDescriptor`: `mainEditorId: null`, `editors: [explorerDescriptor]`, `sidebar: { open, width, activePanel: "explorer" }`. Restore reconstructs Explorer via the direct-construction path (per EW5) since Explorer is not in the main editor registry — restoring the page calls into `PagesPersistenceModel.restorePage` which loops `editors[]` and dispatches the explorer descriptor through the same dynamic-import as create. `mainEditorId === null` skips the main-editor assignment; `sidebar?` triggers `ensurePageNavigatorModel()` on the restored page. No new mechanism; this row is a verification entry. No mockup change required.
- 2026-05-20 — **Walkthrough 24 (Link) complete.** LK1–LK10 all resolved; **zero mockup adjustments** — fourth template-confirmation walkthrough in a row (after Grid, Preview group, Log View). **First sidebar-owning editor in Tier 5** validates the EPIC-028 lifecycle-hook machinery built into the foundation mockups since walkthrough 03 (`beforeNavigateAway` + `onMainEditorChanged`). Final outcomes: (LK1) LinkEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — **sixth Tier 5 editor** in uniform shape; (LK2) state slice: 5 persisted / 11 ride-state stripped / 8 private; `leftPanelWidth` previously forgotten on restart (silent today-bug) incidentally fixed; (LK3) **third instance** of "per-editor cache file → descriptor.state" consolidation (Grid GR4 → Log View LV3 → Link LK3) — today's `restoreSelectionState` + `selectionRestored` one-shot flag both retire; (LK4) three-site lifecycle split mirrors LV4; (LK5) **second instance** of self-write-guard pattern (LV6 → LK5); pattern now standardized; (LK6) `setSidebarPanels(open)` model method called by view's useEffect (gated on `mainEditor === model` for demote-safe no-op); (LK7) **first text-bearing-editor exercise** of `beforeNavigateAway` override — `contributesPanels()` discriminator establishes second valid shape alongside Archive S8's sourceLink-based pattern; today's view-side cleanup early-return (`LinkEditor.tsx:144-152`) **disappears entirely**; (LK8) **first text-bearing-editor exercise** of `onMainEditorChanged` override — adjusts panel list to standalone-secondary shape on demote; today's `LinkCategorySecondaryEditor.updatePanels` useEffect **disappears entirely**; (LK9) typed accessors on LinkEditor class (`get treeProvider()` + `selectByHref(href)` + `selectionState`); CategoryEditor's `findTreeProviderHost` flips to `instanceof` chain; **three `(m as any)` casts retire across writer + reader**; (LK10) filename `.link.json` priority 70 + content-peek priority 60 (mirrors LV10/GR10 calibration); minimal queue `{ focus }` only. **Headline observations:** (1) Tier 5 template carries cleanly across the **most lifecycle-heavy editor yet** — the eight-piece template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) absorbs the two new lifecycle overrides without strain; (2) the `beforeNavigateAway` + `onMainEditorChanged` hooks — designed in walkthrough 03 for exactly this purpose — fit alongside the existing override list with zero base-class machinery growth; (3) **first demonstration that the two hooks compose cleanly** as a "demote-and-re-shape" recipe (preserve panels via `beforeNavigateAway` → page sets new main → respond to demote via `onMainEditorChanged`); (4) **first typed retirement of duck-typed model writes** via typed-accessor pattern on LinkEditor — sets the precedent for Archive + Explorer (walkthrough 30) which also expose tree-provider sources via the same accessor name; (5) the Tier 5 class repetition count grows to **six editors** with the same ~80-LOC CONTENT_HOST_TRAIT + adoptHost + switchFrom + restore + dispose skeleton; PV1's "re-evaluate after walkthroughs 23–29" continues to apply. B1 LK7 doc-comment refinement NOT landed — Archive's S8 pattern stays canonical in mockup; LinkEditor demonstrates second valid shape by example. Walkthroughs 25 (Todo) and 26 (Rest Client) are next — both sidebar-owning editors that will follow the LK6 + LK7 + LK8 + LK9 recipe.
- 2026-05-20 — **Walkthrough 24 (Link) initial draft written.** LK1–LK10 proposed for review. **First sidebar-owning editor in Tier 5** — exercises `beforeNavigateAway` + `onMainEditorChanged` hooks deferred from walkthrough 03; resolves today's "demote survives as secondary" view-side cleanup-function hack; retires three `(m as any)` duck-typed model writes in `LinkCategorySecondaryEditor.tsx:37-52` for typed accessors on LinkEditor class. Key proposals: (LK1) LinkEditor IS mainEditor + TextFileModel host with CONTENT_HOST_TRAIT — sixth Tier 5 editor in uniform shape; (LK2) state slice partitioning into 5 persisted / 11 ride-state / 8 private fields; (LK3) selection-state cache file (`<host.id>-link-editor.txt`) folds into `EditorDescriptor.state` — third instance of cache-file → descriptor pattern (after Grid GR4 + Log View LV3); (LK4) three-site lifecycle split (restore initial parse + adoptHost content subscription + dispose flush) mirrors LV4; (LK5) `skipNextContentUpdate` self-write guard preserved verbatim — second instance after LV6; (LK6) sidebar panel registration becomes `setSidebarPanels(open)` model method called from view's useEffect (gated on `mainEditor === model` for demote-safe no-op); (LK7) `beforeNavigateAway` override survives the editor as secondary if `contributesPanels()` is true (replaces today's view-side cleanup early-return); (LK8) `onMainEditorChanged` override drops "link-hostnames" + conditionally drops "link-tags" on demote (replaces today's `LinkCategorySecondaryEditor.updatePanels` useEffect at lines 57-71); (LK9) typed TreeProvider accessors on LinkEditor (`get treeProvider()`, `selectByHref(href)`, `selectionState`) — three duck-typed `(m as any).treeProvider = …` writes retire across writer + reader; (LK10) registry `accepts()` filename `.link.json` priority 70 + content-peek `"type":"link-editor"` + `"links"` priority 60 fallback; minimal queue `{focus}` only. Zero mockup changes expected — Tier 5 template (state slice + queue unions + view + accepts + lifecycle overrides + persistence + optional overrides + CONTENT_HOST_TRAIT) carries LinkEditor end-to-end; the `beforeNavigateAway` + `onMainEditorChanged` hooks are already present in `mockups/EditorModel.ts:292-303`. Optional B1 doc-comment refinement on `beforeNavigateAway` flagged but non-essential. Awaiting user review concern-by-concern.
- 2026-05-20 — Walkthrough 11 / EW8 resolved. **EW8 — Singleton dedup pattern**: option (a) — keep the explicit `findPage` short-circuit at the top of `requireWellKnownPage`. Short-circuits before the editor module's dynamic-import on a hot path (clicking a well-known menu item when the page is already open is a frequent operation). Saving the Monaco / LogView module load is real (non-trivial bundle). About / Settings / Storybook keep their implicit-via-addPage dedup (no explicit check; `addPage`'s `findPage` collision check + `showPage` handles it) — their imports are small and they're rarely opened more than once. Rejected (b) rely on `addPage`'s implicit check, drop the explicit one (saves four lines but loses the hot-path module-load skip) and (c) move the explicit check into `addPage` with a fast-path return (couples singleton-dedup concern with general add-page). Documented as a hot-path optimization comment at the call site in `requireWellKnownPage`. No mockup change required.
- 2026-05-20 — Walkthrough 11 / EW7 resolved. **EW7 — `WellKnownPageDef` future extensibility**: option (a) — keep minimal `{ id, title, editor, language }`. YAGNI; the two existing well-known pages don't need defaults beyond what's in the today-def. Rejected (b) widen to `{ id, editorId, state?, host? }` mirroring `EditorDescriptor` (lets defs specify arbitrary state including things that make no sense for singletons like `filePath`) and (c) half-step `content?: string` (matches no concrete request). Future fields added when concrete needs land. No file edit to `well-known-pages.ts`.
- 2026-05-20 — Walkthrough 11 / EW6 resolved. **EW6 — `WellKnownPageDef.language` placement**: option (a) — `applyRestoreData` with synthetic `HostDescriptor.state.language`. Same lane as EW2 — language is host-owned post-EPIC-028 (`TextFileModel.state.language`), and the canonical phase-2 entry point per P6 threads any host-state into `restore()` cleanly. The well-known `applyRestoreData` blob bundles `title` (editor's own state) + `host.state.language` (host's state) in a single call: `editor.applyRestoreData({ title: def.title, host: { kind: "textFile", state: { language: def.language } } })`. Rejected (b) post-restore direct mutation (races with `restore()`; bypasses canonical contract) and (c) top-level `language?` shortcut field on `RestoreData` (adds convenience for one caller; layer leak — `RestoreData` doesn't know which subclasses host language). No mockup change required.
- 2026-05-20 — Walkthrough 11 / EW5 resolved. **EW5 — `addEmptyPageWithNavPanel` Explorer creation**: option (a) — direct construction. `addEmptyPageWithNavPanel` does `const { ExplorerEditorModel } = await import(...) + new ExplorerEditorModel() + explorer.state.update(s => { s.rootPath = folderPath }) + await explorer.restore() + explorer.secondaryEditor = ["file-explorer"] + page.attach(explorer) + page.ensurePageNavigatorModel() + addPage(null, page)`. Preserves today's "Explorer is not in main editor registry" pattern (see today's code at `PagesLifecycleModel.newEditorModelFromState` lines 87-90 — `state.type === "fileExplorer"` is handled by direct dynamic-import). Explorer is the only secondary-only editor today; one-off direct construction matches the today-pattern and avoids inventing a registry for one editor. Side effect: today's `page.createExplorer(folderPath)` PageModel method dissolves — its logic inlines into `addEmptyPageWithNavPanel`; PageModel stays editor-agnostic. Rejected (b) separate secondary-editor-only registry mirroring `editorRegistry` (registry for one consumer) and (c) `category === "secondary-only"` flag in the main registry (adds a category for one editor). Revisit if a second secondary-only editor surfaces. No mockup change required.
- 2026-05-20 — Walkthrough 11 / EW4 resolved. **EW4 — Page id and editor id namespace overlap in well-known flow**: option (a) — same string in both namespaces by explicit design. `def.id` becomes both the editor's instance id (via EW3's `createEditor(def.editor, def.id)`) AND the page's id (via `new PageModel(def.id)`). The two namespaces happen to use the same string because each well-known singleton has one "identity"; `findPage` resolves either way per walkthroughs 01 / A8 + 03 + 07 / GK10's unified-editors[] migration; cache-key continuity (EW3) keys on the same string; dedup query reads cleanly without a lookup table. Rejected (b) fresh editor uuid + `def.id` only for page id (loses cache continuity from EW3 unless we also pass `def.id` as a separate cache prefix) and (c) fresh uuids for both + semantic-id-to-page-id registry table (adds infrastructure for no observable benefit). Documented as a singleton-id design choice in `well-known-pages.ts` comment block. No mockup change required.
- 2026-05-20 — Walkthrough 11 / EW3 resolved. **EW3 — `requireWellKnownPage` instance-id propagation**: option (a) — `await editorRegistry.createEditor(def.editor, def.id)` propagates the well-known id as the editor's instance id at construction time, per P6 / M5's `instanceId` parameter. No post-construction id mutation; today's `s.id = id` line deleted. Cache files keyed on `def.id` survive restarts — `mcp-ui-log-host.txt` (host content) and any editor-specific state files (`mcp-ui-log-log-view.json` etc.) persist across reboots, so log content is preserved. Rejected (b) post-construction `editor.id = def.id` mutation (opens a cache-key-mismatch window between construction and the assignment). No mockup change required — M5/P6's existing `instanceId` param carries a new caller.
- 2026-05-20 — Walkthrough 11 / EW2 resolved. **EW2 — `addEditorPage` shape post-S10**: option (a) — `applyRestoreData` with a synthetic `HostDescriptor`. New shape: `await editorRegistry.createEditor(editorId) + editor.applyRestoreData({ title, host: { kind: "textFile", state: { content: content ?? "", language } } }) + await editor.restore() + addPage(editor)`. Replaces today's `newTextFileModel("") + s.editor = X + changeContent(content) + restore()` shape (the `state.editor` field is retired by S10; today's "single TextFileModel with editor flag" anti-pattern dissolves into per-class registry construction). Threads through the canonical phase-2 entry point per P6's `RestoreData<S>` contract — same lane session-restore and `openFile` use, no new shape. Side effect: `addEditorPage` becomes async; runtime positional-args guard + standalone-editor reject preserved verbatim. Layering note acceptable in scope — `addEditorPage` accepts text-bearing editor ids only (the standalone-reject guard enforces this at runtime), so the "textFile" host kind string is always correct. Rejected (b) direct post-restore state mutation (races with `restore()` and duplicates the creation path) and (c) registry `createEditor(editorId, instanceId?, initialState?)` overload (one-caller third parameter). No mockup change required — P6's existing primitive carries a new caller.
- 2026-05-20 — Walkthrough 11 / EW1 resolved. **EW1 — `addEmptyPage` shape**: option (a) — two-call shape `editorRegistry.createEditor("monaco") + await editor.restore() + addPage(editor)`. Confirms L1 (hardcode `"monaco"` — Persephone is a Notepad replacement by design) and A7 (three-phase lifecycle pattern; `addEmptyPage` skips phase 2 since there's no setup data). Side effect: `addEmptyPage` becomes async (returns `Promise<PageModel>`); callers in async chains pick up the await transparently. Script API surface (`pages.addEmpty()`) wrapping deferred to walkthrough 12's facade work. Rejected (b) `editorRegistry.createEmpty(editorId)` helper — one-caller convenience for what's already a two-line idiom; matches every other lifecycle entry point. No mockup change required.
- 2026-05-20 — **Walkthrough 11 (Empty pages & well-known pages) initial draft written.** EW1–EW10 proposed for review. Tier 3 opener — single-walkthrough tier covering four distinct page shapes: empty Monaco (`addEmptyPage`), sidebar-only with no main editor (`addEmptyPageWithNavPanel`), singleton-editor with fixed page id (About/Settings/Storybook), and well-known singletons (mcp-ui-log/mcp-server-log). Mostly a verification-and-tie-down walkthrough — every recommendation flows from earlier resolutions (L1, L3, P6, P8, M5, S10). Key proposals: (EW1) `addEmptyPage` becomes async — `createEditor("monaco") + restore()` confirms L1 + A7; (EW2) `addEditorPage` rewrites to `createEditor(editorId) + applyRestoreData({title, host: {kind: "textFile", state: {content, language}}}) + restore()` replacing today's `newTextFileModel + s.editor = X` shape (`state.editor` retired by S10); (EW3) `requireWellKnownPage` uses `createEditor(def.editor, def.id)` so the well-known id propagates to the editor's instance id via P6/M5's `instanceId` param — cache files key on `def.id` and survive restarts; (EW4) page-id-and-editor-id namespaces overlap intentionally in well-known flow — same string in both, `findPage` resolves either way; (EW5) Explorer creation in `addEmptyPageWithNavPanel` stays direct construction (`new ExplorerEditorModel`) since Explorer is secondary-only and not in the main editor registry; today's `page.createExplorer` PageModel method dissolves; (EW6) language flows through `applyRestoreData({host: {state: {language}}})` — same lane as content per P6; (EW7) `WellKnownPageDef` shape stays minimal — `{id, title, editor, language}`; future fields added when concrete needs land (YAGNI); (EW8) keep explicit `findPage` short-circuit at top of `requireWellKnownPage` — avoids dynamic-import of editor module on hot path (clicking already-open menu item); About/Settings/Storybook keep implicit-via-addPage dedup; (EW9) empty-with-sidebar persistence already covered by P8 + P5 — verification only; (EW10) `isFreshEmpty()` Monaco override checks `host.content === "" && filePath === undefined && !modified && editor.title === ""` — title check is the discriminator that distinguishes `addEmptyPage()` (default title) from `addEditorPage("monaco", "js", "Custom Title")` (user-set title). No mockup adjustments expected — every proposal collapses to "confirms earlier resolution"; if user review surfaces a real foundation gap, B1+ entries land. Awaiting user review concern-by-concern.
- 2026-05-20 — Walkthrough 10 / TC11 resolved. **TC11 — Encryption indicator placement**: option (a) — no new UI. C8's "encryption padlock in PageToolbar" was a hypothetical illustration of host-instanceof branching, not a today-shape needing migration — there's no padlock inside `TextEditorView`'s body chrome today. PageTab's lock icon stays (walkthrough 08 / T2); `ActiveEditor.tsx` dissolves entirely per TC3 (no `state.editor` dispatch per S10); its only surviving responsibility — encrypted-content fallback to Monaco — becomes a one-line guard inside each text-bearing editor's view or inside `<TextChrome>` before rendering children (functionally equivalent; walkthrough 20 picks exact placement). Rejected (b) padlock in `<PageFooter>` and (c) padlock in `<PageToolbar>` — both add new UI outside epic scope. No mockup change.
- 2026-05-20 — Walkthrough 10 / TC10 resolved. **TC10 — Footer portal retirement and per-editor footer status**: option (a) — `<TextChrome>`'s `footerContributions?: ReactNode` prop (already part of B1's signature). Editor's view passes a node rendered inside the footer row before the encoding label. Same shape and pattern as TC1's `toolbarContributions`. Deletions: `editorFooterRefLast` field + `setFooterRefLast` setter on TextEditorModel + NoteItemEditModel; `TextFooter.tsx`'s portal `<div>` block (file dissolves with TextEditorView per TC3); five editors' `createPortal(…, editorFooterRefLast)` blocks rewrite to pass JSX through `footerContributions` (Grid row count, Todo counts, Link status, Graph node count, Notebook note count). NoteItemEditModel branch ignores `footerContributions` (no footer row per TC5). Rejected (b) `getFooterStatus?: () => ReactNode` method (can't subscribe to state — needs a component anyway) and (c) inline per-editor footer (drops consistent encoding-label position). No mockup change beyond B1's existing prop.
- 2026-05-20 — Walkthrough 10 / TC9 resolved + small mockup adjustment landed. **TC9 — `model.handleKeyDown` migration**: option (a) — `<TextChrome>` binds `onKeyDown` on its root and delegates to `editor.contentHost?.handleKeyDown?.(e)`. Host owns the keystroke handling — Ctrl+S/Ctrl+Shift+S/F5/F2 all read or mutate host content. TextFileModel's existing `TextFileActionsModel.handleKeyDown` survives as-is; just the binding moves from TextEditorView's root (deleted per TC3) to the chrome's root. NoteItemEditModel branch also calls `host.handleKeyDown?.(e)`; NoteItemEditModel can opt in (deferred to walkthrough 29). Rejected (b) shared helper with internal branching (splits dispatch from action) and (c) per-editor onKeyDown (duplicates Ctrl+S across 13 editors). **Mockup adjustment**: added optional `handleKeyDown?(e: React.KeyboardEvent): void` to `IContentHost` (B2 — was previously dropped, repurposed for this) so the chrome's delegation is type-safe without an instanceof cast. Walkthrough 20 finalizes Monaco's handler body; walkthrough 29 decides per-note shortcuts for NoteItemEditModel.
- 2026-05-20 — Walkthrough 10 / TC8 resolved. **TC8 — Focus management migration**: option (b) — `<TextChrome>` owns the focus subscription on its outer panel. Per TC1 (a), the chrome already renders the outer `<Panel>`; making that panel focusable (`tabIndex={0}` + ref + `useEffect` subscribing to `pagesModel.onFocus` with 200ms refocus) centralizes the behavior at the chrome layer — same shape as today's `TextEditorView` subscription, just at a different layer. NoteItemEditModel branch gets the same subscription on its outer panel (one shared hook regardless of host class). Rejected (a) shared `useEditorFocus(model)` hook each text-bearing editor calls (13 imports for one behavior) and (c) skip / rely on browser default focus (regresses today's 200ms refocus that exists because Monaco's mount dance takes a moment to settle). Non-text editors handle focus per-editor — same as today. No mockup change beyond B1's `useEffect` sketch.
- 2026-05-20 — Walkthrough 10 / TC7 resolved. **TC7 — `<EditorOverlay>` shape and lifetime**: option (c) — drop `<EditorOverlay>` as a chrome piece entirely. Notebook is the only consumer; pulling overlay out of `<TextChrome>` removes a shared surface that exists for one editor. Notebook restructures to inline-compose `<ExpandedNoteView>` over its body when `pageState.expandedNoteId` is set — `<Panel position="relative">{body}{expandedNote && <div className="editor-overlay">…</div>}</Panel>`. The `editor-overlay` CSS class survives (provides absolute positioning); only the portal indirection goes away. Deletions: `editorOverlayRef` field + `setEditorOverlayRef` setter on TextEditorModel; the overlay `<div>` inside the doomed `TextEditorView.tsx` (file dissolves per TC3); the `createPortal(<ExpandedNoteView …>, model.editorOverlayRef!)` block at `NotebookEditor.tsx:293-306` (replaced by inline JSX). Rejected (a) chrome slot + React context (adds context boundary for one consumer) and (b) `editor.getOverlayContent?()` (moves indirection from portal to method call). No mockup change.
- 2026-05-20 — Walkthrough 10 / TC6 resolved. **TC6 — Script panel ownership**: option (a) — stay host-owned. `TextFileModel.script: ScriptPanelModel` survives unchanged in shape; chrome reads `(host as TextFileModel).script` inside the TextFileModel branch with additional `host.script != null` guard. Script execution conceptually tied to the file (host.content + host.filePath); switching editors shouldn't dump in-progress script. Cache file `<editor.id>-script-panel.json` (walkthrough 04 / C9) keys on `editor.id` which transfers across `switchFrom` per P6 — panel survives editor swap for free. Real-code nit deferred to walkthrough 20: `ScriptPanelModel.restore(id)` must take the editor id at restore time (the cache file is editor-keyed), so the call moves from `TextFileModel.restore()` to the editor's `restore()` method. Rejected (b) editor-attached (loses panel-survives-switch property) and (c) chrome-owned (no persistence, throwaway). No mockup change; B3 dropped (no `EditorModel.script?` accessor needed).
- 2026-05-20 — Walkthrough 10 / TC5 resolved. **TC5 — `<TextChrome>` minimal-chrome contract for `NoteItemEditModel`**: option (a) — single `<TextChrome>` component, internal host-instanceof branch (TC2) drops the page-level pieces. NoteItemEditModel branch renders: focusable outer `<Panel>` + `<PageToolbar>` with note's language menu + editor's `toolbarContributions` + Run/Run-all (when script) + auto-inserted switch widget + body. NO script panel, NO footer, NO overlay, NO Compare button. `NoteItemToolbar.tsx` dissolves — its responsibilities (language menu + Run + portal slots + switch) migrate into the NoteItemEditModel branch; the note's title moves to the editor view's `toolbarContributions` prop. NoteItemEditModel's three portal refs retire (toolbar pair already retired by walkthrough 09 / PT9; footer ref retires here since the NoteItemEditModel branch has no footer). Rejected (b) per-host chrome variants `<TextChrome>` vs. `<NoteChrome>` (editor doesn't know host class at compile time — same Monaco runs against both hosts; dispatch must happen at render time inside one shared wrapper) and (c) explicit framing variant (functionally identical to (a)). No mockup change beyond B1's existing two-branch sketch.
- 2026-05-20 — Walkthrough 10 / TC4 resolved. **TC4 — Toolbar contribution order in `<PageToolbar>` row**: option (a) — text-host buttons → editor contributions → spacer → Show-resources → switch widget. Concrete left-to-right: NavPanel (auto, PT5) → Compare → Run → Run-all → editor's `toolbarContributions` prop (Markdown view-mode, Grid filters, compact-mode button, etc.) → spacer (auto) → Show-resources → switch widget (auto, PT2). Matches today's dominant `TextToolbar.tsx` pattern (Show-resources on the right of spacer; Compare/Run/Run-all on the left). Drops today's rare `First` portal slot (Grid/Link/Notebook used it but visual difference is negligible). Rejected (b) editor contributions first / text-host buttons after and (c) split `leftToolbar` / `rightToolbar` slot pair (re-creates First/Last bucket pair PT3 already rejected). Encoded inside `<TextChrome>`'s `<PageToolbar>` children — sub-component render order in B1 mockup follows this list.
- 2026-05-20 — Walkthrough 10 / TC3 resolved. **TC3 — `TextEditorView` dissolution**: option (a) — each text-bearing editor's loaded module composes `<TextChrome>` around its body directly. PT1 (b) already committed to "each editor's view composes shared chrome components directly"; (a) extends that to walkthrough 10's chrome — each of the 13 text-bearing editors gets its own React module exporting a component that wraps body in `<TextChrome>` inline (10–15 lines). `TextEditorView.tsx` is deleted; `ActiveEditor.tsx` ALSO dissolves (it was the dispatch between Monaco and other editors based on `state.editor` which S10 retired; with no `state.editor` field, no dispatch left to do — each editor IS the editor). Rejected (b) `<AsyncEditor>` auto-wraps when `editor.contentHost != null` (the "magic frame" pattern PT1 rejected) and (c) per-module declarative `usesTextChrome?` flag (adds registry-side concept for a view-level decision). No mockup change required.
- 2026-05-20 — Walkthrough 10 / TC2 resolved. **TC2 — Host-capability discovery**: option (a) — inline instanceof at the chrome's consumer. `<TextChrome>` reads `model.contentHost` and branches: `if (host instanceof TextFileModel) … else if (host instanceof NoteItemEditModel) … else return null`. Confirms C1's instanceof choice. Considered following GK2's centralized-helper pattern with new editor-keyed helpers (`getTextFileHost(model)` + `getNoteItemHost(model)` — same shape as GK2 but EditorModel-keyed rather than pageId-keyed, since the chrome reads `model.contentHost` directly while GK2's pageId-keyed helper resolves to the page's MAIN editor's host — which for embedded note editors gives the notebook's TextFileModel, not the note's NoteItemEditModel). Decision: keep inline instanceof — only two host classes and one consumer (`<TextChrome>` + its sub-components); the centralization payoff that justified GK2 across 14 PageTab callsites doesn't apply here. Sub-components receive typed host as a prop (`<CompareButton host={textHost} … />`) so the instanceof doesn't recur. Same conceptual pattern as GK2 still applies if a future third host or third consumer surfaces — promote to editor-keyed helpers then. Rejected (b) sub-traits on `IContentHost` and (c) capability flags on `EditorModel`. No mockup change; no `IContentHost` change; no helper module added.
- 2026-05-20 — Walkthrough 10 / TC1 resolved. **TC1 — Shape of shared chrome**: option (a) — single `<TextChrome model>{body}</TextChrome>` wrapper component with two named slot props (`toolbarContributions?: ReactNode` for inside-toolbar custom buttons, sits between text-host buttons and the spacer; `footerContributions?: ReactNode` for inside-footer status, sits before the encoding label). The chrome owns the page-level uniform pieces (text-host action buttons, script panel, footer row, overlay branching); editor-specific contributions go through the slots. Fragments and conditional contributions work via standard React; reactivity stays the editor's internal concern. Example: MarkdownView passes `toolbarContributions={<><CompactModeButton model={model} /></>}` to surface its compact-mode button between the text-host actions and the spacer. Rejected (b) separately-imported chrome pieces (5+ imports per editor, no opt-out benefit) and (c) hybrid wrapper with single unnamed `toolbar` prop (PT3 already rejected single-named slots over inline composition). Constraint accepted: one toolbar slot before the spacer (post-spacer placement deferred — YAGNI today). Drives B1 mockup later.
- 2026-05-20 — **Walkthrough 10 (TextChrome) initial draft written.** TC1–TC11 proposed for review. Key proposals: single shared `<TextChrome>` wrapper each text-bearing editor's view composes around its body — internal host-instanceof branching (TextFileModel → full chrome; NoteItemEditModel → minimal chrome) per C1 (TC1, TC2). `TextEditorView.tsx` dissolves; each text-bearing editor's loaded module composes `<TextChrome>` directly — no AsyncEditor auto-wrap (TC3). Toolbar row order: NavPanel (auto) → Compare → Run → Run-all → editor contributions → spacer → Show-resources → switch widget (auto) (TC4). Minimal-chrome branch for NoteItemEditModel drops script panel + footer row + overlay + Compare button; keeps language menu + Run buttons + switch widget; `NoteItemToolbar.tsx` dissolves into the NoteItemEditModel branch (TC5). Script panel stays host-owned (`TextFileModel.script: ScriptPanelModel` survives unchanged) — switch-across-editors persistence via existing `<editor.id>-script-panel.json` cache file from walkthrough 04 / C9 (TC6). Notebook expanded-note overlay relocates from page-level portal (`editorOverlayRef` deleted) to Notebook-internal inline composition over its body — no `<EditorOverlay>` chrome piece (TC7). Focus management subscription absorbs into `<TextChrome>`'s outer panel (TC8). `model.handleKeyDown` migrates to chrome's `onKeyDown` delegating to `host.handleKeyDown?.(e)` (TC9). Footer portal `editorFooterRefLast` retires via `footerContributions?: ReactNode` prop on `<TextChrome>`, sitting before the encoding label (TC10). No new encryption UI — encrypted-fallback-to-Monaco stays a per-editor view guard since `ActiveEditor.tsx` also dissolves with `state.editor` retired per S10 (TC11). One mockup adjustment proposed: B1 — new `mockups/TextChrome.tsx` shared chrome wrapper with full sub-component sketches (CompareButton, RunButtons, ShowResourcesButton, ScriptToggleButton, EncodingLabel, NoteLanguageMenu). B2 dropped per TC2 (confirm C1 instanceof). B3 dropped per TC6 (script stays host-owned). Net effect when concerns land: 13 text-bearing editors get a 10–15 line view that wraps body in `<TextChrome>`; the four portal refs are fully retired across both TextEditorModel and NoteItemEditModel; `TextEditorView.tsx` + `TextToolbar.tsx` + `TextFooter.tsx` + `ActiveEditor.tsx` + `NoteItemToolbar.tsx` all dissolve; the last Tier 2 walkthrough before Tier 3 (Special pages). Awaiting user review concern-by-concern.
- 2026-05-20 — **Walkthrough 09 (Page-level toolbar) complete.** PT1–PT10 all resolved; three mockup adjustments landed — B1 (new `mockups/PageToolbar.tsx` — page-level toolbar host auto-rendering NavPanel button left + switch widget right), B2 (`EditorModel.hasTextSelection?(): boolean` optional method — Monaco-only override for Run-all-script visibility), B3 (`EditorModel.getNavigatorTarget(): { pipe?, filePath? } | null` accessor — per-editor declaration of NavPanel button target). Net effect: the page-level toolbar layer becomes a single shared `<PageToolbar model={editor}>{contributions}</PageToolbar>` component each editor's view composes inline. Two auto-rendered slots (NavPanel left, switch widget right) replace today's six per-editor inline NavPanel IconButton blocks AND today's TextToolbar-baked switch widget. Editor-specific contributions sit as JSX children — no portals, no chrome wrapper. `TextEditorView` dissolves; `RenderEditor`'s category branch collapses to a single uniform `<AsyncEditor>` path; `TextToolbar.tsx` deletes as a separate component (text-host-specific actions migrate to walkthrough 10's `<TextChrome>`); the four portal refs on TextEditorModel + NoteItemEditModel retire (toolbar pair here; footer + overlay in walkthrough 10); ten editor views rewrite `createPortal` toolbar contributions to inline children. NavPanel button reclassified from text-host-specific to page-level uniform affordance via PT5 reframe — the helper trio (`getNavigatorTarget` + `page.canOpenNavigator` + `page.toggleNavigator`) makes the button a single declarative read with zero per-editor IconButton wiring. Walkthrough 10's `<TextChrome>` consumes only the four remaining text-host-specific buttons (Compare, Run, Run-all, Show-resources) plus the footer + overlay + script panel. Second Tier 2 walkthrough done; walkthrough 10 (TextChrome) is the last before Tier 3.
- 2026-05-20 — Walkthrough 09 / PT10 resolved + B1 mockup landed. **PT10 — Switch widget visibility predicate**: option (c) — render only when `findCompatibleEditors().length >= 2` AND the current editor is in the list. Strict version of "at least two compatible alternatives, current included" — guarantees SegmentedControl always has a non-null selected value. Rejected (a) `>= 2` without "current in list" guard (would render with no selected value if current editor isn't in alternatives — a registry/host-incompatibility bug, but widget shouldn't paper over it) and (b) `>= 1` (one-option pickers are ugly UX). Encoded inside `<PageToolbar>`'s `<SwitchWidget>` per B1. **B1 mockup**: new file `mockups/PageToolbar.tsx` created with full doc block + auto-rendered NavPanel button (PT5 / B3) + switch widget (PT2 / PT10) + inline editor contributions (PT3); wraps real-code `EditorToolbar` as the row container; "What's gone vs. today's pattern" footer enumerates the eight today-shape mechanisms it retires (portal refs, portal-target divs, createPortal blocks, six per-editor NavPanel IconButtons, `getSwitchOptions`+`detectedContentEditor` weave, `model.changeEditor`).
- 2026-05-20 — Walkthrough 09 / PT9 resolved. **PT9 — Portal refs retirement scope**: option (a) — walkthrough 09 retires `editorToolbarRefFirst` + `editorToolbarRefLast` only; the footer ref + overlay ref defer to walkthrough 10. Each walkthrough owns the portion of TextEditorView it scopes — 09 = toolbar refs; 10 = footer, overlay, script panel. Real-code consequences for 09: delete the toolbar ref fields/setters from `TextEditorModel.ts:159-178` + `NoteItemEditModel.ts:193-244`; remove portal `<div ref={…} />` markers from `TextToolbar.tsx`; rewrite ten editor views (Grid, Markdown, Mermaid, SVG, Todo, Link, LogView, Draw, Graph, Notebook + per-note) to compose toolbar contributions inline as children of `<PageToolbar>` per PT3 (a). Rejected (b) defer all four refs to 10 (lumps too much) and (c) retire all four in 09 (lumps 10's domain — footer, overlay, script panel, host-instanceof). No mockup change required.
- 2026-05-20 — Walkthrough 09 / PT8 resolved. **PT8 — Show-resources button placement**: option (a) — lives inside walkthrough 10's `<TextChrome>` shared component. Pure host-content operation; reads host language for visibility (`language === "html"`); onClick imports `extractHtmlResources` and opens links via `pagesModel.openLinks`. No editor-view dependency. Same host-language-driven pattern as Run-script (PT7) but simpler — no selection probe. Walkthrough 09 confirms the slot; walkthrough 10 implements (left/right position relative to Run/Run-all decided there). Rejected (b) Monaco-only contribution (HTML in Grid/Markdown mode should still expose resources — operation is on host.content, not Monaco). No mockup change required.
- 2026-05-20 — Walkthrough 09 / PT7 resolved + B2 mockup landed. **PT7 — Run-script / Run-all buttons placement**: option (c) — split along host vs. editor-view capability boundary. Run-script visibility lives in `<TextChrome>` and reads host language (`isScriptLanguage(language)`); onClick calls `host.runScript()` (host owns script execution against host.content). Run-all additionally requires `editor.hasTextSelection?.() === true` — Monaco-view-only capability; other editors don't override `hasTextSelection?` so Run-all hides automatically. Preserves today's UX (Run-all Monaco-only) without leaking Monaco specifics into `<TextChrome>`. Position confirmed (after Compare, before spacer); walkthrough 10 implements `<TextChrome>`; walkthrough 20 finalizes Monaco's override. Rejected (a) `<TextChrome>` renders both based on host language alone (Monaco-internal selection leaks out) and (b) per-editor toolbar contribution (script-language file in Grid/Markdown would lose Run button — semantically wrong). **B2 mockup**: `hasTextSelection?(): boolean` optional method added to `mockups/EditorModel.ts` with full doc block + header note; promoted to a `SELECTION_TRAIT` only if a second selection-aware editor lands.
- 2026-05-20 — Walkthrough 09 / PT6 resolved. **PT6 — Compare-with-left button placement**: option (a) — lives inside walkthrough 10's `<TextChrome>` shared component. Host-aware (TextFileModel pair); visibility via `pagesModel.query.canCompare(leftId, page.id)` (CK3); onClick via `pagesModel.layout.enterCompareMode(page.id)` (CK4 — single page-level entry point, accepts either side). Walkthrough 09 confirms position (after NavPanel slot, inline as `<TextChrome>` child); walkthrough 10 implements. NavPanel exits `<TextChrome>` per PT5; Compare stays because it depends on the host type, not the page-level navigator predicate. Rejected (b) page-level `<PageToolbar>` slot (couples generic toolbar with text-host concerns) and (c) right-side action group near switch widget (third visual position without justification). No mockup change required.
- 2026-05-20 — Walkthrough 09 / PT5 resolved (reframed then resolved) + B3 mockup landed. **PT5 — NavPanel button placement**: initial text-only proposal corrected by user — NavPanel is NOT text-only; rendered today by SIX editors (Text, PDF, Image, Video, Archive, Category) via the same `page.canOpenNavigator(pipe, filePath)` predicate. Resolved option (a): auto-render inside `<PageToolbar>` via new `EditorModel.getNavigatorTarget(): { pipe?, filePath? } | null` accessor. Per-editor overrides: null (default) = no button; `{}` = always-show with no-args toggle (Archive/Category); `{ pipe, filePath }` = page predicate gates rendering (Text via host fields, PDF/Image via editor fields, Video via filePath only). Eliminates 6 copy-pasted IconButton blocks. Parallel to PT2's switch widget pattern — page-level uniform affordances live inside `<PageToolbar>`. NavPanel is OUT of walkthrough 10's `<TextChrome>` (which keeps only text-host-specific actions: Compare, Run, Run-all, Show-resources). Rejected (b) per-editor inline shared `<NavPanelButton>` component (still six call sites) and (c) two-field accessor `navigatorPipe` + `navigatorFilePath` (splits one concept into two flat fields). **B3 mockup**: `getNavigatorTarget()` accessor added to `mockups/EditorModel.ts` with full doc block + header note; imports `IContentPipe` type. Base returns null; subclass overrides per the three target shapes documented inline.
- 2026-05-20 — Walkthrough 09 / PT4 resolved. **PT4 — `RenderEditor` category branch retirement**: option (a) — single uniform path. RenderEditor loads via `editorRegistry.loadModule(editor.editorId)` and renders `<module.Component model={editor} />`; the two-branch dispatch (`category === "standalone"` vs. content-view → TextEditorView) collapses. Today's `state.use(s => s.type)` subscription goes away (no `type` field; per S10). TextEditorView dissolves — focus management becomes a hook (`useEditorFocus`) or absorbs into walkthrough 10's `<TextChrome>`; keyboard routing moves to the editor's view; chrome composes inline per PT1 (b) + PT3 (a). Rejected (b) keep two-branch with `traits.has(CONTENT_HOST_TRAIT)` discriminator (re-creates the today-structure that S10 / C1 explicitly retired) and (c) three branches (over-design). No mockup change required.
- 2026-05-20 — Walkthrough 09 / PT3 resolved. **PT3 — Editor-specific toolbar contribution mechanism**: option (a) — inline composition. Each editor's view renders `<PageToolbar model={model}>{contributions}</PageToolbar>` with toolbar controls as JSX children. Today's portal contortion existed because the editor sat INSIDE TextEditorView; once the editor IS the chrome's parent (PT1 (b)), portals dissolve. All four portal refs on TextEditorModel retire — walkthrough 09 deletes the toolbar pair (PT9); 10 deletes footer + overlay. Slot ordering handled by `<PageToolbar>` itself (children before `<Spacer />`, switch widget after, per PT2). Rejected (b) `firstPlace` / `lastPlace` slot props (re-introduces two-bucket discipline that no longer has structural meaning) and (c) per-module `renderToolbar?` field (API surface for a non-problem). No new mockup primitive required.
- 2026-05-20 — Walkthrough 09 / PT2 resolved. **PT2 — Switch widget mount point**: option (a) — inside the shared `<PageToolbar>` component, auto-rendered. `<PageToolbar model={editor}>{children}</PageToolbar>` composes children before `<Spacer />` and `<SwitchWidget />` after; switch sits on the right side uniformly. `<SwitchWidget>` reads `model.findCompatibleEditors()` and calls `model.page?.switchMainEditor(v)` on change. Call site collapses to `<PageToolbar model={editor}>{contributions}</PageToolbar>`. Rejected (b) explicit per-editor render (boilerplate; opt-out is YAGNI) and (c) page-level wrapper in RenderEditor (loses cohesion, contradicts PT1). Drives B1 mockup (new `mockups/PageToolbar.tsx`).
- 2026-05-20 — Walkthrough 09 / PT1 resolved. **PT1 — Page-level toolbar location**: option (b) — each editor's view composes shared chrome components directly. Aligns with C8 on the concerns log. RenderEditor renders `<editor.Component model={editor} />`; the editor's view picks its chrome: text-bearing editors render `<PageToolbar>{contributions}</PageToolbar>` + body + script panel + footer + overlay; non-text editors render bare content. Shared chrome components carry host-instanceof branching internally (C1). Rejected (a) wrapper in RenderEditor (pushes complexity into a frame-with-slots contract) and (c) hybrid pre-rendered frames (dead UI for non-text editors). TextEditorView dissolves under PT4. No mockup change required.
- 2026-05-20 — Walkthrough 09 (Page-level toolbar) initial analysis written. PT1–PT10 proposed for review. Key proposals: (PT1) each editor's view composes shared chrome directly per C8 — TextEditorView wrapper dissolves; (PT2) switch widget auto-rendered inside shared `<PageToolbar>` on the right (after `<Spacer />`); (PT3) portal refs (`editorToolbarRefFirst`/`Last`) deleted — editor views inline-compose `<PageToolbar model>{contributions}</PageToolbar>` since each editor IS at the chrome layer now; (PT4) `RenderEditor` category branch retires — single uniform `<AsyncEditor>` path for every editor; (PT5–PT8) NavPanel + Compare + Run/Run-all + Show-resources all live inside walkthrough 10's `<TextChrome>` (host-instanceof-gated); 09 confirms positions, 10 implements; (PT7) `EditorModel.hasTextSelection?(): boolean` optional method for Run-all visibility (Monaco-only); (PT9) walkthrough 09 retires toolbar refs only — footer ref + overlay ref defer to 10; (PT10) switch widget visibility: `findCompatibleEditors().length >= 2` AND current editor in the list. Two mockup adjustments proposed: B1 (new `mockups/PageToolbar.tsx` shared component absorbing the switch widget), B2 (`EditorModel.hasTextSelection?(): boolean` optional method). Awaiting user review concern-by-concern.
- 2026-05-20 — **Walkthrough 08 (Page tabs) complete.** T1–T10 all resolved; mockup adjustments B1 + B2 landed; GK2's helper signature refined cross-walkthrough. Net effect: the tab strip becomes a clean consumer of three centralized primitives — `pagesModel.query.getTextFileHost(pageId)` (text-file host access for predicates + methods), `page.getDescriptor()` (single source of truth for the new `PageDescriptor` shape), and `editor.contentHost` (the underlying accessor that the GK2 helper wraps). Real-code surface is narrow — `PageTab.tsx` + `PageTabs.tsx` + the `PagesQueryModel` helper add + per-text-bearing-editor `contentHost` getter overrides in walkthroughs 20–29. First Tier 2 walkthrough done; walkthroughs 09 (page-level toolbar / switch widget) and 10 (TextChrome) consume the same `EditorModel.contentHost` accessor B2 added, so the foundation primitives are in place for the rest of Tier 2.
- 2026-05-20 — Walkthrough 08 / T10 resolved (confirmation) + B2 mockup landed. **T10 — Same-window reorder trait payload**: option (a) — no change. `setTraitDragData(TraitTypeId.PageTab, { key: page.id })` + `pagesModel.moveTab(data.key, id)` works as-is; `findPage` is already migrated to unified `editors[]`. **B2 mockup**: `EditorModel.contentHost: IContentHost | null` accessor added to `mockups/EditorModel.ts`. Base returns null; text-bearing subclasses override to return their `_host` field. Consumed by `PagesQueryModel.getTextFileHost` (the T2 helper), the switch widget (walkthrough 09), and TextChrome (walkthrough 10 / C1). PageTab routes through the GK2 helper rather than calling the accessor directly — keeps the host-type check centralized.
- 2026-05-20 — Walkthrough 08 / T9 resolved (confirmation). **T9 — `handleDragEnd` interaction with `movePageOut`'s `saveState`**: option (a) — no change in the tab. Walkthrough 05 / M3 puts the awaitable flush inside `PagesLifecycleModel.movePageOut`; `handleDragEnd` stays a pure `api.addDragEvent(getDragData())` dispatch. No mockup change required.
- 2026-05-20 — Walkthrough 08 / T8 resolved (confirmation). **T8 — `handleClick` Ctrl+group invariants under unified `editors[]`**: option (a) — no change. `pagesModel.groupTabs(activeId, pageId, true)` resolves correctly via `findPage`, already migrated to unified `editors[]` by walkthroughs 01 / A8 + 03 (confirmed by walkthrough 07 / GK10). Both inputs are page ids; direct page-id match. No mockup change required.
- 2026-05-20 — Walkthrough 08 / T7 resolved (confirmation). **T7 — `closeClick` redundant `fixCompareMode()` call**: option (a) — drop the call. Resolved upstream by walkthrough 06 / CK7 (compare cleanup folded into `ungroup`) + walkthrough 07 / GK4 (drop the explicit call from `PageTab.closeClick`); T7 is a confirmation entry so the implementation-time checklist for `PageTab.tsx` doesn't miss the line. No mockup change required.
- 2026-05-20 — Walkthrough 08 / T6 resolved. **T6 — Pinned-tab encryption-width branch**: option (a-helper) — inline the T2 GK2 helper at `PageTabs.tsx:206`: `const host = pagesModel.query.getTextFileHost(p.id); const isEnc = host && (host.encrypted || host.decrypted);` Same canonical predicate as T2; the (a-inline) `editor.contentHost instanceof TextFileModel` variant from the original proposal is superseded by T2's helper resolution. Widths and `+2` column gap stay inline in `PageTabs.tsx` — no `pinnedWidth(page)` math helper (one callsite). No mockup change required.
- 2026-05-20 — Walkthrough 08 / T5 resolved. **T5 — Audio fields duck-typing**: option (a) — keep `_anyTabAudible`, `pageMuted`, `toggleMuteAll` duck-typed via `(editor as any)?.toggleMuteAll` and `useOptionalState` selector defaults. BrowserEditor is the sole consumer; YAGNI on a formal `editor.audio?: AudioCapability` interface or `AUDIO_TRAIT` registration. Revisit if a second audio source lands. No mockup change required.
- 2026-05-20 — Walkthrough 08 / T4 resolved. **T4 — `noLanguage` / `getIcon` discoverability**: option (a) — keep both as explicit EditorModel base fields (already in `mockups/EditorModel.ts:419-421`). Per-editor opt-in identical to today's pattern; tab reads them as direct properties on `editor`. UI flag is independent of structural fact — bundling "show language picker" with `traits.has(CONTENT_HOST_TRAIT)` would couple a UI concern to a structural trait. No mockup change required.
- 2026-05-20 — Walkthrough 08 / T3 resolved + B1 mockup landed. **T3 — Drag payload composition**: option (a) — add `PageModel.getDescriptor(): PageDescriptor` as the single source of truth for the new descriptor shape (walkthrough 04 / P1). Consumed by `PagesPersistenceModel.saveState` (per-window save), `PageTab.getDragData` (cross-window drag payload), and `PagesLifecycleModel.duplicatePage` (with fresh ids). Tab's `getDragData` collapses to `{ ...windowMetadata, page: page.getDescriptor() }`. Mirrors `EditorModel.getRestoreData()` philosophy — each layer describes itself; many consumers read the same shape. Eliminates the drift risk between persistence and drag-payload inline composition. **B1 mockup**: `getDescriptor()` method added to `mockups/PageModel.ts` (between `reconcileVisibility` and `saveState`); imports `PageDescriptor` from `PersistenceTypes`; "Updated by walkthrough 08" header block + "NEW" footnote entry added. Body composes per-editor `getRestoreData()` + sidebar metadata (when `pageNavigatorModel` is present).
- 2026-05-20 — Walkthrough 08 / T1 + T2 resolved. **T1 — Tab read-surface**: option (a) — `PageTab` subscribes to two parallel `use()` stores (`editor.state.use()` for editor-owned fields + `editor.contentHost?.state.use()` for host-owned fields). **T2 — `isTextFileModel` retirement**: option (b1) — refines GK2's helper into a single typed accessor `pagesModel.query.getTextFileHost(pageId): TextFileModel | null` (was `hasTextFileHost: boolean`). One helper, two use cases (truthy check + method access); subsumes GK2's boolean predicate; lets `requireGroupedText` drop its trailing `as unknown as TextFileModel` cast. CK3's `canCompare(leftId, rightId)` stays a separate two-id predicate (no host to return). Updated cross-references in walkthroughs 02 / 06 / 07 (status checklist, mockup-snapshot section) and `concerns.md` (GK2 row marked "Resolved 2026-05-19; refined 2026-05-20 by walkthrough 08 / T2"; T1 + T2 rows added to the Resolved table).
- 2026-05-19 — Walkthrough 08 (Page tabs) initial analysis written. T1–T10 proposed for review. Key proposals: tab subscribes to TWO state stores (`editor.state.use()` + `editor.contentHost?.state.use()`) since the editor/host split moves `language`/`filePath`/`encrypted`/`temp` to the host while `title`/`modified`/`favicon`/audio stay on the editor — T1. Retire `isTextFileModel(editor)` across 14 PageTab.tsx callsites; new idiom is `editor.contentHost instanceof TextFileModel` (assign-once-then-narrow pattern); host-typed method calls (`saveFile`, `renameFile`, `showEncryptionDialog`, `encryptWithCurrentPassword`, `makeUnencrypted`) move to the host accessor — T2. Add `PageModel.getDescriptor(): PageDescriptor` (B1 mockup) as the single source of truth for the new descriptor shape from walkthrough 04 / P1 — consumed by `PagesPersistenceModel.saveState` and `PageTab.getDragData`, plus optionally `duplicatePage`; collapses `getDragData` to a one-liner and eliminates the drift risk between two inline descriptor builders — T3. Add `EditorModel.contentHost: IContentHost | null` accessor on the base class (B2 mockup; today's mockup comment at line 352-354 already foreshadows it); text-bearing subclasses override to return their `_host` field; non-host editors inherit the `null` default — T2 + T4. Keep `noLanguage` and `getIcon` as explicit EditorModel base fields rather than deriving from trait presence (UI flag is independent of structural fact) — T4. Keep audio fields (`_anyTabAudible`, `pageMuted`, `toggleMuteAll`) duck-typed since BrowserEditor is the only consumer — T5. Pinned-tab encryption-width branch in `PageTabs.tsx` uses the same T2 idiom — T6. T7 (drop `fixCompareMode()` from `closeClick`) confirmed via walkthrough 06 / CK7 + 07 / GK4; T8 (Ctrl+click `groupTabs`) confirmed via 07 / GK10; T9 (drag-out doesn't need its own `saveState` — `movePageOut` carries the invariant per 05 / M3) confirmed; T10 (same-window reorder trait payload is page-id-keyed) confirmed. Awaiting user review concern-by-concern.
- 2026-05-19 — Walkthrough 07 (Grouped pages) complete. GK1–GK10 resolved. Key decisions: parallel `leftRight`/`rightLeft` maps on `PagesModel.state` survive unchanged — drift is structurally impossible since every mutation site writes both, and grow-into-a-record (c) stays available as additive future work (GK1); `pagesModel.query.hasTextFileHost(pageId): boolean` lands as the centralized host-instanceof predicate replacing today's scattered `mainEditor?.type !== "textFile"` checks, mirroring walkthrough 06 / CK3's `canCompare` pattern (GK2); `fixGrouping` keeps its two-pass body (swap-direction fix + dangling-group cleanup) as a single sweep run from `removePage` and `moveTabByIndex` — splitting adds names without value; pushing into single callers loses robustness against future call sites forgetting one pass (GK3); `PageTab.closeClick`'s grouped branch drops the now-redundant `pagesModel.fixCompareMode()` call, collapsing to `ungroup → showPage` since CK7 made `ungroup` carry the compare-cleanup obligation (GK4); multi-window transfer of a grouped page preserves today's "group dissolves on source" behavior — cross-window drag is a single-tab gesture by design, simultaneous dual-tab drag impossible (same reasoning as walkthrough 06 / CK9 for compare pairs), atomic pair transfer (b) requires `PageDragData` shape changes for no observable user benefit (GK5); `ImperativeSplitter.widthK` stays non-persisted — UX nicety orthogonal to EPIC-028's editor refactor, deferable as additive `WindowState.splitRatios?` later per P10's additive-doesn't-bump-schemaVersion contract (GK6); two-pages-per-group invariant is permanent design — no N-page generalization, matches CK1's simplest-shape reasoning (GK7); pin/group interaction preserved as-is — emergent benign behavior from parallel-maps storage independent of the pin section, `AppPageManager.findGroupId` still resolves correctly across the pinned/unpinned section boundary (GK8); script API surface unchanged — thin `PageCollectionWrapper` delegates, no EPIC-028 motivation to break working scripts (GK9); `isGrouped(editorId)` resolves correctly via the already-updated `findPage` (walkthrough 01 / A8 + 03 migrated `findPage` to `p.id || p.editors.some(e => e.id === id)`), semantics identical to today, no new change required (GK10). Mockups touched: none. Migration scope handed off: real-code only — add `PagesQueryModel.hasTextFileHost(pageId)` helper (GK2); rewrite `PagesLifecycleModel.requireGroupedText` to use it (GK2); delete trailing `this.fixCompareMode()` inside `PagesLayoutModel.fixGrouping` (CK7); delete `pagesModel.fixCompareMode()` from `PageTab.closeClick` grouped branch (GK4). `AppPageManager` / `GroupContainer` / `ImperativeSplitter` / `PagesPersistenceModel` (groupings part) / script wrappers / the core mutation primitives (`group`, `ungroup`, `groupTabs`) and remaining `fixGrouping` body carry over verbatim. Net effect: lightest walkthrough so far — grouped pages are page-level layout state operating on page ids, mostly orthogonal to the editor/host refactor; the editor refactor's only touch point is GK2's discriminator flip, plus GK4's call-site cleanup that drops out of walkthrough 06 / CK7.
